# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import encode_path_param
from ..core.pagination import AsyncPager, SyncPager
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..errors.bad_request_error import BadRequestError
from ..errors.conflict_error import ConflictError
from ..errors.forbidden_error import ForbiddenError
from ..errors.not_found_error import NotFoundError
from ..errors.unauthorized_error import UnauthorizedError
from ..types.v1error_response import V1ErrorResponse
from .types.create_transfers_request_type import CreateTransfersRequestType
from .types.create_transfers_response import CreateTransfersResponse
from .types.list_recipients_transfers_response import ListRecipientsTransfersResponse
from .types.list_recipients_transfers_response_data_item import ListRecipientsTransfersResponseDataItem
from .types.list_transfers_request_direction import ListTransfersRequestDirection
from .types.list_transfers_request_order import ListTransfersRequestOrder
from .types.list_transfers_response import ListTransfersResponse
from .types.list_transfers_response_data_item import ListTransfersResponseDataItem
from .types.retrieve_transfers_response import RetrieveTransfersResponse
from pydantic import ValidationError

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawTransfersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self,
        *,
        origin_id: typing.Optional[str] = None,
        destination_id: typing.Optional[str] = None,
        order: typing.Optional[ListTransfersRequestOrder] = None,
        direction: typing.Optional[ListTransfersRequestDirection] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ListTransfersResponseDataItem, ListTransfersResponse]:
        """
        Lists an account's transfers.

        Parameters
        ----------
        origin_id : typing.Optional[str]
            Filter to transfers sent from this account. Provide this or destination_id.

        destination_id : typing.Optional[str]
            Filter to transfers received by this account. Provide this or origin_id.

        order : typing.Optional[ListTransfersRequestOrder]
            Sort column. Defaults to created_at.

        direction : typing.Optional[ListTransfersRequestDirection]
            Sort direction. Defaults to desc.

        created_before : typing.Optional[str]
            Only transfers created strictly before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Only transfers created strictly after this ISO 8601 timestamp.

        first : typing.Optional[int]
            Number of transfers to return from the start of the window.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            Number of transfers to return from the end of the window.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ListTransfersResponseDataItem, ListTransfersResponse]
            transfers listed
        """
        _response = self._client_wrapper.httpx_client.request(
            "transfers",
            method="GET",
            params={
                "origin_id": origin_id,
                "destination_id": destination_id,
                "order": order,
                "direction": direction,
                "created_before": created_before,
                "created_after": created_after,
                "first": first,
                "after": after,
                "last": last,
                "before": before,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListTransfersResponse,
                    parse_obj_as(
                        type_=ListTransfersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""
                    _get_next = lambda: self.list(
                        origin_id=origin_id,
                        destination_id=destination_id,
                        order=order,
                        direction=direction,
                        created_before=created_before,
                        created_after=created_after,
                        first=first,
                        after=_parsed_next,
                        last=last,
                        before=before,
                        request_options=request_options,
                    )
                return SyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def create(
        self,
        *,
        amount: float,
        origin_id: str,
        currency: typing.Optional[str] = OMIT,
        destination_id: typing.Optional[str] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        idempotence_key: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        notes: typing.Optional[str] = OMIT,
        redeemable_count: typing.Optional[int] = OMIT,
        type: typing.Optional[CreateTransfersRequestType] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[CreateTransfersResponse]:
        """
        Moves money between accounts, or into a claim link anyone with the URL can redeem.

        Parameters
        ----------
        amount : float
            The amount to move, in the transfer currency. For example 25.00.

        origin_id : str
            The account sending the funds. A user ID (user_xxx), account ID (biz_xxx), or ledger account ID (ldgr_xxx).

        currency : typing.Optional[str]
            Currency, such as `usd`. Required for ledger transfers.

        destination_id : typing.Optional[str]
            The recipient. Required for ledger and wallet_send (a user_/biz_/ldgr_ ID, or — for sends — an email). Omit for claim_link.

        expires_at : typing.Optional[dt.datetime]
            claim_link only. Link expiry as an ISO 8601 timestamp. Defaults to 24 hours from creation.

        idempotence_key : typing.Optional[str]
            Ledger transfers and wallet sends. A unique key that makes retries safe. Retrying with the same key returns the original transfer, or attaches to the original wallet send, instead of moving money twice.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Ledger transfers only. Custom key-value pairs attached to the transfer. Max 50 keys, 100 chars per key, 500 chars per string value.

        notes : typing.Optional[str]
            Ledger transfers only. A short note describing the transfer.

        redeemable_count : typing.Optional[int]
            claim_link only. How many different users can claim the link. Defaults to 1.

        type : typing.Optional[CreateTransfersRequestType]
            The kind of money movement, which decides what comes back. Defaults to ledger. `ledger` moves credit between two Whop balances and returns a `transfer`; `wallet_send` sends USDT from the origin account's Ethereum wallet and returns a `send`; `claim_link` funds a shareable link anyone with the URL can redeem and returns a `claim_link`. A `ledger` transfer from a stablecoin-rails account settles on-chain when covered, and still returns a `transfer`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[CreateTransfersResponse]
            money movement created
        """
        _response = self._client_wrapper.httpx_client.request(
            "transfers",
            method="POST",
            json={
                "amount": amount,
                "currency": currency,
                "destination_id": destination_id,
                "expires_at": expires_at,
                "idempotence_key": idempotence_key,
                "metadata": metadata,
                "notes": notes,
                "origin_id": origin_id,
                "redeemable_count": redeemable_count,
                "type": type,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CreateTransfersResponse,
                    parse_obj_as(
                        type_=CreateTransfersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def list_recipients(
        self,
        *,
        origin_id: str,
        query: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ListRecipientsTransfersResponseDataItem, ListRecipientsTransfersResponse]:
        """
        Lists the people and accounts you can send money to.

        Parameters
        ----------
        origin_id : str
            The account sending the money: a company account ID (`biz_`), or a user ID (`user_`) for that user's own personal balance.

        query : typing.Optional[str]
            Search anyone on Whop by name or username, plus your own accounts by name or ID. An exact business ID (`biz_`) returns that business first. Omit it to get the team around the balance, the people you follow, and your own accounts. The list is the same whether the balance belongs to a company or to you. Searching from a `biz_` origin additionally requires the member:basic:read scope. A credential scoped to a single company is the exception to the search itself: it only ever sees that company's own people. Complete email addresses return no matches.

        first : typing.Optional[int]
            Number of recipients per page. Search queries preserve the dashboard's 20-result maximum.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ListRecipientsTransfersResponseDataItem, ListRecipientsTransfersResponse]
            recipients listed
        """
        _response = self._client_wrapper.httpx_client.request(
            "transfers/recipients",
            method="GET",
            params={
                "origin_id": origin_id,
                "query": query,
                "first": first,
                "after": after,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListRecipientsTransfersResponse,
                    parse_obj_as(
                        type_=ListRecipientsTransfersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""
                    _get_next = lambda: self.list_recipients(
                        origin_id=origin_id,
                        query=query,
                        first=first,
                        after=_parsed_next,
                        request_options=request_options,
                    )
                return SyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[RetrieveTransfersResponse]:
        """
        Retrieves a single transfer.

        Parameters
        ----------
        id : str
            The transfer ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[RetrieveTransfersResponse]
            transfer retrieved
        """
        _response = self._client_wrapper.httpx_client.request(
            f"transfers/{encode_path_param(id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    RetrieveTransfersResponse,
                    parse_obj_as(
                        type_=RetrieveTransfersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawTransfersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self,
        *,
        origin_id: typing.Optional[str] = None,
        destination_id: typing.Optional[str] = None,
        order: typing.Optional[ListTransfersRequestOrder] = None,
        direction: typing.Optional[ListTransfersRequestDirection] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ListTransfersResponseDataItem, ListTransfersResponse]:
        """
        Lists an account's transfers.

        Parameters
        ----------
        origin_id : typing.Optional[str]
            Filter to transfers sent from this account. Provide this or destination_id.

        destination_id : typing.Optional[str]
            Filter to transfers received by this account. Provide this or origin_id.

        order : typing.Optional[ListTransfersRequestOrder]
            Sort column. Defaults to created_at.

        direction : typing.Optional[ListTransfersRequestDirection]
            Sort direction. Defaults to desc.

        created_before : typing.Optional[str]
            Only transfers created strictly before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Only transfers created strictly after this ISO 8601 timestamp.

        first : typing.Optional[int]
            Number of transfers to return from the start of the window.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            Number of transfers to return from the end of the window.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ListTransfersResponseDataItem, ListTransfersResponse]
            transfers listed
        """
        _response = await self._client_wrapper.httpx_client.request(
            "transfers",
            method="GET",
            params={
                "origin_id": origin_id,
                "destination_id": destination_id,
                "order": order,
                "direction": direction,
                "created_before": created_before,
                "created_after": created_after,
                "first": first,
                "after": after,
                "last": last,
                "before": before,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListTransfersResponse,
                    parse_obj_as(
                        type_=ListTransfersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""

                    async def _get_next():
                        return await self.list(
                            origin_id=origin_id,
                            destination_id=destination_id,
                            order=order,
                            direction=direction,
                            created_before=created_before,
                            created_after=created_after,
                            first=first,
                            after=_parsed_next,
                            last=last,
                            before=before,
                            request_options=request_options,
                        )

                return AsyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def create(
        self,
        *,
        amount: float,
        origin_id: str,
        currency: typing.Optional[str] = OMIT,
        destination_id: typing.Optional[str] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        idempotence_key: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        notes: typing.Optional[str] = OMIT,
        redeemable_count: typing.Optional[int] = OMIT,
        type: typing.Optional[CreateTransfersRequestType] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[CreateTransfersResponse]:
        """
        Moves money between accounts, or into a claim link anyone with the URL can redeem.

        Parameters
        ----------
        amount : float
            The amount to move, in the transfer currency. For example 25.00.

        origin_id : str
            The account sending the funds. A user ID (user_xxx), account ID (biz_xxx), or ledger account ID (ldgr_xxx).

        currency : typing.Optional[str]
            Currency, such as `usd`. Required for ledger transfers.

        destination_id : typing.Optional[str]
            The recipient. Required for ledger and wallet_send (a user_/biz_/ldgr_ ID, or — for sends — an email). Omit for claim_link.

        expires_at : typing.Optional[dt.datetime]
            claim_link only. Link expiry as an ISO 8601 timestamp. Defaults to 24 hours from creation.

        idempotence_key : typing.Optional[str]
            Ledger transfers and wallet sends. A unique key that makes retries safe. Retrying with the same key returns the original transfer, or attaches to the original wallet send, instead of moving money twice.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Ledger transfers only. Custom key-value pairs attached to the transfer. Max 50 keys, 100 chars per key, 500 chars per string value.

        notes : typing.Optional[str]
            Ledger transfers only. A short note describing the transfer.

        redeemable_count : typing.Optional[int]
            claim_link only. How many different users can claim the link. Defaults to 1.

        type : typing.Optional[CreateTransfersRequestType]
            The kind of money movement, which decides what comes back. Defaults to ledger. `ledger` moves credit between two Whop balances and returns a `transfer`; `wallet_send` sends USDT from the origin account's Ethereum wallet and returns a `send`; `claim_link` funds a shareable link anyone with the URL can redeem and returns a `claim_link`. A `ledger` transfer from a stablecoin-rails account settles on-chain when covered, and still returns a `transfer`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[CreateTransfersResponse]
            money movement created
        """
        _response = await self._client_wrapper.httpx_client.request(
            "transfers",
            method="POST",
            json={
                "amount": amount,
                "currency": currency,
                "destination_id": destination_id,
                "expires_at": expires_at,
                "idempotence_key": idempotence_key,
                "metadata": metadata,
                "notes": notes,
                "origin_id": origin_id,
                "redeemable_count": redeemable_count,
                "type": type,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CreateTransfersResponse,
                    parse_obj_as(
                        type_=CreateTransfersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def list_recipients(
        self,
        *,
        origin_id: str,
        query: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ListRecipientsTransfersResponseDataItem, ListRecipientsTransfersResponse]:
        """
        Lists the people and accounts you can send money to.

        Parameters
        ----------
        origin_id : str
            The account sending the money: a company account ID (`biz_`), or a user ID (`user_`) for that user's own personal balance.

        query : typing.Optional[str]
            Search anyone on Whop by name or username, plus your own accounts by name or ID. An exact business ID (`biz_`) returns that business first. Omit it to get the team around the balance, the people you follow, and your own accounts. The list is the same whether the balance belongs to a company or to you. Searching from a `biz_` origin additionally requires the member:basic:read scope. A credential scoped to a single company is the exception to the search itself: it only ever sees that company's own people. Complete email addresses return no matches.

        first : typing.Optional[int]
            Number of recipients per page. Search queries preserve the dashboard's 20-result maximum.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ListRecipientsTransfersResponseDataItem, ListRecipientsTransfersResponse]
            recipients listed
        """
        _response = await self._client_wrapper.httpx_client.request(
            "transfers/recipients",
            method="GET",
            params={
                "origin_id": origin_id,
                "query": query,
                "first": first,
                "after": after,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListRecipientsTransfersResponse,
                    parse_obj_as(
                        type_=ListRecipientsTransfersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""

                    async def _get_next():
                        return await self.list_recipients(
                            origin_id=origin_id,
                            query=query,
                            first=first,
                            after=_parsed_next,
                            request_options=request_options,
                        )

                return AsyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[RetrieveTransfersResponse]:
        """
        Retrieves a single transfer.

        Parameters
        ----------
        id : str
            The transfer ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[RetrieveTransfersResponse]
            transfer retrieved
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"transfers/{encode_path_param(id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    RetrieveTransfersResponse,
                    parse_obj_as(
                        type_=RetrieveTransfersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
