# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.ad import Ad
from .raw_client import AsyncRawAdsClient, RawAdsClient
from .types.create_ads_request_call_to_action import CreateAdsRequestCallToAction
from .types.create_ads_request_creatives_item import CreateAdsRequestCreativesItem
from .types.create_ads_request_lead_form import CreateAdsRequestLeadForm
from .types.create_ads_request_messaging_config import CreateAdsRequestMessagingConfig
from .types.create_ads_request_post_source import CreateAdsRequestPostSource
from .types.create_ads_request_social_accounts_item import CreateAdsRequestSocialAccountsItem
from .types.delete_ads_response import DeleteAdsResponse
from .types.duplicate_ads_response import DuplicateAdsResponse
from .types.list_ads_request_attribution_model import ListAdsRequestAttributionModel
from .types.list_ads_request_direction import ListAdsRequestDirection
from .types.list_ads_request_order import ListAdsRequestOrder
from .types.list_ads_request_status import ListAdsRequestStatus
from .types.list_ads_response import ListAdsResponse
from .types.retrieve_ads_request_attribution_model import RetrieveAdsRequestAttributionModel
from .types.update_ads_request_call_to_action import UpdateAdsRequestCallToAction
from .types.update_ads_request_creatives_item import UpdateAdsRequestCreativesItem
from .types.update_ads_request_lead_form import UpdateAdsRequestLeadForm
from .types.update_ads_request_messaging_config import UpdateAdsRequestMessagingConfig
from .types.update_ads_request_post_source import UpdateAdsRequestPostSource
from .types.update_ads_request_social_accounts_item import UpdateAdsRequestSocialAccountsItem

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AdsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAdsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAdsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAdsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        ad_campaign_id: typing.Optional[str] = None,
        ad_campaign_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        ad_group_id: typing.Optional[str] = None,
        ad_group_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        status: typing.Optional[ListAdsRequestStatus] = None,
        query: typing.Optional[str] = None,
        order: typing.Optional[ListAdsRequestOrder] = None,
        direction: typing.Optional[ListAdsRequestDirection] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        stats_from: typing.Optional[str] = None,
        stats_to: typing.Optional[str] = None,
        time_zone: typing.Optional[str] = None,
        attribution_model: typing.Optional[ListAdsRequestAttributionModel] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Ad, ListAdsResponse]:
        """
        Lists the ads for an account, with stats over the requested window.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The account the ads belong to. Defaults to the account-scoped key's own account.

        ad_campaign_id : typing.Optional[str]
            Only return ads in this ad campaign.

        ad_campaign_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only return ads in these ad campaigns (max 100). Repeat the parameter for each id (ad_campaign_ids=a&ad_campaign_ids=b).

        ad_group_id : typing.Optional[str]
            Only return ads in this ad group.

        ad_group_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only return ads in these ad groups (max 100). Repeat the parameter for each id (ad_group_ids=a&ad_group_ids=b).

        status : typing.Optional[ListAdsRequestStatus]
            Only return ads with this status.

        query : typing.Optional[str]
            Filter ads by a title or ID substring.

        order : typing.Optional[ListAdsRequestOrder]
            The field to sort by. Defaults to created_at. Stat columns (spend, impressions, …) rank over the stats_from/stats_to window across the whole list, not just the current page. results, cost_per_result and return_on_ad_spend rank by the same Whop pixel-attributed values the response reports.

        direction : typing.Optional[ListAdsRequestDirection]
            The sort direction. Defaults to desc.

        created_before : typing.Optional[str]
            Only return ads created before this timestamp.

        created_after : typing.Optional[str]
            Only return ads created after this timestamp.

        stats_from : typing.Optional[str]
            Start of the stats window. Defaults to all-time.

        stats_to : typing.Optional[str]
            End of the stats window. Defaults to now.

        time_zone : typing.Optional[str]
            IANA timezone (e.g. America/New_York) the stats window is interpreted in. Bare stats_from/stats_to dates resolve to day boundaries on this clock. Defaults to UTC.

        attribution_model : typing.Optional[ListAdsRequestAttributionModel]
            Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise.

        first : typing.Optional[int]
            The number of ads to return.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            The number of ads to return from the end of the range.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Ad, ListAdsResponse]
            ads listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.ads.list(
            ad_campaign_ids=["adcamp_xxxxxxxxxxxxxx"],
            ad_group_ids=["adgrp_xxxxxxxxxxxxxx"],
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            ad_campaign_id=ad_campaign_id,
            ad_campaign_ids=ad_campaign_ids,
            ad_group_id=ad_group_id,
            ad_group_ids=ad_group_ids,
            status=status,
            query=query,
            order=order,
            direction=direction,
            created_before=created_before,
            created_after=created_after,
            stats_from=stats_from,
            stats_to=stats_to,
            time_zone=time_zone,
            attribution_model=attribution_model,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        ad_group: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        ad_group_id: typing.Optional[str] = OMIT,
        call_to_action: typing.Optional[CreateAdsRequestCallToAction] = OMIT,
        creatives: typing.Optional[typing.Sequence[CreateAdsRequestCreativesItem]] = OMIT,
        descriptions: typing.Optional[typing.Sequence[str]] = OMIT,
        headlines: typing.Optional[typing.Sequence[str]] = OMIT,
        lead_form: typing.Optional[CreateAdsRequestLeadForm] = OMIT,
        lead_form_id: typing.Optional[str] = OMIT,
        messaging_config: typing.Optional[CreateAdsRequestMessagingConfig] = OMIT,
        multi_advertiser_ads: typing.Optional[bool] = OMIT,
        post_id: typing.Optional[str] = OMIT,
        post_source: typing.Optional[CreateAdsRequestPostSource] = OMIT,
        primary_texts: typing.Optional[typing.Sequence[str]] = OMIT,
        social_accounts: typing.Optional[typing.Sequence[CreateAdsRequestSocialAccountsItem]] = OMIT,
        title: typing.Optional[str] = OMIT,
        url: typing.Optional[str] = OMIT,
        url_parameters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Ad:
        """
        Creates an ad in an ad group.

        Parameters
        ----------
        ad_group : typing.Optional[typing.Dict[str, typing.Any]]
            An inline ad group to create (same shape as POST /ad_groups, including ad_campaign_id). Creates the ad group and the ad together. Provide this OR ad_group_id.

        ad_group_id : typing.Optional[str]
            The existing ad group to create the ad in. Provide this OR ad_group, not both.

        call_to_action : typing.Optional[CreateAdsRequestCallToAction]
            The call-to-action button shown on the ad.

        creatives : typing.Optional[typing.Sequence[CreateAdsRequestCreativesItem]]
            The ad's creative assets. Each entry is an uploaded file id with an optional format; omit format for the original asset. Two or more entries with no format become a carousel (2-10 attachments), in order, sharing the ad's copy.

        descriptions : typing.Optional[typing.Sequence[str]]
            The description variants shown on the ad.

        headlines : typing.Optional[typing.Sequence[str]]
            The headline variants shown on the ad.

        lead_form : typing.Optional[CreateAdsRequestLeadForm]
            Instant lead form for the ad. Only allowed when the ad group's conversion_location is an instant-form destination (instant_forms, instant_forms_and_messenger, website_and_instant_forms). Mutually exclusive with lead_form_id.

        lead_form_id : typing.Optional[str]
            Use an existing instant form instead of creating one — the form's platform ID, from a form already on the ad's Facebook page. Only allowed when the ad group's conversion_location is an instant-form destination. Mutually exclusive with lead_form.

        messaging_config : typing.Optional[CreateAdsRequestMessagingConfig]
            Click-to-message welcome copy: the greeting (message) and the ice-breaker prompt (keyword).

        multi_advertiser_ads : typing.Optional[bool]
            Whether the ad can appear alongside other advertisers' ads in the same unit. Defaults to true.

        post_id : typing.Optional[str]
            Promote an existing post instead of uploading creatives — a Facebook post or Instagram media id. Mutually exclusive with creatives. Pair with post_source.

        post_source : typing.Optional[CreateAdsRequestPostSource]
            Identifies the network that owns `post_id`. The source is inferred from the ID shape when omitted.

        primary_texts : typing.Optional[typing.Sequence[str]]
            The primary text variants shown in the ad body.

        social_accounts : typing.Optional[typing.Sequence[CreateAdsRequestSocialAccountsItem]]
            The social accounts the ad runs under — a connected Facebook page and, optionally, an Instagram profile.

        title : typing.Optional[str]
            The display name of the ad.

        url : typing.Optional[str]
            The URL the ad links to. Query parameters are merged into url_parameters, so the stored URL is always bare.

        url_parameters : typing.Optional[typing.Dict[str, typing.Any]]
            Query parameters to append to the destination URL, keyed by parameter name. Merged with any query string on `url`. Whop adds its own click-attribution parameters; those are reserved and rejected if you set them (utm_meta_ad_id, utm_meta_adset_id, utm_meta_campaign_id, utm_source, utm_placement, utm_medium, utm_content, utm_adset, utm_whop, wacid, wasid, waid, tw_source, tw_adid).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad created from an existing post

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ads.create()
        """
        _response = self._raw_client.create(
            ad_group=ad_group,
            ad_group_id=ad_group_id,
            call_to_action=call_to_action,
            creatives=creatives,
            descriptions=descriptions,
            headlines=headlines,
            lead_form=lead_form,
            lead_form_id=lead_form_id,
            messaging_config=messaging_config,
            multi_advertiser_ads=multi_advertiser_ads,
            post_id=post_id,
            post_source=post_source,
            primary_texts=primary_texts,
            social_accounts=social_accounts,
            title=title,
            url=url,
            url_parameters=url_parameters,
            request_options=request_options,
        )
        return _response.data

    def retrieve(
        self,
        id: str,
        *,
        stats_from: typing.Optional[str] = None,
        stats_to: typing.Optional[str] = None,
        time_zone: typing.Optional[str] = None,
        attribution_model: typing.Optional[RetrieveAdsRequestAttributionModel] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Ad:
        """
        Retrieves a single ad with stats over the requested window.

        Parameters
        ----------
        id : str
            The ad ID.

        stats_from : typing.Optional[str]
            Start of the stats window.

        stats_to : typing.Optional[str]
            End of the stats window.

        time_zone : typing.Optional[str]
            IANA timezone the stats window is interpreted in. Defaults to UTC.

        attribution_model : typing.Optional[RetrieveAdsRequestAttributionModel]
            Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ads.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(
            id,
            stats_from=stats_from,
            stats_to=stats_to,
            time_zone=time_zone,
            attribution_model=attribution_model,
            request_options=request_options,
        )
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeleteAdsResponse:
        """
        Deletes an ad.

        Parameters
        ----------
        id : str
            The ad ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteAdsResponse
            ad deleted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ads.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        call_to_action: typing.Optional[UpdateAdsRequestCallToAction] = OMIT,
        creatives: typing.Optional[typing.Sequence[UpdateAdsRequestCreativesItem]] = OMIT,
        descriptions: typing.Optional[typing.Sequence[str]] = OMIT,
        headlines: typing.Optional[typing.Sequence[str]] = OMIT,
        lead_form: typing.Optional[UpdateAdsRequestLeadForm] = OMIT,
        lead_form_id: typing.Optional[str] = OMIT,
        messaging_config: typing.Optional[UpdateAdsRequestMessagingConfig] = OMIT,
        multi_advertiser_ads: typing.Optional[bool] = OMIT,
        post_id: typing.Optional[str] = OMIT,
        post_source: typing.Optional[UpdateAdsRequestPostSource] = OMIT,
        primary_texts: typing.Optional[typing.Sequence[str]] = OMIT,
        social_accounts: typing.Optional[typing.Sequence[UpdateAdsRequestSocialAccountsItem]] = OMIT,
        title: typing.Optional[str] = OMIT,
        url: typing.Optional[str] = OMIT,
        url_parameters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Ad:
        """
        Updates an ad's editable fields.

        Parameters
        ----------
        id : str
            The ad ID.

        call_to_action : typing.Optional[UpdateAdsRequestCallToAction]
            The call-to-action button shown on the ad.

        creatives : typing.Optional[typing.Sequence[UpdateAdsRequestCreativesItem]]
            The ad's creative assets. Each entry is an uploaded file id with an optional format; omit format for the original asset. Replaces a live ad's creative on the platform. Two or more entries with no format replace it with a carousel (2-10 attachments), in order, sharing the ad's copy.

        descriptions : typing.Optional[typing.Sequence[str]]
            The description variants shown on the ad.

        headlines : typing.Optional[typing.Sequence[str]]
            The headline variants shown on the ad.

        lead_form : typing.Optional[UpdateAdsRequestLeadForm]
            Instant lead form for the ad. Only allowed when the ad group's conversion_location is an instant-form destination (instant_forms, instant_forms_and_messenger, website_and_instant_forms). Mutually exclusive with lead_form_id.

        lead_form_id : typing.Optional[str]
            Use an existing instant form instead of creating one — the form's platform ID, from a form already on the ad's Facebook page. Only allowed when the ad group's conversion_location is an instant-form destination. Mutually exclusive with lead_form. Replaces a stored lead_form.

        messaging_config : typing.Optional[UpdateAdsRequestMessagingConfig]
            Click-to-message welcome copy: the greeting (message) and the ice-breaker prompt (keyword).

        multi_advertiser_ads : typing.Optional[bool]
            Whether the ad can appear alongside other advertisers' ads in the same unit. Defaults to true.

        post_id : typing.Optional[str]
            Promote an existing post instead of uploading creatives — a Facebook post or Instagram media id. Mutually exclusive with creatives. Pair with post_source.

        post_source : typing.Optional[UpdateAdsRequestPostSource]
            Identifies the network that owns `post_id`. The source is inferred from the ID shape when omitted.

        primary_texts : typing.Optional[typing.Sequence[str]]
            The primary text variants shown in the ad body.

        social_accounts : typing.Optional[typing.Sequence[UpdateAdsRequestSocialAccountsItem]]
            The social accounts the ad runs under — a connected Facebook page and, optionally, an Instagram profile.

        title : typing.Optional[str]
            The display name of the ad.

        url : typing.Optional[str]
            The URL the ad links to. Query parameters are merged into url_parameters, so the stored URL is always bare.

        url_parameters : typing.Optional[typing.Dict[str, typing.Any]]
            Query parameters to append to the destination URL, keyed by parameter name. Merged with any query string on `url`. Whop adds its own click-attribution parameters; those are reserved and rejected if you set them (utm_meta_ad_id, utm_meta_adset_id, utm_meta_campaign_id, utm_source, utm_placement, utm_medium, utm_content, utm_adset, utm_whop, wacid, wasid, waid, tw_source, tw_adid).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad switched to an existing post

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ads.update(
            id="id",
        )
        """
        _response = self._raw_client.update(
            id,
            call_to_action=call_to_action,
            creatives=creatives,
            descriptions=descriptions,
            headlines=headlines,
            lead_form=lead_form,
            lead_form_id=lead_form_id,
            messaging_config=messaging_config,
            multi_advertiser_ads=multi_advertiser_ads,
            post_id=post_id,
            post_source=post_source,
            primary_texts=primary_texts,
            social_accounts=social_accounts,
            title=title,
            url=url,
            url_parameters=url_parameters,
            request_options=request_options,
        )
        return _response.data

    def duplicate(
        self,
        id: str,
        *,
        count: typing.Optional[int] = OMIT,
        preserve_engagement: typing.Optional[bool] = OMIT,
        target_ad_group_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DuplicateAdsResponse:
        """
        Copies the ad into its own ad group, or into target_ad_group_id (which must belong to the same account and be compatible with the ad). Copies keep the source ad's active/paused state.

        Parameters
        ----------
        id : str
            The ad ID.

        count : typing.Optional[int]
            Number of copies to create (1-10). Defaults to 1.

        preserve_engagement : typing.Optional[bool]
            Whether the copies keep the original post's engagement (likes, comments, shares). Defaults to false.

        target_ad_group_id : typing.Optional[str]
            Ad group to duplicate into. Defaults to the ad's own ad group.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DuplicateAdsResponse
            ad duplicated

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ads.duplicate(
            id="id",
        )
        """
        _response = self._raw_client.duplicate(
            id,
            count=count,
            preserve_engagement=preserve_engagement,
            target_ad_group_id=target_ad_group_id,
            request_options=request_options,
        )
        return _response.data

    def pause(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Ad:
        """
        Pauses an active ad.

        Parameters
        ----------
        id : str
            The ad ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad paused

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ads.pause(
            id="id",
        )
        """
        _response = self._raw_client.pause(id, request_options=request_options)
        return _response.data

    def unpause(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Ad:
        """
        Resumes a paused ad.

        Parameters
        ----------
        id : str
            The ad ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad unpaused

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ads.unpause(
            id="id",
        )
        """
        _response = self._raw_client.unpause(id, request_options=request_options)
        return _response.data


class AsyncAdsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAdsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAdsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAdsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        ad_campaign_id: typing.Optional[str] = None,
        ad_campaign_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        ad_group_id: typing.Optional[str] = None,
        ad_group_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        status: typing.Optional[ListAdsRequestStatus] = None,
        query: typing.Optional[str] = None,
        order: typing.Optional[ListAdsRequestOrder] = None,
        direction: typing.Optional[ListAdsRequestDirection] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        stats_from: typing.Optional[str] = None,
        stats_to: typing.Optional[str] = None,
        time_zone: typing.Optional[str] = None,
        attribution_model: typing.Optional[ListAdsRequestAttributionModel] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Ad, ListAdsResponse]:
        """
        Lists the ads for an account, with stats over the requested window.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The account the ads belong to. Defaults to the account-scoped key's own account.

        ad_campaign_id : typing.Optional[str]
            Only return ads in this ad campaign.

        ad_campaign_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only return ads in these ad campaigns (max 100). Repeat the parameter for each id (ad_campaign_ids=a&ad_campaign_ids=b).

        ad_group_id : typing.Optional[str]
            Only return ads in this ad group.

        ad_group_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only return ads in these ad groups (max 100). Repeat the parameter for each id (ad_group_ids=a&ad_group_ids=b).

        status : typing.Optional[ListAdsRequestStatus]
            Only return ads with this status.

        query : typing.Optional[str]
            Filter ads by a title or ID substring.

        order : typing.Optional[ListAdsRequestOrder]
            The field to sort by. Defaults to created_at. Stat columns (spend, impressions, …) rank over the stats_from/stats_to window across the whole list, not just the current page. results, cost_per_result and return_on_ad_spend rank by the same Whop pixel-attributed values the response reports.

        direction : typing.Optional[ListAdsRequestDirection]
            The sort direction. Defaults to desc.

        created_before : typing.Optional[str]
            Only return ads created before this timestamp.

        created_after : typing.Optional[str]
            Only return ads created after this timestamp.

        stats_from : typing.Optional[str]
            Start of the stats window. Defaults to all-time.

        stats_to : typing.Optional[str]
            End of the stats window. Defaults to now.

        time_zone : typing.Optional[str]
            IANA timezone (e.g. America/New_York) the stats window is interpreted in. Bare stats_from/stats_to dates resolve to day boundaries on this clock. Defaults to UTC.

        attribution_model : typing.Optional[ListAdsRequestAttributionModel]
            Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise.

        first : typing.Optional[int]
            The number of ads to return.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            The number of ads to return from the end of the range.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Ad, ListAdsResponse]
            ads listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.ads.list(
                ad_campaign_ids=["adcamp_xxxxxxxxxxxxxx"],
                ad_group_ids=["adgrp_xxxxxxxxxxxxxx"],
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            ad_campaign_id=ad_campaign_id,
            ad_campaign_ids=ad_campaign_ids,
            ad_group_id=ad_group_id,
            ad_group_ids=ad_group_ids,
            status=status,
            query=query,
            order=order,
            direction=direction,
            created_before=created_before,
            created_after=created_after,
            stats_from=stats_from,
            stats_to=stats_to,
            time_zone=time_zone,
            attribution_model=attribution_model,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        ad_group: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        ad_group_id: typing.Optional[str] = OMIT,
        call_to_action: typing.Optional[CreateAdsRequestCallToAction] = OMIT,
        creatives: typing.Optional[typing.Sequence[CreateAdsRequestCreativesItem]] = OMIT,
        descriptions: typing.Optional[typing.Sequence[str]] = OMIT,
        headlines: typing.Optional[typing.Sequence[str]] = OMIT,
        lead_form: typing.Optional[CreateAdsRequestLeadForm] = OMIT,
        lead_form_id: typing.Optional[str] = OMIT,
        messaging_config: typing.Optional[CreateAdsRequestMessagingConfig] = OMIT,
        multi_advertiser_ads: typing.Optional[bool] = OMIT,
        post_id: typing.Optional[str] = OMIT,
        post_source: typing.Optional[CreateAdsRequestPostSource] = OMIT,
        primary_texts: typing.Optional[typing.Sequence[str]] = OMIT,
        social_accounts: typing.Optional[typing.Sequence[CreateAdsRequestSocialAccountsItem]] = OMIT,
        title: typing.Optional[str] = OMIT,
        url: typing.Optional[str] = OMIT,
        url_parameters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Ad:
        """
        Creates an ad in an ad group.

        Parameters
        ----------
        ad_group : typing.Optional[typing.Dict[str, typing.Any]]
            An inline ad group to create (same shape as POST /ad_groups, including ad_campaign_id). Creates the ad group and the ad together. Provide this OR ad_group_id.

        ad_group_id : typing.Optional[str]
            The existing ad group to create the ad in. Provide this OR ad_group, not both.

        call_to_action : typing.Optional[CreateAdsRequestCallToAction]
            The call-to-action button shown on the ad.

        creatives : typing.Optional[typing.Sequence[CreateAdsRequestCreativesItem]]
            The ad's creative assets. Each entry is an uploaded file id with an optional format; omit format for the original asset. Two or more entries with no format become a carousel (2-10 attachments), in order, sharing the ad's copy.

        descriptions : typing.Optional[typing.Sequence[str]]
            The description variants shown on the ad.

        headlines : typing.Optional[typing.Sequence[str]]
            The headline variants shown on the ad.

        lead_form : typing.Optional[CreateAdsRequestLeadForm]
            Instant lead form for the ad. Only allowed when the ad group's conversion_location is an instant-form destination (instant_forms, instant_forms_and_messenger, website_and_instant_forms). Mutually exclusive with lead_form_id.

        lead_form_id : typing.Optional[str]
            Use an existing instant form instead of creating one — the form's platform ID, from a form already on the ad's Facebook page. Only allowed when the ad group's conversion_location is an instant-form destination. Mutually exclusive with lead_form.

        messaging_config : typing.Optional[CreateAdsRequestMessagingConfig]
            Click-to-message welcome copy: the greeting (message) and the ice-breaker prompt (keyword).

        multi_advertiser_ads : typing.Optional[bool]
            Whether the ad can appear alongside other advertisers' ads in the same unit. Defaults to true.

        post_id : typing.Optional[str]
            Promote an existing post instead of uploading creatives — a Facebook post or Instagram media id. Mutually exclusive with creatives. Pair with post_source.

        post_source : typing.Optional[CreateAdsRequestPostSource]
            Identifies the network that owns `post_id`. The source is inferred from the ID shape when omitted.

        primary_texts : typing.Optional[typing.Sequence[str]]
            The primary text variants shown in the ad body.

        social_accounts : typing.Optional[typing.Sequence[CreateAdsRequestSocialAccountsItem]]
            The social accounts the ad runs under — a connected Facebook page and, optionally, an Instagram profile.

        title : typing.Optional[str]
            The display name of the ad.

        url : typing.Optional[str]
            The URL the ad links to. Query parameters are merged into url_parameters, so the stored URL is always bare.

        url_parameters : typing.Optional[typing.Dict[str, typing.Any]]
            Query parameters to append to the destination URL, keyed by parameter name. Merged with any query string on `url`. Whop adds its own click-attribution parameters; those are reserved and rejected if you set them (utm_meta_ad_id, utm_meta_adset_id, utm_meta_campaign_id, utm_source, utm_placement, utm_medium, utm_content, utm_adset, utm_whop, wacid, wasid, waid, tw_source, tw_adid).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad created from an existing post

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ads.create()


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            ad_group=ad_group,
            ad_group_id=ad_group_id,
            call_to_action=call_to_action,
            creatives=creatives,
            descriptions=descriptions,
            headlines=headlines,
            lead_form=lead_form,
            lead_form_id=lead_form_id,
            messaging_config=messaging_config,
            multi_advertiser_ads=multi_advertiser_ads,
            post_id=post_id,
            post_source=post_source,
            primary_texts=primary_texts,
            social_accounts=social_accounts,
            title=title,
            url=url,
            url_parameters=url_parameters,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(
        self,
        id: str,
        *,
        stats_from: typing.Optional[str] = None,
        stats_to: typing.Optional[str] = None,
        time_zone: typing.Optional[str] = None,
        attribution_model: typing.Optional[RetrieveAdsRequestAttributionModel] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Ad:
        """
        Retrieves a single ad with stats over the requested window.

        Parameters
        ----------
        id : str
            The ad ID.

        stats_from : typing.Optional[str]
            Start of the stats window.

        stats_to : typing.Optional[str]
            End of the stats window.

        time_zone : typing.Optional[str]
            IANA timezone the stats window is interpreted in. Defaults to UTC.

        attribution_model : typing.Optional[RetrieveAdsRequestAttributionModel]
            Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ads.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(
            id,
            stats_from=stats_from,
            stats_to=stats_to,
            time_zone=time_zone,
            attribution_model=attribution_model,
            request_options=request_options,
        )
        return _response.data

    async def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeleteAdsResponse:
        """
        Deletes an ad.

        Parameters
        ----------
        id : str
            The ad ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteAdsResponse
            ad deleted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ads.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        call_to_action: typing.Optional[UpdateAdsRequestCallToAction] = OMIT,
        creatives: typing.Optional[typing.Sequence[UpdateAdsRequestCreativesItem]] = OMIT,
        descriptions: typing.Optional[typing.Sequence[str]] = OMIT,
        headlines: typing.Optional[typing.Sequence[str]] = OMIT,
        lead_form: typing.Optional[UpdateAdsRequestLeadForm] = OMIT,
        lead_form_id: typing.Optional[str] = OMIT,
        messaging_config: typing.Optional[UpdateAdsRequestMessagingConfig] = OMIT,
        multi_advertiser_ads: typing.Optional[bool] = OMIT,
        post_id: typing.Optional[str] = OMIT,
        post_source: typing.Optional[UpdateAdsRequestPostSource] = OMIT,
        primary_texts: typing.Optional[typing.Sequence[str]] = OMIT,
        social_accounts: typing.Optional[typing.Sequence[UpdateAdsRequestSocialAccountsItem]] = OMIT,
        title: typing.Optional[str] = OMIT,
        url: typing.Optional[str] = OMIT,
        url_parameters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Ad:
        """
        Updates an ad's editable fields.

        Parameters
        ----------
        id : str
            The ad ID.

        call_to_action : typing.Optional[UpdateAdsRequestCallToAction]
            The call-to-action button shown on the ad.

        creatives : typing.Optional[typing.Sequence[UpdateAdsRequestCreativesItem]]
            The ad's creative assets. Each entry is an uploaded file id with an optional format; omit format for the original asset. Replaces a live ad's creative on the platform. Two or more entries with no format replace it with a carousel (2-10 attachments), in order, sharing the ad's copy.

        descriptions : typing.Optional[typing.Sequence[str]]
            The description variants shown on the ad.

        headlines : typing.Optional[typing.Sequence[str]]
            The headline variants shown on the ad.

        lead_form : typing.Optional[UpdateAdsRequestLeadForm]
            Instant lead form for the ad. Only allowed when the ad group's conversion_location is an instant-form destination (instant_forms, instant_forms_and_messenger, website_and_instant_forms). Mutually exclusive with lead_form_id.

        lead_form_id : typing.Optional[str]
            Use an existing instant form instead of creating one — the form's platform ID, from a form already on the ad's Facebook page. Only allowed when the ad group's conversion_location is an instant-form destination. Mutually exclusive with lead_form. Replaces a stored lead_form.

        messaging_config : typing.Optional[UpdateAdsRequestMessagingConfig]
            Click-to-message welcome copy: the greeting (message) and the ice-breaker prompt (keyword).

        multi_advertiser_ads : typing.Optional[bool]
            Whether the ad can appear alongside other advertisers' ads in the same unit. Defaults to true.

        post_id : typing.Optional[str]
            Promote an existing post instead of uploading creatives — a Facebook post or Instagram media id. Mutually exclusive with creatives. Pair with post_source.

        post_source : typing.Optional[UpdateAdsRequestPostSource]
            Identifies the network that owns `post_id`. The source is inferred from the ID shape when omitted.

        primary_texts : typing.Optional[typing.Sequence[str]]
            The primary text variants shown in the ad body.

        social_accounts : typing.Optional[typing.Sequence[UpdateAdsRequestSocialAccountsItem]]
            The social accounts the ad runs under — a connected Facebook page and, optionally, an Instagram profile.

        title : typing.Optional[str]
            The display name of the ad.

        url : typing.Optional[str]
            The URL the ad links to. Query parameters are merged into url_parameters, so the stored URL is always bare.

        url_parameters : typing.Optional[typing.Dict[str, typing.Any]]
            Query parameters to append to the destination URL, keyed by parameter name. Merged with any query string on `url`. Whop adds its own click-attribution parameters; those are reserved and rejected if you set them (utm_meta_ad_id, utm_meta_adset_id, utm_meta_campaign_id, utm_source, utm_placement, utm_medium, utm_content, utm_adset, utm_whop, wacid, wasid, waid, tw_source, tw_adid).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad switched to an existing post

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ads.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            call_to_action=call_to_action,
            creatives=creatives,
            descriptions=descriptions,
            headlines=headlines,
            lead_form=lead_form,
            lead_form_id=lead_form_id,
            messaging_config=messaging_config,
            multi_advertiser_ads=multi_advertiser_ads,
            post_id=post_id,
            post_source=post_source,
            primary_texts=primary_texts,
            social_accounts=social_accounts,
            title=title,
            url=url,
            url_parameters=url_parameters,
            request_options=request_options,
        )
        return _response.data

    async def duplicate(
        self,
        id: str,
        *,
        count: typing.Optional[int] = OMIT,
        preserve_engagement: typing.Optional[bool] = OMIT,
        target_ad_group_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DuplicateAdsResponse:
        """
        Copies the ad into its own ad group, or into target_ad_group_id (which must belong to the same account and be compatible with the ad). Copies keep the source ad's active/paused state.

        Parameters
        ----------
        id : str
            The ad ID.

        count : typing.Optional[int]
            Number of copies to create (1-10). Defaults to 1.

        preserve_engagement : typing.Optional[bool]
            Whether the copies keep the original post's engagement (likes, comments, shares). Defaults to false.

        target_ad_group_id : typing.Optional[str]
            Ad group to duplicate into. Defaults to the ad's own ad group.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DuplicateAdsResponse
            ad duplicated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ads.duplicate(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.duplicate(
            id,
            count=count,
            preserve_engagement=preserve_engagement,
            target_ad_group_id=target_ad_group_id,
            request_options=request_options,
        )
        return _response.data

    async def pause(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Ad:
        """
        Pauses an active ad.

        Parameters
        ----------
        id : str
            The ad ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad paused

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ads.pause(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.pause(id, request_options=request_options)
        return _response.data

    async def unpause(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Ad:
        """
        Resumes a paused ad.

        Parameters
        ----------
        id : str
            The ad ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Ad
            ad unpaused

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ads.unpause(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unpause(id, request_options=request_options)
        return _response.data
