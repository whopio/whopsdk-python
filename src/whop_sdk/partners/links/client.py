# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.pagination import AsyncPager, SyncPager
from ...core.request_options import RequestOptions
from ...types.partner_reward_link import PartnerRewardLink
from .raw_client import AsyncRawLinksClient, RawLinksClient
from .types.list_links_request_status_item import ListLinksRequestStatusItem
from .types.list_links_response import ListLinksResponse


class LinksClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawLinksClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawLinksClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawLinksClient
        """
        return self._raw_client

    def list(
        self,
        *,
        status: typing.Optional[
            typing.Union[ListLinksRequestStatusItem, typing.Sequence[ListLinksRequestStatusItem]]
        ] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PartnerRewardLink, ListLinksResponse]:
        """
        Returns the authenticated user's standard referral URL and a page of their balance reward links, newest first. Expired and fully claimed rewards are included by default; deleted rewards are excluded. Filter status to narrow the promotion links. Users do not need to be enrolled to retrieve their links.

        Parameters
        ----------
        status : typing.Optional[typing.Union[ListLinksRequestStatusItem, typing.Sequence[ListLinksRequestStatusItem]]]
            Filter promotion links by availability. Repeat the status parameter for multiple values.

        first : typing.Optional[int]
            Number of results to return from the start of the range.

        after : typing.Optional[str]
            Return results after this cursor. Use `page_info.end_cursor` from the previous response to fetch the next page.

        last : typing.Optional[int]
            Number of results to return from the end of the range.

        before : typing.Optional[str]
            Return results before this cursor. Use `page_info.start_cursor` from the previous response to fetch the previous page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PartnerRewardLink, ListLinksResponse]
            partner links listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.partners.links.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            status=status, first=first, after=after, last=last, before=before, request_options=request_options
        )


class AsyncLinksClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawLinksClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawLinksClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawLinksClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        status: typing.Optional[
            typing.Union[ListLinksRequestStatusItem, typing.Sequence[ListLinksRequestStatusItem]]
        ] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PartnerRewardLink, ListLinksResponse]:
        """
        Returns the authenticated user's standard referral URL and a page of their balance reward links, newest first. Expired and fully claimed rewards are included by default; deleted rewards are excluded. Filter status to narrow the promotion links. Users do not need to be enrolled to retrieve their links.

        Parameters
        ----------
        status : typing.Optional[typing.Union[ListLinksRequestStatusItem, typing.Sequence[ListLinksRequestStatusItem]]]
            Filter promotion links by availability. Repeat the status parameter for multiple values.

        first : typing.Optional[int]
            Number of results to return from the start of the range.

        after : typing.Optional[str]
            Return results after this cursor. Use `page_info.end_cursor` from the previous response to fetch the next page.

        last : typing.Optional[int]
            Number of results to return from the end of the range.

        before : typing.Optional[str]
            Return results before this cursor. Use `page_info.start_cursor` from the previous response to fetch the previous page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PartnerRewardLink, ListLinksResponse]
            partner links listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.partners.links.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            status=status, first=first, after=after, last=last, before=before, request_options=request_options
        )
