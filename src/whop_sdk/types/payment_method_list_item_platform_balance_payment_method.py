# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .payment_method_list_item_platform_balance_payment_method_icons import (
    PaymentMethodListItemPlatformBalancePaymentMethodIcons,
)
from .payment_method_list_item_platform_balance_payment_method_platform_balance import (
    PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalance,
)
from .payment_method_types import PaymentMethodTypes


class PaymentMethodListItemPlatformBalancePaymentMethod(UniversalBaseModel):
    """
    A Whop balance the buyer can pay with — their own, or an account's they hold permission to spend. Charged by naming its ledger id on a `saved` confirmation token — it is a live wallet, not a stored credential, so it cannot be vaulted or charged off-session.
    """

    created_at: dt.datetime = pydantic.Field()
    """
    The time of the event in ISO 8601 UTC format with millisecond precision
    """

    icons: PaymentMethodListItemPlatformBalancePaymentMethodIcons = pydantic.Field()
    """
    Every rendition of the icon to display this payment method with. A saved card carries its brand's icon (Visa, Mastercard, ...) rather than the generic card art.
    """

    id: str = pydantic.Field()
    """
    Represents a unique identifier that is Base64 obfuscated. It is often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"VXNlci0xMA=="`) or integer (such as `4`) input value will be accepted as an ID.
    """

    payment_method_type: PaymentMethodTypes = pydantic.Field()
    """
    The type of payment instrument stored on file (e.g., card, us_bank_account, cashapp, ideal, sepa_debit).
    """

    platform_balance: PaymentMethodListItemPlatformBalancePaymentMethodPlatformBalance = pydantic.Field()
    """
    What is available to spend, and whether the account may spend it.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
