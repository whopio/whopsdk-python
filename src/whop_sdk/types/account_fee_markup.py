# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .account_fee_markup_source import AccountFeeMarkupSource
from .account_fee_markup_unadjustable_reason import AccountFeeMarkupUnadjustableReason
from .account_fee_rate import AccountFeeRate
from .money import Money


class AccountFeeMarkup(UniversalBaseModel):
    adjustable: bool = pydantic.Field()
    """
    Whether the caller may change this markup through `PATCH`. True for the platform's team holding the `company:update_child_fees` scope.
    """

    default: AccountFeeRate = pydantic.Field()
    """
    What applies if this row is cleared: the platform's default for all its connected accounts, or zero.
    """

    fixed: Money = pydantic.Field()
    """
    The amount the platform adds per event. Zero when no markup is set.
    """

    maximum: AccountFeeRate = pydantic.Field()
    """
    The highest markup the platform may set.
    """

    percentage: float = pydantic.Field()
    """
    The percentage of the transaction the platform adds, where `2` means 2%. `0` when no markup is set.
    """

    source: AccountFeeMarkupSource = pydantic.Field()
    """
    `custom` when a row is set at this level, `default` when the rate falls through to the platform default or zero.
    """

    unadjustable_reason: typing.Optional[AccountFeeMarkupUnadjustableReason] = pydantic.Field(default=None)
    """
    Why the caller may not change this markup, or `null` when `adjustable`.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
