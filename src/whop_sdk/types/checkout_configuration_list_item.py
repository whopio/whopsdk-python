# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .checkout_configuration_list_item_payment_method_configuration import (
    CheckoutConfigurationListItemPaymentMethodConfiguration,
)
from .checkout_configuration_list_item_plan import CheckoutConfigurationListItemPlan
from .checkout_modes import CheckoutModes
from .currencies import Currencies


class CheckoutConfigurationListItem(UniversalBaseModel):
    """
    A checkout configuration is a reusable configuration for a checkout, including the plan, affiliate, and custom metadata. Payments and memberships created from a checkout session inherit its metadata.
    """

    affiliate_code: typing.Optional[str] = pydantic.Field(default=None)
    """
    The affiliate code to use for the checkout configuration
    """

    company_id: str = pydantic.Field()
    """
    The ID of the company to use for the checkout configuration
    """

    currency: typing.Optional[Currencies] = pydantic.Field(default=None)
    """
    The currency to use for the configuration when in 'setup' mode. This is used to target which currency specific payment methods are available. If not provided, it will default to 'usd' when in setup mode.
    """

    id: str = pydantic.Field()
    """
    The unique identifier for the checkout session.
    """

    metadata: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    The metadata to use for the checkout configuration
    """

    mode: CheckoutModes = pydantic.Field()
    """
    The mode of the checkout session.
    """

    payment_method_configuration: typing.Optional[CheckoutConfigurationListItemPaymentMethodConfiguration] = (
        pydantic.Field(default=None)
    )
    """
    The explicit payment method configuration for the session, if any. This currently only works in 'setup' mode. Use the plan's payment_method_configuration for payment method.
    """

    plan: typing.Optional[CheckoutConfigurationListItemPlan] = pydantic.Field(default=None)
    """
    The plan to use for the checkout configuration
    """

    purchase_url: str = pydantic.Field()
    """
    A URL you can send to customers to complete a checkout. It looks like `/checkout/ch_xxxx/`
    """

    redirect_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    The URL to redirect the user to after the checkout configuration is created
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
