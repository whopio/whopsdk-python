# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .payment_instrument_card import PaymentInstrumentCard
from .payment_method_icons import PaymentMethodIcons


class PaymentInstrument(UniversalBaseModel):
    card: typing.Optional[PaymentInstrumentCard] = pydantic.Field(default=None)
    """
    Card payments only: the card's network and last four.
    """

    display_name: str = pydantic.Field()
    """
    Buyer-facing instrument name — "Visa •••• 4242" when the card surfaced, else the method's own name ("Klarna").
    """

    icons: PaymentMethodIcons = pydantic.Field()
    """
    The standard icon set: square and card shapes, each in light and dark colorways.
    """

    installment_count: typing.Optional[float] = pydantic.Field(default=None)
    """
    Installment methods only: how many payments the charge splits into. Data, not copy — compose and translate the label client-side.
    """

    payment_method_type: str = pydantic.Field()
    """
    The payment method type identifier, e.g. `card`, `klarna`, `apple_pay`.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
