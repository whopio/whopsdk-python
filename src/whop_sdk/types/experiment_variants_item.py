# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .experiment_resource_reference import ExperimentResourceReference


class ExperimentVariantsItem(UniversalBaseModel):
    name: str = pydantic.Field()
    """
    Treatment identifier. `control` is reserved — it is the implicit remainder.
    """

    ranges: typing.Optional[typing.List[typing.List[int]]] = pydantic.Field(default=None)
    """
    Granted bucket ranges (1% units, end-exclusive) recording this arm's allocation history. Server-managed; ranges are only ever appended, which is what keeps assignments permanent.
    """

    related_resource: typing.Optional[ExperimentResourceReference] = None
    weight: int = pydantic.Field()
    """
    Percentage of all users assigned to this treatment, 1–100. All weights together sum to at most 100; the remainder is control.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
