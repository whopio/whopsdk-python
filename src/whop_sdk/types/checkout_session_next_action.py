# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .checkout_session_await_claim_action_state import CheckoutSessionAwaitClaimActionState
from .checkout_session_complete_action_kind import CheckoutSessionCompleteActionKind
from .checkout_session_upgrade_authentication_action_sign_in_intent import (
    CheckoutSessionUpgradeAuthenticationActionSignInIntent,
)


class CheckoutSessionNextAction_Authenticate(UniversalBaseModel):
    """
    The ceremony to run. `type` picks the shape, and each type carries only the fields it needs, so switching on `type` gives you exactly that action's payload. Every type carries `blocking`, which says whether the sale depends on this one.
    """

    type: typing.Literal["authenticate"] = "authenticate"
    blocking: bool

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CheckoutSessionNextAction_AwaitClaim(UniversalBaseModel):
    """
    The ceremony to run. `type` picks the shape, and each type carries only the fields it needs, so switching on `type` gives you exactly that action's payload. Every type carries `blocking`, which says whether the sale depends on this one.
    """

    type: typing.Literal["await_claim"] = "await_claim"
    blocking: bool
    state: CheckoutSessionAwaitClaimActionState

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CheckoutSessionNextAction_Complete(UniversalBaseModel):
    """
    The ceremony to run. `type` picks the shape, and each type carries only the fields it needs, so switching on `type` gives you exactly that action's payload. Every type carries `blocking`, which says whether the sale depends on this one.
    """

    type: typing.Literal["complete"] = "complete"
    blocking: bool
    client_secret: str
    kind: CheckoutSessionCompleteActionKind

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CheckoutSessionNextAction_Redirect(UniversalBaseModel):
    """
    The ceremony to run. `type` picks the shape, and each type carries only the fields it needs, so switching on `type` gives you exactly that action's payload. Every type carries `blocking`, which says whether the sale depends on this one.
    """

    type: typing.Literal["redirect"] = "redirect"
    blocking: bool
    destination_url: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CheckoutSessionNextAction_UpgradeAuthentication(UniversalBaseModel):
    """
    The ceremony to run. `type` picks the shape, and each type carries only the fields it needs, so switching on `type` gives you exactly that action's payload. Every type carries `blocking`, which says whether the sale depends on this one.
    """

    type: typing.Literal["upgrade_authentication"] = "upgrade_authentication"
    blocking: bool
    email: str
    session_intent_id: typing.Optional[str] = None
    sign_in_intent: CheckoutSessionUpgradeAuthenticationActionSignInIntent

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CheckoutSessionNextAction_VerifyPhone(UniversalBaseModel):
    """
    The ceremony to run. `type` picks the shape, and each type carries only the fields it needs, so switching on `type` gives you exactly that action's payload. Every type carries `blocking`, which says whether the sale depends on this one.
    """

    type: typing.Literal["verify_phone"] = "verify_phone"
    blocking: bool

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CheckoutSessionNextAction_WaitForPayment(UniversalBaseModel):
    """
    The ceremony to run. `type` picks the shape, and each type carries only the fields it needs, so switching on `type` gives you exactly that action's payload. Every type carries `blocking`, which says whether the sale depends on this one.
    """

    type: typing.Literal["wait_for_payment"] = "wait_for_payment"
    blocking: bool
    poll_after_seconds: int

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


CheckoutSessionNextAction = typing_extensions.Annotated[
    typing.Union[
        CheckoutSessionNextAction_Authenticate,
        CheckoutSessionNextAction_AwaitClaim,
        CheckoutSessionNextAction_Complete,
        CheckoutSessionNextAction_Redirect,
        CheckoutSessionNextAction_UpgradeAuthentication,
        CheckoutSessionNextAction_VerifyPhone,
        CheckoutSessionNextAction_WaitForPayment,
    ],
    pydantic.Field(discriminator="type"),
]
