# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .money import Money


class CheckoutSessionBreakdownUpcoming_Installments(UniversalBaseModel):
    """
    One amount owed after today. `type` picks the shape — `one_time` is a single later charge, `recurring` an open-ended subscription, `installments` a fixed count of payments — and each variant carries only its own fields, so switching on `type` gives you exactly that entry's payload.
    """

    type: typing.Literal["installments"] = "installments"
    amount: Money
    charge_at: str
    description: str
    interval_days: int
    post_promo_amount: typing.Optional[Money] = None
    remaining: int
    trial_days: typing.Optional[int] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CheckoutSessionBreakdownUpcoming_OneTime(UniversalBaseModel):
    """
    One amount owed after today. `type` picks the shape — `one_time` is a single later charge, `recurring` an open-ended subscription, `installments` a fixed count of payments — and each variant carries only its own fields, so switching on `type` gives you exactly that entry's payload.
    """

    type: typing.Literal["one_time"] = "one_time"
    amount: Money
    charge_at: str
    description: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CheckoutSessionBreakdownUpcoming_Recurring(UniversalBaseModel):
    """
    One amount owed after today. `type` picks the shape — `one_time` is a single later charge, `recurring` an open-ended subscription, `installments` a fixed count of payments — and each variant carries only its own fields, so switching on `type` gives you exactly that entry's payload.
    """

    type: typing.Literal["recurring"] = "recurring"
    amount: Money
    charge_at: str
    description: str
    interval_days: int
    post_promo_amount: typing.Optional[Money] = None
    trial_days: typing.Optional[int] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


CheckoutSessionBreakdownUpcoming = typing_extensions.Annotated[
    typing.Union[
        CheckoutSessionBreakdownUpcoming_Installments,
        CheckoutSessionBreakdownUpcoming_OneTime,
        CheckoutSessionBreakdownUpcoming_Recurring,
    ],
    pydantic.Field(discriminator="type"),
]
