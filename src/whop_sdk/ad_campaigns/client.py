# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.ad_campaign import AdCampaign
from .raw_client import AsyncRawAdCampaignsClient, RawAdCampaignsClient
from .types.create_ad_campaigns_request_bid_type import CreateAdCampaignsRequestBidType
from .types.create_ad_campaigns_request_budget_optimization import CreateAdCampaignsRequestBudgetOptimization
from .types.create_ad_campaigns_request_budget_type import CreateAdCampaignsRequestBudgetType
from .types.create_ad_campaigns_request_objective import CreateAdCampaignsRequestObjective
from .types.create_ad_campaigns_request_platform import CreateAdCampaignsRequestPlatform
from .types.create_ad_campaigns_request_special_ad_categories_item import (
    CreateAdCampaignsRequestSpecialAdCategoriesItem,
)
from .types.delete_ad_campaigns_response import DeleteAdCampaignsResponse
from .types.duplicate_ad_campaigns_response import DuplicateAdCampaignsResponse
from .types.list_ad_campaigns_request_attribution_model import ListAdCampaignsRequestAttributionModel
from .types.list_ad_campaigns_request_direction import ListAdCampaignsRequestDirection
from .types.list_ad_campaigns_request_order import ListAdCampaignsRequestOrder
from .types.list_ad_campaigns_request_status import ListAdCampaignsRequestStatus
from .types.list_ad_campaigns_response import ListAdCampaignsResponse
from .types.retrieve_ad_campaigns_request_attribution_model import RetrieveAdCampaignsRequestAttributionModel
from .types.update_ad_campaigns_request_bid_type import UpdateAdCampaignsRequestBidType
from .types.update_ad_campaigns_request_budget_optimization import UpdateAdCampaignsRequestBudgetOptimization
from .types.update_ad_campaigns_request_special_ad_categories_item import (
    UpdateAdCampaignsRequestSpecialAdCategoriesItem,
)
from .types.update_ad_campaigns_request_status import UpdateAdCampaignsRequestStatus

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AdCampaignsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAdCampaignsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAdCampaignsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAdCampaignsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        status: typing.Optional[ListAdCampaignsRequestStatus] = None,
        query: typing.Optional[str] = None,
        order: typing.Optional[ListAdCampaignsRequestOrder] = None,
        direction: typing.Optional[ListAdCampaignsRequestDirection] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        stats_from: typing.Optional[str] = None,
        stats_to: typing.Optional[str] = None,
        time_zone: typing.Optional[str] = None,
        attribution_model: typing.Optional[ListAdCampaignsRequestAttributionModel] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[AdCampaign, ListAdCampaignsResponse]:
        """
        Lists the ad campaigns for an account, with stats over the requested window.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The account the campaigns belong to. Defaults to the account-scoped key's own account.

        status : typing.Optional[ListAdCampaignsRequestStatus]
            Only return campaigns with this status.

        query : typing.Optional[str]
            Filter campaigns by a title or ID substring.

        order : typing.Optional[ListAdCampaignsRequestOrder]
            The field to sort by. Defaults to created_at. Stat columns (spend, impressions, …) rank over the stats_from/stats_to window across the whole list, not just the current page. results, cost_per_result and return_on_ad_spend rank by the same Whop pixel-attributed values the response reports.

        direction : typing.Optional[ListAdCampaignsRequestDirection]
            The sort direction. Defaults to desc.

        created_before : typing.Optional[str]
            Only return campaigns created before this timestamp.

        created_after : typing.Optional[str]
            Only return campaigns created after this timestamp.

        stats_from : typing.Optional[str]
            Start of the stats window. Defaults to all-time.

        stats_to : typing.Optional[str]
            End of the stats window. Defaults to now.

        time_zone : typing.Optional[str]
            IANA timezone (e.g. America/New_York) the stats window is interpreted in. Bare stats_from/stats_to dates resolve to day boundaries on this clock. Defaults to UTC.

        attribution_model : typing.Optional[ListAdCampaignsRequestAttributionModel]
            Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise.

        first : typing.Optional[int]
            The number of campaigns to return.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            The number of campaigns to return from the end of the range.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[AdCampaign, ListAdCampaignsResponse]
            ad campaigns listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.ad_campaigns.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            status=status,
            query=query,
            order=order,
            direction=direction,
            created_before=created_before,
            created_after=created_after,
            stats_from=stats_from,
            stats_to=stats_to,
            time_zone=time_zone,
            attribution_model=attribution_model,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        objective: CreateAdCampaignsRequestObjective,
        platform: CreateAdCampaignsRequestPlatform,
        title: str,
        account_id: typing.Optional[str] = OMIT,
        bid_type: typing.Optional[CreateAdCampaignsRequestBidType] = OMIT,
        budget_amount: typing.Optional[float] = OMIT,
        budget_optimization: typing.Optional[CreateAdCampaignsRequestBudgetOptimization] = OMIT,
        budget_type: typing.Optional[CreateAdCampaignsRequestBudgetType] = OMIT,
        desired_cost_per_result: typing.Optional[float] = OMIT,
        ends_at: typing.Optional[str] = OMIT,
        special_ad_categories: typing.Optional[typing.Sequence[CreateAdCampaignsRequestSpecialAdCategoriesItem]] = OMIT,
        starts_at: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AdCampaign:
        """
        Creates an ad campaign for an account.

        Parameters
        ----------
        objective : CreateAdCampaignsRequestObjective
            The goal the campaign optimizes toward.

        platform : CreateAdCampaignsRequestPlatform
            The ad network the campaign runs on.

        title : str
            The title of the campaign.

        account_id : typing.Optional[str]
            The account to create the campaign under. Defaults to the account-scoped key's own account.

        bid_type : typing.Optional[CreateAdCampaignsRequestBidType]
            How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, `maximum_target` never bids above a cap. Only for campaigns that own the budget.

        budget_amount : typing.Optional[float]
            The campaign's budget, in the ad account's currency. Required when budget_optimization is `ad_campaign`; omit when each ad group sets its own budget.

        budget_optimization : typing.Optional[CreateAdCampaignsRequestBudgetOptimization]
            Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). Defaults to `ad_group`.

        budget_type : typing.Optional[CreateAdCampaignsRequestBudgetType]
            Whether the budget is spent per day (`daily`) or over the campaign's full run (`lifetime`). Defaults to `daily`.

        desired_cost_per_result : typing.Optional[float]
            Cost per result to aim for (`average_target`) or never exceed (`maximum_target`). Only for campaigns that own the budget.

        ends_at : typing.Optional[str]
            When the campaign stops delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget.

        special_ad_categories : typing.Optional[typing.Sequence[CreateAdCampaignsRequestSpecialAdCategoriesItem]]
            Regulated categories the campaign falls under. Ads in these categories are subject to extra targeting restrictions.

        starts_at : typing.Optional[str]
            When the campaign starts delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ad_campaigns.create(
            objective="awareness",
            platform="meta",
            title="Now hiring mobile detailers — Austin",
        )
        """
        _response = self._raw_client.create(
            objective=objective,
            platform=platform,
            title=title,
            account_id=account_id,
            bid_type=bid_type,
            budget_amount=budget_amount,
            budget_optimization=budget_optimization,
            budget_type=budget_type,
            desired_cost_per_result=desired_cost_per_result,
            ends_at=ends_at,
            special_ad_categories=special_ad_categories,
            starts_at=starts_at,
            request_options=request_options,
        )
        return _response.data

    def retrieve(
        self,
        id: str,
        *,
        stats_from: typing.Optional[str] = None,
        stats_to: typing.Optional[str] = None,
        time_zone: typing.Optional[str] = None,
        attribution_model: typing.Optional[RetrieveAdCampaignsRequestAttributionModel] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AdCampaign:
        """
        Retrieves a single ad campaign with stats over the requested window.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        stats_from : typing.Optional[str]
            Start of the stats window.

        stats_to : typing.Optional[str]
            End of the stats window.

        time_zone : typing.Optional[str]
            IANA timezone the stats window is interpreted in. Defaults to UTC.

        attribution_model : typing.Optional[RetrieveAdCampaignsRequestAttributionModel]
            Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ad_campaigns.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(
            id,
            stats_from=stats_from,
            stats_to=stats_to,
            time_zone=time_zone,
            attribution_model=attribution_model,
            request_options=request_options,
        )
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeleteAdCampaignsResponse:
        """
        Deletes an ad campaign and archives it on the ad platform (cascades to ad groups and ads).

        Parameters
        ----------
        id : str
            The ad campaign ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteAdCampaignsResponse
            ad campaign deleted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ad_campaigns.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        bid_type: typing.Optional[UpdateAdCampaignsRequestBidType] = OMIT,
        budget_amount: typing.Optional[float] = OMIT,
        budget_optimization: typing.Optional[UpdateAdCampaignsRequestBudgetOptimization] = OMIT,
        ends_at: typing.Optional[str] = OMIT,
        special_ad_categories: typing.Optional[typing.Sequence[UpdateAdCampaignsRequestSpecialAdCategoriesItem]] = OMIT,
        starts_at: typing.Optional[str] = OMIT,
        status: typing.Optional[UpdateAdCampaignsRequestStatus] = OMIT,
        title: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AdCampaign:
        """
        Updates an ad campaign's editable fields (title, budget, schedule, bid strategy, special ad categories, and, before launch, budget optimization), and launches a draft campaign by setting status to active. Objective, budget type and desired cost per result are fixed at creation and cannot be changed.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        bid_type : typing.Optional[UpdateAdCampaignsRequestBidType]
            How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, `maximum_target` never bids above a cap. Switching to `minimum_cost` clears the cap amounts stored on the campaign's ad groups. Only for campaigns that own the budget.

        budget_amount : typing.Optional[float]
            The campaign budget, in the account's currency. Interpreted as daily or lifetime per the campaign's existing budget type.

        budget_optimization : typing.Optional[UpdateAdCampaignsRequestBudgetOptimization]
            Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). Only changeable before the campaign is live on the ad network; switching to `ad_campaign` requires budget_amount in the same request, and switching to `ad_group` clears the campaign budget.

        ends_at : typing.Optional[str]
            When the campaign stops delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget.

        special_ad_categories : typing.Optional[typing.Sequence[UpdateAdCampaignsRequestSpecialAdCategoriesItem]]
            Regulated categories the campaign falls under. Editable on any campaign, draft or launched; pass an empty array to clear.

        starts_at : typing.Optional[str]
            When the campaign starts delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget.

        status : typing.Optional[UpdateAdCampaignsRequestStatus]
            Set to active to launch a draft campaign (moderates and pushes it live). Live-campaign pause and resume use the pause and unpause actions.

        title : typing.Optional[str]
            The name of the campaign.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign updated

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ad_campaigns.update(
            id="id",
        )
        """
        _response = self._raw_client.update(
            id,
            bid_type=bid_type,
            budget_amount=budget_amount,
            budget_optimization=budget_optimization,
            ends_at=ends_at,
            special_ad_categories=special_ad_categories,
            starts_at=starts_at,
            status=status,
            title=title,
            request_options=request_options,
        )
        return _response.data

    def duplicate(
        self,
        id: str,
        *,
        count: typing.Optional[int] = OMIT,
        preserve_engagement: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DuplicateAdCampaignsResponse:
        """
        Creates copies of the campaign in `duplicating` status and returns them; each copy transitions to `draft` once duplication completes. Poll each returned campaign until it leaves `duplicating` — a copy that could not be completed is deleted and returns 404.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        count : typing.Optional[int]
            Number of copies to create (1-10). Defaults to 1.

        preserve_engagement : typing.Optional[bool]
            Whether the copied ads keep the original posts' engagement (likes, comments, shares). Defaults to false.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DuplicateAdCampaignsResponse
            ad campaign duplication started

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ad_campaigns.duplicate(
            id="id",
        )
        """
        _response = self._raw_client.duplicate(
            id, count=count, preserve_engagement=preserve_engagement, request_options=request_options
        )
        return _response.data

    def pause(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AdCampaign:
        """
        Pauses an active ad campaign.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign paused

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ad_campaigns.pause(
            id="id",
        )
        """
        _response = self._raw_client.pause(id, request_options=request_options)
        return _response.data

    def retry_payment(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AdCampaign:
        """
        Retries billing for an ad campaign whose payment previously failed.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            payment retried

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ad_campaigns.retry_payment(
            id="id",
        )
        """
        _response = self._raw_client.retry_payment(id, request_options=request_options)
        return _response.data

    def unpause(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AdCampaign:
        """
        Resumes a paused ad campaign.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign unpaused

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.ad_campaigns.unpause(
            id="id",
        )
        """
        _response = self._raw_client.unpause(id, request_options=request_options)
        return _response.data


class AsyncAdCampaignsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAdCampaignsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAdCampaignsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAdCampaignsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        status: typing.Optional[ListAdCampaignsRequestStatus] = None,
        query: typing.Optional[str] = None,
        order: typing.Optional[ListAdCampaignsRequestOrder] = None,
        direction: typing.Optional[ListAdCampaignsRequestDirection] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        stats_from: typing.Optional[str] = None,
        stats_to: typing.Optional[str] = None,
        time_zone: typing.Optional[str] = None,
        attribution_model: typing.Optional[ListAdCampaignsRequestAttributionModel] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[AdCampaign, ListAdCampaignsResponse]:
        """
        Lists the ad campaigns for an account, with stats over the requested window.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The account the campaigns belong to. Defaults to the account-scoped key's own account.

        status : typing.Optional[ListAdCampaignsRequestStatus]
            Only return campaigns with this status.

        query : typing.Optional[str]
            Filter campaigns by a title or ID substring.

        order : typing.Optional[ListAdCampaignsRequestOrder]
            The field to sort by. Defaults to created_at. Stat columns (spend, impressions, …) rank over the stats_from/stats_to window across the whole list, not just the current page. results, cost_per_result and return_on_ad_spend rank by the same Whop pixel-attributed values the response reports.

        direction : typing.Optional[ListAdCampaignsRequestDirection]
            The sort direction. Defaults to desc.

        created_before : typing.Optional[str]
            Only return campaigns created before this timestamp.

        created_after : typing.Optional[str]
            Only return campaigns created after this timestamp.

        stats_from : typing.Optional[str]
            Start of the stats window. Defaults to all-time.

        stats_to : typing.Optional[str]
            End of the stats window. Defaults to now.

        time_zone : typing.Optional[str]
            IANA timezone (e.g. America/New_York) the stats window is interpreted in. Bare stats_from/stats_to dates resolve to day boundaries on this clock. Defaults to UTC.

        attribution_model : typing.Optional[ListAdCampaignsRequestAttributionModel]
            Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise.

        first : typing.Optional[int]
            The number of campaigns to return.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            The number of campaigns to return from the end of the range.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[AdCampaign, ListAdCampaignsResponse]
            ad campaigns listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.ad_campaigns.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            status=status,
            query=query,
            order=order,
            direction=direction,
            created_before=created_before,
            created_after=created_after,
            stats_from=stats_from,
            stats_to=stats_to,
            time_zone=time_zone,
            attribution_model=attribution_model,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        objective: CreateAdCampaignsRequestObjective,
        platform: CreateAdCampaignsRequestPlatform,
        title: str,
        account_id: typing.Optional[str] = OMIT,
        bid_type: typing.Optional[CreateAdCampaignsRequestBidType] = OMIT,
        budget_amount: typing.Optional[float] = OMIT,
        budget_optimization: typing.Optional[CreateAdCampaignsRequestBudgetOptimization] = OMIT,
        budget_type: typing.Optional[CreateAdCampaignsRequestBudgetType] = OMIT,
        desired_cost_per_result: typing.Optional[float] = OMIT,
        ends_at: typing.Optional[str] = OMIT,
        special_ad_categories: typing.Optional[typing.Sequence[CreateAdCampaignsRequestSpecialAdCategoriesItem]] = OMIT,
        starts_at: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AdCampaign:
        """
        Creates an ad campaign for an account.

        Parameters
        ----------
        objective : CreateAdCampaignsRequestObjective
            The goal the campaign optimizes toward.

        platform : CreateAdCampaignsRequestPlatform
            The ad network the campaign runs on.

        title : str
            The title of the campaign.

        account_id : typing.Optional[str]
            The account to create the campaign under. Defaults to the account-scoped key's own account.

        bid_type : typing.Optional[CreateAdCampaignsRequestBidType]
            How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, `maximum_target` never bids above a cap. Only for campaigns that own the budget.

        budget_amount : typing.Optional[float]
            The campaign's budget, in the ad account's currency. Required when budget_optimization is `ad_campaign`; omit when each ad group sets its own budget.

        budget_optimization : typing.Optional[CreateAdCampaignsRequestBudgetOptimization]
            Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). Defaults to `ad_group`.

        budget_type : typing.Optional[CreateAdCampaignsRequestBudgetType]
            Whether the budget is spent per day (`daily`) or over the campaign's full run (`lifetime`). Defaults to `daily`.

        desired_cost_per_result : typing.Optional[float]
            Cost per result to aim for (`average_target`) or never exceed (`maximum_target`). Only for campaigns that own the budget.

        ends_at : typing.Optional[str]
            When the campaign stops delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget.

        special_ad_categories : typing.Optional[typing.Sequence[CreateAdCampaignsRequestSpecialAdCategoriesItem]]
            Regulated categories the campaign falls under. Ads in these categories are subject to extra targeting restrictions.

        starts_at : typing.Optional[str]
            When the campaign starts delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ad_campaigns.create(
                objective="awareness",
                platform="meta",
                title="Now hiring mobile detailers — Austin",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            objective=objective,
            platform=platform,
            title=title,
            account_id=account_id,
            bid_type=bid_type,
            budget_amount=budget_amount,
            budget_optimization=budget_optimization,
            budget_type=budget_type,
            desired_cost_per_result=desired_cost_per_result,
            ends_at=ends_at,
            special_ad_categories=special_ad_categories,
            starts_at=starts_at,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(
        self,
        id: str,
        *,
        stats_from: typing.Optional[str] = None,
        stats_to: typing.Optional[str] = None,
        time_zone: typing.Optional[str] = None,
        attribution_model: typing.Optional[RetrieveAdCampaignsRequestAttributionModel] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AdCampaign:
        """
        Retrieves a single ad campaign with stats over the requested window.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        stats_from : typing.Optional[str]
            Start of the stats window.

        stats_to : typing.Optional[str]
            End of the stats window.

        time_zone : typing.Optional[str]
            IANA timezone the stats window is interpreted in. Defaults to UTC.

        attribution_model : typing.Optional[RetrieveAdCampaignsRequestAttributionModel]
            Attribution model the conversion stats count under (defaults to last_touch). Under both models a journey with any whop ad touch attributes to whop; the model picks which whop touch credits the entity and which non-whop source wins otherwise.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ad_campaigns.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(
            id,
            stats_from=stats_from,
            stats_to=stats_to,
            time_zone=time_zone,
            attribution_model=attribution_model,
            request_options=request_options,
        )
        return _response.data

    async def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteAdCampaignsResponse:
        """
        Deletes an ad campaign and archives it on the ad platform (cascades to ad groups and ads).

        Parameters
        ----------
        id : str
            The ad campaign ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteAdCampaignsResponse
            ad campaign deleted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ad_campaigns.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        bid_type: typing.Optional[UpdateAdCampaignsRequestBidType] = OMIT,
        budget_amount: typing.Optional[float] = OMIT,
        budget_optimization: typing.Optional[UpdateAdCampaignsRequestBudgetOptimization] = OMIT,
        ends_at: typing.Optional[str] = OMIT,
        special_ad_categories: typing.Optional[typing.Sequence[UpdateAdCampaignsRequestSpecialAdCategoriesItem]] = OMIT,
        starts_at: typing.Optional[str] = OMIT,
        status: typing.Optional[UpdateAdCampaignsRequestStatus] = OMIT,
        title: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AdCampaign:
        """
        Updates an ad campaign's editable fields (title, budget, schedule, bid strategy, special ad categories, and, before launch, budget optimization), and launches a draft campaign by setting status to active. Objective, budget type and desired cost per result are fixed at creation and cannot be changed.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        bid_type : typing.Optional[UpdateAdCampaignsRequestBidType]
            How delivery bids in the ad auction: `minimum_cost` gets the most results for the budget, `average_target` holds an average cost per result, `maximum_target` never bids above a cap. Switching to `minimum_cost` clears the cap amounts stored on the campaign's ad groups. Only for campaigns that own the budget.

        budget_amount : typing.Optional[float]
            The campaign budget, in the account's currency. Interpreted as daily or lifetime per the campaign's existing budget type.

        budget_optimization : typing.Optional[UpdateAdCampaignsRequestBudgetOptimization]
            Which level owns the budget: the whole campaign (`ad_campaign`) or each ad group individually (`ad_group`). Only changeable before the campaign is live on the ad network; switching to `ad_campaign` requires budget_amount in the same request, and switching to `ad_group` clears the campaign budget.

        ends_at : typing.Optional[str]
            When the campaign stops delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget.

        special_ad_categories : typing.Optional[typing.Sequence[UpdateAdCampaignsRequestSpecialAdCategoriesItem]]
            Regulated categories the campaign falls under. Editable on any campaign, draft or launched; pass an empty array to clear.

        starts_at : typing.Optional[str]
            When the campaign starts delivering, as an ISO 8601 timestamp. Only for campaigns that own the budget.

        status : typing.Optional[UpdateAdCampaignsRequestStatus]
            Set to active to launch a draft campaign (moderates and pushes it live). Live-campaign pause and resume use the pause and unpause actions.

        title : typing.Optional[str]
            The name of the campaign.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign updated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ad_campaigns.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            bid_type=bid_type,
            budget_amount=budget_amount,
            budget_optimization=budget_optimization,
            ends_at=ends_at,
            special_ad_categories=special_ad_categories,
            starts_at=starts_at,
            status=status,
            title=title,
            request_options=request_options,
        )
        return _response.data

    async def duplicate(
        self,
        id: str,
        *,
        count: typing.Optional[int] = OMIT,
        preserve_engagement: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DuplicateAdCampaignsResponse:
        """
        Creates copies of the campaign in `duplicating` status and returns them; each copy transitions to `draft` once duplication completes. Poll each returned campaign until it leaves `duplicating` — a copy that could not be completed is deleted and returns 404.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        count : typing.Optional[int]
            Number of copies to create (1-10). Defaults to 1.

        preserve_engagement : typing.Optional[bool]
            Whether the copied ads keep the original posts' engagement (likes, comments, shares). Defaults to false.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DuplicateAdCampaignsResponse
            ad campaign duplication started

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ad_campaigns.duplicate(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.duplicate(
            id, count=count, preserve_engagement=preserve_engagement, request_options=request_options
        )
        return _response.data

    async def pause(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AdCampaign:
        """
        Pauses an active ad campaign.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign paused

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ad_campaigns.pause(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.pause(id, request_options=request_options)
        return _response.data

    async def retry_payment(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AdCampaign:
        """
        Retries billing for an ad campaign whose payment previously failed.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            payment retried

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ad_campaigns.retry_payment(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retry_payment(id, request_options=request_options)
        return _response.data

    async def unpause(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AdCampaign:
        """
        Resumes a paused ad campaign.

        Parameters
        ----------
        id : str
            The ad campaign ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AdCampaign
            ad campaign unpaused

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ad_campaigns.unpause(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unpause(id, request_options=request_options)
        return _response.data
