# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.cashback_rule import CashbackRule
from .raw_client import AsyncRawCashbackRulesClient, RawCashbackRulesClient
from .types.list_cashback_rules_request_direction import ListCashbackRulesRequestDirection
from .types.list_cashback_rules_request_order import ListCashbackRulesRequestOrder
from .types.list_cashback_rules_response import ListCashbackRulesResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class CashbackRulesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCashbackRulesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCashbackRulesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCashbackRulesClient
        """
        return self._raw_client

    def create(
        self,
        *,
        merchant_category_code: str,
        merchant_name: str,
        rate_bps: int,
        starts_at: dt.datetime,
        description: typing.Optional[str] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        scoped_account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CashbackRule:
        """
        Creates a future-dated card cashback rule funded by the authenticated platform account. Requires payout:transfer_funds. Both the raw merchant name and four-digit MCC are required. Optionally limit the rule to one direct connected account. The funding account is derived from the credential and cannot be supplied. Creation does not transfer funds. Supports Idempotency-Key for safe retries.

        Parameters
        ----------
        merchant_category_code : str
            Four-digit MCC, including leading zeros. Must match together with merchant_name.

        merchant_name : str
            Raw merchant name reported by the card provider, not the enriched display name. Matched with the MCC; not a substring or wildcard.

        rate_bps : int
            Cashback rate in basis points: 500 means 5%.

        starts_at : dt.datetime
            Inclusive start, strictly later than the current time, as an ISO 8601 timestamp.

        description : typing.Optional[str]
            Optional description of the rule.

        expires_at : typing.Optional[dt.datetime]
            Exclusive end, strictly later than starts_at. Omit or set null for no expiration.

        scoped_account_id : typing.Optional[str]
            Account ID prefixed biz_ belonging to a direct connected account. Omit or set null to designate all direct connected accounts.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CashbackRule
            cashback rule created

        Examples
        --------
        import datetime

        from whop_sdk import Whop

        client = Whop(
            "2026-09-09-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.cashback_rules.create(
            merchant_category_code="5734",
            merchant_name="ACME SOFTWARE",
            rate_bps=500,
            starts_at=datetime.datetime.fromisoformat(
                "2026-01-01 12:00:00+00:00",
            ),
        )
        """
        _response = self._raw_client.create(
            merchant_category_code=merchant_category_code,
            merchant_name=merchant_name,
            rate_bps=rate_bps,
            starts_at=starts_at,
            description=description,
            expires_at=expires_at,
            scoped_account_id=scoped_account_id,
            request_options=request_options,
        )
        return _response.data

    def list(
        self,
        *,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListCashbackRulesRequestOrder] = None,
        direction: typing.Optional[ListCashbackRulesRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[CashbackRule, ListCashbackRulesResponse]:
        """
        Lists all cashback rules funded by the authenticated platform account. Includes scheduled, expired, and discarded rules. Requires payout:transfer:read. Account-scoped credentials are required; there is no caller-supplied funding-account filter.

        Parameters
        ----------
        first : typing.Optional[int]
            Number of rules to return from the start of the page.

        after : typing.Optional[str]
            Return rules after this cursor.

        last : typing.Optional[int]
            Number of rules to return from the end of the page.

        before : typing.Optional[str]
            Return rules before this cursor.

        order : typing.Optional[ListCashbackRulesRequestOrder]
            Field to sort by. Defaults to created_at.

        direction : typing.Optional[ListCashbackRulesRequestDirection]
            Sort direction. Defaults to desc.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[CashbackRule, ListCashbackRulesResponse]
            cashback rules listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-09-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.cashback_rules.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            first=first,
            after=after,
            last=last,
            before=before,
            order=order,
            direction=direction,
            request_options=request_options,
        )


class AsyncCashbackRulesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCashbackRulesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCashbackRulesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCashbackRulesClient
        """
        return self._raw_client

    async def create(
        self,
        *,
        merchant_category_code: str,
        merchant_name: str,
        rate_bps: int,
        starts_at: dt.datetime,
        description: typing.Optional[str] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        scoped_account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CashbackRule:
        """
        Creates a future-dated card cashback rule funded by the authenticated platform account. Requires payout:transfer_funds. Both the raw merchant name and four-digit MCC are required. Optionally limit the rule to one direct connected account. The funding account is derived from the credential and cannot be supplied. Creation does not transfer funds. Supports Idempotency-Key for safe retries.

        Parameters
        ----------
        merchant_category_code : str
            Four-digit MCC, including leading zeros. Must match together with merchant_name.

        merchant_name : str
            Raw merchant name reported by the card provider, not the enriched display name. Matched with the MCC; not a substring or wildcard.

        rate_bps : int
            Cashback rate in basis points: 500 means 5%.

        starts_at : dt.datetime
            Inclusive start, strictly later than the current time, as an ISO 8601 timestamp.

        description : typing.Optional[str]
            Optional description of the rule.

        expires_at : typing.Optional[dt.datetime]
            Exclusive end, strictly later than starts_at. Omit or set null for no expiration.

        scoped_account_id : typing.Optional[str]
            Account ID prefixed biz_ belonging to a direct connected account. Omit or set null to designate all direct connected accounts.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CashbackRule
            cashback rule created

        Examples
        --------
        import asyncio
        import datetime

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-09-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cashback_rules.create(
                merchant_category_code="5734",
                merchant_name="ACME SOFTWARE",
                rate_bps=500,
                starts_at=datetime.datetime.fromisoformat(
                    "2026-01-01 12:00:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            merchant_category_code=merchant_category_code,
            merchant_name=merchant_name,
            rate_bps=rate_bps,
            starts_at=starts_at,
            description=description,
            expires_at=expires_at,
            scoped_account_id=scoped_account_id,
            request_options=request_options,
        )
        return _response.data

    async def list(
        self,
        *,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListCashbackRulesRequestOrder] = None,
        direction: typing.Optional[ListCashbackRulesRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[CashbackRule, ListCashbackRulesResponse]:
        """
        Lists all cashback rules funded by the authenticated platform account. Includes scheduled, expired, and discarded rules. Requires payout:transfer:read. Account-scoped credentials are required; there is no caller-supplied funding-account filter.

        Parameters
        ----------
        first : typing.Optional[int]
            Number of rules to return from the start of the page.

        after : typing.Optional[str]
            Return rules after this cursor.

        last : typing.Optional[int]
            Number of rules to return from the end of the page.

        before : typing.Optional[str]
            Return rules before this cursor.

        order : typing.Optional[ListCashbackRulesRequestOrder]
            Field to sort by. Defaults to created_at.

        direction : typing.Optional[ListCashbackRulesRequestDirection]
            Sort direction. Defaults to desc.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[CashbackRule, ListCashbackRulesResponse]
            cashback rules listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-09-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.cashback_rules.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            first=first,
            after=after,
            last=last,
            before=before,
            order=order,
            direction=direction,
            request_options=request_options,
        )
