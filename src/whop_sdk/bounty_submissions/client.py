# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.bounty_submission import BountySubmission
from .raw_client import AsyncRawBountySubmissionsClient, RawBountySubmissionsClient
from .types.create_bounty_submissions_request_deliverable import CreateBountySubmissionsRequestDeliverable
from .types.create_bounty_submissions_request_metadata import CreateBountySubmissionsRequestMetadata
from .types.delete_bounty_submissions_response import DeleteBountySubmissionsResponse
from .types.list_bounty_submissions_request_direction import ListBountySubmissionsRequestDirection
from .types.list_bounty_submissions_request_order import ListBountySubmissionsRequestOrder
from .types.list_bounty_submissions_request_status import ListBountySubmissionsRequestStatus
from .types.list_bounty_submissions_response import ListBountySubmissionsResponse
from .types.submit_bounty_submissions_request_deliverable import SubmitBountySubmissionsRequestDeliverable

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class BountySubmissionsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawBountySubmissionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawBountySubmissionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawBountySubmissionsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        bounty_id: typing.Optional[str] = None,
        status: typing.Optional[ListBountySubmissionsRequestStatus] = None,
        created_after: typing.Optional[str] = None,
        created_before: typing.Optional[str] = None,
        order: typing.Optional[ListBountySubmissionsRequestOrder] = None,
        direction: typing.Optional[ListBountySubmissionsRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[BountySubmission, ListBountySubmissionsResponse]:
        """
        Lists bounty submissions visible to the credential — for a user token, the submissions they authored plus those on bounties they posted; for an account API key, the submissions on the account's bounties. For the anonymous view of one bounty's reviewed work, use the submissions list under the bounty instead.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Scope the list to submissions on this account's bounties (`biz_` tag). Requires read access to the account.

        bounty_id : typing.Optional[str]
            Only submissions on this bounty (`bnty_` tag).

        status : typing.Optional[ListBountySubmissionsRequestStatus]
            Filter by lifecycle state.

        created_after : typing.Optional[str]
            Only submissions created after this ISO 8601 timestamp.

        created_before : typing.Optional[str]
            Only submissions created before this ISO 8601 timestamp.

        order : typing.Optional[ListBountySubmissionsRequestOrder]
            Sort field.

        direction : typing.Optional[ListBountySubmissionsRequestDirection]
            Sort direction.

        first : typing.Optional[int]
            Number of submissions to return from the start of the window.

        after : typing.Optional[str]
            Cursor to paginate forwards from.

        last : typing.Optional[int]
            Number of submissions to return from the end of the window.

        before : typing.Optional[str]
            Cursor to paginate backwards from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[BountySubmission, ListBountySubmissionsResponse]
            bounty submissions listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.bounty_submissions.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            bounty_id=bounty_id,
            status=status,
            created_after=created_after,
            created_before=created_before,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        bounty_id: str,
        affiliate_code: typing.Optional[str] = OMIT,
        deliverable: typing.Optional[CreateBountySubmissionsRequestDeliverable] = OMIT,
        metadata: typing.Optional[CreateBountySubmissionsRequestMetadata] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BountySubmission:
        """
        Creates a submission on a workforce bounty. Include a `deliverable` payload — any combination of links and uploaded files, with at least one of the two — and the submission goes straight to review; create is the only step. For `data_capture` bounties, omit the deliverable: this starts a claimed attempt whose proof accumulates server-side, and the separate submit endpoint sends it to review once complete. Requires a user credential — account API keys cannot author submissions.

        Parameters
        ----------
        bounty_id : str
            The bounty to submit to (`bnty_` tag).

        affiliate_code : typing.Optional[str]
            Affiliate code crediting the referrer, when the worker arrived through one.

        deliverable : typing.Optional[CreateBountySubmissionsRequestDeliverable]
            The submitted work. Combine `urls`, `file_ids`, and `caption` freely; at least one link or file is required.

        metadata : typing.Optional[CreateBountySubmissionsRequestMetadata]
            Optional capture metadata describing where and how the footage was recorded. Persisted on the submission. On a `data_capture` bounty every field except `fov` is required whenever metadata is provided.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BountySubmission
            submission created with an inline deliverable

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.bounty_submissions.create(
            bounty_id="bnty_xxxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.create(
            bounty_id=bounty_id,
            affiliate_code=affiliate_code,
            deliverable=deliverable,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BountySubmission:
        """
        Retrieves one bounty submission the credential can see — one the caller authored, or one on a bounty they posted or their account owns. Reading another member's work on an account's bounty takes `account_id`, the same way the list does.

        Parameters
        ----------
        id : str
            The bounty submission to act on (`btys_` tag).

        account_id : typing.Optional[str]
            Read the submission as this account (`biz_` tag), scoping the lookup to its bounties rather than the caller's own work. Requires read access to the account. Without it the lookup covers only what the credential owns — the submissions the caller authored plus those on bounties they posted.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BountySubmission
            bounty submission returned

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.bounty_submissions.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, account_id=account_id, request_options=request_options)
        return _response.data

    def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteBountySubmissionsResponse:
        """
        Cancels the caller's own active attempt on a bounty and discards any accumulated capture clips. Only the worker who started the attempt can cancel it — account API keys cannot.

        Parameters
        ----------
        id : str
            The bounty submission to act on (`btys_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteBountySubmissionsResponse
            attempt cancelled

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.bounty_submissions.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def submit(
        self,
        id: str,
        *,
        deliverable: typing.Optional[SubmitBountySubmissionsRequestDeliverable] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BountySubmission:
        """
        Submits a claimed attempt for review. A livestream attempt needs an ended proof stream and can attach an optional `deliverable` — links, files, and a caption in any combination; if the attempt already went to review when its stream ended, the payload attaches to it once, until reviewers start voting. A data capture attempt instead needs enough validated clip time and takes no payload. Only the worker who started the attempt can submit it — account API keys cannot.

        Parameters
        ----------
        id : str
            The claimed attempt to submit for review (`btys_` tag).

        deliverable : typing.Optional[SubmitBountySubmissionsRequestDeliverable]
            Work to attach to the submission. Combine `urls`, `file_ids`, and `caption` freely; all are optional.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BountySubmission
            attempt submitted for review

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.bounty_submissions.submit(
            id="id",
        )
        """
        _response = self._raw_client.submit(id, deliverable=deliverable, request_options=request_options)
        return _response.data


class AsyncBountySubmissionsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawBountySubmissionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawBountySubmissionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawBountySubmissionsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        bounty_id: typing.Optional[str] = None,
        status: typing.Optional[ListBountySubmissionsRequestStatus] = None,
        created_after: typing.Optional[str] = None,
        created_before: typing.Optional[str] = None,
        order: typing.Optional[ListBountySubmissionsRequestOrder] = None,
        direction: typing.Optional[ListBountySubmissionsRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[BountySubmission, ListBountySubmissionsResponse]:
        """
        Lists bounty submissions visible to the credential — for a user token, the submissions they authored plus those on bounties they posted; for an account API key, the submissions on the account's bounties. For the anonymous view of one bounty's reviewed work, use the submissions list under the bounty instead.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Scope the list to submissions on this account's bounties (`biz_` tag). Requires read access to the account.

        bounty_id : typing.Optional[str]
            Only submissions on this bounty (`bnty_` tag).

        status : typing.Optional[ListBountySubmissionsRequestStatus]
            Filter by lifecycle state.

        created_after : typing.Optional[str]
            Only submissions created after this ISO 8601 timestamp.

        created_before : typing.Optional[str]
            Only submissions created before this ISO 8601 timestamp.

        order : typing.Optional[ListBountySubmissionsRequestOrder]
            Sort field.

        direction : typing.Optional[ListBountySubmissionsRequestDirection]
            Sort direction.

        first : typing.Optional[int]
            Number of submissions to return from the start of the window.

        after : typing.Optional[str]
            Cursor to paginate forwards from.

        last : typing.Optional[int]
            Number of submissions to return from the end of the window.

        before : typing.Optional[str]
            Cursor to paginate backwards from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[BountySubmission, ListBountySubmissionsResponse]
            bounty submissions listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.bounty_submissions.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            bounty_id=bounty_id,
            status=status,
            created_after=created_after,
            created_before=created_before,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        bounty_id: str,
        affiliate_code: typing.Optional[str] = OMIT,
        deliverable: typing.Optional[CreateBountySubmissionsRequestDeliverable] = OMIT,
        metadata: typing.Optional[CreateBountySubmissionsRequestMetadata] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BountySubmission:
        """
        Creates a submission on a workforce bounty. Include a `deliverable` payload — any combination of links and uploaded files, with at least one of the two — and the submission goes straight to review; create is the only step. For `data_capture` bounties, omit the deliverable: this starts a claimed attempt whose proof accumulates server-side, and the separate submit endpoint sends it to review once complete. Requires a user credential — account API keys cannot author submissions.

        Parameters
        ----------
        bounty_id : str
            The bounty to submit to (`bnty_` tag).

        affiliate_code : typing.Optional[str]
            Affiliate code crediting the referrer, when the worker arrived through one.

        deliverable : typing.Optional[CreateBountySubmissionsRequestDeliverable]
            The submitted work. Combine `urls`, `file_ids`, and `caption` freely; at least one link or file is required.

        metadata : typing.Optional[CreateBountySubmissionsRequestMetadata]
            Optional capture metadata describing where and how the footage was recorded. Persisted on the submission. On a `data_capture` bounty every field except `fov` is required whenever metadata is provided.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BountySubmission
            submission created with an inline deliverable

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.bounty_submissions.create(
                bounty_id="bnty_xxxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            bounty_id=bounty_id,
            affiliate_code=affiliate_code,
            deliverable=deliverable,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BountySubmission:
        """
        Retrieves one bounty submission the credential can see — one the caller authored, or one on a bounty they posted or their account owns. Reading another member's work on an account's bounty takes `account_id`, the same way the list does.

        Parameters
        ----------
        id : str
            The bounty submission to act on (`btys_` tag).

        account_id : typing.Optional[str]
            Read the submission as this account (`biz_` tag), scoping the lookup to its bounties rather than the caller's own work. Requires read access to the account. Without it the lookup covers only what the credential owns — the submissions the caller authored plus those on bounties they posted.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BountySubmission
            bounty submission returned

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.bounty_submissions.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, account_id=account_id, request_options=request_options)
        return _response.data

    async def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteBountySubmissionsResponse:
        """
        Cancels the caller's own active attempt on a bounty and discards any accumulated capture clips. Only the worker who started the attempt can cancel it — account API keys cannot.

        Parameters
        ----------
        id : str
            The bounty submission to act on (`btys_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteBountySubmissionsResponse
            attempt cancelled

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.bounty_submissions.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def submit(
        self,
        id: str,
        *,
        deliverable: typing.Optional[SubmitBountySubmissionsRequestDeliverable] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BountySubmission:
        """
        Submits a claimed attempt for review. A livestream attempt needs an ended proof stream and can attach an optional `deliverable` — links, files, and a caption in any combination; if the attempt already went to review when its stream ended, the payload attaches to it once, until reviewers start voting. A data capture attempt instead needs enough validated clip time and takes no payload. Only the worker who started the attempt can submit it — account API keys cannot.

        Parameters
        ----------
        id : str
            The claimed attempt to submit for review (`btys_` tag).

        deliverable : typing.Optional[SubmitBountySubmissionsRequestDeliverable]
            Work to attach to the submission. Combine `urls`, `file_ids`, and `caption` freely; all are optional.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BountySubmission
            attempt submitted for review

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.bounty_submissions.submit(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit(id, deliverable=deliverable, request_options=request_options)
        return _response.data
