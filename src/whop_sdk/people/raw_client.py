# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.datetime_utils import serialize_datetime
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import encode_path_param
from ..core.pagination import AsyncPager, SyncPager
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..errors.bad_request_error import BadRequestError
from ..errors.not_found_error import NotFoundError
from ..errors.unauthorized_error import UnauthorizedError
from .types.list_people_request_attribution_model import ListPeopleRequestAttributionModel
from .types.list_people_request_direction import ListPeopleRequestDirection
from .types.list_people_request_order import ListPeopleRequestOrder
from .types.list_people_response import ListPeopleResponse
from .types.list_people_response_data_item import ListPeopleResponseDataItem
from .types.retrieve_people_response import RetrievePeopleResponse
from pydantic import ValidationError


class RawPeopleClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        query: typing.Optional[str] = None,
        source: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        attribution_model: typing.Optional[ListPeopleRequestAttributionModel] = None,
        event_name: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        custom_event: typing.Optional[str] = None,
        event_from: typing.Optional[dt.datetime] = None,
        event_to: typing.Optional[dt.datetime] = None,
        audience_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        email: typing.Optional[str] = None,
        phone: typing.Optional[str] = None,
        country: typing.Optional[str] = None,
        has_purchased: typing.Optional[bool] = None,
        contactable: typing.Optional[bool] = None,
        first_seen_within_days: typing.Optional[int] = None,
        last_seen_within_days: typing.Optional[int] = None,
        first_seen_after: typing.Optional[dt.datetime] = None,
        first_seen_before: typing.Optional[dt.datetime] = None,
        last_seen_after: typing.Optional[dt.datetime] = None,
        last_seen_before: typing.Optional[dt.datetime] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListPeopleRequestOrder] = None,
        direction: typing.Optional[ListPeopleRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ListPeopleResponseDataItem, ListPeopleResponse]:
        """
        Lists the people (visitors and customers) of an account: the identity-linked person profiles aggregated from every pixel, payment, and platform event — identities, purchases and LTV, geo/device profile, traffic sources, and first/last marketing touches.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Optional for account API keys; required for credentials that can access multiple accounts.

        query : typing.Optional[str]
            Search people by name, email, phone, or whop user ID (case-insensitive substring match).

        source : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only include people acquired from any of these sources — canonical paths (whop:<campaign>:<group>:<ad>, ext:<platform>:..., referrer:<domain>, direct, other), exact or with a trailing :* prefix. The same vocabulary the events / people metrics use.

        attribution_model : typing.Optional[ListPeopleRequestAttributionModel]
            Attribution model the source filter matches against (defaults to last_touch).

        event_name : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only include people who fired any of these events, e.g. payment.completed or page.checkout.view.

        custom_event : typing.Optional[str]
            Only include people who fired this custom pixel event.

        event_from : typing.Optional[dt.datetime]
            With event_to plus an event or source filter, switches to exact-population mode: person ids are resolved and paginated on the events side within this window (the same query the people metric counts), then hydrated per page.

        event_to : typing.Optional[dt.datetime]
            The inclusive end of the event window for exact-population mode.

        audience_id : typing.Optional[str]
            Only include people in this audience. An audience that keeps itself up to date resolves to the People filters that define it, so this always reflects who matches now; uploaded lists and point-in-time snapshots match their recorded members.

        user_id : typing.Optional[str]
            Only include the person linked to this whop user ID.

        email : typing.Optional[str]
            Only include the person linked to this email address.

        phone : typing.Optional[str]
            Only include the person linked to this phone number.

        country : typing.Optional[str]
            Only include people whose most recent visit came from this ISO 3166-1 alpha-2 country code.

        has_purchased : typing.Optional[bool]
            true for customers only, false for people who have never purchased.

        contactable : typing.Optional[bool]
            true for people who have an email address or phone number — the ones an ad platform can match.

        first_seen_within_days : typing.Optional[int]
            Only include people first seen within this many days, as a rolling window.

        last_seen_within_days : typing.Optional[int]
            Only include people last seen within this many days, as a rolling window.

        first_seen_after : typing.Optional[dt.datetime]
            Only include people first seen at or after this ISO 8601 timestamp.

        first_seen_before : typing.Optional[dt.datetime]
            Only include people first seen before this ISO 8601 timestamp.

        last_seen_after : typing.Optional[dt.datetime]
            Only include people last seen at or after this ISO 8601 timestamp.

        last_seen_before : typing.Optional[dt.datetime]
            Only include people last seen before this ISO 8601 timestamp.

        first : typing.Optional[int]
            The number of people to return (default 100, max 100).

        after : typing.Optional[str]
            A cursor for fetching people after a previous page.

        before : typing.Optional[str]
            A cursor for fetching people before a later page.

        order : typing.Optional[ListPeopleRequestOrder]
            Column to sort by. Defaults to last_seen_at.

        direction : typing.Optional[ListPeopleRequestDirection]
            Sort direction. Defaults to desc.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ListPeopleResponseDataItem, ListPeopleResponse]
            people listed
        """
        _response = self._client_wrapper.httpx_client.request(
            "people",
            method="GET",
            params={
                "account_id": account_id,
                "query": query,
                "source": source,
                "attribution_model": attribution_model,
                "event_name": event_name,
                "custom_event": custom_event,
                "event_from": serialize_datetime(event_from) if event_from is not None else None,
                "event_to": serialize_datetime(event_to) if event_to is not None else None,
                "audience_id": audience_id,
                "user_id": user_id,
                "email": email,
                "phone": phone,
                "country": country,
                "has_purchased": has_purchased,
                "contactable": contactable,
                "first_seen_within_days": first_seen_within_days,
                "last_seen_within_days": last_seen_within_days,
                "first_seen_after": serialize_datetime(first_seen_after) if first_seen_after is not None else None,
                "first_seen_before": serialize_datetime(first_seen_before) if first_seen_before is not None else None,
                "last_seen_after": serialize_datetime(last_seen_after) if last_seen_after is not None else None,
                "last_seen_before": serialize_datetime(last_seen_before) if last_seen_before is not None else None,
                "first": first,
                "after": after,
                "before": before,
                "order": order,
                "direction": direction,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListPeopleResponse,
                    parse_obj_as(
                        type_=ListPeopleResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""
                    _get_next = lambda: self.list(
                        account_id=account_id,
                        query=query,
                        source=source,
                        attribution_model=attribution_model,
                        event_name=event_name,
                        custom_event=custom_event,
                        event_from=event_from,
                        event_to=event_to,
                        audience_id=audience_id,
                        user_id=user_id,
                        email=email,
                        phone=phone,
                        country=country,
                        has_purchased=has_purchased,
                        contactable=contactable,
                        first_seen_within_days=first_seen_within_days,
                        last_seen_within_days=last_seen_within_days,
                        first_seen_after=first_seen_after,
                        first_seen_before=first_seen_before,
                        last_seen_after=last_seen_after,
                        last_seen_before=last_seen_before,
                        first=first,
                        after=_parsed_next,
                        before=before,
                        order=order,
                        direction=direction,
                        request_options=request_options,
                    )
                return SyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[RetrievePeopleResponse]:
        """
        Retrieves one person for an account. The identifier can be a person ID (prefixed `prsn_`), a user ID (prefixed `user_`), an email address, or a phone number — merged people resolve to the surviving profile.

        Parameters
        ----------
        id : str
            The person ID, user ID, email address, or phone number to look up.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Optional for account API keys; required for credentials that can access multiple accounts.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[RetrievePeopleResponse]
            person retrieved
        """
        _response = self._client_wrapper.httpx_client.request(
            f"people/{encode_path_param(id)}",
            method="GET",
            params={
                "account_id": account_id,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    RetrievePeopleResponse,
                    parse_obj_as(
                        type_=RetrievePeopleResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawPeopleClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        query: typing.Optional[str] = None,
        source: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        attribution_model: typing.Optional[ListPeopleRequestAttributionModel] = None,
        event_name: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        custom_event: typing.Optional[str] = None,
        event_from: typing.Optional[dt.datetime] = None,
        event_to: typing.Optional[dt.datetime] = None,
        audience_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        email: typing.Optional[str] = None,
        phone: typing.Optional[str] = None,
        country: typing.Optional[str] = None,
        has_purchased: typing.Optional[bool] = None,
        contactable: typing.Optional[bool] = None,
        first_seen_within_days: typing.Optional[int] = None,
        last_seen_within_days: typing.Optional[int] = None,
        first_seen_after: typing.Optional[dt.datetime] = None,
        first_seen_before: typing.Optional[dt.datetime] = None,
        last_seen_after: typing.Optional[dt.datetime] = None,
        last_seen_before: typing.Optional[dt.datetime] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListPeopleRequestOrder] = None,
        direction: typing.Optional[ListPeopleRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ListPeopleResponseDataItem, ListPeopleResponse]:
        """
        Lists the people (visitors and customers) of an account: the identity-linked person profiles aggregated from every pixel, payment, and platform event — identities, purchases and LTV, geo/device profile, traffic sources, and first/last marketing touches.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Optional for account API keys; required for credentials that can access multiple accounts.

        query : typing.Optional[str]
            Search people by name, email, phone, or whop user ID (case-insensitive substring match).

        source : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only include people acquired from any of these sources — canonical paths (whop:<campaign>:<group>:<ad>, ext:<platform>:..., referrer:<domain>, direct, other), exact or with a trailing :* prefix. The same vocabulary the events / people metrics use.

        attribution_model : typing.Optional[ListPeopleRequestAttributionModel]
            Attribution model the source filter matches against (defaults to last_touch).

        event_name : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only include people who fired any of these events, e.g. payment.completed or page.checkout.view.

        custom_event : typing.Optional[str]
            Only include people who fired this custom pixel event.

        event_from : typing.Optional[dt.datetime]
            With event_to plus an event or source filter, switches to exact-population mode: person ids are resolved and paginated on the events side within this window (the same query the people metric counts), then hydrated per page.

        event_to : typing.Optional[dt.datetime]
            The inclusive end of the event window for exact-population mode.

        audience_id : typing.Optional[str]
            Only include people in this audience. An audience that keeps itself up to date resolves to the People filters that define it, so this always reflects who matches now; uploaded lists and point-in-time snapshots match their recorded members.

        user_id : typing.Optional[str]
            Only include the person linked to this whop user ID.

        email : typing.Optional[str]
            Only include the person linked to this email address.

        phone : typing.Optional[str]
            Only include the person linked to this phone number.

        country : typing.Optional[str]
            Only include people whose most recent visit came from this ISO 3166-1 alpha-2 country code.

        has_purchased : typing.Optional[bool]
            true for customers only, false for people who have never purchased.

        contactable : typing.Optional[bool]
            true for people who have an email address or phone number — the ones an ad platform can match.

        first_seen_within_days : typing.Optional[int]
            Only include people first seen within this many days, as a rolling window.

        last_seen_within_days : typing.Optional[int]
            Only include people last seen within this many days, as a rolling window.

        first_seen_after : typing.Optional[dt.datetime]
            Only include people first seen at or after this ISO 8601 timestamp.

        first_seen_before : typing.Optional[dt.datetime]
            Only include people first seen before this ISO 8601 timestamp.

        last_seen_after : typing.Optional[dt.datetime]
            Only include people last seen at or after this ISO 8601 timestamp.

        last_seen_before : typing.Optional[dt.datetime]
            Only include people last seen before this ISO 8601 timestamp.

        first : typing.Optional[int]
            The number of people to return (default 100, max 100).

        after : typing.Optional[str]
            A cursor for fetching people after a previous page.

        before : typing.Optional[str]
            A cursor for fetching people before a later page.

        order : typing.Optional[ListPeopleRequestOrder]
            Column to sort by. Defaults to last_seen_at.

        direction : typing.Optional[ListPeopleRequestDirection]
            Sort direction. Defaults to desc.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ListPeopleResponseDataItem, ListPeopleResponse]
            people listed
        """
        _response = await self._client_wrapper.httpx_client.request(
            "people",
            method="GET",
            params={
                "account_id": account_id,
                "query": query,
                "source": source,
                "attribution_model": attribution_model,
                "event_name": event_name,
                "custom_event": custom_event,
                "event_from": serialize_datetime(event_from) if event_from is not None else None,
                "event_to": serialize_datetime(event_to) if event_to is not None else None,
                "audience_id": audience_id,
                "user_id": user_id,
                "email": email,
                "phone": phone,
                "country": country,
                "has_purchased": has_purchased,
                "contactable": contactable,
                "first_seen_within_days": first_seen_within_days,
                "last_seen_within_days": last_seen_within_days,
                "first_seen_after": serialize_datetime(first_seen_after) if first_seen_after is not None else None,
                "first_seen_before": serialize_datetime(first_seen_before) if first_seen_before is not None else None,
                "last_seen_after": serialize_datetime(last_seen_after) if last_seen_after is not None else None,
                "last_seen_before": serialize_datetime(last_seen_before) if last_seen_before is not None else None,
                "first": first,
                "after": after,
                "before": before,
                "order": order,
                "direction": direction,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListPeopleResponse,
                    parse_obj_as(
                        type_=ListPeopleResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""

                    async def _get_next():
                        return await self.list(
                            account_id=account_id,
                            query=query,
                            source=source,
                            attribution_model=attribution_model,
                            event_name=event_name,
                            custom_event=custom_event,
                            event_from=event_from,
                            event_to=event_to,
                            audience_id=audience_id,
                            user_id=user_id,
                            email=email,
                            phone=phone,
                            country=country,
                            has_purchased=has_purchased,
                            contactable=contactable,
                            first_seen_within_days=first_seen_within_days,
                            last_seen_within_days=last_seen_within_days,
                            first_seen_after=first_seen_after,
                            first_seen_before=first_seen_before,
                            last_seen_after=last_seen_after,
                            last_seen_before=last_seen_before,
                            first=first,
                            after=_parsed_next,
                            before=before,
                            order=order,
                            direction=direction,
                            request_options=request_options,
                        )

                return AsyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[RetrievePeopleResponse]:
        """
        Retrieves one person for an account. The identifier can be a person ID (prefixed `prsn_`), a user ID (prefixed `user_`), an email address, or a phone number — merged people resolve to the surviving profile.

        Parameters
        ----------
        id : str
            The person ID, user ID, email address, or phone number to look up.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Optional for account API keys; required for credentials that can access multiple accounts.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[RetrievePeopleResponse]
            person retrieved
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"people/{encode_path_param(id)}",
            method="GET",
            params={
                "account_id": account_id,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    RetrievePeopleResponse,
                    parse_obj_as(
                        type_=RetrievePeopleResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
