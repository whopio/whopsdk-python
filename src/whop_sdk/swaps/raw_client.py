# This file was auto-generated by Fern from our API Definition.

import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import encode_path_param
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..core.serialization import convert_and_respect_annotation_metadata
from ..errors.bad_request_error import BadRequestError
from ..errors.conflict_error import ConflictError
from ..errors.forbidden_error import ForbiddenError
from ..errors.not_found_error import NotFoundError
from ..types.v1error_response import V1ErrorResponse
from .types.create_quote_swaps_request_from_chain import CreateQuoteSwapsRequestFromChain
from .types.create_quote_swaps_request_to_chain import CreateQuoteSwapsRequestToChain
from .types.create_quote_swaps_response import CreateQuoteSwapsResponse
from .types.create_swaps_request_from_chain import CreateSwapsRequestFromChain
from .types.create_swaps_request_to_chain import CreateSwapsRequestToChain
from .types.create_swaps_response import CreateSwapsResponse
from .types.list_swaps_response import ListSwapsResponse
from .types.retrieve_swaps_response import RetrieveSwapsResponse
from pydantic import ValidationError

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawSwapsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self, *, account_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[ListSwapsResponse]:
        """
        Retrieve the account's completed or pending swaps — currently just the latest one.

        Parameters
        ----------
        account_id : str
            Business or user account ID (biz_* / user_*).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[ListSwapsResponse]
            swaps listed
        """
        _response = self._client_wrapper.httpx_client.request(
            "swaps",
            method="GET",
            params={
                "account_id": account_id,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListSwapsResponse,
                    parse_obj_as(
                        type_=ListSwapsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def create(
        self,
        *,
        account_id: str,
        from_token: str,
        to_token: str,
        amount: typing.Optional[str] = OMIT,
        from_chain: typing.Optional[CreateSwapsRequestFromChain] = OMIT,
        slippage_bps: typing.Optional[int] = OMIT,
        to_amount: typing.Optional[str] = OMIT,
        to_chain: typing.Optional[CreateSwapsRequestToChain] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[CreateSwapsResponse]:
        """
        Swaps one token for another from the account's wallet, or converts between fiat currencies in the account's ledger at the mid-market rate. Crypto swaps finish in the background — check the swap for its status.

        Parameters
        ----------
        account_id : str
            Business or user account ID (biz_* / user_*).

        from_token : str
            Source token contract address or ticker symbol, such as "USDT".

        to_token : str
            Destination token contract address or ticker symbol, such as "XAUT".

        amount : typing.Optional[str]
            Source token amount. Required for crypto swaps. For fiat pairs: the amount of from_token to convert at the mid-market rate; omit (along with to_amount) to repay the full negative to_token balance instead.

        from_chain : typing.Optional[CreateSwapsRequestFromChain]
            Source chain name or chain ID. Defaults to the source token's chain when omitted.

        slippage_bps : typing.Optional[int]
            Maximum slippage tolerance in basis points.

        to_amount : typing.Optional[str]
            Fiat pairs only: sizes a partial repayment of the negative to_token balance, denominated in to_token. Must not exceed the debt. Mutually exclusive with amount.

        to_chain : typing.Optional[CreateSwapsRequestToChain]
            Destination chain name or chain ID. Defaults to the destination token's chain when omitted.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[CreateSwapsResponse]
            swap created
        """
        _response = self._client_wrapper.httpx_client.request(
            "swaps",
            method="POST",
            json={
                "account_id": account_id,
                "amount": amount,
                "from_chain": convert_and_respect_annotation_metadata(
                    object_=from_chain, annotation=typing.Optional[CreateSwapsRequestFromChain], direction="write"
                ),
                "from_token": from_token,
                "slippage_bps": slippage_bps,
                "to_amount": to_amount,
                "to_chain": convert_and_respect_annotation_metadata(
                    object_=to_chain, annotation=typing.Optional[CreateSwapsRequestToChain], direction="write"
                ),
                "to_token": to_token,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CreateSwapsResponse,
                    parse_obj_as(
                        type_=CreateSwapsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def create_quote(
        self,
        *,
        amount: str,
        from_token: str,
        to_token: str,
        from_address: typing.Optional[str] = OMIT,
        from_chain: typing.Optional[CreateQuoteSwapsRequestFromChain] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        slippage_bps: typing.Optional[int] = OMIT,
        to_address: typing.Optional[str] = OMIT,
        to_chain: typing.Optional[CreateQuoteSwapsRequestToChain] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[CreateQuoteSwapsResponse]:
        """
        Previews the price of a swap. Fiat pairs quote the in-ledger mid-market conversion — the same rate creating the swap fills at. No funds move and nothing is saved.

        Parameters
        ----------
        amount : str
            Source token amount.

        from_token : str
            Source token contract address or ticker symbol, such as "USDT".

        to_token : str
            Destination token contract address or ticker symbol, such as "XAUT".

        from_address : typing.Optional[str]
            Source wallet address used for the quote.

        from_chain : typing.Optional[CreateQuoteSwapsRequestFromChain]
            Source chain name or chain ID. Defaults to the source token's chain when omitted.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Metadata to include with the quote response.

        slippage_bps : typing.Optional[int]
            Maximum slippage tolerance in basis points.

        to_address : typing.Optional[str]
            Destination wallet address used for the quote.

        to_chain : typing.Optional[CreateQuoteSwapsRequestToChain]
            Destination chain name or chain ID. Defaults to the destination token's chain when omitted.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[CreateQuoteSwapsResponse]
            swap quote created
        """
        _response = self._client_wrapper.httpx_client.request(
            "swaps/quote",
            method="POST",
            json={
                "amount": amount,
                "from_address": from_address,
                "from_chain": convert_and_respect_annotation_metadata(
                    object_=from_chain, annotation=typing.Optional[CreateQuoteSwapsRequestFromChain], direction="write"
                ),
                "from_token": from_token,
                "metadata": metadata,
                "slippage_bps": slippage_bps,
                "to_address": to_address,
                "to_chain": convert_and_respect_annotation_metadata(
                    object_=to_chain, annotation=typing.Optional[CreateQuoteSwapsRequestToChain], direction="write"
                ),
                "to_token": to_token,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CreateQuoteSwapsResponse,
                    parse_obj_as(
                        type_=CreateQuoteSwapsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[RetrieveSwapsResponse]:
        """
        Retrieves a single swap and its status.

        Parameters
        ----------
        id : str
            Swap ID returned from POST /swaps.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[RetrieveSwapsResponse]
            swap status retrieved
        """
        _response = self._client_wrapper.httpx_client.request(
            f"swaps/{encode_path_param(id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    RetrieveSwapsResponse,
                    parse_obj_as(
                        type_=RetrieveSwapsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawSwapsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self, *, account_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[ListSwapsResponse]:
        """
        Retrieve the account's completed or pending swaps — currently just the latest one.

        Parameters
        ----------
        account_id : str
            Business or user account ID (biz_* / user_*).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[ListSwapsResponse]
            swaps listed
        """
        _response = await self._client_wrapper.httpx_client.request(
            "swaps",
            method="GET",
            params={
                "account_id": account_id,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListSwapsResponse,
                    parse_obj_as(
                        type_=ListSwapsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def create(
        self,
        *,
        account_id: str,
        from_token: str,
        to_token: str,
        amount: typing.Optional[str] = OMIT,
        from_chain: typing.Optional[CreateSwapsRequestFromChain] = OMIT,
        slippage_bps: typing.Optional[int] = OMIT,
        to_amount: typing.Optional[str] = OMIT,
        to_chain: typing.Optional[CreateSwapsRequestToChain] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[CreateSwapsResponse]:
        """
        Swaps one token for another from the account's wallet, or converts between fiat currencies in the account's ledger at the mid-market rate. Crypto swaps finish in the background — check the swap for its status.

        Parameters
        ----------
        account_id : str
            Business or user account ID (biz_* / user_*).

        from_token : str
            Source token contract address or ticker symbol, such as "USDT".

        to_token : str
            Destination token contract address or ticker symbol, such as "XAUT".

        amount : typing.Optional[str]
            Source token amount. Required for crypto swaps. For fiat pairs: the amount of from_token to convert at the mid-market rate; omit (along with to_amount) to repay the full negative to_token balance instead.

        from_chain : typing.Optional[CreateSwapsRequestFromChain]
            Source chain name or chain ID. Defaults to the source token's chain when omitted.

        slippage_bps : typing.Optional[int]
            Maximum slippage tolerance in basis points.

        to_amount : typing.Optional[str]
            Fiat pairs only: sizes a partial repayment of the negative to_token balance, denominated in to_token. Must not exceed the debt. Mutually exclusive with amount.

        to_chain : typing.Optional[CreateSwapsRequestToChain]
            Destination chain name or chain ID. Defaults to the destination token's chain when omitted.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[CreateSwapsResponse]
            swap created
        """
        _response = await self._client_wrapper.httpx_client.request(
            "swaps",
            method="POST",
            json={
                "account_id": account_id,
                "amount": amount,
                "from_chain": convert_and_respect_annotation_metadata(
                    object_=from_chain, annotation=typing.Optional[CreateSwapsRequestFromChain], direction="write"
                ),
                "from_token": from_token,
                "slippage_bps": slippage_bps,
                "to_amount": to_amount,
                "to_chain": convert_and_respect_annotation_metadata(
                    object_=to_chain, annotation=typing.Optional[CreateSwapsRequestToChain], direction="write"
                ),
                "to_token": to_token,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CreateSwapsResponse,
                    parse_obj_as(
                        type_=CreateSwapsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def create_quote(
        self,
        *,
        amount: str,
        from_token: str,
        to_token: str,
        from_address: typing.Optional[str] = OMIT,
        from_chain: typing.Optional[CreateQuoteSwapsRequestFromChain] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        slippage_bps: typing.Optional[int] = OMIT,
        to_address: typing.Optional[str] = OMIT,
        to_chain: typing.Optional[CreateQuoteSwapsRequestToChain] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[CreateQuoteSwapsResponse]:
        """
        Previews the price of a swap. Fiat pairs quote the in-ledger mid-market conversion — the same rate creating the swap fills at. No funds move and nothing is saved.

        Parameters
        ----------
        amount : str
            Source token amount.

        from_token : str
            Source token contract address or ticker symbol, such as "USDT".

        to_token : str
            Destination token contract address or ticker symbol, such as "XAUT".

        from_address : typing.Optional[str]
            Source wallet address used for the quote.

        from_chain : typing.Optional[CreateQuoteSwapsRequestFromChain]
            Source chain name or chain ID. Defaults to the source token's chain when omitted.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Metadata to include with the quote response.

        slippage_bps : typing.Optional[int]
            Maximum slippage tolerance in basis points.

        to_address : typing.Optional[str]
            Destination wallet address used for the quote.

        to_chain : typing.Optional[CreateQuoteSwapsRequestToChain]
            Destination chain name or chain ID. Defaults to the destination token's chain when omitted.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[CreateQuoteSwapsResponse]
            swap quote created
        """
        _response = await self._client_wrapper.httpx_client.request(
            "swaps/quote",
            method="POST",
            json={
                "amount": amount,
                "from_address": from_address,
                "from_chain": convert_and_respect_annotation_metadata(
                    object_=from_chain, annotation=typing.Optional[CreateQuoteSwapsRequestFromChain], direction="write"
                ),
                "from_token": from_token,
                "metadata": metadata,
                "slippage_bps": slippage_bps,
                "to_address": to_address,
                "to_chain": convert_and_respect_annotation_metadata(
                    object_=to_chain, annotation=typing.Optional[CreateQuoteSwapsRequestToChain], direction="write"
                ),
                "to_token": to_token,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CreateQuoteSwapsResponse,
                    parse_obj_as(
                        type_=CreateQuoteSwapsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[RetrieveSwapsResponse]:
        """
        Retrieves a single swap and its status.

        Parameters
        ----------
        id : str
            Swap ID returned from POST /swaps.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[RetrieveSwapsResponse]
            swap status retrieved
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"swaps/{encode_path_param(id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    RetrieveSwapsResponse,
                    parse_obj_as(
                        type_=RetrieveSwapsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
