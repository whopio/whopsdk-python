# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.pagination import AsyncPager, SyncPager
from ...core.request_options import RequestOptions
from ...types.notification_topic import NotificationTopic
from .raw_client import AsyncRawTopicsClient, RawTopicsClient
from .types.list_topics_request_topic_type import ListTopicsRequestTopicType
from .types.list_topics_response import ListTopicsResponse


class TopicsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawTopicsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawTopicsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawTopicsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        topic_type: typing.Optional[ListTopicsRequestTopicType] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[NotificationTopic, ListTopicsResponse]:
        """
        Lists the platform's visible notification topics — the categories users can set notification preferences on. App-created topics are internal and not returned.

        Parameters
        ----------
        topic_type : typing.Optional[ListTopicsRequestTopicType]
            Only return topics of this scope: `user` (member notifications) or `account_team` (team notifications).

        first : typing.Optional[int]
            The number of topics to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns topics after this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[NotificationTopic, ListTopicsResponse]
            notification topics listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.notifications.topics.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(topic_type=topic_type, first=first, after=after, request_options=request_options)


class AsyncTopicsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawTopicsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawTopicsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawTopicsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        topic_type: typing.Optional[ListTopicsRequestTopicType] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[NotificationTopic, ListTopicsResponse]:
        """
        Lists the platform's visible notification topics — the categories users can set notification preferences on. App-created topics are internal and not returned.

        Parameters
        ----------
        topic_type : typing.Optional[ListTopicsRequestTopicType]
            Only return topics of this scope: `user` (member notifications) or `account_team` (team notifications).

        first : typing.Optional[int]
            The number of topics to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns topics after this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[NotificationTopic, ListTopicsResponse]
            notification topics listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.notifications.topics.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            topic_type=topic_type, first=first, after=after, request_options=request_options
        )
