# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ...core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .form_company_accounts_request_founders_item_address import FormCompanyAccountsRequestFoundersItemAddress
from .form_company_accounts_request_founders_item_roles_item import FormCompanyAccountsRequestFoundersItemRolesItem


class FormCompanyAccountsRequestFoundersItem(UniversalBaseModel):
    address: FormCompanyAccountsRequestFoundersItemAddress = pydantic.Field()
    """
    Founder's personal address.
    """

    date_of_birth: typing.Optional[str] = pydantic.Field(default=None)
    """
    Formatted as `YYYY-MM-DD`.
    """

    email: str
    first_name: str
    is_primary: bool = pydantic.Field()
    """
    Marks the responsible party for the filing. Exactly one founder must be primary.
    """

    last_name: str
    ownership_percentage: typing.Optional[float] = pydantic.Field(default=None)
    """
    The founder's ownership share: greater than `0`, at most `100`. Shares across founders must total `100`. Required when `entity_type` is `llc`; ignored for C-Corps.
    """

    phone: str = pydantic.Field()
    """
    Phone number in E.164 format, for example `+12125550100`.
    """

    roles: typing.Optional[typing.List[FormCompanyAccountsRequestFoundersItemRolesItem]] = pydantic.Field(default=None)
    """
    Officer roles held by the member — one member can hold several. Required (at least one role) for every member when `entity_type` is `c_corp`; ignored for LLCs. Across all members every role must be covered; `president`, `secretary` and `treasurer` may each be held by only one member, while `director` may repeat.
    """

    ssn: typing.Optional[str] = pydantic.Field(default=None)
    """
    The founder's US Social Security Number. Leave empty if the founder is not a US resident. Non-US founders can request expedited EIN processing via the `expedite_ein` option.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
