# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.review import Review
from ..types.review_list_item import ReviewListItem
from .raw_client import AsyncRawReviewsClient, RawReviewsClient
from .types.list_reviews_response import ListReviewsResponse


class ReviewsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawReviewsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawReviewsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawReviewsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        product_id: str,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        min_stars: typing.Optional[int] = None,
        max_stars: typing.Optional[int] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ReviewListItem, ListReviewsResponse]:
        """
        Returns a paginated list of customer reviews for a specific product, with optional filtering by star rating and creation date.

        Parameters
        ----------
        product_id : str
            The unique identifier of the product to list reviews for.

        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        min_stars : typing.Optional[int]
            The minimum star rating to include in results, from 1 to 5 inclusive.

        max_stars : typing.Optional[int]
            The maximum star rating to include in results, from 1 to 5 inclusive.

        created_before : typing.Optional[dt.datetime]
            Only return reviews created before this timestamp.

        created_after : typing.Optional[dt.datetime]
            Only return reviews created after this timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ReviewListItem, ListReviewsResponse]
            A successful response

        Examples
        --------
        import datetime

        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.reviews.list(
            first=42,
            last=42,
            product_id="prod_xxxxxxxxxxxxx",
            min_stars=42,
            max_stars=42,
            created_before=datetime.datetime.fromisoformat(
                "2023-12-01 05:00:00+00:00",
            ),
            created_after=datetime.datetime.fromisoformat(
                "2023-12-01 05:00:00+00:00",
            ),
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            product_id=product_id,
            after=after,
            before=before,
            first=first,
            last=last,
            min_stars=min_stars,
            max_stars=max_stars,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Review:
        """
        Retrieves the details of an existing review.

        Parameters
        ----------
        id : str
            The unique identifier of the review to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Review
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.reviews.retrieve(
            id="rev_xxxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data


class AsyncReviewsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawReviewsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawReviewsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawReviewsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        product_id: str,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        min_stars: typing.Optional[int] = None,
        max_stars: typing.Optional[int] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ReviewListItem, ListReviewsResponse]:
        """
        Returns a paginated list of customer reviews for a specific product, with optional filtering by star rating and creation date.

        Parameters
        ----------
        product_id : str
            The unique identifier of the product to list reviews for.

        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        min_stars : typing.Optional[int]
            The minimum star rating to include in results, from 1 to 5 inclusive.

        max_stars : typing.Optional[int]
            The maximum star rating to include in results, from 1 to 5 inclusive.

        created_before : typing.Optional[dt.datetime]
            Only return reviews created before this timestamp.

        created_after : typing.Optional[dt.datetime]
            Only return reviews created after this timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ReviewListItem, ListReviewsResponse]
            A successful response

        Examples
        --------
        import asyncio
        import datetime

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.reviews.list(
                first=42,
                last=42,
                product_id="prod_xxxxxxxxxxxxx",
                min_stars=42,
                max_stars=42,
                created_before=datetime.datetime.fromisoformat(
                    "2023-12-01 05:00:00+00:00",
                ),
                created_after=datetime.datetime.fromisoformat(
                    "2023-12-01 05:00:00+00:00",
                ),
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            product_id=product_id,
            after=after,
            before=before,
            first=first,
            last=last,
            min_stars=min_stars,
            max_stars=max_stars,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Review:
        """
        Retrieves the details of an existing review.

        Parameters
        ----------
        id : str
            The unique identifier of the review to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Review
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.reviews.retrieve(
                id="rev_xxxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data
