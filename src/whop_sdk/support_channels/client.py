# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.direction import Direction
from ..types.message_channel_order import MessageChannelOrder
from ..types.support_channel import SupportChannel
from ..types.support_channel_list_item import SupportChannelListItem
from ..types.support_channel_view import SupportChannelView
from .raw_client import AsyncRawSupportChannelsClient, RawSupportChannelsClient
from .types.list_support_channels_response import ListSupportChannelsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class SupportChannelsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawSupportChannelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawSupportChannelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSupportChannelsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        company_id: typing.Optional[str] = None,
        view: typing.Optional[SupportChannelView] = None,
        open: typing.Optional[bool] = None,
        direction: typing.Optional[Direction] = None,
        order: typing.Optional[MessageChannelOrder] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[SupportChannelListItem, ListSupportChannelsResponse]:
        """
        Returns a paginated list of support channels for a specific company, with optional filtering by resolution status and custom sorting.

        Required permissions:
         - `support_chat:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        company_id : typing.Optional[str]
            The unique identifier of the company to list support channels for. Includes channels of child companies. When omitted, returns support channels across all companies the user has access to.

        view : typing.Optional[SupportChannelView]

        open : typing.Optional[bool]
            Whether to filter by open or resolved support channels. Set to true to only return channels awaiting a response, or false for resolved channels.

        direction : typing.Optional[Direction]

        order : typing.Optional[MessageChannelOrder]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[SupportChannelListItem, ListSupportChannelsResponse]
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.support_channels.list(
            first=42,
            last=42,
            company_id="biz_xxxxxxxxxxxxxx",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            after=after,
            before=before,
            first=first,
            last=last,
            company_id=company_id,
            view=view,
            open=open,
            direction=direction,
            order=order,
            request_options=request_options,
        )

    def create(
        self,
        *,
        company_id: str,
        user_id: str,
        custom_name: typing.Optional[str] = OMIT,
        notifications_enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SupportChannel:
        """
        Open a new support channel between a company team member and a customer. Returns the existing channel if one already exists for that user.

        Required permissions:
         - `support_chat:create`

        Parameters
        ----------
        company_id : str
            The unique identifier of the company to create the support channel in.

        user_id : str
            The user ID (e.g. 'user_xxxxx') or username of the customer to open a support channel for.

        custom_name : typing.Optional[str]
            Optional custom display name for the support channel.

        notifications_enabled : typing.Optional[bool]
            Whether Whop app notifications are enabled for this support channel. Webhooks still fire.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SupportChannel
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.support_channels.create(
            company_id="biz_xxxxxxxxxxxxxx",
            user_id="user_xxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.create(
            company_id=company_id,
            user_id=user_id,
            custom_name=custom_name,
            notifications_enabled=notifications_enabled,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> SupportChannel:
        """
        Retrieves the details of an existing support channel.

        Required permissions:
         - `support_chat:read`

        Parameters
        ----------
        id : str
            The unique identifier of the support channel to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SupportChannel
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.support_channels.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data


class AsyncSupportChannelsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawSupportChannelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSupportChannelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSupportChannelsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        company_id: typing.Optional[str] = None,
        view: typing.Optional[SupportChannelView] = None,
        open: typing.Optional[bool] = None,
        direction: typing.Optional[Direction] = None,
        order: typing.Optional[MessageChannelOrder] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[SupportChannelListItem, ListSupportChannelsResponse]:
        """
        Returns a paginated list of support channels for a specific company, with optional filtering by resolution status and custom sorting.

        Required permissions:
         - `support_chat:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        company_id : typing.Optional[str]
            The unique identifier of the company to list support channels for. Includes channels of child companies. When omitted, returns support channels across all companies the user has access to.

        view : typing.Optional[SupportChannelView]

        open : typing.Optional[bool]
            Whether to filter by open or resolved support channels. Set to true to only return channels awaiting a response, or false for resolved channels.

        direction : typing.Optional[Direction]

        order : typing.Optional[MessageChannelOrder]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[SupportChannelListItem, ListSupportChannelsResponse]
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.support_channels.list(
                first=42,
                last=42,
                company_id="biz_xxxxxxxxxxxxxx",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            after=after,
            before=before,
            first=first,
            last=last,
            company_id=company_id,
            view=view,
            open=open,
            direction=direction,
            order=order,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        company_id: str,
        user_id: str,
        custom_name: typing.Optional[str] = OMIT,
        notifications_enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SupportChannel:
        """
        Open a new support channel between a company team member and a customer. Returns the existing channel if one already exists for that user.

        Required permissions:
         - `support_chat:create`

        Parameters
        ----------
        company_id : str
            The unique identifier of the company to create the support channel in.

        user_id : str
            The user ID (e.g. 'user_xxxxx') or username of the customer to open a support channel for.

        custom_name : typing.Optional[str]
            Optional custom display name for the support channel.

        notifications_enabled : typing.Optional[bool]
            Whether Whop app notifications are enabled for this support channel. Webhooks still fire.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SupportChannel
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.support_channels.create(
                company_id="biz_xxxxxxxxxxxxxx",
                user_id="user_xxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            company_id=company_id,
            user_id=user_id,
            custom_name=custom_name,
            notifications_enabled=notifications_enabled,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> SupportChannel:
        """
        Retrieves the details of an existing support channel.

        Required permissions:
         - `support_chat:read`

        Parameters
        ----------
        id : str
            The unique identifier of the support channel to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SupportChannel
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.support_channels.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data
