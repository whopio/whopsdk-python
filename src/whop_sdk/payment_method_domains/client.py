# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.payment_method_domain import PaymentMethodDomain
from .raw_client import AsyncRawPaymentMethodDomainsClient, RawPaymentMethodDomainsClient
from .types.delete_payment_method_domains_response import DeletePaymentMethodDomainsResponse
from .types.list_payment_method_domains_request_direction import ListPaymentMethodDomainsRequestDirection
from .types.list_payment_method_domains_request_order import ListPaymentMethodDomainsRequestOrder
from .types.list_payment_method_domains_request_provider import ListPaymentMethodDomainsRequestProvider
from .types.list_payment_method_domains_request_status import ListPaymentMethodDomainsRequestStatus
from .types.list_payment_method_domains_response import ListPaymentMethodDomainsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PaymentMethodDomainsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPaymentMethodDomainsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPaymentMethodDomainsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPaymentMethodDomainsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        hostname: typing.Optional[str] = None,
        status: typing.Optional[ListPaymentMethodDomainsRequestStatus] = None,
        provider: typing.Optional[ListPaymentMethodDomainsRequestProvider] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        order: typing.Optional[ListPaymentMethodDomainsRequestOrder] = None,
        direction: typing.Optional[ListPaymentMethodDomainsRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PaymentMethodDomain, ListPaymentMethodDomainsResponse]:
        """
        Lists payment method domains. Without `account_id`, returns the caller's own domains and those of every connected account.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Only domains registered for this account (`biz_` tag). Defaults to the caller's account plus its connected accounts.

        hostname : typing.Optional[str]
            Only the domain with this exact hostname.

        status : typing.Optional[ListPaymentMethodDomainsRequestStatus]
            Only domains with this verification status.

        provider : typing.Optional[ListPaymentMethodDomainsRequestProvider]
            Only domains registered with this wallet provider.

        created_before : typing.Optional[dt.datetime]
            Only domains created before this ISO 8601 timestamp.

        created_after : typing.Optional[dt.datetime]
            Only domains created after this ISO 8601 timestamp.

        order : typing.Optional[ListPaymentMethodDomainsRequestOrder]
            Sort field.

        direction : typing.Optional[ListPaymentMethodDomainsRequestDirection]
            Sort direction.

        first : typing.Optional[int]
            Number of domains to return from the start of the window.

        after : typing.Optional[str]
            Cursor to paginate forwards from.

        last : typing.Optional[int]
            Number of domains to return from the end of the window.

        before : typing.Optional[str]
            Cursor to paginate backwards from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PaymentMethodDomain, ListPaymentMethodDomainsResponse]
            payment method domains listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.payment_method_domains.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            hostname=hostname,
            status=status,
            provider=provider,
            created_before=created_before,
            created_after=created_after,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        hostname: str,
        account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentMethodDomain:
        """
        Registers a hostname with the wallet provider and attempts verification inline. Returns `verified` when the provider fetched the domain-association file (for Apple Pay, `/.well-known/apple-developer-merchantid-domain-association`), or `pending` when it could not — host the file, then retry with the verify endpoint.

        Parameters
        ----------
        hostname : str
            Hostname to register (e.g. `checkout.shinetime.example`).

        account_id : typing.Optional[str]
            Account to register the domain for (`biz_` tag). Defaults to the caller's account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentMethodDomain
            domain registered as pending when verification fails

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_method_domains.create(
            hostname="pending.shinetime.example",
        )
        """
        _response = self._raw_client.create(hostname=hostname, account_id=account_id, request_options=request_options)
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentMethodDomain:
        """
        Retrieves a payment method domain to check its verification status.

        Parameters
        ----------
        id : str
            The unique identifier of the payment method domain, prefixed `pmd_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentMethodDomain
            payment method domain retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_method_domains.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeletePaymentMethodDomainsResponse:
        """
        Unregisters a payment method domain so its wallet payment methods stop rendering there.

        Parameters
        ----------
        id : str
            The unique identifier of the payment method domain, prefixed `pmd_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletePaymentMethodDomainsResponse
            payment method domain deleted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_method_domains.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def verify(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentMethodDomain:
        """
        Re-attempts provider verification of a pending domain once the association file is hosted. Fails with a `bad_request` explaining what to fix; verifying an already `verified` domain is a no-op.

        Parameters
        ----------
        id : str
            The unique identifier of the payment method domain, prefixed `pmd_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentMethodDomain
            already verified domain is a no-op

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_method_domains.verify(
            id="id",
        )
        """
        _response = self._raw_client.verify(id, request_options=request_options)
        return _response.data


class AsyncPaymentMethodDomainsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPaymentMethodDomainsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPaymentMethodDomainsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPaymentMethodDomainsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        hostname: typing.Optional[str] = None,
        status: typing.Optional[ListPaymentMethodDomainsRequestStatus] = None,
        provider: typing.Optional[ListPaymentMethodDomainsRequestProvider] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        order: typing.Optional[ListPaymentMethodDomainsRequestOrder] = None,
        direction: typing.Optional[ListPaymentMethodDomainsRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PaymentMethodDomain, ListPaymentMethodDomainsResponse]:
        """
        Lists payment method domains. Without `account_id`, returns the caller's own domains and those of every connected account.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Only domains registered for this account (`biz_` tag). Defaults to the caller's account plus its connected accounts.

        hostname : typing.Optional[str]
            Only the domain with this exact hostname.

        status : typing.Optional[ListPaymentMethodDomainsRequestStatus]
            Only domains with this verification status.

        provider : typing.Optional[ListPaymentMethodDomainsRequestProvider]
            Only domains registered with this wallet provider.

        created_before : typing.Optional[dt.datetime]
            Only domains created before this ISO 8601 timestamp.

        created_after : typing.Optional[dt.datetime]
            Only domains created after this ISO 8601 timestamp.

        order : typing.Optional[ListPaymentMethodDomainsRequestOrder]
            Sort field.

        direction : typing.Optional[ListPaymentMethodDomainsRequestDirection]
            Sort direction.

        first : typing.Optional[int]
            Number of domains to return from the start of the window.

        after : typing.Optional[str]
            Cursor to paginate forwards from.

        last : typing.Optional[int]
            Number of domains to return from the end of the window.

        before : typing.Optional[str]
            Cursor to paginate backwards from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PaymentMethodDomain, ListPaymentMethodDomainsResponse]
            payment method domains listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.payment_method_domains.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            hostname=hostname,
            status=status,
            provider=provider,
            created_before=created_before,
            created_after=created_after,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        hostname: str,
        account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentMethodDomain:
        """
        Registers a hostname with the wallet provider and attempts verification inline. Returns `verified` when the provider fetched the domain-association file (for Apple Pay, `/.well-known/apple-developer-merchantid-domain-association`), or `pending` when it could not — host the file, then retry with the verify endpoint.

        Parameters
        ----------
        hostname : str
            Hostname to register (e.g. `checkout.shinetime.example`).

        account_id : typing.Optional[str]
            Account to register the domain for (`biz_` tag). Defaults to the caller's account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentMethodDomain
            domain registered as pending when verification fails

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_method_domains.create(
                hostname="pending.shinetime.example",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            hostname=hostname, account_id=account_id, request_options=request_options
        )
        return _response.data

    async def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PaymentMethodDomain:
        """
        Retrieves a payment method domain to check its verification status.

        Parameters
        ----------
        id : str
            The unique identifier of the payment method domain, prefixed `pmd_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentMethodDomain
            payment method domain retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_method_domains.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeletePaymentMethodDomainsResponse:
        """
        Unregisters a payment method domain so its wallet payment methods stop rendering there.

        Parameters
        ----------
        id : str
            The unique identifier of the payment method domain, prefixed `pmd_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletePaymentMethodDomainsResponse
            payment method domain deleted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_method_domains.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def verify(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentMethodDomain:
        """
        Re-attempts provider verification of a pending domain once the association file is hosted. Fails with a `bad_request` explaining what to fix; verifying an already `verified` domain is a no-op.

        Parameters
        ----------
        id : str
            The unique identifier of the payment method domain, prefixed `pmd_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentMethodDomain
            already verified domain is a no-op

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_method_domains.verify(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.verify(id, request_options=request_options)
        return _response.data
