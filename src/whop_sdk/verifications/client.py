# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawVerificationsClient, RawVerificationsClient
from .types.create_verifications_request_body import CreateVerificationsRequestBody
from .types.create_verifications_response import CreateVerificationsResponse
from .types.list_verifications_request_direction import ListVerificationsRequestDirection
from .types.list_verifications_request_order import ListVerificationsRequestOrder
from .types.list_verifications_response import ListVerificationsResponse
from .types.retrieve_verifications_response import RetrieveVerificationsResponse
from .types.update_verifications_request_body import UpdateVerificationsRequestBody
from .types.update_verifications_response import UpdateVerificationsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class VerificationsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawVerificationsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawVerificationsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawVerificationsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: str,
        order: typing.Optional[ListVerificationsRequestOrder] = None,
        direction: typing.Optional[ListVerificationsRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListVerificationsResponse:
        """
        Returns verifications for an account, including their status and any required actions.

        Parameters
        ----------
        account_id : str
            Account or user ID whose verifications you want to list. Use a `biz_` account ID, or the caller's `user_` ID for personal verifications.

        order : typing.Optional[ListVerificationsRequestOrder]
            Field used to sort returned verifications.

        direction : typing.Optional[ListVerificationsRequestDirection]
            Sort direction for returned verifications.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListVerificationsResponse
            OK

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.verifications.list(
            account_id="account_id",
        )
        """
        _response = self._raw_client.list(
            account_id=account_id, order=order, direction=direction, request_options=request_options
        )
        return _response.data

    def create(
        self,
        *,
        account_id: str,
        request: CreateVerificationsRequestBody,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateVerificationsResponse:
        """
        Starts a hosted verification session for an account or user, or returns the active session when one already exists. Any fields you include in the request body are used to prefill the session. Send `documents` (with `document_type`) to instead verify the person from identity documents included in this request — no hosted session involved. Send `share_token` to reuse a verification another Sumsub account has already completed for this person, instead of verifying them again. If the account already has an `approved` verification the request is rejected; unlink it first to start a new one.

        Parameters
        ----------
        account_id : str
            Account or user ID whose identity you want to verify. Use a `biz_` account ID for account verifications, or the caller's `user_` ID for personal verification.

        request : CreateVerificationsRequestBody

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateVerificationsResponse
            OK

        Examples
        --------
        from whop_sdk import Whop
        from whop_sdk.verifications import CreateVerificationsRequestBody_Individual

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.verifications.create(
            account_id="account_id",
            request=CreateVerificationsRequestBody_Individual(),
        )
        """
        _response = self._raw_client.create(account_id=account_id, request=request, request_options=request_options)
        return _response.data

    def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> RetrieveVerificationsResponse:
        """
        Returns verifications for an account, including their status and any required actions.

        Parameters
        ----------
        id : str
            Verification profile ID, prefixed `idpf_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrieveVerificationsResponse
            verification retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.verifications.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        request: UpdateVerificationsRequestBody,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateVerificationsResponse:
        """
        Updates editable profile details or submits answers for items returned in `requested_information`. Once a verification is `approved` its profile details are locked and can no longer be edited.

        Parameters
        ----------
        id : str
            Verification profile ID, prefixed `idpf_`.

        request : UpdateVerificationsRequestBody

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateVerificationsResponse
            verification updated

        Examples
        --------
        from whop_sdk import Whop
        from whop_sdk.verifications import UpdateVerificationsRequestBodyPersonalAddress

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.verifications.update(
            id="id",
            request=UpdateVerificationsRequestBodyPersonalAddress(),
        )
        """
        _response = self._raw_client.update(id, request=request, request_options=request_options)
        return _response.data


class AsyncVerificationsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawVerificationsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawVerificationsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawVerificationsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: str,
        order: typing.Optional[ListVerificationsRequestOrder] = None,
        direction: typing.Optional[ListVerificationsRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListVerificationsResponse:
        """
        Returns verifications for an account, including their status and any required actions.

        Parameters
        ----------
        account_id : str
            Account or user ID whose verifications you want to list. Use a `biz_` account ID, or the caller's `user_` ID for personal verifications.

        order : typing.Optional[ListVerificationsRequestOrder]
            Field used to sort returned verifications.

        direction : typing.Optional[ListVerificationsRequestDirection]
            Sort direction for returned verifications.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListVerificationsResponse
            OK

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.verifications.list(
                account_id="account_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            account_id=account_id, order=order, direction=direction, request_options=request_options
        )
        return _response.data

    async def create(
        self,
        *,
        account_id: str,
        request: CreateVerificationsRequestBody,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateVerificationsResponse:
        """
        Starts a hosted verification session for an account or user, or returns the active session when one already exists. Any fields you include in the request body are used to prefill the session. Send `documents` (with `document_type`) to instead verify the person from identity documents included in this request — no hosted session involved. Send `share_token` to reuse a verification another Sumsub account has already completed for this person, instead of verifying them again. If the account already has an `approved` verification the request is rejected; unlink it first to start a new one.

        Parameters
        ----------
        account_id : str
            Account or user ID whose identity you want to verify. Use a `biz_` account ID for account verifications, or the caller's `user_` ID for personal verification.

        request : CreateVerificationsRequestBody

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateVerificationsResponse
            OK

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop
        from whop_sdk.verifications import CreateVerificationsRequestBody_Individual

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.verifications.create(
                account_id="account_id",
                request=CreateVerificationsRequestBody_Individual(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            account_id=account_id, request=request, request_options=request_options
        )
        return _response.data

    async def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> RetrieveVerificationsResponse:
        """
        Returns verifications for an account, including their status and any required actions.

        Parameters
        ----------
        id : str
            Verification profile ID, prefixed `idpf_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrieveVerificationsResponse
            verification retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.verifications.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        request: UpdateVerificationsRequestBody,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateVerificationsResponse:
        """
        Updates editable profile details or submits answers for items returned in `requested_information`. Once a verification is `approved` its profile details are locked and can no longer be edited.

        Parameters
        ----------
        id : str
            Verification profile ID, prefixed `idpf_`.

        request : UpdateVerificationsRequestBody

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateVerificationsResponse
            verification updated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop
        from whop_sdk.verifications import UpdateVerificationsRequestBodyPersonalAddress

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.verifications.update(
                id="id",
                request=UpdateVerificationsRequestBodyPersonalAddress(),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(id, request=request, request_options=request_options)
        return _response.data
