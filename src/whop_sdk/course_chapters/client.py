# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.course_chapter import CourseChapter
from ..types.course_chapter_list_item import CourseChapterListItem
from .raw_client import AsyncRawCourseChaptersClient, RawCourseChaptersClient
from .types.list_course_chapters_response import ListCourseChaptersResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class CourseChaptersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCourseChaptersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCourseChaptersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCourseChaptersClient
        """
        return self._raw_client

    def list(
        self,
        *,
        course_id: str,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[CourseChapterListItem, ListCourseChaptersResponse]:
        """
        Returns a paginated list of chapters within a course, ordered by position.

        Required permissions:
         - `courses:read`

        Parameters
        ----------
        course_id : str
            The unique identifier of the course to list chapters for.

        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[CourseChapterListItem, ListCourseChaptersResponse]
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.course_chapters.list(
            first=42,
            last=42,
            course_id="cors_xxxxxxxxxxxxx",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            course_id=course_id, after=after, before=before, first=first, last=last, request_options=request_options
        )

    def create(
        self,
        *,
        course_id: str,
        title: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CourseChapter:
        """
        Create a new chapter within a course to organize lessons into sections.

        Required permissions:
         - `courses:update`

        Parameters
        ----------
        course_id : str
            The unique identifier of the course to create the chapter in (e.g., "course_XXXXX").

        title : typing.Optional[str]
            The display title of the chapter (e.g., "Module 1: Introduction").

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CourseChapter
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.course_chapters.create(
            course_id="cors_xxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.create(course_id=course_id, title=title, request_options=request_options)
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> CourseChapter:
        """
        Retrieves the details of an existing course chapter.

        Required permissions:
         - `courses:read`

        Parameters
        ----------
        id : str
            The unique identifier of the chapter to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CourseChapter
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.course_chapters.retrieve(
            id="chap_xxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> bool:
        """
        Permanently delete a chapter and all of its lessons from a course.

        Required permissions:
         - `courses:update`

        Parameters
        ----------
        id : str
            The unique identifier of the chapter to delete (e.g., "chap_XXXXX").

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        bool
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.course_chapters.delete(
            id="chap_xxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(self, id: str, *, title: str, request_options: typing.Optional[RequestOptions] = None) -> CourseChapter:
        """
        Update a chapter's title within a course.

        Required permissions:
         - `courses:update`

        Parameters
        ----------
        id : str
            The unique identifier of the chapter to update (e.g., "chap_XXXXX").

        title : str
            The new display title of the chapter (e.g., "Module 1: Introduction").

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CourseChapter
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.course_chapters.update(
            id="chap_xxxxxxxxxxxxx",
            title="title",
        )
        """
        _response = self._raw_client.update(id, title=title, request_options=request_options)
        return _response.data


class AsyncCourseChaptersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCourseChaptersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCourseChaptersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCourseChaptersClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        course_id: str,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[CourseChapterListItem, ListCourseChaptersResponse]:
        """
        Returns a paginated list of chapters within a course, ordered by position.

        Required permissions:
         - `courses:read`

        Parameters
        ----------
        course_id : str
            The unique identifier of the course to list chapters for.

        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[CourseChapterListItem, ListCourseChaptersResponse]
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.course_chapters.list(
                first=42,
                last=42,
                course_id="cors_xxxxxxxxxxxxx",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            course_id=course_id, after=after, before=before, first=first, last=last, request_options=request_options
        )

    async def create(
        self,
        *,
        course_id: str,
        title: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CourseChapter:
        """
        Create a new chapter within a course to organize lessons into sections.

        Required permissions:
         - `courses:update`

        Parameters
        ----------
        course_id : str
            The unique identifier of the course to create the chapter in (e.g., "course_XXXXX").

        title : typing.Optional[str]
            The display title of the chapter (e.g., "Module 1: Introduction").

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CourseChapter
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.course_chapters.create(
                course_id="cors_xxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(course_id=course_id, title=title, request_options=request_options)
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> CourseChapter:
        """
        Retrieves the details of an existing course chapter.

        Required permissions:
         - `courses:read`

        Parameters
        ----------
        id : str
            The unique identifier of the chapter to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CourseChapter
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.course_chapters.retrieve(
                id="chap_xxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> bool:
        """
        Permanently delete a chapter and all of its lessons from a course.

        Required permissions:
         - `courses:update`

        Parameters
        ----------
        id : str
            The unique identifier of the chapter to delete (e.g., "chap_XXXXX").

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        bool
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.course_chapters.delete(
                id="chap_xxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self, id: str, *, title: str, request_options: typing.Optional[RequestOptions] = None
    ) -> CourseChapter:
        """
        Update a chapter's title within a course.

        Required permissions:
         - `courses:update`

        Parameters
        ----------
        id : str
            The unique identifier of the chapter to update (e.g., "chap_XXXXX").

        title : str
            The new display title of the chapter (e.g., "Module 1: Introduction").

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CourseChapter
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.course_chapters.update(
                id="chap_xxxxxxxxxxxxx",
                title="title",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(id, title=title, request_options=request_options)
        return _response.data
