# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.audience import Audience
from .raw_client import AsyncRawAudiencesClient, RawAudiencesClient
from .types.create_audiences_request_audience_type import CreateAudiencesRequestAudienceType
from .types.create_audiences_request_column_mapping import CreateAudiencesRequestColumnMapping
from .types.create_audiences_request_engagement import CreateAudiencesRequestEngagement
from .types.create_audiences_request_source_type import CreateAudiencesRequestSourceType
from .types.create_audiences_response import CreateAudiencesResponse
from .types.delete_audiences_response import DeleteAudiencesResponse
from .types.list_audiences_request_audience_type import ListAudiencesRequestAudienceType
from .types.list_audiences_request_source_type import ListAudiencesRequestSourceType
from .types.list_audiences_response import ListAudiencesResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AudiencesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAudiencesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAudiencesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAudiencesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: str,
        audience_id: typing.Optional[str] = None,
        audience_type: typing.Optional[ListAudiencesRequestAudienceType] = None,
        source_type: typing.Optional[ListAudiencesRequestSourceType] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Audience, ListAudiencesResponse]:
        """
        List custom and lookalike audiences for an account. Pass `audience_id` to return a specific audience.

        Parameters
        ----------
        account_id : str
            Account ID, prefixed `biz_`.

        audience_id : typing.Optional[str]
            Audience ID, prefixed `adaud_`, used to filter the response to one audience.

        audience_type : typing.Optional[ListAudiencesRequestAudienceType]
            Filter by custom or lookalike audiences.

        source_type : typing.Optional[ListAudiencesRequestSourceType]
            Filter by uploaded customer lists, Whop People filters, or social engagement.

        first : typing.Optional[int]
            Number of audiences to return. Defaults to 20; maximum 100.

        after : typing.Optional[str]
            Cursor for the next page of audiences.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Audience, ListAudiencesResponse]
            Audiences listed.

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.audiences.list(
            account_id="account_id",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            audience_id=audience_id,
            audience_type=audience_type,
            source_type=source_type,
            first=first,
            after=after,
            request_options=request_options,
        )

    def create(
        self,
        *,
        account_id: str,
        audience_type: typing.Optional[CreateAudiencesRequestAudienceType] = OMIT,
        auto_refresh: typing.Optional[bool] = OMIT,
        column_mapping: typing.Optional[CreateAudiencesRequestColumnMapping] = OMIT,
        count: typing.Optional[int] = OMIT,
        engagement: typing.Optional[CreateAudiencesRequestEngagement] = OMIT,
        file_id: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        name: typing.Optional[str] = OMIT,
        percentage: typing.Optional[int] = OMIT,
        source_audience_id: typing.Optional[str] = OMIT,
        source_type: typing.Optional[CreateAudiencesRequestSourceType] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateAudiencesResponse:
        """
        Create an audience from a customer list, your account's Whop People data, or engagement with videos, lead forms, Instagram profiles, or Facebook pages. Create lookalike audiences to reach people similar to an existing audience. Processing runs asynchronously. Custom creation returns one audience; lookalike creation returns the requested similarity bands in `data`.

        Parameters
        ----------
        account_id : str
            Account ID, prefixed `biz_`.

        audience_type : typing.Optional[CreateAudiencesRequestAudienceType]
            Audience type. Defaults to `custom`.

        auto_refresh : typing.Optional[bool]
            Filter audiences only, and set only at creation. `true` (the default) rebuilds membership from the filters twice a day. `false` keeps whoever matched at creation and never rebuilds.

        column_mapping : typing.Optional[CreateAudiencesRequestColumnMapping]
            CSV audiences only. Maps supported identity fields to CSV column headers. Map at least one of `email` or `phone`.

        count : typing.Optional[int]
            Lookalikes only. Number of lookalike audiences to create (1–6).

        engagement : typing.Optional[CreateAudiencesRequestEngagement]
            Rules for membership based on social engagement. Requires a connected social account with advertising access.

        file_id : typing.Optional[str]
            CSV audiences only. The uploaded customer CSV — a file id (`file_...`) returned by `POST /files`.

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter audiences only. The People filters that define membership, keyed exactly as `GET /people` accepts them — for example `{"os": "iOS", "country": "US"}`. Date filters must be rolling windows — `first_seen_within_days` or `last_seen_within_days` — so the audience re-anchors on every refresh; fixed dates such as `first_seen_after` are rejected. Source values are canonical source paths (`whop:<campaign>:<group>:<ad>`, `ext:<platform>:...`, `referrer:<domain>`, `direct`), exact or with a trailing `:*` wildcard.

        name : typing.Optional[str]
            Audience display name. Required for custom audiences; lookalike names are generated from the source audience.

        percentage : typing.Optional[int]
            Lookalikes only. Total similarity reach as a whole percent (1–20), sliced evenly across `count` — must be divisible by `count`. For example, 3 audiences at 6% creates 0–2%, 2–4%, and 4–6% bands.

        source_audience_id : typing.Optional[str]
            Lookalikes only. The ready custom audience (`adaud_`) to build from; uploaded and People audiences need at least 100 matched people. Meta validates engagement audience eligibility when creating the lookalike.

        source_type : typing.Optional[CreateAudiencesRequestSourceType]
            Custom audience source. Inferred from `engagement`, then `filters`, otherwise defaults to `csv_upload`. Supply only the fields for the selected source.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateAudiencesResponse
            Audience created. Custom creation returns one audience; lookalike creation returns an array in `data`.

        Examples
        --------
        from whop_sdk import AudienceEngagementRule_FacebookPage, Whop
        from whop_sdk.audiences import CreateAudiencesRequestEngagement

        client = Whop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.audiences.create(
            account_id="biz_xxxxxxxxxxxxxx",
            engagement=CreateAudiencesRequestEngagement(
                include=[
                    AudienceEngagementRule_FacebookPage(
                        event="engaged",
                        retention_days=30,
                        social_account_id="sacc_xxxxxxxxxxxxxx",
                    )
                ],
                platform="meta",
            ),
            name="Page engagers",
            source_type="engagement",
        )
        """
        _response = self._raw_client.create(
            account_id=account_id,
            audience_type=audience_type,
            auto_refresh=auto_refresh,
            column_mapping=column_mapping,
            count=count,
            engagement=engagement,
            file_id=file_id,
            filters=filters,
            name=name,
            percentage=percentage,
            source_audience_id=source_audience_id,
            source_type=source_type,
            request_options=request_options,
        )
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeleteAudiencesResponse:
        """
        Deletes an audience so it is no longer available for targeting.

        Parameters
        ----------
        id : str
            Audience ID, prefixed `adaud_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteAudiencesResponse
            Audience deleted.

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.audiences.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        name: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Audience:
        """
        Renames an audience. For an audience built from People filters that keeps itself up to date, pass `filters` to replace them, which rebuilds membership immediately. Whether an audience auto refreshes is set when it is created.

        Parameters
        ----------
        id : str
            Audience ID, prefixed `adaud_`.

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Replaces the People filters that define membership. The whole definition is replaced rather than merged, so send every filter you want to keep — a filter you leave out stops applying. Keys and values are the ones `GET /people` accepts, such as an `os` of `iOS` or a `country` of `US`, and at least one filter is required. Date filters must be rolling windows — `first_seen_within_days` or `last_seen_within_days` — so the audience re-anchors every time it rebuilds; fixed dates such as `first_seen_after` are rejected, as is `audience_id`. An array value holds at most 500 items, and each value at most 10 KB. Only an audience with a `source_type` of `people_filter` and `auto_refresh` of `true` accepts filters: an uploaded list has no filters to replace, and with auto refresh off the audience keeps the people it matched when it was built, so create a new audience instead.

        name : typing.Optional[str]
            New audience display name. A blank value is ignored rather than clearing the name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Audience
            Audience updated.

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.audiences.update(
            id="id",
        )
        """
        _response = self._raw_client.update(id, filters=filters, name=name, request_options=request_options)
        return _response.data

    def add_people(self, id: str, *, file_id: str, request_options: typing.Optional[RequestOptions] = None) -> Audience:
        """
        Adds users from a new CSV file to an existing uploaded custom audience. The file uses the audience's saved column mapping, processing happens in the background, and existing audience members remain unchanged.

        Parameters
        ----------
        id : str
            Audience ID, prefixed `adaud_`.

        file_id : str
            The new customer CSV — a file id (`file_...`) returned by `POST /files`. Its headers must match the audience's saved column mapping.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Audience
            Audience upload accepted.

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.audiences.add_people(
            id="id",
            file_id="file_xxxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.add_people(id, file_id=file_id, request_options=request_options)
        return _response.data


class AsyncAudiencesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAudiencesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAudiencesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAudiencesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: str,
        audience_id: typing.Optional[str] = None,
        audience_type: typing.Optional[ListAudiencesRequestAudienceType] = None,
        source_type: typing.Optional[ListAudiencesRequestSourceType] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Audience, ListAudiencesResponse]:
        """
        List custom and lookalike audiences for an account. Pass `audience_id` to return a specific audience.

        Parameters
        ----------
        account_id : str
            Account ID, prefixed `biz_`.

        audience_id : typing.Optional[str]
            Audience ID, prefixed `adaud_`, used to filter the response to one audience.

        audience_type : typing.Optional[ListAudiencesRequestAudienceType]
            Filter by custom or lookalike audiences.

        source_type : typing.Optional[ListAudiencesRequestSourceType]
            Filter by uploaded customer lists, Whop People filters, or social engagement.

        first : typing.Optional[int]
            Number of audiences to return. Defaults to 20; maximum 100.

        after : typing.Optional[str]
            Cursor for the next page of audiences.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Audience, ListAudiencesResponse]
            Audiences listed.

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.audiences.list(
                account_id="account_id",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            audience_id=audience_id,
            audience_type=audience_type,
            source_type=source_type,
            first=first,
            after=after,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        account_id: str,
        audience_type: typing.Optional[CreateAudiencesRequestAudienceType] = OMIT,
        auto_refresh: typing.Optional[bool] = OMIT,
        column_mapping: typing.Optional[CreateAudiencesRequestColumnMapping] = OMIT,
        count: typing.Optional[int] = OMIT,
        engagement: typing.Optional[CreateAudiencesRequestEngagement] = OMIT,
        file_id: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        name: typing.Optional[str] = OMIT,
        percentage: typing.Optional[int] = OMIT,
        source_audience_id: typing.Optional[str] = OMIT,
        source_type: typing.Optional[CreateAudiencesRequestSourceType] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateAudiencesResponse:
        """
        Create an audience from a customer list, your account's Whop People data, or engagement with videos, lead forms, Instagram profiles, or Facebook pages. Create lookalike audiences to reach people similar to an existing audience. Processing runs asynchronously. Custom creation returns one audience; lookalike creation returns the requested similarity bands in `data`.

        Parameters
        ----------
        account_id : str
            Account ID, prefixed `biz_`.

        audience_type : typing.Optional[CreateAudiencesRequestAudienceType]
            Audience type. Defaults to `custom`.

        auto_refresh : typing.Optional[bool]
            Filter audiences only, and set only at creation. `true` (the default) rebuilds membership from the filters twice a day. `false` keeps whoever matched at creation and never rebuilds.

        column_mapping : typing.Optional[CreateAudiencesRequestColumnMapping]
            CSV audiences only. Maps supported identity fields to CSV column headers. Map at least one of `email` or `phone`.

        count : typing.Optional[int]
            Lookalikes only. Number of lookalike audiences to create (1–6).

        engagement : typing.Optional[CreateAudiencesRequestEngagement]
            Rules for membership based on social engagement. Requires a connected social account with advertising access.

        file_id : typing.Optional[str]
            CSV audiences only. The uploaded customer CSV — a file id (`file_...`) returned by `POST /files`.

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter audiences only. The People filters that define membership, keyed exactly as `GET /people` accepts them — for example `{"os": "iOS", "country": "US"}`. Date filters must be rolling windows — `first_seen_within_days` or `last_seen_within_days` — so the audience re-anchors on every refresh; fixed dates such as `first_seen_after` are rejected. Source values are canonical source paths (`whop:<campaign>:<group>:<ad>`, `ext:<platform>:...`, `referrer:<domain>`, `direct`), exact or with a trailing `:*` wildcard.

        name : typing.Optional[str]
            Audience display name. Required for custom audiences; lookalike names are generated from the source audience.

        percentage : typing.Optional[int]
            Lookalikes only. Total similarity reach as a whole percent (1–20), sliced evenly across `count` — must be divisible by `count`. For example, 3 audiences at 6% creates 0–2%, 2–4%, and 4–6% bands.

        source_audience_id : typing.Optional[str]
            Lookalikes only. The ready custom audience (`adaud_`) to build from; uploaded and People audiences need at least 100 matched people. Meta validates engagement audience eligibility when creating the lookalike.

        source_type : typing.Optional[CreateAudiencesRequestSourceType]
            Custom audience source. Inferred from `engagement`, then `filters`, otherwise defaults to `csv_upload`. Supply only the fields for the selected source.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateAudiencesResponse
            Audience created. Custom creation returns one audience; lookalike creation returns an array in `data`.

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop, AudienceEngagementRule_FacebookPage
        from whop_sdk.audiences import CreateAudiencesRequestEngagement

        client = AsyncWhop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.audiences.create(
                account_id="biz_xxxxxxxxxxxxxx",
                engagement=CreateAudiencesRequestEngagement(
                    include=[
                        AudienceEngagementRule_FacebookPage(
                            event="engaged",
                            retention_days=30,
                            social_account_id="sacc_xxxxxxxxxxxxxx",
                        )
                    ],
                    platform="meta",
                ),
                name="Page engagers",
                source_type="engagement",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            account_id=account_id,
            audience_type=audience_type,
            auto_refresh=auto_refresh,
            column_mapping=column_mapping,
            count=count,
            engagement=engagement,
            file_id=file_id,
            filters=filters,
            name=name,
            percentage=percentage,
            source_audience_id=source_audience_id,
            source_type=source_type,
            request_options=request_options,
        )
        return _response.data

    async def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteAudiencesResponse:
        """
        Deletes an audience so it is no longer available for targeting.

        Parameters
        ----------
        id : str
            Audience ID, prefixed `adaud_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteAudiencesResponse
            Audience deleted.

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.audiences.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        name: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Audience:
        """
        Renames an audience. For an audience built from People filters that keeps itself up to date, pass `filters` to replace them, which rebuilds membership immediately. Whether an audience auto refreshes is set when it is created.

        Parameters
        ----------
        id : str
            Audience ID, prefixed `adaud_`.

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Replaces the People filters that define membership. The whole definition is replaced rather than merged, so send every filter you want to keep — a filter you leave out stops applying. Keys and values are the ones `GET /people` accepts, such as an `os` of `iOS` or a `country` of `US`, and at least one filter is required. Date filters must be rolling windows — `first_seen_within_days` or `last_seen_within_days` — so the audience re-anchors every time it rebuilds; fixed dates such as `first_seen_after` are rejected, as is `audience_id`. An array value holds at most 500 items, and each value at most 10 KB. Only an audience with a `source_type` of `people_filter` and `auto_refresh` of `true` accepts filters: an uploaded list has no filters to replace, and with auto refresh off the audience keeps the people it matched when it was built, so create a new audience instead.

        name : typing.Optional[str]
            New audience display name. A blank value is ignored rather than clearing the name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Audience
            Audience updated.

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.audiences.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(id, filters=filters, name=name, request_options=request_options)
        return _response.data

    async def add_people(
        self, id: str, *, file_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> Audience:
        """
        Adds users from a new CSV file to an existing uploaded custom audience. The file uses the audience's saved column mapping, processing happens in the background, and existing audience members remain unchanged.

        Parameters
        ----------
        id : str
            Audience ID, prefixed `adaud_`.

        file_id : str
            The new customer CSV — a file id (`file_...`) returned by `POST /files`. Its headers must match the audience's saved column mapping.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Audience
            Audience upload accepted.

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-11-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.audiences.add_people(
                id="id",
                file_id="file_xxxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.add_people(id, file_id=file_id, request_options=request_options)
        return _response.data
