# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.product import Product
from ..types.product_list_item import ProductListItem
from .raw_client import AsyncRawProductsClient, RawProductsClient
from .types.create_products_request_custom_cta import CreateProductsRequestCustomCta
from .types.create_products_request_global_affiliate_status import CreateProductsRequestGlobalAffiliateStatus
from .types.create_products_request_member_affiliate_status import CreateProductsRequestMemberAffiliateStatus
from .types.delete_products_response import DeleteProductsResponse
from .types.list_products_request_direction import ListProductsRequestDirection
from .types.list_products_request_plan_types_item import ListProductsRequestPlanTypesItem
from .types.list_products_response import ListProductsResponse
from .types.update_products_request_banner_image import UpdateProductsRequestBannerImage

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ProductsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawProductsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawProductsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawProductsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        query: typing.Optional[str] = None,
        marketplace_category_route: typing.Optional[str] = None,
        plan_types: typing.Optional[
            typing.Union[ListProductsRequestPlanTypesItem, typing.Sequence[ListProductsRequestPlanTypesItem]]
        ] = None,
        price_minimum: typing.Optional[float] = None,
        price_maximum: typing.Optional[float] = None,
        visibilities: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        access_pass_types: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        labels: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        direction: typing.Optional[ListProductsRequestDirection] = None,
        order: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        created_before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ProductListItem, ListProductsResponse]:
        """
        Returns a paginated list of products. Omit `account_id` to search the public marketplace.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The unique identifier of the account to list products for. Omit to search the public marketplace.

        query : typing.Optional[str]
            Ranked search against product title and headline. Omit to browse by recency.

        marketplace_category_route : typing.Optional[str]
            Only return marketplace products assigned to this category route, such as `trading`.

        plan_types : typing.Optional[typing.Union[ListProductsRequestPlanTypesItem, typing.Sequence[ListProductsRequestPlanTypesItem]]]
            Filter to products with a buyable plan of these billing models, such as `one_time` or `renewal`.

        price_minimum : typing.Optional[float]
            Only return products whose advertised buyable plan has a displayed price of at least this amount. Recurring plans use renewal price.

        price_maximum : typing.Optional[float]
            Only return products whose advertised buyable plan has a displayed price of at most this amount. Recurring plans use renewal price.

        visibilities : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only products matching these visibility states. Ignored on the public marketplace list, which only returns visible products.

        access_pass_types : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only products matching these types.

        labels : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only products carrying all of these labels. Labels are matched lowercased.

        direction : typing.Optional[ListProductsRequestDirection]
            The sort direction for results. Defaults to descending.

        order : typing.Optional[str]
            The field to sort results by. Account lists default to `created_at`. Marketplace lists default to `discoverable_at` and accept `created_at` or `discoverable_at`. Cannot be combined with `query`.

        first : typing.Optional[int]
            The number of products to return (default and max 100).

        after : typing.Optional[str]
            A cursor; returns products after this position.

        last : typing.Optional[int]
            The number of products to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns products before this position.

        created_after : typing.Optional[str]
            Only return products created after this ISO 8601 timestamp.

        created_before : typing.Optional[str]
            Only return products created before this ISO 8601 timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ProductListItem, ListProductsResponse]
            marketplace products listed without an account

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.products.list(
            visibilities=["visible"],
            access_pass_types=["regular"],
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            query=query,
            marketplace_category_route=marketplace_category_route,
            plan_types=plan_types,
            price_minimum=price_minimum,
            price_maximum=price_maximum,
            visibilities=visibilities,
            access_pass_types=access_pass_types,
            labels=labels,
            direction=direction,
            order=order,
            first=first,
            after=after,
            last=last,
            before=before,
            created_after=created_after,
            created_before=created_before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        title: str,
        account_id: typing.Optional[str] = OMIT,
        collect_shipping_address: typing.Optional[bool] = OMIT,
        custom_cta: typing.Optional[CreateProductsRequestCustomCta] = OMIT,
        custom_cta_url: typing.Optional[str] = OMIT,
        custom_statement_descriptor: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        global_affiliate_percentage: typing.Optional[float] = OMIT,
        global_affiliate_status: typing.Optional[CreateProductsRequestGlobalAffiliateStatus] = OMIT,
        headline: typing.Optional[str] = OMIT,
        labels: typing.Optional[typing.Sequence[str]] = OMIT,
        member_affiliate_percentage: typing.Optional[float] = OMIT,
        member_affiliate_status: typing.Optional[CreateProductsRequestMemberAffiliateStatus] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        product_tax_code_id: typing.Optional[str] = OMIT,
        redirect_purchase_url: typing.Optional[str] = OMIT,
        route: typing.Optional[str] = OMIT,
        send_welcome_message: typing.Optional[bool] = OMIT,
        visibility: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Product:
        """
        Creates a new product for an account.

        Parameters
        ----------
        title : str
            The display name of the product. Maximum 80 characters.

        account_id : typing.Optional[str]
            The unique identifier of the account to create this product for.

        collect_shipping_address : typing.Optional[bool]
            Whether to collect a shipping address at checkout.

        custom_cta : typing.Optional[CreateProductsRequestCustomCta]
            The call-to-action button label.

        custom_cta_url : typing.Optional[str]
            A URL the call-to-action button links to.

        custom_statement_descriptor : typing.Optional[str]
            Custom bank statement descriptor. Must start with WHOP*.

        description : typing.Optional[str]
            A written description displayed on the product page.

        global_affiliate_percentage : typing.Optional[float]
            The commission rate affiliates earn.

        global_affiliate_status : typing.Optional[CreateProductsRequestGlobalAffiliateStatus]
            The enrollment status in the global affiliate program.

        headline : typing.Optional[str]
            A short marketing headline for the product page.

        labels : typing.Optional[typing.Sequence[str]]
            Labels used to group products into collections. Stored lowercased and de-duplicated. Maximum 20 labels, 50 characters each.

        member_affiliate_percentage : typing.Optional[float]
            The commission rate members earn.

        member_affiliate_status : typing.Optional[CreateProductsRequestMemberAffiliateStatus]
            The enrollment status in the member affiliate program.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the product.

        product_tax_code_id : typing.Optional[str]
            The unique identifier of the tax classification code. See the available [product categories](https://docs.numeral.com/essentials/product-categories).

        redirect_purchase_url : typing.Optional[str]
            A URL to redirect the customer to after purchase.

        route : typing.Optional[str]
            The URL slug for the product's public link.

        send_welcome_message : typing.Optional[bool]
            Whether to send an automated welcome message via support chat when a user joins this product. Defaults to true.

        visibility : typing.Optional[str]
            Whether the product is visible to customers.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.products.create(
            title="Interior Deep Clean",
        )
        """
        _response = self._raw_client.create(
            title=title,
            account_id=account_id,
            collect_shipping_address=collect_shipping_address,
            custom_cta=custom_cta,
            custom_cta_url=custom_cta_url,
            custom_statement_descriptor=custom_statement_descriptor,
            description=description,
            global_affiliate_percentage=global_affiliate_percentage,
            global_affiliate_status=global_affiliate_status,
            headline=headline,
            labels=labels,
            member_affiliate_percentage=member_affiliate_percentage,
            member_affiliate_status=member_affiliate_status,
            metadata=metadata,
            product_tax_code_id=product_tax_code_id,
            redirect_purchase_url=redirect_purchase_url,
            route=route,
            send_welcome_message=send_welcome_message,
            visibility=visibility,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Product:
        """
        Retrieves a product. Public — no credentials.

        Parameters
        ----------
        id : str
            The unique identifier of the product.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product retrieved without authentication

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.products.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeleteProductsResponse:
        """
        Deletes a product. Only products with no memberships, entries, reviews, or invoices can be deleted.

        Parameters
        ----------
        id : str
            The unique identifier of the product.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteProductsResponse
            product deleted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.products.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        banner_image: typing.Optional[UpdateProductsRequestBannerImage] = OMIT,
        description: typing.Optional[str] = OMIT,
        headline: typing.Optional[str] = OMIT,
        labels: typing.Optional[typing.Sequence[str]] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        product_tax_code_id: typing.Optional[str] = OMIT,
        send_welcome_message: typing.Optional[bool] = OMIT,
        title: typing.Optional[str] = OMIT,
        visibility: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Product:
        """
        Updates an existing product.

        Parameters
        ----------
        id : str
            The unique identifier of the product.

        banner_image : typing.Optional[UpdateProductsRequestBannerImage]
            A wide image for the product, shown on the product page and on listing cards. Pass `{ id }` for an existing attachment or `{ direct_upload_id }` for a completed direct upload; `null` removes it.

        description : typing.Optional[str]
            A written description displayed on the product page.

        headline : typing.Optional[str]
            A short marketing headline for the product page.

        labels : typing.Optional[typing.Sequence[str]]
            Labels used to group products into collections. Replaces the existing labels. Send an empty array to clear them.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the product.

        product_tax_code_id : typing.Optional[str]
            The unique identifier of the tax classification code. See the available [product categories](https://docs.numeral.com/essentials/product-categories).

        send_welcome_message : typing.Optional[bool]
            Whether to send an automated welcome message via support chat when a user joins this product.

        title : typing.Optional[str]
            The display name of the product.

        visibility : typing.Optional[str]
            Whether the product is visible to customers.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product tax code updated

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.products.update(
            id="id",
        )
        """
        _response = self._raw_client.update(
            id,
            banner_image=banner_image,
            description=description,
            headline=headline,
            labels=labels,
            metadata=metadata,
            product_tax_code_id=product_tax_code_id,
            send_welcome_message=send_welcome_message,
            title=title,
            visibility=visibility,
            request_options=request_options,
        )
        return _response.data

    def publish(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Product:
        """
        Submits a product to the whop.com marketplace for review. The product moves to `pending_review`; a Whop reviewer approves it before it goes live.

        Parameters
        ----------
        id : str
            The unique identifier of the product, prefixed `prod_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product submitted for review

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.products.publish(
            id="id",
        )
        """
        _response = self._raw_client.publish(id, request_options=request_options)
        return _response.data

    def unpublish(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Product:
        """
        Removes a product from the whop.com marketplace. The product moves to `not_available`.

        Parameters
        ----------
        id : str
            The unique identifier of the product, prefixed `prod_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product removed from the marketplace

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.products.unpublish(
            id="id",
        )
        """
        _response = self._raw_client.unpublish(id, request_options=request_options)
        return _response.data


class AsyncProductsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawProductsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawProductsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawProductsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        query: typing.Optional[str] = None,
        marketplace_category_route: typing.Optional[str] = None,
        plan_types: typing.Optional[
            typing.Union[ListProductsRequestPlanTypesItem, typing.Sequence[ListProductsRequestPlanTypesItem]]
        ] = None,
        price_minimum: typing.Optional[float] = None,
        price_maximum: typing.Optional[float] = None,
        visibilities: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        access_pass_types: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        labels: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        direction: typing.Optional[ListProductsRequestDirection] = None,
        order: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        created_before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ProductListItem, ListProductsResponse]:
        """
        Returns a paginated list of products. Omit `account_id` to search the public marketplace.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The unique identifier of the account to list products for. Omit to search the public marketplace.

        query : typing.Optional[str]
            Ranked search against product title and headline. Omit to browse by recency.

        marketplace_category_route : typing.Optional[str]
            Only return marketplace products assigned to this category route, such as `trading`.

        plan_types : typing.Optional[typing.Union[ListProductsRequestPlanTypesItem, typing.Sequence[ListProductsRequestPlanTypesItem]]]
            Filter to products with a buyable plan of these billing models, such as `one_time` or `renewal`.

        price_minimum : typing.Optional[float]
            Only return products whose advertised buyable plan has a displayed price of at least this amount. Recurring plans use renewal price.

        price_maximum : typing.Optional[float]
            Only return products whose advertised buyable plan has a displayed price of at most this amount. Recurring plans use renewal price.

        visibilities : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only products matching these visibility states. Ignored on the public marketplace list, which only returns visible products.

        access_pass_types : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only products matching these types.

        labels : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only products carrying all of these labels. Labels are matched lowercased.

        direction : typing.Optional[ListProductsRequestDirection]
            The sort direction for results. Defaults to descending.

        order : typing.Optional[str]
            The field to sort results by. Account lists default to `created_at`. Marketplace lists default to `discoverable_at` and accept `created_at` or `discoverable_at`. Cannot be combined with `query`.

        first : typing.Optional[int]
            The number of products to return (default and max 100).

        after : typing.Optional[str]
            A cursor; returns products after this position.

        last : typing.Optional[int]
            The number of products to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns products before this position.

        created_after : typing.Optional[str]
            Only return products created after this ISO 8601 timestamp.

        created_before : typing.Optional[str]
            Only return products created before this ISO 8601 timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ProductListItem, ListProductsResponse]
            marketplace products listed without an account

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.products.list(
                visibilities=["visible"],
                access_pass_types=["regular"],
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            query=query,
            marketplace_category_route=marketplace_category_route,
            plan_types=plan_types,
            price_minimum=price_minimum,
            price_maximum=price_maximum,
            visibilities=visibilities,
            access_pass_types=access_pass_types,
            labels=labels,
            direction=direction,
            order=order,
            first=first,
            after=after,
            last=last,
            before=before,
            created_after=created_after,
            created_before=created_before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        title: str,
        account_id: typing.Optional[str] = OMIT,
        collect_shipping_address: typing.Optional[bool] = OMIT,
        custom_cta: typing.Optional[CreateProductsRequestCustomCta] = OMIT,
        custom_cta_url: typing.Optional[str] = OMIT,
        custom_statement_descriptor: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        global_affiliate_percentage: typing.Optional[float] = OMIT,
        global_affiliate_status: typing.Optional[CreateProductsRequestGlobalAffiliateStatus] = OMIT,
        headline: typing.Optional[str] = OMIT,
        labels: typing.Optional[typing.Sequence[str]] = OMIT,
        member_affiliate_percentage: typing.Optional[float] = OMIT,
        member_affiliate_status: typing.Optional[CreateProductsRequestMemberAffiliateStatus] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        product_tax_code_id: typing.Optional[str] = OMIT,
        redirect_purchase_url: typing.Optional[str] = OMIT,
        route: typing.Optional[str] = OMIT,
        send_welcome_message: typing.Optional[bool] = OMIT,
        visibility: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Product:
        """
        Creates a new product for an account.

        Parameters
        ----------
        title : str
            The display name of the product. Maximum 80 characters.

        account_id : typing.Optional[str]
            The unique identifier of the account to create this product for.

        collect_shipping_address : typing.Optional[bool]
            Whether to collect a shipping address at checkout.

        custom_cta : typing.Optional[CreateProductsRequestCustomCta]
            The call-to-action button label.

        custom_cta_url : typing.Optional[str]
            A URL the call-to-action button links to.

        custom_statement_descriptor : typing.Optional[str]
            Custom bank statement descriptor. Must start with WHOP*.

        description : typing.Optional[str]
            A written description displayed on the product page.

        global_affiliate_percentage : typing.Optional[float]
            The commission rate affiliates earn.

        global_affiliate_status : typing.Optional[CreateProductsRequestGlobalAffiliateStatus]
            The enrollment status in the global affiliate program.

        headline : typing.Optional[str]
            A short marketing headline for the product page.

        labels : typing.Optional[typing.Sequence[str]]
            Labels used to group products into collections. Stored lowercased and de-duplicated. Maximum 20 labels, 50 characters each.

        member_affiliate_percentage : typing.Optional[float]
            The commission rate members earn.

        member_affiliate_status : typing.Optional[CreateProductsRequestMemberAffiliateStatus]
            The enrollment status in the member affiliate program.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the product.

        product_tax_code_id : typing.Optional[str]
            The unique identifier of the tax classification code. See the available [product categories](https://docs.numeral.com/essentials/product-categories).

        redirect_purchase_url : typing.Optional[str]
            A URL to redirect the customer to after purchase.

        route : typing.Optional[str]
            The URL slug for the product's public link.

        send_welcome_message : typing.Optional[bool]
            Whether to send an automated welcome message via support chat when a user joins this product. Defaults to true.

        visibility : typing.Optional[str]
            Whether the product is visible to customers.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.products.create(
                title="Interior Deep Clean",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            title=title,
            account_id=account_id,
            collect_shipping_address=collect_shipping_address,
            custom_cta=custom_cta,
            custom_cta_url=custom_cta_url,
            custom_statement_descriptor=custom_statement_descriptor,
            description=description,
            global_affiliate_percentage=global_affiliate_percentage,
            global_affiliate_status=global_affiliate_status,
            headline=headline,
            labels=labels,
            member_affiliate_percentage=member_affiliate_percentage,
            member_affiliate_status=member_affiliate_status,
            metadata=metadata,
            product_tax_code_id=product_tax_code_id,
            redirect_purchase_url=redirect_purchase_url,
            route=route,
            send_welcome_message=send_welcome_message,
            visibility=visibility,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Product:
        """
        Retrieves a product. Public — no credentials.

        Parameters
        ----------
        id : str
            The unique identifier of the product.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product retrieved without authentication

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.products.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeleteProductsResponse:
        """
        Deletes a product. Only products with no memberships, entries, reviews, or invoices can be deleted.

        Parameters
        ----------
        id : str
            The unique identifier of the product.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteProductsResponse
            product deleted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.products.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        banner_image: typing.Optional[UpdateProductsRequestBannerImage] = OMIT,
        description: typing.Optional[str] = OMIT,
        headline: typing.Optional[str] = OMIT,
        labels: typing.Optional[typing.Sequence[str]] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        product_tax_code_id: typing.Optional[str] = OMIT,
        send_welcome_message: typing.Optional[bool] = OMIT,
        title: typing.Optional[str] = OMIT,
        visibility: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Product:
        """
        Updates an existing product.

        Parameters
        ----------
        id : str
            The unique identifier of the product.

        banner_image : typing.Optional[UpdateProductsRequestBannerImage]
            A wide image for the product, shown on the product page and on listing cards. Pass `{ id }` for an existing attachment or `{ direct_upload_id }` for a completed direct upload; `null` removes it.

        description : typing.Optional[str]
            A written description displayed on the product page.

        headline : typing.Optional[str]
            A short marketing headline for the product page.

        labels : typing.Optional[typing.Sequence[str]]
            Labels used to group products into collections. Replaces the existing labels. Send an empty array to clear them.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the product.

        product_tax_code_id : typing.Optional[str]
            The unique identifier of the tax classification code. See the available [product categories](https://docs.numeral.com/essentials/product-categories).

        send_welcome_message : typing.Optional[bool]
            Whether to send an automated welcome message via support chat when a user joins this product.

        title : typing.Optional[str]
            The display name of the product.

        visibility : typing.Optional[str]
            Whether the product is visible to customers.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product tax code updated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.products.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            banner_image=banner_image,
            description=description,
            headline=headline,
            labels=labels,
            metadata=metadata,
            product_tax_code_id=product_tax_code_id,
            send_welcome_message=send_welcome_message,
            title=title,
            visibility=visibility,
            request_options=request_options,
        )
        return _response.data

    async def publish(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Product:
        """
        Submits a product to the whop.com marketplace for review. The product moves to `pending_review`; a Whop reviewer approves it before it goes live.

        Parameters
        ----------
        id : str
            The unique identifier of the product, prefixed `prod_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product submitted for review

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.products.publish(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.publish(id, request_options=request_options)
        return _response.data

    async def unpublish(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Product:
        """
        Removes a product from the whop.com marketplace. The product moves to `not_available`.

        Parameters
        ----------
        id : str
            The unique identifier of the product, prefixed `prod_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Product
            product removed from the marketplace

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.products.unpublish(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unpublish(id, request_options=request_options)
        return _response.data
