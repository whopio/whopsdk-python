# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.account_recommended_action_chain import AccountRecommendedActionChain
from .raw_client import AsyncRawRecommendedActionsClient, RawRecommendedActionsClient
from .types.list_executions_recommended_actions_response import ListExecutionsRecommendedActionsResponse
from .types.list_recommended_actions_response import ListRecommendedActionsResponse
from .types.run_recommended_actions_response import RunRecommendedActionsResponse


class RecommendedActionsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawRecommendedActionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawRecommendedActionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawRecommendedActionsClient
        """
        return self._raw_client

    def list(
        self, *, account_id: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> ListRecommendedActionsResponse:
        """
        Lists the recommended action chains for an account — short sequences of actions (create a product, price it, publish it) the account should run next, gated on what it already has.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListRecommendedActionsResponse
            defaults to the API key's own account

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.recommended_actions.list()
        """
        _response = self._raw_client.list(account_id=account_id, request_options=request_options)
        return _response.data

    def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AccountRecommendedActionChain:
        """
        Retrieves a recommended action chain by id, including chains that have already been run. Seeded chains are reconstructed from their hard-coded chain; generated chains are read from the account's stored chain, with each step's filled-in input.

        Parameters
        ----------
        id : str
            Chain ID from the list endpoint, e.g. `rac_seed_start_selling_9f2c1a7b04`.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AccountRecommendedActionChain
            generated chain retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.recommended_actions.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, account_id=account_id, request_options=request_options)
        return _response.data

    def run(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RunRecommendedActionsResponse:
        """
        Records that the caller ran a recommended action chain. Nothing is executed server-side yet — the client follows the chain's step CTAs itself; this writes the `recommended_action_chain.executed` analytics event.

        Parameters
        ----------
        id : str
            Chain ID from the list endpoint, e.g. `rac_seed_start_selling_9f2c1a7b04`.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RunRecommendedActionsResponse
            run recorded

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.recommended_actions.run(
            id="id",
        )
        """
        _response = self._raw_client.run(id, account_id=account_id, request_options=request_options)
        return _response.data

    def list_executions(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListExecutionsRecommendedActionsResponse:
        """
        Lists the per-step record of a recommended action chain the server ran — one entry per step in position order, each carrying its current status and, once the step completed, the API response it produced. A chain that was never run server-side returns an empty list.

        Parameters
        ----------
        id : str
            Chain ID from the list endpoint.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListExecutionsRecommendedActionsResponse
            executions listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.recommended_actions.list_executions(
            id="id",
        )
        """
        _response = self._raw_client.list_executions(id, account_id=account_id, request_options=request_options)
        return _response.data


class AsyncRecommendedActionsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawRecommendedActionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawRecommendedActionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawRecommendedActionsClient
        """
        return self._raw_client

    async def list(
        self, *, account_id: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> ListRecommendedActionsResponse:
        """
        Lists the recommended action chains for an account — short sequences of actions (create a product, price it, publish it) the account should run next, gated on what it already has.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListRecommendedActionsResponse
            defaults to the API key's own account

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.recommended_actions.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(account_id=account_id, request_options=request_options)
        return _response.data

    async def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AccountRecommendedActionChain:
        """
        Retrieves a recommended action chain by id, including chains that have already been run. Seeded chains are reconstructed from their hard-coded chain; generated chains are read from the account's stored chain, with each step's filled-in input.

        Parameters
        ----------
        id : str
            Chain ID from the list endpoint, e.g. `rac_seed_start_selling_9f2c1a7b04`.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AccountRecommendedActionChain
            generated chain retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.recommended_actions.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, account_id=account_id, request_options=request_options)
        return _response.data

    async def run(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RunRecommendedActionsResponse:
        """
        Records that the caller ran a recommended action chain. Nothing is executed server-side yet — the client follows the chain's step CTAs itself; this writes the `recommended_action_chain.executed` analytics event.

        Parameters
        ----------
        id : str
            Chain ID from the list endpoint, e.g. `rac_seed_start_selling_9f2c1a7b04`.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RunRecommendedActionsResponse
            run recorded

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.recommended_actions.run(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.run(id, account_id=account_id, request_options=request_options)
        return _response.data

    async def list_executions(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListExecutionsRecommendedActionsResponse:
        """
        Lists the per-step record of a recommended action chain the server ran — one entry per step in position order, each carrying its current status and, once the step completed, the API response it produced. A chain that was never run server-side returns an empty list.

        Parameters
        ----------
        id : str
            Chain ID from the list endpoint.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListExecutionsRecommendedActionsResponse
            executions listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.recommended_actions.list_executions(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_executions(id, account_id=account_id, request_options=request_options)
        return _response.data
