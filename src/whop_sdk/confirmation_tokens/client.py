# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.confirmation_token import ConfirmationToken
from .raw_client import AsyncRawConfirmationTokensClient, RawConfirmationTokensClient
from .types.create_confirmation_tokens_request_billing_details import CreateConfirmationTokensRequestBillingDetails
from .types.create_confirmation_tokens_request_payment_method import CreateConfirmationTokensRequestPaymentMethod
from .types.create_confirmation_tokens_request_setup_future_usage import CreateConfirmationTokensRequestSetupFutureUsage

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ConfirmationTokensClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawConfirmationTokensClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawConfirmationTokensClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawConfirmationTokensClient
        """
        return self._raw_client

    def create(
        self,
        *,
        account_id: str,
        payment_method: CreateConfirmationTokensRequestPaymentMethod,
        billing_details: typing.Optional[CreateConfirmationTokensRequestBillingDetails] = OMIT,
        browser_info: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        return_url: typing.Optional[str] = OMIT,
        setup_future_usage: typing.Optional[CreateConfirmationTokensRequestSetupFutureUsage] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ConfirmationToken:
        """
        Mints a single-use, short-lived confirmation token from what the buyer entered on your collection surface: the payment method payload, billing details, and attested save consent. Public and rate-limited — the account_id in the body scopes the token but does not authenticate. Confirm it with POST /payments from your server.

        Parameters
        ----------
        account_id : str
            The account (biz_) this token is scoped to — the publishable identity.

        payment_method : CreateConfirmationTokensRequestPaymentMethod
            The collected method: `type` names the payment method, `category` names the payload shape, and the category-keyed object carries the payload. Wallets are the exception: their payload rides the type key (`apple_pay` / `google_pay`). Send exactly the one payload arm the category selects — extra arms are rejected. Redirect-flow methods (category `redirect`, `bank_transfer`, `voucher`, and redirect wallets like `cashapp`) collect nothing and send no payload arm.

        billing_details : typing.Optional[CreateConfirmationTokensRequestBillingDetails]
            Billing details collected with the method. `email` is always required; cards additionally require `name` and an address with `line1` and `country`.

        browser_info : typing.Optional[typing.Dict[str, typing.Any]]
            Screen/runtime facts from the buyer's browser (platform, screen dimensions, language, ...) used for authentication ceremonies. Header-derived fields are captured server-side.

        return_url : typing.Optional[str]
            Where redirect flows send the buyer, carried onto the confirm that consumes this token.

        setup_future_usage : typing.Optional[CreateConfirmationTokensRequestSetupFutureUsage]
            The save-consent state your surface displayed when the buyer confirmed. Confirm may vault only if attested here.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ConfirmationToken
            confirmation token created from a card token intent

        Examples
        --------
        from whop_sdk import Whop
        from whop_sdk.confirmation_tokens import (
            CreateConfirmationTokensRequestBillingDetails,
            CreateConfirmationTokensRequestPaymentMethod,
            CreateConfirmationTokensRequestPaymentMethodCard,
        )

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.confirmation_tokens.create(
            account_id="biz_xxxxxxxxxxxxxx",
            billing_details=CreateConfirmationTokensRequestBillingDetails(
                address={
                    "city": "Austin",
                    "country": "US",
                    "line1": "123 Main St",
                    "postal_code": "78701",
                },
                email="buyer@example.com",
                name="Buyer Name",
            ),
            payment_method=CreateConfirmationTokensRequestPaymentMethod(
                card=CreateConfirmationTokensRequestPaymentMethodCard(
                    brand="visa",
                    last4="4242",
                    token_intent="bt_ti_123",
                ),
                category="card",
                type="card",
            ),
            setup_future_usage="off_session",
        )
        """
        _response = self._raw_client.create(
            account_id=account_id,
            payment_method=payment_method,
            billing_details=billing_details,
            browser_info=browser_info,
            return_url=return_url,
            setup_future_usage=setup_future_usage,
            request_options=request_options,
        )
        return _response.data

    def retrieve(
        self, id: str, *, account_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> ConfirmationToken:
        """
        Retrieves a token's display-safe preview — never the underlying payment credential. Public and rate-limited: the account_id query param must match the account the token was minted for.

        Parameters
        ----------
        id : str
            Confirmation token ID, prefixed `ctok_`.

        account_id : str
            The account (biz_) the token was minted for.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ConfirmationToken
            confirmation token retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.confirmation_tokens.retrieve(
            id="id",
            account_id="account_id",
        )
        """
        _response = self._raw_client.retrieve(id, account_id=account_id, request_options=request_options)
        return _response.data


class AsyncConfirmationTokensClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawConfirmationTokensClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawConfirmationTokensClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawConfirmationTokensClient
        """
        return self._raw_client

    async def create(
        self,
        *,
        account_id: str,
        payment_method: CreateConfirmationTokensRequestPaymentMethod,
        billing_details: typing.Optional[CreateConfirmationTokensRequestBillingDetails] = OMIT,
        browser_info: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        return_url: typing.Optional[str] = OMIT,
        setup_future_usage: typing.Optional[CreateConfirmationTokensRequestSetupFutureUsage] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ConfirmationToken:
        """
        Mints a single-use, short-lived confirmation token from what the buyer entered on your collection surface: the payment method payload, billing details, and attested save consent. Public and rate-limited — the account_id in the body scopes the token but does not authenticate. Confirm it with POST /payments from your server.

        Parameters
        ----------
        account_id : str
            The account (biz_) this token is scoped to — the publishable identity.

        payment_method : CreateConfirmationTokensRequestPaymentMethod
            The collected method: `type` names the payment method, `category` names the payload shape, and the category-keyed object carries the payload. Wallets are the exception: their payload rides the type key (`apple_pay` / `google_pay`). Send exactly the one payload arm the category selects — extra arms are rejected. Redirect-flow methods (category `redirect`, `bank_transfer`, `voucher`, and redirect wallets like `cashapp`) collect nothing and send no payload arm.

        billing_details : typing.Optional[CreateConfirmationTokensRequestBillingDetails]
            Billing details collected with the method. `email` is always required; cards additionally require `name` and an address with `line1` and `country`.

        browser_info : typing.Optional[typing.Dict[str, typing.Any]]
            Screen/runtime facts from the buyer's browser (platform, screen dimensions, language, ...) used for authentication ceremonies. Header-derived fields are captured server-side.

        return_url : typing.Optional[str]
            Where redirect flows send the buyer, carried onto the confirm that consumes this token.

        setup_future_usage : typing.Optional[CreateConfirmationTokensRequestSetupFutureUsage]
            The save-consent state your surface displayed when the buyer confirmed. Confirm may vault only if attested here.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ConfirmationToken
            confirmation token created from a card token intent

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop
        from whop_sdk.confirmation_tokens import (
            CreateConfirmationTokensRequestBillingDetails,
            CreateConfirmationTokensRequestPaymentMethod,
            CreateConfirmationTokensRequestPaymentMethodCard,
        )

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.confirmation_tokens.create(
                account_id="biz_xxxxxxxxxxxxxx",
                billing_details=CreateConfirmationTokensRequestBillingDetails(
                    address={
                        "city": "Austin",
                        "country": "US",
                        "line1": "123 Main St",
                        "postal_code": "78701",
                    },
                    email="buyer@example.com",
                    name="Buyer Name",
                ),
                payment_method=CreateConfirmationTokensRequestPaymentMethod(
                    card=CreateConfirmationTokensRequestPaymentMethodCard(
                        brand="visa",
                        last4="4242",
                        token_intent="bt_ti_123",
                    ),
                    category="card",
                    type="card",
                ),
                setup_future_usage="off_session",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            account_id=account_id,
            payment_method=payment_method,
            billing_details=billing_details,
            browser_info=browser_info,
            return_url=return_url,
            setup_future_usage=setup_future_usage,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(
        self, id: str, *, account_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> ConfirmationToken:
        """
        Retrieves a token's display-safe preview — never the underlying payment credential. Public and rate-limited: the account_id query param must match the account the token was minted for.

        Parameters
        ----------
        id : str
            Confirmation token ID, prefixed `ctok_`.

        account_id : str
            The account (biz_) the token was minted for.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ConfirmationToken
            confirmation token retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.confirmation_tokens.retrieve(
                id="id",
                account_id="account_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, account_id=account_id, request_options=request_options)
        return _response.data
