# This file was auto-generated by Fern from our API Definition.

import typing

from .....core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from .....core.pagination import AsyncPager, SyncPager
from .....core.request_options import RequestOptions
from .....types.user_notification_preference import UserNotificationPreference
from .raw_client import AsyncRawTopicsClient, RawTopicsClient
from .types.list_topics_request_channel import ListTopicsRequestChannel
from .types.list_topics_response import ListTopicsResponse


class TopicsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawTopicsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawTopicsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawTopicsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        channel: typing.Optional[ListTopicsRequestChannel] = None,
        account_id: typing.Optional[str] = None,
        team_account_id: typing.Optional[str] = None,
        experience_id: typing.Optional[str] = None,
        topic_id: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[UserNotificationPreference, ListTopicsResponse]:
        """
        Lists the authenticated user's topic-scoped notification preferences, plus user-agnostic platform defaults. Each filter matches preferences scoped to its value or not narrowed on that dimension. Per-experience levels are listed separately, by `GET /users/me/preferences/notifications/experiences`.

        Parameters
        ----------
        channel : typing.Optional[ListTopicsRequestChannel]
            Only return preferences for this delivery channel (or not narrowed to a channel).

        account_id : typing.Optional[str]
            Only return preferences scoped to this account's member notifications (`biz_` tag).

        team_account_id : typing.Optional[str]
            Only return preferences scoped to this account's team notifications (`biz_` tag).

        experience_id : typing.Optional[str]
            Only return preferences scoped to this experience (`exp_` tag).

        topic_id : typing.Optional[str]
            Only return preferences scoped to this notification topic (`topic_` tag).

        first : typing.Optional[int]
            The number of preferences to return.

        after : typing.Optional[str]
            A cursor; returns preferences after this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[UserNotificationPreference, ListTopicsResponse]
            preferences listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.users.preferences.notifications.topics.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            channel=channel,
            account_id=account_id,
            team_account_id=team_account_id,
            experience_id=experience_id,
            topic_id=topic_id,
            first=first,
            after=after,
            request_options=request_options,
        )


class AsyncTopicsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawTopicsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawTopicsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawTopicsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        channel: typing.Optional[ListTopicsRequestChannel] = None,
        account_id: typing.Optional[str] = None,
        team_account_id: typing.Optional[str] = None,
        experience_id: typing.Optional[str] = None,
        topic_id: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[UserNotificationPreference, ListTopicsResponse]:
        """
        Lists the authenticated user's topic-scoped notification preferences, plus user-agnostic platform defaults. Each filter matches preferences scoped to its value or not narrowed on that dimension. Per-experience levels are listed separately, by `GET /users/me/preferences/notifications/experiences`.

        Parameters
        ----------
        channel : typing.Optional[ListTopicsRequestChannel]
            Only return preferences for this delivery channel (or not narrowed to a channel).

        account_id : typing.Optional[str]
            Only return preferences scoped to this account's member notifications (`biz_` tag).

        team_account_id : typing.Optional[str]
            Only return preferences scoped to this account's team notifications (`biz_` tag).

        experience_id : typing.Optional[str]
            Only return preferences scoped to this experience (`exp_` tag).

        topic_id : typing.Optional[str]
            Only return preferences scoped to this notification topic (`topic_` tag).

        first : typing.Optional[int]
            The number of preferences to return.

        after : typing.Optional[str]
            A cursor; returns preferences after this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[UserNotificationPreference, ListTopicsResponse]
            preferences listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.users.preferences.notifications.topics.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            channel=channel,
            account_id=account_id,
            team_account_id=team_account_id,
            experience_id=experience_id,
            topic_id=topic_id,
            first=first,
            after=after,
            request_options=request_options,
        )
