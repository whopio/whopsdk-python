# This file was auto-generated by Fern from our API Definition.

import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import encode_path_param
from ..core.pagination import AsyncPager, SyncPager
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..core.serialization import convert_and_respect_annotation_metadata
from ..errors.bad_request_error import BadRequestError
from ..errors.forbidden_error import ForbiddenError
from ..errors.not_found_error import NotFoundError
from ..errors.unauthorized_error import UnauthorizedError
from ..types.user import User
from .types.check_access_users_response import CheckAccessUsersResponse
from .types.list_users_response import ListUsersResponse
from .types.me_users_request_interval import MeUsersRequestInterval
from .types.recommend_actions_users_response import RecommendActionsUsersResponse
from .types.retrieve_users_request_interval import RetrieveUsersRequestInterval
from .types.update_me_users_request_banner import UpdateMeUsersRequestBanner
from .types.update_me_users_request_profile_picture import UpdateMeUsersRequestProfilePicture
from .types.update_users_request_banner import UpdateUsersRequestBanner
from .types.update_users_request_profile_picture import UpdateUsersRequestProfilePicture
from pydantic import ValidationError

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawUsersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self,
        *,
        query: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[User, ListUsersResponse]:
        """
        Search for users by name or username, ranked by social proximity to the authenticated user. Returns the user's most recently followed users when no query is given.

        Parameters
        ----------
        query : typing.Optional[str]
            A search term to filter users by name or username.

        first : typing.Optional[int]
            The number of users to return (max 50).

        after : typing.Optional[str]
            A cursor; returns users after this position.

        last : typing.Optional[int]
            The number of users to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns users before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[User, ListUsersResponse]
            users listed
        """
        _response = self._client_wrapper.httpx_client.request(
            "users",
            method="GET",
            params={
                "query": query,
                "first": first,
                "after": after,
                "last": last,
                "before": before,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListUsersResponse,
                    parse_obj_as(
                        type_=ListUsersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""
                    _get_next = lambda: self.list(
                        query=query,
                        first=first,
                        after=_parsed_next,
                        last=last,
                        before=before,
                        request_options=request_options,
                    )
                return SyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def me(
        self,
        *,
        account_id: typing.Optional[str] = None,
        include_balance_history: typing.Optional[bool] = None,
        from_: typing.Optional[str] = None,
        to: typing.Optional[str] = None,
        interval: typing.Optional[MeUsersRequestInterval] = None,
        time_zone: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[User]:
        """
        Retrieves the authenticated user — the self view of the user object. Same shape as `GET /users/{id}`, with the self-only fields populated: `email` (email-read scope), `staff` (Whop staff only, staff-read scope), `balance` and `earnings_usd` (balance-read scope), the opt-in `balance_history`, and every linked social account.

        Parameters
        ----------
        account_id : typing.Optional[str]
            When set, returns your account-specific profile overrides for this account.

        include_balance_history : typing.Optional[bool]
            Also compute your balance history (opt-in; runs a heavier query). Ignored for callers without balance-read scope.

        from_ : typing.Optional[str]
            Balance-history window start, ISO 8601 date or datetime. Defaults to 30 days ago. Only used with `include_balance_history`.

        to : typing.Optional[str]
            Balance-history window end, ISO 8601 date or datetime. Defaults to now. Only used with `include_balance_history`.

        interval : typing.Optional[MeUsersRequestInterval]
            Balance-history point granularity. Defaults to `day`. Only used with `include_balance_history`.

        time_zone : typing.Optional[str]
            IANA time zone the balance-history points are bucketed in. Defaults to `UTC`. Only used with `include_balance_history`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[User]
            current user retrieved
        """
        _response = self._client_wrapper.httpx_client.request(
            "users/me",
            method="GET",
            params={
                "account_id": account_id,
                "include_balance_history": include_balance_history,
                "from": from_,
                "to": to,
                "interval": interval,
                "time_zone": time_zone,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    User,
                    parse_obj_as(
                        type_=User,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def update_me(
        self,
        *,
        account_id: typing.Optional[str] = None,
        banner: typing.Optional[UpdateMeUsersRequestBanner] = OMIT,
        bio: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        profile_picture: typing.Optional[UpdateMeUsersRequestProfilePicture] = OMIT,
        username: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[User]:
        """
        Updates the authenticated user's global profile, or their profile override for an account when account_id is given. Not available to API keys.

        Parameters
        ----------
        account_id : typing.Optional[str]
            When set, updates the authenticated user's profile override for this account instead of their global profile.

        banner : typing.Optional[UpdateMeUsersRequestBanner]

        bio : typing.Optional[str]

        name : typing.Optional[str]

        profile_picture : typing.Optional[UpdateMeUsersRequestProfilePicture]

        username : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[User]
            current user updated
        """
        _response = self._client_wrapper.httpx_client.request(
            "users/me",
            method="PATCH",
            params={
                "account_id": account_id,
            },
            json={
                "banner": convert_and_respect_annotation_metadata(
                    object_=banner, annotation=typing.Optional[UpdateMeUsersRequestBanner], direction="write"
                ),
                "bio": bio,
                "name": name,
                "profile_picture": convert_and_respect_annotation_metadata(
                    object_=profile_picture, annotation=UpdateMeUsersRequestProfilePicture, direction="write"
                ),
                "username": username,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    User,
                    parse_obj_as(
                        type_=User,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        include_balance_history: typing.Optional[bool] = None,
        from_: typing.Optional[str] = None,
        to: typing.Optional[str] = None,
        interval: typing.Optional[RetrieveUsersRequestInterval] = None,
        time_zone: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[User]:
        """
        Retrieves a user by `user_` tag or username, or the authenticated user with the reserved id `me`. Profiles include linked social accounts — reading your own profile returns every linked account, other profiles only what is public on Whop (the primary Discord and the X account). The self-only fields are populated only when the id is `me`: `email` (email-read scope), `staff` (Whop staff only, staff-read scope), `balance` and `earnings_usd` (balance-read scope), and the opt-in `balance_history`. They are always `null` when addressing a user by tag or username.

        Parameters
        ----------
        id : str
            User ID (prefixed `user_`), username, or `me` for the authenticated user.

        account_id : typing.Optional[str]
            When set, returns the user's account-specific profile overrides for this account.

        include_balance_history : typing.Optional[bool]
            Also compute your balance history (opt-in; runs a heavier query). Only applies when the id is `me`; ignored for callers without balance-read scope.

        from_ : typing.Optional[str]
            Balance-history window start, ISO 8601 date or datetime. Defaults to 30 days ago. Only used with `include_balance_history`.

        to : typing.Optional[str]
            Balance-history window end, ISO 8601 date or datetime. Defaults to now. Only used with `include_balance_history`.

        interval : typing.Optional[RetrieveUsersRequestInterval]
            Balance-history point granularity. Defaults to `day`. Only used with `include_balance_history`.

        time_zone : typing.Optional[str]
            IANA time zone the balance-history points are bucketed in. Defaults to `UTC`. Only used with `include_balance_history`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[User]
            user retrieved
        """
        _response = self._client_wrapper.httpx_client.request(
            f"users/{encode_path_param(id)}",
            method="GET",
            params={
                "account_id": account_id,
                "include_balance_history": include_balance_history,
                "from": from_,
                "to": to,
                "interval": interval,
                "time_zone": time_zone,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    User,
                    parse_obj_as(
                        type_=User,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def update(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        banner: typing.Optional[UpdateUsersRequestBanner] = OMIT,
        bio: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        profile_picture: typing.Optional[UpdateUsersRequestProfilePicture] = OMIT,
        username: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[User]:
        """
        Updates a user, addressed by `user_` tag, username, or the reserved id `me` for the authenticated user. A user token updates their own global profile; an API key updates the user's account-specific profile override (account_id required).

        Parameters
        ----------
        id : str
            User ID (prefixed `user_`), username, or `me` for the authenticated user.

        account_id : typing.Optional[str]
            The account whose profile override to update. Required for API key callers.

        banner : typing.Optional[UpdateUsersRequestBanner]

        bio : typing.Optional[str]

        name : typing.Optional[str]

        profile_picture : typing.Optional[UpdateUsersRequestProfilePicture]

        username : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[User]
            user updated
        """
        _response = self._client_wrapper.httpx_client.request(
            f"users/{encode_path_param(id)}",
            method="PATCH",
            params={
                "account_id": account_id,
            },
            json={
                "banner": convert_and_respect_annotation_metadata(
                    object_=banner, annotation=typing.Optional[UpdateUsersRequestBanner], direction="write"
                ),
                "bio": bio,
                "name": name,
                "profile_picture": convert_and_respect_annotation_metadata(
                    object_=profile_picture, annotation=UpdateUsersRequestProfilePicture, direction="write"
                ),
                "username": username,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    User,
                    parse_obj_as(
                        type_=User,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def check_access(
        self, id: str, resource_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[CheckAccessUsersResponse]:
        """
        Checks whether a user has access to an account, product, or experience the caller can reach.

        Parameters
        ----------
        id : str
            The user_ tag or username to check access for.

        resource_id : str
            An account (biz_), product (prod_), or experience (exp_) ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[CheckAccessUsersResponse]
            access checked
        """
        _response = self._client_wrapper.httpx_client.request(
            f"users/{encode_path_param(id)}/access/{encode_path_param(resource_id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CheckAccessUsersResponse,
                    parse_obj_as(
                        type_=CheckAccessUsersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def recommend_actions(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[RecommendActionsUsersResponse]:
        """
        Lists the recommended actions computed for the user: personal suggestions (e.g. start a business or become an affiliate) pooled with the highest-impact actions across the accounts the user owns. Business actions are tagged with their `account_id`/`account_name`; personal actions leave those `null`. Self-only: `id` must be `me` or the authenticated user's own tag/username.

        Parameters
        ----------
        id : str
            `me`, or the authenticated user's own `user_` tag or username.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[RecommendActionsUsersResponse]
            recommended actions listed
        """
        _response = self._client_wrapper.httpx_client.request(
            f"users/{encode_path_param(id)}/recommend_actions",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    RecommendActionsUsersResponse,
                    parse_obj_as(
                        type_=RecommendActionsUsersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawUsersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self,
        *,
        query: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[User, ListUsersResponse]:
        """
        Search for users by name or username, ranked by social proximity to the authenticated user. Returns the user's most recently followed users when no query is given.

        Parameters
        ----------
        query : typing.Optional[str]
            A search term to filter users by name or username.

        first : typing.Optional[int]
            The number of users to return (max 50).

        after : typing.Optional[str]
            A cursor; returns users after this position.

        last : typing.Optional[int]
            The number of users to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns users before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[User, ListUsersResponse]
            users listed
        """
        _response = await self._client_wrapper.httpx_client.request(
            "users",
            method="GET",
            params={
                "query": query,
                "first": first,
                "after": after,
                "last": last,
                "before": before,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListUsersResponse,
                    parse_obj_as(
                        type_=ListUsersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""

                    async def _get_next():
                        return await self.list(
                            query=query,
                            first=first,
                            after=_parsed_next,
                            last=last,
                            before=before,
                            request_options=request_options,
                        )

                return AsyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def me(
        self,
        *,
        account_id: typing.Optional[str] = None,
        include_balance_history: typing.Optional[bool] = None,
        from_: typing.Optional[str] = None,
        to: typing.Optional[str] = None,
        interval: typing.Optional[MeUsersRequestInterval] = None,
        time_zone: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[User]:
        """
        Retrieves the authenticated user — the self view of the user object. Same shape as `GET /users/{id}`, with the self-only fields populated: `email` (email-read scope), `staff` (Whop staff only, staff-read scope), `balance` and `earnings_usd` (balance-read scope), the opt-in `balance_history`, and every linked social account.

        Parameters
        ----------
        account_id : typing.Optional[str]
            When set, returns your account-specific profile overrides for this account.

        include_balance_history : typing.Optional[bool]
            Also compute your balance history (opt-in; runs a heavier query). Ignored for callers without balance-read scope.

        from_ : typing.Optional[str]
            Balance-history window start, ISO 8601 date or datetime. Defaults to 30 days ago. Only used with `include_balance_history`.

        to : typing.Optional[str]
            Balance-history window end, ISO 8601 date or datetime. Defaults to now. Only used with `include_balance_history`.

        interval : typing.Optional[MeUsersRequestInterval]
            Balance-history point granularity. Defaults to `day`. Only used with `include_balance_history`.

        time_zone : typing.Optional[str]
            IANA time zone the balance-history points are bucketed in. Defaults to `UTC`. Only used with `include_balance_history`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[User]
            current user retrieved
        """
        _response = await self._client_wrapper.httpx_client.request(
            "users/me",
            method="GET",
            params={
                "account_id": account_id,
                "include_balance_history": include_balance_history,
                "from": from_,
                "to": to,
                "interval": interval,
                "time_zone": time_zone,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    User,
                    parse_obj_as(
                        type_=User,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def update_me(
        self,
        *,
        account_id: typing.Optional[str] = None,
        banner: typing.Optional[UpdateMeUsersRequestBanner] = OMIT,
        bio: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        profile_picture: typing.Optional[UpdateMeUsersRequestProfilePicture] = OMIT,
        username: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[User]:
        """
        Updates the authenticated user's global profile, or their profile override for an account when account_id is given. Not available to API keys.

        Parameters
        ----------
        account_id : typing.Optional[str]
            When set, updates the authenticated user's profile override for this account instead of their global profile.

        banner : typing.Optional[UpdateMeUsersRequestBanner]

        bio : typing.Optional[str]

        name : typing.Optional[str]

        profile_picture : typing.Optional[UpdateMeUsersRequestProfilePicture]

        username : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[User]
            current user updated
        """
        _response = await self._client_wrapper.httpx_client.request(
            "users/me",
            method="PATCH",
            params={
                "account_id": account_id,
            },
            json={
                "banner": convert_and_respect_annotation_metadata(
                    object_=banner, annotation=typing.Optional[UpdateMeUsersRequestBanner], direction="write"
                ),
                "bio": bio,
                "name": name,
                "profile_picture": convert_and_respect_annotation_metadata(
                    object_=profile_picture, annotation=UpdateMeUsersRequestProfilePicture, direction="write"
                ),
                "username": username,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    User,
                    parse_obj_as(
                        type_=User,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        include_balance_history: typing.Optional[bool] = None,
        from_: typing.Optional[str] = None,
        to: typing.Optional[str] = None,
        interval: typing.Optional[RetrieveUsersRequestInterval] = None,
        time_zone: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[User]:
        """
        Retrieves a user by `user_` tag or username, or the authenticated user with the reserved id `me`. Profiles include linked social accounts — reading your own profile returns every linked account, other profiles only what is public on Whop (the primary Discord and the X account). The self-only fields are populated only when the id is `me`: `email` (email-read scope), `staff` (Whop staff only, staff-read scope), `balance` and `earnings_usd` (balance-read scope), and the opt-in `balance_history`. They are always `null` when addressing a user by tag or username.

        Parameters
        ----------
        id : str
            User ID (prefixed `user_`), username, or `me` for the authenticated user.

        account_id : typing.Optional[str]
            When set, returns the user's account-specific profile overrides for this account.

        include_balance_history : typing.Optional[bool]
            Also compute your balance history (opt-in; runs a heavier query). Only applies when the id is `me`; ignored for callers without balance-read scope.

        from_ : typing.Optional[str]
            Balance-history window start, ISO 8601 date or datetime. Defaults to 30 days ago. Only used with `include_balance_history`.

        to : typing.Optional[str]
            Balance-history window end, ISO 8601 date or datetime. Defaults to now. Only used with `include_balance_history`.

        interval : typing.Optional[RetrieveUsersRequestInterval]
            Balance-history point granularity. Defaults to `day`. Only used with `include_balance_history`.

        time_zone : typing.Optional[str]
            IANA time zone the balance-history points are bucketed in. Defaults to `UTC`. Only used with `include_balance_history`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[User]
            user retrieved
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"users/{encode_path_param(id)}",
            method="GET",
            params={
                "account_id": account_id,
                "include_balance_history": include_balance_history,
                "from": from_,
                "to": to,
                "interval": interval,
                "time_zone": time_zone,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    User,
                    parse_obj_as(
                        type_=User,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def update(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        banner: typing.Optional[UpdateUsersRequestBanner] = OMIT,
        bio: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        profile_picture: typing.Optional[UpdateUsersRequestProfilePicture] = OMIT,
        username: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[User]:
        """
        Updates a user, addressed by `user_` tag, username, or the reserved id `me` for the authenticated user. A user token updates their own global profile; an API key updates the user's account-specific profile override (account_id required).

        Parameters
        ----------
        id : str
            User ID (prefixed `user_`), username, or `me` for the authenticated user.

        account_id : typing.Optional[str]
            The account whose profile override to update. Required for API key callers.

        banner : typing.Optional[UpdateUsersRequestBanner]

        bio : typing.Optional[str]

        name : typing.Optional[str]

        profile_picture : typing.Optional[UpdateUsersRequestProfilePicture]

        username : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[User]
            user updated
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"users/{encode_path_param(id)}",
            method="PATCH",
            params={
                "account_id": account_id,
            },
            json={
                "banner": convert_and_respect_annotation_metadata(
                    object_=banner, annotation=typing.Optional[UpdateUsersRequestBanner], direction="write"
                ),
                "bio": bio,
                "name": name,
                "profile_picture": convert_and_respect_annotation_metadata(
                    object_=profile_picture, annotation=UpdateUsersRequestProfilePicture, direction="write"
                ),
                "username": username,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    User,
                    parse_obj_as(
                        type_=User,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def check_access(
        self, id: str, resource_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[CheckAccessUsersResponse]:
        """
        Checks whether a user has access to an account, product, or experience the caller can reach.

        Parameters
        ----------
        id : str
            The user_ tag or username to check access for.

        resource_id : str
            An account (biz_), product (prod_), or experience (exp_) ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[CheckAccessUsersResponse]
            access checked
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"users/{encode_path_param(id)}/access/{encode_path_param(resource_id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    CheckAccessUsersResponse,
                    parse_obj_as(
                        type_=CheckAccessUsersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def recommend_actions(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[RecommendActionsUsersResponse]:
        """
        Lists the recommended actions computed for the user: personal suggestions (e.g. start a business or become an affiliate) pooled with the highest-impact actions across the accounts the user owns. Business actions are tagged with their `account_id`/`account_name`; personal actions leave those `null`. Self-only: `id` must be `me` or the authenticated user's own tag/username.

        Parameters
        ----------
        id : str
            `me`, or the authenticated user's own `user_` tag or username.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[RecommendActionsUsersResponse]
            recommended actions listed
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"users/{encode_path_param(id)}/recommend_actions",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    RecommendActionsUsersResponse,
                    parse_obj_as(
                        type_=RecommendActionsUsersResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
