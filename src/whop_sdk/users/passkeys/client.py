# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.pagination import AsyncPager, SyncPager
from ...core.request_options import RequestOptions
from ...types.passkey import Passkey
from .raw_client import AsyncRawPasskeysClient, RawPasskeysClient
from .types.challenge_passkeys_request_challenge_type import ChallengePasskeysRequestChallengeType
from .types.challenge_passkeys_response import ChallengePasskeysResponse
from .types.delete_passkeys_response import DeletePasskeysResponse
from .types.list_passkeys_request_direction import ListPasskeysRequestDirection
from .types.list_passkeys_request_order import ListPasskeysRequestOrder
from .types.list_passkeys_response import ListPasskeysResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PasskeysClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPasskeysClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPasskeysClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPasskeysClient
        """
        return self._raw_client

    def list(
        self,
        *,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListPasskeysRequestOrder] = None,
        direction: typing.Optional[ListPasskeysRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Passkey, ListPasskeysResponse]:
        """
        Lists the authenticated user's own passkeys, newest first. The list is always the caller's own; there is no parameter for reading another user's passkeys. Requires a user session: an API key or an OAuth token is refused, because a passkey confirms the account holder before a sensitive action and no app may enumerate one.

        Parameters
        ----------
        first : typing.Optional[int]
            The number of passkeys to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns passkeys after this position.

        last : typing.Optional[int]
            The number of passkeys to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns passkeys before this position.

        order : typing.Optional[ListPasskeysRequestOrder]
            The field to sort passkeys by.

        direction : typing.Optional[ListPasskeysRequestDirection]
            Sort direction.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Passkey, ListPasskeysResponse]
            passkeys listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.users.passkeys.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            first=first,
            after=after,
            last=last,
            before=before,
            order=order,
            direction=direction,
            request_options=request_options,
        )

    def create(
        self,
        *,
        attestation_object: str,
        client_data_json: str,
        credential_id: str,
        nickname: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Passkey:
        """
        Registers a passkey for the authenticated user from the attestation a browser produced for a `registration` challenge. Mint that challenge first with `POST /users/me/passkeys/challenge`; it is single-use and expires 5 minutes after it is issued. Requires a user session.

        Parameters
        ----------
        attestation_object : str
            The `attestationObject` from the WebAuthn attestation response, base64url-encoded.

        client_data_json : str
            The `clientDataJSON` from the WebAuthn attestation response, base64url-encoded.

        credential_id : str
            The WebAuthn credential ID the authenticator returned, base64url-encoded.

        nickname : str
            A name for this passkey, usually the device it lives on. 255 characters or fewer.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Passkey
            passkey registered

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.users.passkeys.create(
            attestation_object="YXR0ZXN0YXRpb24",
            client_data_json="Y2xpZW50LWRhdGE",
            credential_id="bmV3LWNyZWRlbnRpYWw",
            nickname="Work laptop",
        )
        """
        _response = self._raw_client.create(
            attestation_object=attestation_object,
            client_data_json=client_data_json,
            credential_id=credential_id,
            nickname=nickname,
            request_options=request_options,
        )
        return _response.data

    def challenge(
        self,
        *,
        challenge_type: ChallengePasskeysRequestChallengeType,
        passkey_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChallengePasskeysResponse:
        """
        Mints the challenge a browser needs to run a WebAuthn ceremony against the authenticated user's own passkeys. A `registration` challenge enrolls a new passkey; a `deletion` challenge is bound to the one passkey named by `passkey_id` and proves the user still holds it. Challenges are single-use and expire 5 minutes after they are issued, so send a fresh `Idempotency-Key` per ceremony — a replayed key returns the original challenge, which may already have expired. Requires a user session.

        Parameters
        ----------
        challenge_type : ChallengePasskeysRequestChallengeType
            The ceremony this challenge is for.

        passkey_id : typing.Optional[str]
            The passkey the ceremony targets, prefixed `wcred_`. Required when `challenge_type` is `deletion`, ignored otherwise.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChallengePasskeysResponse
            challenge minted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.users.passkeys.challenge(
            challenge_type="registration",
        )
        """
        _response = self._raw_client.challenge(
            challenge_type=challenge_type, passkey_id=passkey_id, request_options=request_options
        )
        return _response.data

    def delete(
        self,
        id: str,
        *,
        authenticator_data: str,
        client_data_json: str,
        signature: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeletePasskeysResponse:
        """
        Deletes one of the authenticated user's own passkeys. The request body carries a WebAuthn assertion from the passkey being deleted, so possession of the credential is proven before it is removed: mint a `deletion` challenge for it first, run the ceremony with that passkey, and send the result here. Deleting the user's last passkey is allowed — their other step-up factors remain. Requires a user session.

        Parameters
        ----------
        id : str
            Passkey ID, prefixed `wcred_`.

        authenticator_data : str
            The `authenticatorData` from the WebAuthn assertion, base64url-encoded.

        client_data_json : str
            The `clientDataJSON` from the WebAuthn assertion, base64url-encoded.

        signature : str
            The `signature` from the WebAuthn assertion, base64url-encoded.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletePasskeysResponse
            passkey deleted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.users.passkeys.delete(
            id="id",
            authenticator_data="YXV0aGVudGljYXRvci1kYXRh",
            client_data_json="Y2xpZW50LWRhdGE",
            signature="c2lnbmF0dXJl",
        )
        """
        _response = self._raw_client.delete(
            id,
            authenticator_data=authenticator_data,
            client_data_json=client_data_json,
            signature=signature,
            request_options=request_options,
        )
        return _response.data


class AsyncPasskeysClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPasskeysClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPasskeysClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPasskeysClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListPasskeysRequestOrder] = None,
        direction: typing.Optional[ListPasskeysRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Passkey, ListPasskeysResponse]:
        """
        Lists the authenticated user's own passkeys, newest first. The list is always the caller's own; there is no parameter for reading another user's passkeys. Requires a user session: an API key or an OAuth token is refused, because a passkey confirms the account holder before a sensitive action and no app may enumerate one.

        Parameters
        ----------
        first : typing.Optional[int]
            The number of passkeys to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns passkeys after this position.

        last : typing.Optional[int]
            The number of passkeys to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns passkeys before this position.

        order : typing.Optional[ListPasskeysRequestOrder]
            The field to sort passkeys by.

        direction : typing.Optional[ListPasskeysRequestDirection]
            Sort direction.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Passkey, ListPasskeysResponse]
            passkeys listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.users.passkeys.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            first=first,
            after=after,
            last=last,
            before=before,
            order=order,
            direction=direction,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        attestation_object: str,
        client_data_json: str,
        credential_id: str,
        nickname: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Passkey:
        """
        Registers a passkey for the authenticated user from the attestation a browser produced for a `registration` challenge. Mint that challenge first with `POST /users/me/passkeys/challenge`; it is single-use and expires 5 minutes after it is issued. Requires a user session.

        Parameters
        ----------
        attestation_object : str
            The `attestationObject` from the WebAuthn attestation response, base64url-encoded.

        client_data_json : str
            The `clientDataJSON` from the WebAuthn attestation response, base64url-encoded.

        credential_id : str
            The WebAuthn credential ID the authenticator returned, base64url-encoded.

        nickname : str
            A name for this passkey, usually the device it lives on. 255 characters or fewer.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Passkey
            passkey registered

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.users.passkeys.create(
                attestation_object="YXR0ZXN0YXRpb24",
                client_data_json="Y2xpZW50LWRhdGE",
                credential_id="bmV3LWNyZWRlbnRpYWw",
                nickname="Work laptop",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            attestation_object=attestation_object,
            client_data_json=client_data_json,
            credential_id=credential_id,
            nickname=nickname,
            request_options=request_options,
        )
        return _response.data

    async def challenge(
        self,
        *,
        challenge_type: ChallengePasskeysRequestChallengeType,
        passkey_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChallengePasskeysResponse:
        """
        Mints the challenge a browser needs to run a WebAuthn ceremony against the authenticated user's own passkeys. A `registration` challenge enrolls a new passkey; a `deletion` challenge is bound to the one passkey named by `passkey_id` and proves the user still holds it. Challenges are single-use and expire 5 minutes after they are issued, so send a fresh `Idempotency-Key` per ceremony — a replayed key returns the original challenge, which may already have expired. Requires a user session.

        Parameters
        ----------
        challenge_type : ChallengePasskeysRequestChallengeType
            The ceremony this challenge is for.

        passkey_id : typing.Optional[str]
            The passkey the ceremony targets, prefixed `wcred_`. Required when `challenge_type` is `deletion`, ignored otherwise.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChallengePasskeysResponse
            challenge minted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.users.passkeys.challenge(
                challenge_type="registration",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.challenge(
            challenge_type=challenge_type, passkey_id=passkey_id, request_options=request_options
        )
        return _response.data

    async def delete(
        self,
        id: str,
        *,
        authenticator_data: str,
        client_data_json: str,
        signature: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeletePasskeysResponse:
        """
        Deletes one of the authenticated user's own passkeys. The request body carries a WebAuthn assertion from the passkey being deleted, so possession of the credential is proven before it is removed: mint a `deletion` challenge for it first, run the ceremony with that passkey, and send the result here. Deleting the user's last passkey is allowed — their other step-up factors remain. Requires a user session.

        Parameters
        ----------
        id : str
            Passkey ID, prefixed `wcred_`.

        authenticator_data : str
            The `authenticatorData` from the WebAuthn assertion, base64url-encoded.

        client_data_json : str
            The `clientDataJSON` from the WebAuthn assertion, base64url-encoded.

        signature : str
            The `signature` from the WebAuthn assertion, base64url-encoded.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletePasskeysResponse
            passkey deleted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.users.passkeys.delete(
                id="id",
                authenticator_data="YXV0aGVudGljYXRvci1kYXRh",
                client_data_json="Y2xpZW50LWRhdGE",
                signature="c2lnbmF0dXJl",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(
            id,
            authenticator_data=authenticator_data,
            client_data_json=client_data_json,
            signature=signature,
            request_options=request_options,
        )
        return _response.data
