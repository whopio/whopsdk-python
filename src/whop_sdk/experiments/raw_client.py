# This file was auto-generated by Fern from our API Definition.

import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import encode_path_param
from ..core.pagination import AsyncPager, SyncPager
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..core.serialization import convert_and_respect_annotation_metadata
from ..errors.bad_request_error import BadRequestError
from ..errors.conflict_error import ConflictError
from ..errors.not_found_error import NotFoundError
from ..types.experiment import Experiment
from ..types.experiment_resource_reference import ExperimentResourceReference
from ..types.exposures_experiments_request_subject import ExposuresExperimentsRequestSubject
from ..types.v1error_response import V1ErrorResponse
from .types.create_experiments_request_bucket_by import CreateExperimentsRequestBucketBy
from .types.create_experiments_request_control import CreateExperimentsRequestControl
from .types.create_experiments_request_targeting_rules_item import CreateExperimentsRequestTargetingRulesItem
from .types.create_experiments_request_variants_item import CreateExperimentsRequestVariantsItem
from .types.exposures_experiments_response import ExposuresExperimentsResponse
from .types.list_experiments_request_direction import ListExperimentsRequestDirection
from .types.list_experiments_request_order import ListExperimentsRequestOrder
from .types.list_experiments_request_status import ListExperimentsRequestStatus
from .types.list_experiments_response import ListExperimentsResponse
from .types.update_experiments_request_control import UpdateExperimentsRequestControl
from .types.update_experiments_request_targeting_rules_item import UpdateExperimentsRequestTargetingRulesItem
from .types.update_experiments_request_variants_item import UpdateExperimentsRequestVariantsItem
from pydantic import ValidationError

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawExperimentsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        related_resource: typing.Optional[ExperimentResourceReference] = None,
        status: typing.Optional[ListExperimentsRequestStatus] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListExperimentsRequestOrder] = None,
        direction: typing.Optional[ListExperimentsRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Experiment, ListExperimentsResponse]:
        """
        Lists experiments for one account with experiment:read permission. Omit account_id or pass internal to list internal experiments, which requires Whop internal access.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Owning account ID. Omit or pass internal for Whop internal experiments; internal access is required.

        related_resource : typing.Optional[ExperimentResourceReference]
            Filter by related resource; requires account_id.

        status : typing.Optional[ListExperimentsRequestStatus]
            Only experiments with this status.

        first : typing.Optional[int]
            The number of experiments to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns experiments after this position.

        last : typing.Optional[int]
            The number of experiments to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns experiments before this position.

        order : typing.Optional[ListExperimentsRequestOrder]
            The field to sort experiments by.

        direction : typing.Optional[ListExperimentsRequestDirection]
            Sort direction.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Experiment, ListExperimentsResponse]
            experiments listed
        """
        _response = self._client_wrapper.httpx_client.request(
            "experiments",
            method="GET",
            params={
                "account_id": account_id,
                "related_resource": convert_and_respect_annotation_metadata(
                    object_=related_resource, annotation=ExperimentResourceReference, direction="write"
                ),
                "status": status,
                "first": first,
                "after": after,
                "last": last,
                "before": before,
                "order": order,
                "direction": direction,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListExperimentsResponse,
                    parse_obj_as(
                        type_=ListExperimentsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""
                    _get_next = lambda: self.list(
                        account_id=account_id,
                        related_resource=related_resource,
                        status=status,
                        first=first,
                        after=_parsed_next,
                        last=last,
                        before=before,
                        order=order,
                        direction=direction,
                        request_options=request_options,
                    )
                return SyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def create(
        self,
        *,
        account_id: str,
        flag_key: str,
        bucket_by: typing.Optional[CreateExperimentsRequestBucketBy] = OMIT,
        control: typing.Optional[CreateExperimentsRequestControl] = OMIT,
        feature_flag_only: typing.Optional[bool] = OMIT,
        hypothesis: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        related_resource: typing.Optional[ExperimentResourceReference] = OMIT,
        targeting_rules: typing.Optional[typing.Sequence[CreateExperimentsRequestTargetingRulesItem]] = OMIT,
        variants: typing.Optional[typing.Sequence[CreateExperimentsRequestVariantsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[Experiment]:
        """
        Creates a draft experiment for the specified account. Use internal for a Whop platform experiment.

        Parameters
        ----------
        account_id : str
            Owning account tag or internal. Required; ownership cannot change.

        flag_key : str
            Developer-chosen handle referenced from code, used in evaluation and results calls. Interchangeable with the returned `expt_` id anywhere the API takes an experiment identifier.

        bucket_by : typing.Optional[CreateExperimentsRequestBucketBy]
            Randomization unit, and the only identity the assignment is keyed on — evaluation fails rather than falling back to another. `user` (default) uses `subject[user_id]` for account experiments and the signed-in user for internal experiments; `account` uses `subject[account_id]`, so every user of an account gets the same arm; `anonymous` uses the anonymous id and survives sign-in. Fixed after creation.

        control : typing.Optional[CreateExperimentsRequestControl]

        feature_flag_only : typing.Optional[bool]
            When `true`, creates a binary feature flag rather than a full experiment. Feature flags expose the same evaluation API but do not collect metric results. Defaults to `false`.

        hypothesis : typing.Optional[str]
            Required for full experiments; rejected on feature flags. Structure it as "If we [change] for [cohort], then [measurable behavior] will [increase/decrease], resulting in [business outcome], because [evidence]. Created by [name]." Evidence should be something real — a baseline number, a funnel breakdown — not a guess, and [name] is you, not something to leave blank.

        name : typing.Optional[str]
            Human-readable display name. Defaults to `flag_key` when omitted.

        related_resource : typing.Optional[ExperimentResourceReference]

        targeting_rules : typing.Optional[typing.Sequence[CreateExperimentsRequestTargetingRulesItem]]
            Rules that determine which subjects qualify for the experiment.

        variants : typing.Optional[typing.Sequence[CreateExperimentsRequestVariantsItem]]
            Treatment arms to create. Users outside every arm form the implicit `control` group. Required unless `feature_flag_only` is `true`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Experiment]
            experiment created
        """
        _response = self._client_wrapper.httpx_client.request(
            "experiments",
            method="POST",
            json={
                "account_id": account_id,
                "bucket_by": bucket_by,
                "control": convert_and_respect_annotation_metadata(
                    object_=control, annotation=CreateExperimentsRequestControl, direction="write"
                ),
                "feature_flag_only": feature_flag_only,
                "flag_key": flag_key,
                "hypothesis": hypothesis,
                "name": name,
                "related_resource": convert_and_respect_annotation_metadata(
                    object_=related_resource, annotation=ExperimentResourceReference, direction="write"
                ),
                "targeting_rules": convert_and_respect_annotation_metadata(
                    object_=targeting_rules,
                    annotation=typing.Sequence[CreateExperimentsRequestTargetingRulesItem],
                    direction="write",
                ),
                "variants": convert_and_respect_annotation_metadata(
                    object_=variants,
                    annotation=typing.Sequence[CreateExperimentsRequestVariantsItem],
                    direction="write",
                ),
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def exposures(
        self,
        *,
        subject: typing.Optional[ExposuresExperimentsRequestSubject] = None,
        related_resource: typing.Optional[ExperimentResourceReference] = None,
        flag_key: typing.Optional[str] = None,
        account_id: typing.Optional[str] = None,
        properties: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[ExposuresExperimentsResponse]:
        """
        Evaluates and records an exposure without requiring authentication. When credentials resolve, their authentication method, API key ID, and signed-in user ID are recorded on the exposure event. Pass subject for bucketing identity and account_id for experiment ownership.

        Pass `flag_key` to check a single flag, or omit it to fetch active flags in the account and related resource scope. Internal anonymous callers may use the `x-whop-anonymous-id` header or `ajs_anonymous_id` cookie; explicit `subject[anonymous_id]` takes precedence.

        Assignments use exactly the configured `bucket_by`: `subject[user_id]`, `subject[account_id]`, or `subject[anonymous_id]`. Internal user experiments derive identity from the signed-in session. Missing the required identity fails single evaluation and omits the experiment from batch evaluation. Subjects outside all treatment ranges receive control.

        Pass `subject[account_id]` to enable account-level targeting rules. Pass `properties` as a JSON object to supply the values that `property` targeting conditions match against.

        Parameters
        ----------
        subject : typing.Optional[ExposuresExperimentsRequestSubject]
            Bucketing subject. Ownership is the top-level account_id. Account experiments accept caller-supplied subject IDs; internal experiments derive the user from the session.

        related_resource : typing.Optional[ExperimentResourceReference]
            Restricts batch evaluation to this related resource; omitted batches contain only unbound experiments.

        flag_key : typing.Optional[str]
            Flag or experiment to evaluate — the flag_key handle or the `expt_` id. Omit to return all flags the caller qualifies for.

        account_id : typing.Optional[str]
            Owning account ID or internal. Required when evaluating by flag_key or in a batch; optional for an expt_ ID.

        properties : typing.Optional[str]
            JSON-encoded scalar values that property targeting conditions match against. Numeric and boolean strings are coerced. Nested query keys such as properties[plan]=pro remain accepted for existing callers. For internal experiments, is_internal_user is derived from the session and cannot be overridden.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[ExposuresExperimentsResponse]
            single flag evaluated
        """
        _response = self._client_wrapper.httpx_client.request(
            "experiments/exposures",
            method="GET",
            params={
                "subject": convert_and_respect_annotation_metadata(
                    object_=subject, annotation=ExposuresExperimentsRequestSubject, direction="write"
                ),
                "related_resource": convert_and_respect_annotation_metadata(
                    object_=related_resource, annotation=ExperimentResourceReference, direction="write"
                ),
                "flag_key": flag_key,
                "account_id": account_id,
                "properties": properties,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ExposuresExperimentsResponse,
                    parse_obj_as(
                        type_=ExposuresExperimentsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[Experiment]:
        """
        Retrieves a single experiment or feature flag by its `expt_` id or flag_key handle. Requires the corresponding experiment permission on the owning account, or Whop internal access for internal experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Experiment]
            experiment found
        """
        _response = self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}",
            method="GET",
            params={
                "account_id": account_id,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def update(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        control: typing.Optional[UpdateExperimentsRequestControl] = OMIT,
        hypothesis: typing.Optional[str] = OMIT,
        related_resource: typing.Optional[ExperimentResourceReference] = OMIT,
        targeting_rules: typing.Optional[typing.Sequence[UpdateExperimentsRequestTargetingRulesItem]] = OMIT,
        variants: typing.Optional[typing.Sequence[UpdateExperimentsRequestVariantsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[Experiment]:
        """
        Updates the targeting rules, treatment allocation, metrics, or hypothesis of an existing experiment or feature flag. Weights and metrics can only grow, so enrolled users never change arms and an existing metric is never dropped. Lifecycle moves through the transition endpoints (`activate`, `pause`, `end`), never through this update. Requires the corresponding experiment permission on the owning account, or Whop internal access for internal experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        control : typing.Optional[UpdateExperimentsRequestControl]

        hypothesis : typing.Optional[str]
            Omit to leave unchanged. Send an empty string to clear it. Not accepted on feature flags. When setting it, structure it as "If we [change] for [cohort], then [measurable behavior] will [increase/decrease], resulting in [business outcome], because [evidence]. Created by [name]." same as on create.

        related_resource : typing.Optional[ExperimentResourceReference]

        targeting_rules : typing.Optional[typing.Sequence[UpdateExperimentsRequestTargetingRulesItem]]
            Replace the targeting rules with this set. Omit to leave unchanged.

        variants : typing.Optional[typing.Sequence[UpdateExperimentsRequestVariantsItem]]
            Grow treatment allocation. Pass every existing treatment with an equal-or-higher weight; append new names to add arms. Weights never decrease and arms are never removed. Omit to leave unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Experiment]
            metrics added
        """
        _response = self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}",
            method="PATCH",
            params={
                "account_id": account_id,
            },
            json={
                "control": convert_and_respect_annotation_metadata(
                    object_=control, annotation=UpdateExperimentsRequestControl, direction="write"
                ),
                "hypothesis": hypothesis,
                "related_resource": convert_and_respect_annotation_metadata(
                    object_=related_resource, annotation=ExperimentResourceReference, direction="write"
                ),
                "targeting_rules": convert_and_respect_annotation_metadata(
                    object_=targeting_rules,
                    annotation=typing.Sequence[UpdateExperimentsRequestTargetingRulesItem],
                    direction="write",
                ),
                "variants": convert_and_respect_annotation_metadata(
                    object_=variants,
                    annotation=typing.Sequence[UpdateExperimentsRequestVariantsItem],
                    direction="write",
                ),
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def activate(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        duration_days: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[Experiment]:
        """
        Starts (or resumes) an experiment or feature flag so evaluation begins serving it. Activating a draft stamps `started_at`; resuming a paused experiment keeps the original start. Only drafts and paused experiments can be activated. Requires the corresponding experiment permission on the owning account, or Whop internal access for internal experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        duration_days : typing.Optional[int]
            Reporting window length in days. Omit to use the default. This does not automatically end the experiment.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Experiment]
            experiment activated
        """
        _response = self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}/activate",
            method="POST",
            params={
                "account_id": account_id,
            },
            json={
                "duration_days": duration_days,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def end(
        self,
        id: str,
        *,
        findings: str,
        account_id: typing.Optional[str] = None,
        winning_arm: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[Experiment]:
        """
        Concludes the experiment and records required `findings`. Pass `winning_arm` to serve the winning treatment to everyone; omit it when control won. Ended experiments cannot restart, but may be ended again to correct the winner. Requires experiment:manage on the account, or internal access for platform experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        findings : str
            What you learned and why you chose this outcome. Required.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        winning_arm : typing.Optional[str]
            The treatment that won. Setting it rolls that arm out: every later evaluation returns it to every caller, ignoring targeting and allocation, and exposures stop being recorded. Omit it when control won — an ended experiment with no winning arm evaluates to `control` for everyone, so ending again without one also reverts a rollout recorded by mistake. Not accepted on feature flags, which evaluate to disabled once ended.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Experiment]
            experiment ended
        """
        _response = self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}/end",
            method="POST",
            params={
                "account_id": account_id,
            },
            json={
                "findings": findings,
                "winning_arm": winning_arm,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def pause(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[Experiment]:
        """
        Pauses an active experiment or feature flag: evaluation stops serving it and exposures stop flowing. Assignments are keyed on stable identity, so users return to their original arm when the experiment resumes. Requires the corresponding experiment permission on the owning account, or Whop internal access for internal experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Experiment]
            experiment paused
        """
        _response = self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}/pause",
            method="POST",
            params={
                "account_id": account_id,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawExperimentsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        related_resource: typing.Optional[ExperimentResourceReference] = None,
        status: typing.Optional[ListExperimentsRequestStatus] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListExperimentsRequestOrder] = None,
        direction: typing.Optional[ListExperimentsRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Experiment, ListExperimentsResponse]:
        """
        Lists experiments for one account with experiment:read permission. Omit account_id or pass internal to list internal experiments, which requires Whop internal access.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Owning account ID. Omit or pass internal for Whop internal experiments; internal access is required.

        related_resource : typing.Optional[ExperimentResourceReference]
            Filter by related resource; requires account_id.

        status : typing.Optional[ListExperimentsRequestStatus]
            Only experiments with this status.

        first : typing.Optional[int]
            The number of experiments to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns experiments after this position.

        last : typing.Optional[int]
            The number of experiments to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns experiments before this position.

        order : typing.Optional[ListExperimentsRequestOrder]
            The field to sort experiments by.

        direction : typing.Optional[ListExperimentsRequestDirection]
            Sort direction.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Experiment, ListExperimentsResponse]
            experiments listed
        """
        _response = await self._client_wrapper.httpx_client.request(
            "experiments",
            method="GET",
            params={
                "account_id": account_id,
                "related_resource": convert_and_respect_annotation_metadata(
                    object_=related_resource, annotation=ExperimentResourceReference, direction="write"
                ),
                "status": status,
                "first": first,
                "after": after,
                "last": last,
                "before": before,
                "order": order,
                "direction": direction,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListExperimentsResponse,
                    parse_obj_as(
                        type_=ListExperimentsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""

                    async def _get_next():
                        return await self.list(
                            account_id=account_id,
                            related_resource=related_resource,
                            status=status,
                            first=first,
                            after=_parsed_next,
                            last=last,
                            before=before,
                            order=order,
                            direction=direction,
                            request_options=request_options,
                        )

                return AsyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def create(
        self,
        *,
        account_id: str,
        flag_key: str,
        bucket_by: typing.Optional[CreateExperimentsRequestBucketBy] = OMIT,
        control: typing.Optional[CreateExperimentsRequestControl] = OMIT,
        feature_flag_only: typing.Optional[bool] = OMIT,
        hypothesis: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        related_resource: typing.Optional[ExperimentResourceReference] = OMIT,
        targeting_rules: typing.Optional[typing.Sequence[CreateExperimentsRequestTargetingRulesItem]] = OMIT,
        variants: typing.Optional[typing.Sequence[CreateExperimentsRequestVariantsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[Experiment]:
        """
        Creates a draft experiment for the specified account. Use internal for a Whop platform experiment.

        Parameters
        ----------
        account_id : str
            Owning account tag or internal. Required; ownership cannot change.

        flag_key : str
            Developer-chosen handle referenced from code, used in evaluation and results calls. Interchangeable with the returned `expt_` id anywhere the API takes an experiment identifier.

        bucket_by : typing.Optional[CreateExperimentsRequestBucketBy]
            Randomization unit, and the only identity the assignment is keyed on — evaluation fails rather than falling back to another. `user` (default) uses `subject[user_id]` for account experiments and the signed-in user for internal experiments; `account` uses `subject[account_id]`, so every user of an account gets the same arm; `anonymous` uses the anonymous id and survives sign-in. Fixed after creation.

        control : typing.Optional[CreateExperimentsRequestControl]

        feature_flag_only : typing.Optional[bool]
            When `true`, creates a binary feature flag rather than a full experiment. Feature flags expose the same evaluation API but do not collect metric results. Defaults to `false`.

        hypothesis : typing.Optional[str]
            Required for full experiments; rejected on feature flags. Structure it as "If we [change] for [cohort], then [measurable behavior] will [increase/decrease], resulting in [business outcome], because [evidence]. Created by [name]." Evidence should be something real — a baseline number, a funnel breakdown — not a guess, and [name] is you, not something to leave blank.

        name : typing.Optional[str]
            Human-readable display name. Defaults to `flag_key` when omitted.

        related_resource : typing.Optional[ExperimentResourceReference]

        targeting_rules : typing.Optional[typing.Sequence[CreateExperimentsRequestTargetingRulesItem]]
            Rules that determine which subjects qualify for the experiment.

        variants : typing.Optional[typing.Sequence[CreateExperimentsRequestVariantsItem]]
            Treatment arms to create. Users outside every arm form the implicit `control` group. Required unless `feature_flag_only` is `true`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Experiment]
            experiment created
        """
        _response = await self._client_wrapper.httpx_client.request(
            "experiments",
            method="POST",
            json={
                "account_id": account_id,
                "bucket_by": bucket_by,
                "control": convert_and_respect_annotation_metadata(
                    object_=control, annotation=CreateExperimentsRequestControl, direction="write"
                ),
                "feature_flag_only": feature_flag_only,
                "flag_key": flag_key,
                "hypothesis": hypothesis,
                "name": name,
                "related_resource": convert_and_respect_annotation_metadata(
                    object_=related_resource, annotation=ExperimentResourceReference, direction="write"
                ),
                "targeting_rules": convert_and_respect_annotation_metadata(
                    object_=targeting_rules,
                    annotation=typing.Sequence[CreateExperimentsRequestTargetingRulesItem],
                    direction="write",
                ),
                "variants": convert_and_respect_annotation_metadata(
                    object_=variants,
                    annotation=typing.Sequence[CreateExperimentsRequestVariantsItem],
                    direction="write",
                ),
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def exposures(
        self,
        *,
        subject: typing.Optional[ExposuresExperimentsRequestSubject] = None,
        related_resource: typing.Optional[ExperimentResourceReference] = None,
        flag_key: typing.Optional[str] = None,
        account_id: typing.Optional[str] = None,
        properties: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[ExposuresExperimentsResponse]:
        """
        Evaluates and records an exposure without requiring authentication. When credentials resolve, their authentication method, API key ID, and signed-in user ID are recorded on the exposure event. Pass subject for bucketing identity and account_id for experiment ownership.

        Pass `flag_key` to check a single flag, or omit it to fetch active flags in the account and related resource scope. Internal anonymous callers may use the `x-whop-anonymous-id` header or `ajs_anonymous_id` cookie; explicit `subject[anonymous_id]` takes precedence.

        Assignments use exactly the configured `bucket_by`: `subject[user_id]`, `subject[account_id]`, or `subject[anonymous_id]`. Internal user experiments derive identity from the signed-in session. Missing the required identity fails single evaluation and omits the experiment from batch evaluation. Subjects outside all treatment ranges receive control.

        Pass `subject[account_id]` to enable account-level targeting rules. Pass `properties` as a JSON object to supply the values that `property` targeting conditions match against.

        Parameters
        ----------
        subject : typing.Optional[ExposuresExperimentsRequestSubject]
            Bucketing subject. Ownership is the top-level account_id. Account experiments accept caller-supplied subject IDs; internal experiments derive the user from the session.

        related_resource : typing.Optional[ExperimentResourceReference]
            Restricts batch evaluation to this related resource; omitted batches contain only unbound experiments.

        flag_key : typing.Optional[str]
            Flag or experiment to evaluate — the flag_key handle or the `expt_` id. Omit to return all flags the caller qualifies for.

        account_id : typing.Optional[str]
            Owning account ID or internal. Required when evaluating by flag_key or in a batch; optional for an expt_ ID.

        properties : typing.Optional[str]
            JSON-encoded scalar values that property targeting conditions match against. Numeric and boolean strings are coerced. Nested query keys such as properties[plan]=pro remain accepted for existing callers. For internal experiments, is_internal_user is derived from the session and cannot be overridden.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[ExposuresExperimentsResponse]
            single flag evaluated
        """
        _response = await self._client_wrapper.httpx_client.request(
            "experiments/exposures",
            method="GET",
            params={
                "subject": convert_and_respect_annotation_metadata(
                    object_=subject, annotation=ExposuresExperimentsRequestSubject, direction="write"
                ),
                "related_resource": convert_and_respect_annotation_metadata(
                    object_=related_resource, annotation=ExperimentResourceReference, direction="write"
                ),
                "flag_key": flag_key,
                "account_id": account_id,
                "properties": properties,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ExposuresExperimentsResponse,
                    parse_obj_as(
                        type_=ExposuresExperimentsResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[Experiment]:
        """
        Retrieves a single experiment or feature flag by its `expt_` id or flag_key handle. Requires the corresponding experiment permission on the owning account, or Whop internal access for internal experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Experiment]
            experiment found
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}",
            method="GET",
            params={
                "account_id": account_id,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def update(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        control: typing.Optional[UpdateExperimentsRequestControl] = OMIT,
        hypothesis: typing.Optional[str] = OMIT,
        related_resource: typing.Optional[ExperimentResourceReference] = OMIT,
        targeting_rules: typing.Optional[typing.Sequence[UpdateExperimentsRequestTargetingRulesItem]] = OMIT,
        variants: typing.Optional[typing.Sequence[UpdateExperimentsRequestVariantsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[Experiment]:
        """
        Updates the targeting rules, treatment allocation, metrics, or hypothesis of an existing experiment or feature flag. Weights and metrics can only grow, so enrolled users never change arms and an existing metric is never dropped. Lifecycle moves through the transition endpoints (`activate`, `pause`, `end`), never through this update. Requires the corresponding experiment permission on the owning account, or Whop internal access for internal experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        control : typing.Optional[UpdateExperimentsRequestControl]

        hypothesis : typing.Optional[str]
            Omit to leave unchanged. Send an empty string to clear it. Not accepted on feature flags. When setting it, structure it as "If we [change] for [cohort], then [measurable behavior] will [increase/decrease], resulting in [business outcome], because [evidence]. Created by [name]." same as on create.

        related_resource : typing.Optional[ExperimentResourceReference]

        targeting_rules : typing.Optional[typing.Sequence[UpdateExperimentsRequestTargetingRulesItem]]
            Replace the targeting rules with this set. Omit to leave unchanged.

        variants : typing.Optional[typing.Sequence[UpdateExperimentsRequestVariantsItem]]
            Grow treatment allocation. Pass every existing treatment with an equal-or-higher weight; append new names to add arms. Weights never decrease and arms are never removed. Omit to leave unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Experiment]
            metrics added
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}",
            method="PATCH",
            params={
                "account_id": account_id,
            },
            json={
                "control": convert_and_respect_annotation_metadata(
                    object_=control, annotation=UpdateExperimentsRequestControl, direction="write"
                ),
                "hypothesis": hypothesis,
                "related_resource": convert_and_respect_annotation_metadata(
                    object_=related_resource, annotation=ExperimentResourceReference, direction="write"
                ),
                "targeting_rules": convert_and_respect_annotation_metadata(
                    object_=targeting_rules,
                    annotation=typing.Sequence[UpdateExperimentsRequestTargetingRulesItem],
                    direction="write",
                ),
                "variants": convert_and_respect_annotation_metadata(
                    object_=variants,
                    annotation=typing.Sequence[UpdateExperimentsRequestVariantsItem],
                    direction="write",
                ),
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def activate(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        duration_days: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[Experiment]:
        """
        Starts (or resumes) an experiment or feature flag so evaluation begins serving it. Activating a draft stamps `started_at`; resuming a paused experiment keeps the original start. Only drafts and paused experiments can be activated. Requires the corresponding experiment permission on the owning account, or Whop internal access for internal experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        duration_days : typing.Optional[int]
            Reporting window length in days. Omit to use the default. This does not automatically end the experiment.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Experiment]
            experiment activated
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}/activate",
            method="POST",
            params={
                "account_id": account_id,
            },
            json={
                "duration_days": duration_days,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def end(
        self,
        id: str,
        *,
        findings: str,
        account_id: typing.Optional[str] = None,
        winning_arm: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[Experiment]:
        """
        Concludes the experiment and records required `findings`. Pass `winning_arm` to serve the winning treatment to everyone; omit it when control won. Ended experiments cannot restart, but may be ended again to correct the winner. Requires experiment:manage on the account, or internal access for platform experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        findings : str
            What you learned and why you chose this outcome. Required.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        winning_arm : typing.Optional[str]
            The treatment that won. Setting it rolls that arm out: every later evaluation returns it to every caller, ignoring targeting and allocation, and exposures stop being recorded. Omit it when control won — an ended experiment with no winning arm evaluates to `control` for everyone, so ending again without one also reverts a rollout recorded by mistake. Not accepted on feature flags, which evaluate to disabled once ended.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Experiment]
            experiment ended
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}/end",
            method="POST",
            params={
                "account_id": account_id,
            },
            json={
                "findings": findings,
                "winning_arm": winning_arm,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def pause(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[Experiment]:
        """
        Pauses an active experiment or feature flag: evaluation stops serving it and exposures stop flowing. Assignments are keyed on stable identity, so users return to their original arm when the experiment resumes. Requires the corresponding experiment permission on the owning account, or Whop internal access for internal experiments.

        Parameters
        ----------
        id : str
            The experiment identifier — the `expt_` id or the flag_key handle.

        account_id : typing.Optional[str]
            Owning account or internal. Required when id is a flag key; optional for an expt_ ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Experiment]
            experiment paused
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"experiments/{encode_path_param(id)}/pause",
            method="POST",
            params={
                "account_id": account_id,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Experiment,
                    parse_obj_as(
                        type_=Experiment,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
