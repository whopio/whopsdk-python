# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.dm_channel import DmChannel
from ..types.dm_channel_list_item import DmChannelListItem
from .raw_client import AsyncRawDmChannelsClient, RawDmChannelsClient
from .types.list_dm_channels_response import ListDmChannelsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class DmChannelsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawDmChannelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawDmChannelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawDmChannelsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[DmChannelListItem, ListDmChannelsResponse]:
        """
        Returns a paginated list of DM channels for the currently authenticated user, sorted by most recently active.

        Required permissions:
         - `dms:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        account_id : typing.Optional[str]
            The unique identifier of a company to filter DM channels by. Only returns channels scoped to this company.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[DmChannelListItem, ListDmChannelsResponse]
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.dm_channels.list(
            first=42,
            last=42,
            account_id="biz_xxxxxxxxxxxxxx",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            after=after, before=before, first=first, last=last, account_id=account_id, request_options=request_options
        )

    def create(
        self,
        *,
        with_user_ids: typing.Sequence[str],
        account_id: typing.Optional[str] = OMIT,
        custom_name: typing.Optional[str] = OMIT,
        notifications_enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DmChannel:
        """
        Create a new DM channel between two or more users, optionally scoped to a specific company. Returns the existing channel if one already exists.

        Required permissions:
         - `dms:channel:manage`

        Parameters
        ----------
        with_user_ids : typing.Sequence[str]
            The list of user identifiers to include in the DM channel. Each entry can be an email, username, or user ID (e.g. 'user_xxxxx').

        account_id : typing.Optional[str]
            The unique identifier of the company to scope this DM channel to. When set, the channel is visible only within that company context.

        custom_name : typing.Optional[str]
            A custom display name for the DM channel. For example, 'Project Discussion'.

        notifications_enabled : typing.Optional[bool]
            Whether Whop app notifications are enabled for this direct message channel. Webhooks still fire.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DmChannel
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.dm_channels.create(
            with_user_ids=["with_user_ids"],
        )
        """
        _response = self._raw_client.create(
            with_user_ids=with_user_ids,
            account_id=account_id,
            custom_name=custom_name,
            notifications_enabled=notifications_enabled,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DmChannel:
        """
        Retrieves the details of an existing DM channel.

        Required permissions (one of):
         - `dms:read`
         - `support_chat:read`

        Parameters
        ----------
        id : str
            The unique identifier of the DM channel to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DmChannel
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.dm_channels.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> bool:
        """
        Permanently delete a DM channel and all of its messages. Only an admin of the channel can perform this action.

        Required permissions (one of):
         - `dms:channel:manage`
         - `support_chat:create`

        Parameters
        ----------
        id : str
            The unique identifier of the DM channel to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        bool
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.dm_channels.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        custom_name: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DmChannel:
        """
        Update the settings of an existing DM channel, such as its display name. Only an admin of the channel can perform this action.

        Required permissions (one of):
         - `dms:channel:manage`
         - `support_chat:create`

        Parameters
        ----------
        id : str
            The unique identifier of the DM channel to update.

        custom_name : typing.Optional[str]
            A new custom display name for the DM channel. For example, 'Project Discussion'.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DmChannel
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.dm_channels.update(
            id="id",
        )
        """
        _response = self._raw_client.update(id, custom_name=custom_name, request_options=request_options)
        return _response.data


class AsyncDmChannelsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawDmChannelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawDmChannelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawDmChannelsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[DmChannelListItem, ListDmChannelsResponse]:
        """
        Returns a paginated list of DM channels for the currently authenticated user, sorted by most recently active.

        Required permissions:
         - `dms:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        account_id : typing.Optional[str]
            The unique identifier of a company to filter DM channels by. Only returns channels scoped to this company.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[DmChannelListItem, ListDmChannelsResponse]
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.dm_channels.list(
                first=42,
                last=42,
                account_id="biz_xxxxxxxxxxxxxx",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            after=after, before=before, first=first, last=last, account_id=account_id, request_options=request_options
        )

    async def create(
        self,
        *,
        with_user_ids: typing.Sequence[str],
        account_id: typing.Optional[str] = OMIT,
        custom_name: typing.Optional[str] = OMIT,
        notifications_enabled: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DmChannel:
        """
        Create a new DM channel between two or more users, optionally scoped to a specific company. Returns the existing channel if one already exists.

        Required permissions:
         - `dms:channel:manage`

        Parameters
        ----------
        with_user_ids : typing.Sequence[str]
            The list of user identifiers to include in the DM channel. Each entry can be an email, username, or user ID (e.g. 'user_xxxxx').

        account_id : typing.Optional[str]
            The unique identifier of the company to scope this DM channel to. When set, the channel is visible only within that company context.

        custom_name : typing.Optional[str]
            A custom display name for the DM channel. For example, 'Project Discussion'.

        notifications_enabled : typing.Optional[bool]
            Whether Whop app notifications are enabled for this direct message channel. Webhooks still fire.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DmChannel
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.dm_channels.create(
                with_user_ids=["with_user_ids"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            with_user_ids=with_user_ids,
            account_id=account_id,
            custom_name=custom_name,
            notifications_enabled=notifications_enabled,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DmChannel:
        """
        Retrieves the details of an existing DM channel.

        Required permissions (one of):
         - `dms:read`
         - `support_chat:read`

        Parameters
        ----------
        id : str
            The unique identifier of the DM channel to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DmChannel
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.dm_channels.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> bool:
        """
        Permanently delete a DM channel and all of its messages. Only an admin of the channel can perform this action.

        Required permissions (one of):
         - `dms:channel:manage`
         - `support_chat:create`

        Parameters
        ----------
        id : str
            The unique identifier of the DM channel to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        bool
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.dm_channels.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        custom_name: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DmChannel:
        """
        Update the settings of an existing DM channel, such as its display name. Only an admin of the channel can perform this action.

        Required permissions (one of):
         - `dms:channel:manage`
         - `support_chat:create`

        Parameters
        ----------
        id : str
            The unique identifier of the DM channel to update.

        custom_name : typing.Optional[str]
            A new custom display name for the DM channel. For example, 'Project Discussion'.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DmChannel
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.dm_channels.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(id, custom_name=custom_name, request_options=request_options)
        return _response.data
