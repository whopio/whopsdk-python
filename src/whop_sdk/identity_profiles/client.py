# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.identity_profile import IdentityProfile
from ..types.identity_profile_kinds import IdentityProfileKinds
from ..types.identity_profile_list_item import IdentityProfileListItem
from ..types.identity_profile_statuses import IdentityProfileStatuses
from .raw_client import AsyncRawIdentityProfilesClient, RawIdentityProfilesClient
from .types.list_identity_profile_response import ListIdentityProfileResponse
from .types.list_verifications_identity_profile_response import ListVerificationsIdentityProfileResponse
from .types.list_verifications_identity_profile_response_data_item import (
    ListVerificationsIdentityProfileResponseDataItem,
)


class IdentityProfilesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawIdentityProfilesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawIdentityProfilesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawIdentityProfilesClient
        """
        return self._raw_client

    def list_identity_profile(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        profile_type: typing.Optional[IdentityProfileKinds] = None,
        status: typing.Optional[IdentityProfileStatuses] = None,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[IdentityProfileListItem, ListIdentityProfileResponse]:
        """
        Returns a paginated list of identity profiles. When company_id is provided, lists IPs currently linked to that company's ledger. When omitted, lists IPs linked to any ledger the actor can read (including child companies under a parent).

        Required permissions:
         - `identity:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        profile_type : typing.Optional[IdentityProfileKinds]

        status : typing.Optional[IdentityProfileStatuses]

        account_id : typing.Optional[str]
            The unique identifier of the company to filter to. When omitted, returns IPs across all ledgers the actor can read.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[IdentityProfileListItem, ListIdentityProfileResponse]
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.identity_profiles.list_identity_profile(
            first=42,
            last=42,
            account_id="biz_xxxxxxxxxxxxxx",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_identity_profile(
            after=after,
            before=before,
            first=first,
            last=last,
            profile_type=profile_type,
            status=status,
            account_id=account_id,
            request_options=request_options,
        )

    def retrieve_identity_profile(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> IdentityProfile:
        """
        Retrieves the details of an existing identity profile.

        Required permissions:
         - `identity:read`

        Parameters
        ----------
        id : str
            The unique identifier of the identity profile (idpf_xxx).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IdentityProfile
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.identity_profiles.retrieve_identity_profile(
            id="idpf_xxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.retrieve_identity_profile(id, request_options=request_options)
        return _response.data

    def unlink_identity_profile(
        self, id: str, *, ledger_account_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> IdentityProfile:
        """
        Unlinks an IdentityProfile from a LedgerAccount (flips the matching link to is_current=false).

        Parameters
        ----------
        id : str
            The ID of the IdentityProfile to unlink.

        ledger_account_id : str
            The ID of the LedgerAccount to unlink the identity profile from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IdentityProfile
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.identity_profiles.unlink_identity_profile(
            id="idpf_xxxxxxxxxxxxx",
            ledger_account_id="ldgr_xxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.unlink_identity_profile(
            id, ledger_account_id=ledger_account_id, request_options=request_options
        )
        return _response.data

    def list_verifications_identity_profile(
        self,
        id: str,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ListVerificationsIdentityProfileResponseDataItem, ListVerificationsIdentityProfileResponse]:
        """
        Returns a list of verifications attached to an identity profile, ordered by most recent first.

        Required permissions:
         - `identity:read`

        Parameters
        ----------
        id : str
            The unique identifier of the identity profile (idpf_xxx).

        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ListVerificationsIdentityProfileResponseDataItem, ListVerificationsIdentityProfileResponse]
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.identity_profiles.list_verifications_identity_profile(
            id="idpf_xxxxxxxxxxxxx",
            first=42,
            last=42,
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_verifications_identity_profile(
            id, after=after, before=before, first=first, last=last, request_options=request_options
        )


class AsyncIdentityProfilesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawIdentityProfilesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawIdentityProfilesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawIdentityProfilesClient
        """
        return self._raw_client

    async def list_identity_profile(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        profile_type: typing.Optional[IdentityProfileKinds] = None,
        status: typing.Optional[IdentityProfileStatuses] = None,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[IdentityProfileListItem, ListIdentityProfileResponse]:
        """
        Returns a paginated list of identity profiles. When company_id is provided, lists IPs currently linked to that company's ledger. When omitted, lists IPs linked to any ledger the actor can read (including child companies under a parent).

        Required permissions:
         - `identity:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        profile_type : typing.Optional[IdentityProfileKinds]

        status : typing.Optional[IdentityProfileStatuses]

        account_id : typing.Optional[str]
            The unique identifier of the company to filter to. When omitted, returns IPs across all ledgers the actor can read.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[IdentityProfileListItem, ListIdentityProfileResponse]
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.identity_profiles.list_identity_profile(
                first=42,
                last=42,
                account_id="biz_xxxxxxxxxxxxxx",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_identity_profile(
            after=after,
            before=before,
            first=first,
            last=last,
            profile_type=profile_type,
            status=status,
            account_id=account_id,
            request_options=request_options,
        )

    async def retrieve_identity_profile(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> IdentityProfile:
        """
        Retrieves the details of an existing identity profile.

        Required permissions:
         - `identity:read`

        Parameters
        ----------
        id : str
            The unique identifier of the identity profile (idpf_xxx).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IdentityProfile
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.identity_profiles.retrieve_identity_profile(
                id="idpf_xxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve_identity_profile(id, request_options=request_options)
        return _response.data

    async def unlink_identity_profile(
        self, id: str, *, ledger_account_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> IdentityProfile:
        """
        Unlinks an IdentityProfile from a LedgerAccount (flips the matching link to is_current=false).

        Parameters
        ----------
        id : str
            The ID of the IdentityProfile to unlink.

        ledger_account_id : str
            The ID of the LedgerAccount to unlink the identity profile from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IdentityProfile
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.identity_profiles.unlink_identity_profile(
                id="idpf_xxxxxxxxxxxxx",
                ledger_account_id="ldgr_xxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unlink_identity_profile(
            id, ledger_account_id=ledger_account_id, request_options=request_options
        )
        return _response.data

    async def list_verifications_identity_profile(
        self,
        id: str,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ListVerificationsIdentityProfileResponseDataItem, ListVerificationsIdentityProfileResponse]:
        """
        Returns a list of verifications attached to an identity profile, ordered by most recent first.

        Required permissions:
         - `identity:read`

        Parameters
        ----------
        id : str
            The unique identifier of the identity profile (idpf_xxx).

        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ListVerificationsIdentityProfileResponseDataItem, ListVerificationsIdentityProfileResponse]
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = (
                await client.identity_profiles.list_verifications_identity_profile(
                    id="idpf_xxxxxxxxxxxxx",
                    first=42,
                    last=42,
                )
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_verifications_identity_profile(
            id, after=after, before=before, first=first, last=last, request_options=request_options
        )
