# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.payment_rule import PaymentRule
from .raw_client import AsyncRawPaymentRulesClient, RawPaymentRulesClient
from .types.create_payment_rules_request_action import CreatePaymentRulesRequestAction
from .types.create_payment_rules_request_conditions import CreatePaymentRulesRequestConditions
from .types.list_fields_payment_rules_response import ListFieldsPaymentRulesResponse
from .types.list_payment_rules_request_action import ListPaymentRulesRequestAction
from .types.list_payment_rules_request_direction import ListPaymentRulesRequestDirection
from .types.list_payment_rules_request_order import ListPaymentRulesRequestOrder
from .types.list_payment_rules_request_status import ListPaymentRulesRequestStatus
from .types.list_payment_rules_response import ListPaymentRulesResponse
from .types.replace_payment_rules_request_action import ReplacePaymentRulesRequestAction
from .types.replace_payment_rules_request_conditions import ReplacePaymentRulesRequestConditions

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PaymentRulesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPaymentRulesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPaymentRulesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPaymentRulesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        status: typing.Optional[ListPaymentRulesRequestStatus] = None,
        action: typing.Optional[ListPaymentRulesRequestAction] = None,
        order: typing.Optional[ListPaymentRulesRequestOrder] = None,
        direction: typing.Optional[ListPaymentRulesRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PaymentRule, ListPaymentRulesResponse]:
        """
        Parameters
        ----------
        account_id : typing.Optional[str]
            Only return rules belonging to this account. Defaults to the account the request is acting for.

        status : typing.Optional[ListPaymentRulesRequestStatus]
            Only return rules with this status.

        action : typing.Optional[ListPaymentRulesRequestAction]
            Only return rules that take this action.

        order : typing.Optional[ListPaymentRulesRequestOrder]
            The field to sort by.

        direction : typing.Optional[ListPaymentRulesRequestDirection]
            The sort direction.

        first : typing.Optional[int]
            Number of results to return from the start of the range.

        after : typing.Optional[str]
            Return results after this cursor. Use `page_info.end_cursor` from the previous response to fetch the next page.

        last : typing.Optional[int]
            Number of results to return from the end of the range.

        before : typing.Optional[str]
            Return results before this cursor. Use `page_info.start_cursor` from the previous response to fetch the previous page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PaymentRule, ListPaymentRulesResponse]
            A list of payment rules

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.payment_rules.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            status=status,
            action=action,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        action: CreatePaymentRulesRequestAction,
        conditions: CreatePaymentRulesRequestConditions,
        name: str,
        account_id: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentRule:
        """
        Parameters
        ----------
        action : CreatePaymentRulesRequestAction
            What this account's rule requests when every condition matches. One applicable account-rule action wins, in this order: `allow`, `block`, `review`, `enforce_3ds`. An `allow` overrides this account's other rules, never Whop's own fraud controls. A `review` requests authorization without capture for an eligible on-session card payment through Whop Payments. Automatic capture is scheduled for 48 hours after authorization; capture or void the payment before then to decide sooner. Capture may complete later or fail. Review is skipped for unsupported methods, off-session payments, and payments already configured for manual capture. An `enforce_3ds` is skipped when the account rule cannot apply a challenge. Other 3DS requirements still apply.

        conditions : CreatePaymentRulesRequestConditions
            The conditions a payment is matched against. Up to 10 conditions, and 8 KiB once serialized.

        name : str
            A name for this rule. Up to 255 characters.

        account_id : typing.Optional[str]
            The account to create the rule on. Defaults to the account the request is acting for.

        metadata : typing.Optional[typing.Dict[str, str]]
            Custom string-to-string values for your integration. Maximum 50 keys, 40 characters per key, 500 characters per value.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The created rule

        Examples
        --------
        from whop_sdk import Whop
        from whop_sdk.payment_rules import (
            CreatePaymentRulesRequestConditions,
            CreatePaymentRulesRequestConditionsAllItem,
        )

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_rules.create(
            action="allow",
            conditions=CreatePaymentRulesRequestConditions(
                all_=[
                    CreatePaymentRulesRequestConditionsAllItem(
                        field="risk_score",
                        operator="gte",
                        value=70,
                    )
                ],
            ),
            name="Review risky cards",
        )
        """
        _response = self._raw_client.create(
            action=action,
            conditions=conditions,
            name=name,
            account_id=account_id,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def list_fields(self, *, request_options: typing.Optional[RequestOptions] = None) -> ListFieldsPaymentRulesResponse:
        """
        Small and returned in full on one page.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListFieldsPaymentRulesResponse
            The fields a condition can read, with the operators and values each one accepts

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_rules.list_fields()
        """
        _response = self._raw_client.list_fields(request_options=request_options)
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentRule:
        """
        Parameters
        ----------
        id : str
            The payment rule ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            A partner can read a rule without being able to write one

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_rules.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        The rule stops applying to new payments and is kept, so the payments it already decided still name it.

        Parameters
        ----------
        id : str
            The payment rule ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_rules.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        name: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentRule:
        """
        Changes the rule's name or metadata, keeping its ID and everything recorded against it. What the rule *does* is fixed once created, so the payments it decided keep naming the rule that decided them; use replace to change that.

        Parameters
        ----------
        id : str
            The payment rule ID.

        metadata : typing.Optional[typing.Dict[str, str]]
            Custom string-to-string values for your integration. Maximum 50 keys, 40 characters per key, 500 characters per value.

        name : typing.Optional[str]
            A name for this rule. Up to 255 characters.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The updated rule

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_rules.update(
            id="id",
        )
        """
        _response = self._raw_client.update(id, metadata=metadata, name=name, request_options=request_options)
        return _response.data

    def activate(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentRule:
        """
        Parameters
        ----------
        id : str
            The payment rule ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The activated rule

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_rules.activate(
            id="id",
        )
        """
        _response = self._raw_client.activate(id, request_options=request_options)
        return _response.data

    def deactivate(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentRule:
        """
        The rule stops applying to new payments. It keeps its ID and can be activated again.

        Parameters
        ----------
        id : str
            The payment rule ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The deactivated rule

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_rules.deactivate(
            id="id",
        )
        """
        _response = self._raw_client.deactivate(id, request_options=request_options)
        return _response.data

    def replace(
        self,
        id: str,
        *,
        action: ReplacePaymentRulesRequestAction,
        conditions: ReplacePaymentRulesRequestConditions,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentRule:
        """
        Deletes this rule and creates its successor in one step. The successor carries a new ID and the metadata of the rule it replaced,.

        Parameters
        ----------
        id : str
            The payment rule ID.

        action : ReplacePaymentRulesRequestAction
            What this account's rule requests when every condition matches. One applicable account-rule action wins, in this order: `allow`, `block`, `review`, `enforce_3ds`. An `allow` overrides this account's other rules, never Whop's own fraud controls. A `review` requests authorization without capture for an eligible on-session card payment through Whop Payments. Automatic capture is scheduled for 48 hours after authorization; capture or void the payment before then to decide sooner. Capture may complete later or fail. Review is skipped for unsupported methods, off-session payments, and payments already configured for manual capture. An `enforce_3ds` is skipped when the account rule cannot apply a challenge. Other 3DS requirements still apply.

        conditions : ReplacePaymentRulesRequestConditions
            The conditions a payment is matched against. Up to 10 conditions, and 8 KiB once serialized.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The rule that replaced this one

        Examples
        --------
        from whop_sdk import Whop
        from whop_sdk.payment_rules import (
            ReplacePaymentRulesRequestConditions,
            ReplacePaymentRulesRequestConditionsAllItem,
        )

        client = Whop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payment_rules.replace(
            id="id",
            action="allow",
            conditions=ReplacePaymentRulesRequestConditions(
                all_=[
                    ReplacePaymentRulesRequestConditionsAllItem(
                        field="risk_score",
                        operator="gte",
                        value=70,
                    )
                ],
            ),
        )
        """
        _response = self._raw_client.replace(id, action=action, conditions=conditions, request_options=request_options)
        return _response.data


class AsyncPaymentRulesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPaymentRulesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPaymentRulesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPaymentRulesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        status: typing.Optional[ListPaymentRulesRequestStatus] = None,
        action: typing.Optional[ListPaymentRulesRequestAction] = None,
        order: typing.Optional[ListPaymentRulesRequestOrder] = None,
        direction: typing.Optional[ListPaymentRulesRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PaymentRule, ListPaymentRulesResponse]:
        """
        Parameters
        ----------
        account_id : typing.Optional[str]
            Only return rules belonging to this account. Defaults to the account the request is acting for.

        status : typing.Optional[ListPaymentRulesRequestStatus]
            Only return rules with this status.

        action : typing.Optional[ListPaymentRulesRequestAction]
            Only return rules that take this action.

        order : typing.Optional[ListPaymentRulesRequestOrder]
            The field to sort by.

        direction : typing.Optional[ListPaymentRulesRequestDirection]
            The sort direction.

        first : typing.Optional[int]
            Number of results to return from the start of the range.

        after : typing.Optional[str]
            Return results after this cursor. Use `page_info.end_cursor` from the previous response to fetch the next page.

        last : typing.Optional[int]
            Number of results to return from the end of the range.

        before : typing.Optional[str]
            Return results before this cursor. Use `page_info.start_cursor` from the previous response to fetch the previous page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PaymentRule, ListPaymentRulesResponse]
            A list of payment rules

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.payment_rules.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            status=status,
            action=action,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        action: CreatePaymentRulesRequestAction,
        conditions: CreatePaymentRulesRequestConditions,
        name: str,
        account_id: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentRule:
        """
        Parameters
        ----------
        action : CreatePaymentRulesRequestAction
            What this account's rule requests when every condition matches. One applicable account-rule action wins, in this order: `allow`, `block`, `review`, `enforce_3ds`. An `allow` overrides this account's other rules, never Whop's own fraud controls. A `review` requests authorization without capture for an eligible on-session card payment through Whop Payments. Automatic capture is scheduled for 48 hours after authorization; capture or void the payment before then to decide sooner. Capture may complete later or fail. Review is skipped for unsupported methods, off-session payments, and payments already configured for manual capture. An `enforce_3ds` is skipped when the account rule cannot apply a challenge. Other 3DS requirements still apply.

        conditions : CreatePaymentRulesRequestConditions
            The conditions a payment is matched against. Up to 10 conditions, and 8 KiB once serialized.

        name : str
            A name for this rule. Up to 255 characters.

        account_id : typing.Optional[str]
            The account to create the rule on. Defaults to the account the request is acting for.

        metadata : typing.Optional[typing.Dict[str, str]]
            Custom string-to-string values for your integration. Maximum 50 keys, 40 characters per key, 500 characters per value.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The created rule

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop
        from whop_sdk.payment_rules import (
            CreatePaymentRulesRequestConditions,
            CreatePaymentRulesRequestConditionsAllItem,
        )

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_rules.create(
                action="allow",
                conditions=CreatePaymentRulesRequestConditions(
                    all_=[
                        CreatePaymentRulesRequestConditionsAllItem(
                            field="risk_score",
                            operator="gte",
                            value=70,
                        )
                    ],
                ),
                name="Review risky cards",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            action=action,
            conditions=conditions,
            name=name,
            account_id=account_id,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def list_fields(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ListFieldsPaymentRulesResponse:
        """
        Small and returned in full on one page.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListFieldsPaymentRulesResponse
            The fields a condition can read, with the operators and values each one accepts

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_rules.list_fields()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_fields(request_options=request_options)
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentRule:
        """
        Parameters
        ----------
        id : str
            The payment rule ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            A partner can read a rule without being able to write one

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_rules.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        The rule stops applying to new payments and is kept, so the payments it already decided still name it.

        Parameters
        ----------
        id : str
            The payment rule ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_rules.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        name: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentRule:
        """
        Changes the rule's name or metadata, keeping its ID and everything recorded against it. What the rule *does* is fixed once created, so the payments it decided keep naming the rule that decided them; use replace to change that.

        Parameters
        ----------
        id : str
            The payment rule ID.

        metadata : typing.Optional[typing.Dict[str, str]]
            Custom string-to-string values for your integration. Maximum 50 keys, 40 characters per key, 500 characters per value.

        name : typing.Optional[str]
            A name for this rule. Up to 255 characters.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The updated rule

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_rules.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(id, metadata=metadata, name=name, request_options=request_options)
        return _response.data

    async def activate(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentRule:
        """
        Parameters
        ----------
        id : str
            The payment rule ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The activated rule

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_rules.activate(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.activate(id, request_options=request_options)
        return _response.data

    async def deactivate(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PaymentRule:
        """
        The rule stops applying to new payments. It keeps its ID and can be activated again.

        Parameters
        ----------
        id : str
            The payment rule ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The deactivated rule

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_rules.deactivate(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.deactivate(id, request_options=request_options)
        return _response.data

    async def replace(
        self,
        id: str,
        *,
        action: ReplacePaymentRulesRequestAction,
        conditions: ReplacePaymentRulesRequestConditions,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaymentRule:
        """
        Deletes this rule and creates its successor in one step. The successor carries a new ID and the metadata of the rule it replaced,.

        Parameters
        ----------
        id : str
            The payment rule ID.

        action : ReplacePaymentRulesRequestAction
            What this account's rule requests when every condition matches. One applicable account-rule action wins, in this order: `allow`, `block`, `review`, `enforce_3ds`. An `allow` overrides this account's other rules, never Whop's own fraud controls. A `review` requests authorization without capture for an eligible on-session card payment through Whop Payments. Automatic capture is scheduled for 48 hours after authorization; capture or void the payment before then to decide sooner. Capture may complete later or fail. Review is skipped for unsupported methods, off-session payments, and payments already configured for manual capture. An `enforce_3ds` is skipped when the account rule cannot apply a challenge. Other 3DS requirements still apply.

        conditions : ReplacePaymentRulesRequestConditions
            The conditions a payment is matched against. Up to 10 conditions, and 8 KiB once serialized.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaymentRule
            The rule that replaced this one

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop
        from whop_sdk.payment_rules import (
            ReplacePaymentRulesRequestConditions,
            ReplacePaymentRulesRequestConditionsAllItem,
        )

        client = AsyncWhop(
            "2026-09-15",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payment_rules.replace(
                id="id",
                action="allow",
                conditions=ReplacePaymentRulesRequestConditions(
                    all_=[
                        ReplacePaymentRulesRequestConditionsAllItem(
                            field="risk_score",
                            operator="gte",
                            value=70,
                        )
                    ],
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.replace(
            id, action=action, conditions=conditions, request_options=request_options
        )
        return _response.data
