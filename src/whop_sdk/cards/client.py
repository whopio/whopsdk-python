# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawCardsClient, RawCardsClient
from .types.create_cards_request_spend_limit_frequency import CreateCardsRequestSpendLimitFrequency
from .types.create_cards_response import CreateCardsResponse
from .types.list_cards_response import ListCardsResponse
from .types.retrieve_cards_response import RetrieveCardsResponse
from .types.update_cards_request_billing import UpdateCardsRequestBilling
from .types.update_cards_request_spend_limit_frequency import UpdateCardsRequestSpendLimitFrequency
from .types.update_cards_response import UpdateCardsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class CardsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCardsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCardsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCardsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListCardsResponse:
        """
        Lists the Whop cards of an account or user, including ones still being set up. Team members only see the cards assigned to them.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListCardsResponse
            cards listed for an assigned cardholder without the ledger scope

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.cards.list()
        """
        _response = self._raw_client.list(account_id=account_id, user_id=user_id, request_options=request_options)
        return _response.data

    def create(
        self,
        *,
        account_id: typing.Optional[str] = OMIT,
        assigned_user_id: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        spend_limit: typing.Optional[float] = OMIT,
        spend_limit_frequency: typing.Optional[CreateCardsRequestSpendLimitFrequency] = OMIT,
        transaction_limit: typing.Optional[float] = OMIT,
        user_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateCardsResponse:
        """
        Issue a virtual card, or apply for card issuing.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        assigned_user_id : typing.Optional[str]
            The account member (a user_ identifier) to assign the card to. Required for business card issuing accounts.

        name : typing.Optional[str]
            A display name for the card.

        spend_limit : typing.Optional[float]
            Spending limit amount, in dollars.

        spend_limit_frequency : typing.Optional[CreateCardsRequestSpendLimitFrequency]
            The window the spend limit applies to.

        transaction_limit : typing.Optional[float]
            Per-transaction limit amount, in dollars.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateCardsResponse
            card created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.cards.create()
        """
        _response = self._raw_client.create(
            account_id=account_id,
            assigned_user_id=assigned_user_id,
            name=name,
            spend_limit=spend_limit,
            spend_limit_frequency=spend_limit_frequency,
            transaction_limit=transaction_limit,
            user_id=user_id,
            request_options=request_options,
        )
        return _response.data

    def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RetrieveCardsResponse:
        """
        Retrieve a single card.

        Parameters
        ----------
        id : str
            Card ID to retrieve, prefixed `icrd_`.

        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrieveCardsResponse
            card retrieved with its PIN by the assigned cardholder

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.cards.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(
            id, account_id=account_id, user_id=user_id, request_options=request_options
        )
        return _response.data

    def update(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = OMIT,
        billing: typing.Optional[UpdateCardsRequestBilling] = OMIT,
        canceled: typing.Optional[bool] = OMIT,
        frozen: typing.Optional[bool] = OMIT,
        name: typing.Optional[str] = OMIT,
        pin: typing.Optional[str] = OMIT,
        remove_limit: typing.Optional[bool] = OMIT,
        spend_limit: typing.Optional[float] = OMIT,
        spend_limit_frequency: typing.Optional[UpdateCardsRequestSpendLimitFrequency] = OMIT,
        transaction_limit: typing.Optional[float] = OMIT,
        user_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateCardsResponse:
        """
        Update, freeze, or cancel a card. Updating the card's name, billing address, or limits requires both `payout:account:update` and `company:balance:read`; a card's assigned holder may update their own card's pin and frozen state with any user token.

        Parameters
        ----------
        id : str
            Card ID to retrieve, prefixed `icrd_`.

        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        billing : typing.Optional[UpdateCardsRequestBilling]
            New billing address. Requires line1, city, region, postal_code, and country_code. On an invited card, passing billing alone (as the invited user) completes onboarding and starts card provisioning.

        canceled : typing.Optional[bool]
            Pass `true` to permanently cancel the card. A canceled card cannot be uncanceled. Cannot be combined with other fields.

        frozen : typing.Optional[bool]
            Pass `true` to freeze the card, `false` to unfreeze it. The assigned cardholder may freeze their own card without the payout:account:update scope.

        name : typing.Optional[str]
            A display name for the card.

        pin : typing.Optional[str]
            New 4-digit PIN. Can only be set on a card assigned to the acting user, who may set it without the payout:account:update scope.

        remove_limit : typing.Optional[bool]
            Pass `true` to remove the spending limit (make the card unlimited).

        spend_limit : typing.Optional[float]
            Spending limit amount, in dollars.

        spend_limit_frequency : typing.Optional[UpdateCardsRequestSpendLimitFrequency]
            The window the spend limit applies to.

        transaction_limit : typing.Optional[float]
            Per-transaction limit amount, in dollars.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateCardsResponse
            invited card onboarding completed: card provisioning started

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.cards.update(
            id="id",
        )
        """
        _response = self._raw_client.update(
            id,
            account_id=account_id,
            billing=billing,
            canceled=canceled,
            frozen=frozen,
            name=name,
            pin=pin,
            remove_limit=remove_limit,
            spend_limit=spend_limit,
            spend_limit_frequency=spend_limit_frequency,
            transaction_limit=transaction_limit,
            user_id=user_id,
            request_options=request_options,
        )
        return _response.data


class AsyncCardsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCardsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCardsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCardsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListCardsResponse:
        """
        Lists the Whop cards of an account or user, including ones still being set up. Team members only see the cards assigned to them.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListCardsResponse
            cards listed for an assigned cardholder without the ledger scope

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cards.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(account_id=account_id, user_id=user_id, request_options=request_options)
        return _response.data

    async def create(
        self,
        *,
        account_id: typing.Optional[str] = OMIT,
        assigned_user_id: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        spend_limit: typing.Optional[float] = OMIT,
        spend_limit_frequency: typing.Optional[CreateCardsRequestSpendLimitFrequency] = OMIT,
        transaction_limit: typing.Optional[float] = OMIT,
        user_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateCardsResponse:
        """
        Issue a virtual card, or apply for card issuing.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        assigned_user_id : typing.Optional[str]
            The account member (a user_ identifier) to assign the card to. Required for business card issuing accounts.

        name : typing.Optional[str]
            A display name for the card.

        spend_limit : typing.Optional[float]
            Spending limit amount, in dollars.

        spend_limit_frequency : typing.Optional[CreateCardsRequestSpendLimitFrequency]
            The window the spend limit applies to.

        transaction_limit : typing.Optional[float]
            Per-transaction limit amount, in dollars.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateCardsResponse
            card created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cards.create()


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            account_id=account_id,
            assigned_user_id=assigned_user_id,
            name=name,
            spend_limit=spend_limit,
            spend_limit_frequency=spend_limit_frequency,
            transaction_limit=transaction_limit,
            user_id=user_id,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RetrieveCardsResponse:
        """
        Retrieve a single card.

        Parameters
        ----------
        id : str
            Card ID to retrieve, prefixed `icrd_`.

        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrieveCardsResponse
            card retrieved with its PIN by the assigned cardholder

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cards.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(
            id, account_id=account_id, user_id=user_id, request_options=request_options
        )
        return _response.data

    async def update(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = OMIT,
        billing: typing.Optional[UpdateCardsRequestBilling] = OMIT,
        canceled: typing.Optional[bool] = OMIT,
        frozen: typing.Optional[bool] = OMIT,
        name: typing.Optional[str] = OMIT,
        pin: typing.Optional[str] = OMIT,
        remove_limit: typing.Optional[bool] = OMIT,
        spend_limit: typing.Optional[float] = OMIT,
        spend_limit_frequency: typing.Optional[UpdateCardsRequestSpendLimitFrequency] = OMIT,
        transaction_limit: typing.Optional[float] = OMIT,
        user_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateCardsResponse:
        """
        Update, freeze, or cancel a card. Updating the card's name, billing address, or limits requires both `payout:account:update` and `company:balance:read`; a card's assigned holder may update their own card's pin and frozen state with any user token.

        Parameters
        ----------
        id : str
            Card ID to retrieve, prefixed `icrd_`.

        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        billing : typing.Optional[UpdateCardsRequestBilling]
            New billing address. Requires line1, city, region, postal_code, and country_code. On an invited card, passing billing alone (as the invited user) completes onboarding and starts card provisioning.

        canceled : typing.Optional[bool]
            Pass `true` to permanently cancel the card. A canceled card cannot be uncanceled. Cannot be combined with other fields.

        frozen : typing.Optional[bool]
            Pass `true` to freeze the card, `false` to unfreeze it. The assigned cardholder may freeze their own card without the payout:account:update scope.

        name : typing.Optional[str]
            A display name for the card.

        pin : typing.Optional[str]
            New 4-digit PIN. Can only be set on a card assigned to the acting user, who may set it without the payout:account:update scope.

        remove_limit : typing.Optional[bool]
            Pass `true` to remove the spending limit (make the card unlimited).

        spend_limit : typing.Optional[float]
            Spending limit amount, in dollars.

        spend_limit_frequency : typing.Optional[UpdateCardsRequestSpendLimitFrequency]
            The window the spend limit applies to.

        transaction_limit : typing.Optional[float]
            Per-transaction limit amount, in dollars.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateCardsResponse
            invited card onboarding completed: card provisioning started

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.cards.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            account_id=account_id,
            billing=billing,
            canceled=canceled,
            frozen=frozen,
            name=name,
            pin=pin,
            remove_limit=remove_limit,
            spend_limit=spend_limit,
            spend_limit_frequency=spend_limit_frequency,
            transaction_limit=transaction_limit,
            user_id=user_id,
            request_options=request_options,
        )
        return _response.data
