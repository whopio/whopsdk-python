# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.currencies import Currencies
from ..types.direction import Direction
from ..types.withdrawal import Withdrawal
from ..types.withdrawal_list_item import WithdrawalListItem
from ..types.withdrawal_speeds import WithdrawalSpeeds
from .raw_client import AsyncRawWithdrawalsClient, RawWithdrawalsClient
from .types.generate_pdf_withdrawals_response import GeneratePdfWithdrawalsResponse
from .types.list_withdrawals_response import ListWithdrawalsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class WithdrawalsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawWithdrawalsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawWithdrawalsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawWithdrawalsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        company_id: str,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        direction: typing.Optional[Direction] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[WithdrawalListItem, ListWithdrawalsResponse]:
        """
        Returns a paginated list of withdrawals for a company, with optional sorting and date filtering.

        Required permissions:
         - `payout:withdrawal:read`

        Parameters
        ----------
        company_id : str
            The unique identifier of the company to list withdrawals for.

        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        direction : typing.Optional[Direction]

        created_before : typing.Optional[dt.datetime]
            Only return withdrawals created before this timestamp.

        created_after : typing.Optional[dt.datetime]
            Only return withdrawals created after this timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[WithdrawalListItem, ListWithdrawalsResponse]
            A successful response

        Examples
        --------
        import datetime

        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.withdrawals.list(
            first=42,
            last=42,
            company_id="biz_xxxxxxxxxxxxxx",
            created_before=datetime.datetime.fromisoformat(
                "2023-12-01 05:00:00+00:00",
            ),
            created_after=datetime.datetime.fromisoformat(
                "2023-12-01 05:00:00+00:00",
            ),
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            company_id=company_id,
            after=after,
            before=before,
            first=first,
            last=last,
            direction=direction,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )

    def create(
        self,
        *,
        amount: float,
        company_id: str,
        currency: Currencies,
        acknowledge_bank_warning: typing.Optional[bool] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        payout_method_id: typing.Optional[str] = OMIT,
        platform_covers_fees: typing.Optional[bool] = OMIT,
        speed: typing.Optional[WithdrawalSpeeds] = OMIT,
        statement_descriptor: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Withdrawal:
        """
        Creates a withdrawal request for a ledger account

        Required permissions:
         - `payout:withdraw_funds`
         - `payout:destination:read`

        Parameters
        ----------
        amount : float
            The amount to withdraw in the specified currency

        company_id : str
            The ID of the company to withdraw from.

        currency : Currencies
            The currency that is being withdrawn.

        acknowledge_bank_warning : typing.Optional[bool]
            Set to true to continue when the bank could not confirm the account holder's name, or false to be refused in that case so the creator can fix the account or link their bank first. Omitting the argument skips the warning gate — a client that cannot show the warning keeps its pre-gate behavior.

        idempotency_key : typing.Optional[str]
            A client-generated key that makes retries safe. Retrying with the same key returns the original withdrawal instead of creating a second one.

        payout_method_id : typing.Optional[str]
            The ID of the payout method to use for the withdrawal.

        platform_covers_fees : typing.Optional[bool]
            Whether the platform covers the payout fees.

        speed : typing.Optional[WithdrawalSpeeds]
            The processing speed for the withdrawal. Either standard or instant.

        statement_descriptor : typing.Optional[str]
            Custom statement descriptor for the withdrawal. Must be between 5 and 22 characters and contain only alphanumeric characters.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Withdrawal
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.withdrawals.create(
            amount=6.9,
            company_id="biz_xxxxxxxxxxxxxx",
            currency="usd",
        )
        """
        _response = self._raw_client.create(
            amount=amount,
            company_id=company_id,
            currency=currency,
            acknowledge_bank_warning=acknowledge_bank_warning,
            idempotency_key=idempotency_key,
            payout_method_id=payout_method_id,
            platform_covers_fees=platform_covers_fees,
            speed=speed,
            statement_descriptor=statement_descriptor,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Withdrawal:
        """
        Retrieves the details of an existing withdrawal.

        Required permissions:
         - `payout:withdrawal:read`
         - `payout:destination:read`

        Parameters
        ----------
        id : str
            The unique identifier of the withdrawal to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Withdrawal
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.withdrawals.retrieve(
            id="wdrl_xxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def generate_pdf(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GeneratePdfWithdrawalsResponse:
        """
        Generates a withdrawal PDF invoice and returns a temporary download URL.

        Required permissions:
         - `payout:withdrawal:read`

        Parameters
        ----------
        id : str
            The unique identifier of the withdrawal to generate a PDF for.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeneratePdfWithdrawalsResponse
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.withdrawals.generate_pdf(
            id="wdrl_xxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.generate_pdf(id, request_options=request_options)
        return _response.data


class AsyncWithdrawalsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawWithdrawalsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawWithdrawalsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawWithdrawalsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        company_id: str,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        direction: typing.Optional[Direction] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[WithdrawalListItem, ListWithdrawalsResponse]:
        """
        Returns a paginated list of withdrawals for a company, with optional sorting and date filtering.

        Required permissions:
         - `payout:withdrawal:read`

        Parameters
        ----------
        company_id : str
            The unique identifier of the company to list withdrawals for.

        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        direction : typing.Optional[Direction]

        created_before : typing.Optional[dt.datetime]
            Only return withdrawals created before this timestamp.

        created_after : typing.Optional[dt.datetime]
            Only return withdrawals created after this timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[WithdrawalListItem, ListWithdrawalsResponse]
            A successful response

        Examples
        --------
        import asyncio
        import datetime

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.withdrawals.list(
                first=42,
                last=42,
                company_id="biz_xxxxxxxxxxxxxx",
                created_before=datetime.datetime.fromisoformat(
                    "2023-12-01 05:00:00+00:00",
                ),
                created_after=datetime.datetime.fromisoformat(
                    "2023-12-01 05:00:00+00:00",
                ),
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            company_id=company_id,
            after=after,
            before=before,
            first=first,
            last=last,
            direction=direction,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        amount: float,
        company_id: str,
        currency: Currencies,
        acknowledge_bank_warning: typing.Optional[bool] = OMIT,
        idempotency_key: typing.Optional[str] = OMIT,
        payout_method_id: typing.Optional[str] = OMIT,
        platform_covers_fees: typing.Optional[bool] = OMIT,
        speed: typing.Optional[WithdrawalSpeeds] = OMIT,
        statement_descriptor: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Withdrawal:
        """
        Creates a withdrawal request for a ledger account

        Required permissions:
         - `payout:withdraw_funds`
         - `payout:destination:read`

        Parameters
        ----------
        amount : float
            The amount to withdraw in the specified currency

        company_id : str
            The ID of the company to withdraw from.

        currency : Currencies
            The currency that is being withdrawn.

        acknowledge_bank_warning : typing.Optional[bool]
            Set to true to continue when the bank could not confirm the account holder's name, or false to be refused in that case so the creator can fix the account or link their bank first. Omitting the argument skips the warning gate — a client that cannot show the warning keeps its pre-gate behavior.

        idempotency_key : typing.Optional[str]
            A client-generated key that makes retries safe. Retrying with the same key returns the original withdrawal instead of creating a second one.

        payout_method_id : typing.Optional[str]
            The ID of the payout method to use for the withdrawal.

        platform_covers_fees : typing.Optional[bool]
            Whether the platform covers the payout fees.

        speed : typing.Optional[WithdrawalSpeeds]
            The processing speed for the withdrawal. Either standard or instant.

        statement_descriptor : typing.Optional[str]
            Custom statement descriptor for the withdrawal. Must be between 5 and 22 characters and contain only alphanumeric characters.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Withdrawal
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.withdrawals.create(
                amount=6.9,
                company_id="biz_xxxxxxxxxxxxxx",
                currency="usd",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            amount=amount,
            company_id=company_id,
            currency=currency,
            acknowledge_bank_warning=acknowledge_bank_warning,
            idempotency_key=idempotency_key,
            payout_method_id=payout_method_id,
            platform_covers_fees=platform_covers_fees,
            speed=speed,
            statement_descriptor=statement_descriptor,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Withdrawal:
        """
        Retrieves the details of an existing withdrawal.

        Required permissions:
         - `payout:withdrawal:read`
         - `payout:destination:read`

        Parameters
        ----------
        id : str
            The unique identifier of the withdrawal to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Withdrawal
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.withdrawals.retrieve(
                id="wdrl_xxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def generate_pdf(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> GeneratePdfWithdrawalsResponse:
        """
        Generates a withdrawal PDF invoice and returns a temporary download URL.

        Required permissions:
         - `payout:withdrawal:read`

        Parameters
        ----------
        id : str
            The unique identifier of the withdrawal to generate a PDF for.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GeneratePdfWithdrawalsResponse
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.withdrawals.generate_pdf(
                id="wdrl_xxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.generate_pdf(id, request_options=request_options)
        return _response.data
