# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.account_link import AccountLink
from ..types.account_link_use_cases import AccountLinkUseCases
from .raw_client import AsyncRawAccountLinksClient, RawAccountLinksClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AccountLinksClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAccountLinksClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAccountLinksClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAccountLinksClient
        """
        return self._raw_client

    def create(
        self,
        *,
        company_id: str,
        refresh_url: str,
        return_url: str,
        use_case: AccountLinkUseCases,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AccountLink:
        """
        Generate a URL that directs a sub-merchant to their account portal, such as the hosted payouts dashboard or the KYC onboarding flow.

        Parameters
        ----------
        company_id : str
            The unique identifier of the company to generate the link for, starting with 'biz_'. Must be a sub-merchant of the API key's company.

        refresh_url : str
            The URL to redirect the user to if the session expires and needs to be re-authenticated, such as 'https://example.com/refresh'.

        return_url : str
            The URL to redirect the user to when they want to return to your site, such as 'https://example.com/return'.

        use_case : AccountLinkUseCases
            The purpose of the account link, such as hosted payouts portal or hosted KYC onboarding.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AccountLink
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.account_links.create(
            company_id="biz_xxxxxxxxxxxxxx",
            refresh_url="refresh_url",
            return_url="return_url",
            use_case="account_onboarding",
        )
        """
        _response = self._raw_client.create(
            company_id=company_id,
            refresh_url=refresh_url,
            return_url=return_url,
            use_case=use_case,
            request_options=request_options,
        )
        return _response.data


class AsyncAccountLinksClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAccountLinksClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAccountLinksClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAccountLinksClient
        """
        return self._raw_client

    async def create(
        self,
        *,
        company_id: str,
        refresh_url: str,
        return_url: str,
        use_case: AccountLinkUseCases,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AccountLink:
        """
        Generate a URL that directs a sub-merchant to their account portal, such as the hosted payouts dashboard or the KYC onboarding flow.

        Parameters
        ----------
        company_id : str
            The unique identifier of the company to generate the link for, starting with 'biz_'. Must be a sub-merchant of the API key's company.

        refresh_url : str
            The URL to redirect the user to if the session expires and needs to be re-authenticated, such as 'https://example.com/refresh'.

        return_url : str
            The URL to redirect the user to when they want to return to your site, such as 'https://example.com/return'.

        use_case : AccountLinkUseCases
            The purpose of the account link, such as hosted payouts portal or hosted KYC onboarding.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AccountLink
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.account_links.create(
                company_id="biz_xxxxxxxxxxxxxx",
                refresh_url="refresh_url",
                return_url="return_url",
                use_case="account_onboarding",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            company_id=company_id,
            refresh_url=refresh_url,
            return_url=return_url,
            use_case=use_case,
            request_options=request_options,
        )
        return _response.data
