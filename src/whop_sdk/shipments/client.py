# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.shipment import Shipment
from .raw_client import AsyncRawShipmentsClient, RawShipmentsClient
from .types.list_shipments_request_direction import ListShipmentsRequestDirection
from .types.list_shipments_request_order import ListShipmentsRequestOrder
from .types.list_shipments_request_status import ListShipmentsRequestStatus
from .types.list_shipments_response import ListShipmentsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ShipmentsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawShipmentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawShipmentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawShipmentsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        status: typing.Optional[ListShipmentsRequestStatus] = None,
        payment_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        order: typing.Optional[ListShipmentsRequestOrder] = None,
        direction: typing.Optional[ListShipmentsRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Shipment, ListShipmentsResponse]:
        """
        Returns a paginated list of shipments for an account.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The account to list shipments for. Defaults to the acting account.

        status : typing.Optional[ListShipmentsRequestStatus]
            Filter to shipments with this delivery status.

        payment_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only shipments fulfilling these payments, each prefixed `pay_`. Repeat the parameter to pass several, up to 100 per request — one paginated list covers all of them.

        created_before : typing.Optional[str]
            Return shipments created before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Return shipments created after this ISO 8601 timestamp.

        order : typing.Optional[ListShipmentsRequestOrder]
            The field to sort by.

        direction : typing.Optional[ListShipmentsRequestDirection]
            The sort direction.

        first : typing.Optional[int]
            The number of shipments to return.

        after : typing.Optional[str]
            A cursor; returns shipments after this position.

        last : typing.Optional[int]
            The number of shipments to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns shipments before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Shipment, ListShipmentsResponse]
            excludes the created_after boundary row

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.shipments.list(
            payment_id=["pay_xxxxxxxxxxxxxx"],
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            status=status,
            payment_id=payment_id,
            created_before=created_before,
            created_after=created_after,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        payment_id: str,
        tracking_number: str,
        account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Shipment:
        """
        Attaches a carrier tracking number to a payment and begins tracking it.

        Parameters
        ----------
        payment_id : str
            The payment to attach the shipment to, prefixed `pay_`.

        tracking_number : str
            The carrier-assigned tracking number.

        account_id : typing.Optional[str]
            The unique identifier of the account, prefixed `biz_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Shipment
            shipment created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.shipments.create(
            payment_id="pay_xxxxxxxxxxxxxx",
            tracking_number="1Z999AA10123456784",
        )
        """
        _response = self._raw_client.create(
            payment_id=payment_id,
            tracking_number=tracking_number,
            account_id=account_id,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Shipment:
        """
        Retrieves a shipment by its id, or by the payment id it fulfills.

        Parameters
        ----------
        id : str
            The shipment id (`ship_`), or the payment id (`pay_`) it fulfills.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Shipment
            shipment retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.shipments.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def update(
        self, id: str, *, tracking_number: str, request_options: typing.Optional[RequestOptions] = None
    ) -> Shipment:
        """
        Updates a shipment's tracking number and re-tracks it with the carrier.

        Parameters
        ----------
        id : str
            The shipment id (`ship_`), or the payment id (`pay_`) it fulfills.

        tracking_number : str
            The new carrier-assigned tracking number.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Shipment
            shipment updated

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.shipments.update(
            id="id",
            tracking_number="9400111899223456789012",
        )
        """
        _response = self._raw_client.update(id, tracking_number=tracking_number, request_options=request_options)
        return _response.data


class AsyncShipmentsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawShipmentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawShipmentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawShipmentsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        status: typing.Optional[ListShipmentsRequestStatus] = None,
        payment_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        order: typing.Optional[ListShipmentsRequestOrder] = None,
        direction: typing.Optional[ListShipmentsRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Shipment, ListShipmentsResponse]:
        """
        Returns a paginated list of shipments for an account.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The account to list shipments for. Defaults to the acting account.

        status : typing.Optional[ListShipmentsRequestStatus]
            Filter to shipments with this delivery status.

        payment_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only shipments fulfilling these payments, each prefixed `pay_`. Repeat the parameter to pass several, up to 100 per request — one paginated list covers all of them.

        created_before : typing.Optional[str]
            Return shipments created before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Return shipments created after this ISO 8601 timestamp.

        order : typing.Optional[ListShipmentsRequestOrder]
            The field to sort by.

        direction : typing.Optional[ListShipmentsRequestDirection]
            The sort direction.

        first : typing.Optional[int]
            The number of shipments to return.

        after : typing.Optional[str]
            A cursor; returns shipments after this position.

        last : typing.Optional[int]
            The number of shipments to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns shipments before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Shipment, ListShipmentsResponse]
            excludes the created_after boundary row

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.shipments.list(
                payment_id=["pay_xxxxxxxxxxxxxx"],
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            status=status,
            payment_id=payment_id,
            created_before=created_before,
            created_after=created_after,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        payment_id: str,
        tracking_number: str,
        account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Shipment:
        """
        Attaches a carrier tracking number to a payment and begins tracking it.

        Parameters
        ----------
        payment_id : str
            The payment to attach the shipment to, prefixed `pay_`.

        tracking_number : str
            The carrier-assigned tracking number.

        account_id : typing.Optional[str]
            The unique identifier of the account, prefixed `biz_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Shipment
            shipment created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.shipments.create(
                payment_id="pay_xxxxxxxxxxxxxx",
                tracking_number="1Z999AA10123456784",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            payment_id=payment_id,
            tracking_number=tracking_number,
            account_id=account_id,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Shipment:
        """
        Retrieves a shipment by its id, or by the payment id it fulfills.

        Parameters
        ----------
        id : str
            The shipment id (`ship_`), or the payment id (`pay_`) it fulfills.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Shipment
            shipment retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.shipments.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def update(
        self, id: str, *, tracking_number: str, request_options: typing.Optional[RequestOptions] = None
    ) -> Shipment:
        """
        Updates a shipment's tracking number and re-tracks it with the carrier.

        Parameters
        ----------
        id : str
            The shipment id (`ship_`), or the payment id (`pay_`) it fulfills.

        tracking_number : str
            The new carrier-assigned tracking number.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Shipment
            shipment updated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.shipments.update(
                id="id",
                tracking_number="9400111899223456789012",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(id, tracking_number=tracking_number, request_options=request_options)
        return _response.data
