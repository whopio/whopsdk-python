# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.pagination import AsyncPager, SyncPager
from ...core.request_options import RequestOptions
from .raw_client import AsyncRawSupportedMethodsClient, RawSupportedMethodsClient
from .types.list_supported_methods_response import ListSupportedMethodsResponse
from .types.list_supported_methods_response_data_item import ListSupportedMethodsResponseDataItem


class SupportedMethodsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawSupportedMethodsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawSupportedMethodsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSupportedMethodsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        country: typing.Optional[str] = None,
        amount: typing.Optional[float] = None,
        currency: typing.Optional[str] = None,
        supported_payout_method_id: typing.Optional[str] = None,
        destination_currency: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ListSupportedMethodsResponseDataItem, ListSupportedMethodsResponse]:
        """
        Lists the payout methods an account or user is eligible to add.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        country : typing.Optional[str]
            ISO 3166-1 alpha-2 country code for the bank account or wallet, such as `US`. Defaults to the country of supported_payout_method_id when one is given, otherwise the payout account's country.

        amount : typing.Optional[float]
            Optional payout amount in whole currency units, for example `250.00`. When provided, each destination includes per-currency fee and delivery quotes.

        currency : typing.Optional[str]
            Currency code of the amount, for example `usd`. Only meaningful with amount.

        supported_payout_method_id : typing.Optional[str]
            Narrows the list to one supported payout method (a podst_ identifier) and includes the required_fields needed to save it as a payout method.

        destination_currency : typing.Optional[str]
            Currency the supported payout method would deliver payouts in. Only meaningful with supported_payout_method_id; required fields vary by destination currency.

        first : typing.Optional[int]
            Number of supported payout methods to return from the start of the window.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            Number of supported payout methods to return from the end of the window.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ListSupportedMethodsResponseDataItem, ListSupportedMethodsResponse]
            supported payout methods listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.payouts.supported_methods.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            user_id=user_id,
            country=country,
            amount=amount,
            currency=currency,
            supported_payout_method_id=supported_payout_method_id,
            destination_currency=destination_currency,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )


class AsyncSupportedMethodsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawSupportedMethodsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSupportedMethodsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSupportedMethodsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        country: typing.Optional[str] = None,
        amount: typing.Optional[float] = None,
        currency: typing.Optional[str] = None,
        supported_payout_method_id: typing.Optional[str] = None,
        destination_currency: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ListSupportedMethodsResponseDataItem, ListSupportedMethodsResponse]:
        """
        Lists the payout methods an account or user is eligible to add.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        country : typing.Optional[str]
            ISO 3166-1 alpha-2 country code for the bank account or wallet, such as `US`. Defaults to the country of supported_payout_method_id when one is given, otherwise the payout account's country.

        amount : typing.Optional[float]
            Optional payout amount in whole currency units, for example `250.00`. When provided, each destination includes per-currency fee and delivery quotes.

        currency : typing.Optional[str]
            Currency code of the amount, for example `usd`. Only meaningful with amount.

        supported_payout_method_id : typing.Optional[str]
            Narrows the list to one supported payout method (a podst_ identifier) and includes the required_fields needed to save it as a payout method.

        destination_currency : typing.Optional[str]
            Currency the supported payout method would deliver payouts in. Only meaningful with supported_payout_method_id; required fields vary by destination currency.

        first : typing.Optional[int]
            Number of supported payout methods to return from the start of the window.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            Number of supported payout methods to return from the end of the window.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ListSupportedMethodsResponseDataItem, ListSupportedMethodsResponse]
            supported payout methods listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.payouts.supported_methods.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            user_id=user_id,
            country=country,
            amount=amount,
            currency=currency,
            supported_payout_method_id=supported_payout_method_id,
            destination_currency=destination_currency,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )
