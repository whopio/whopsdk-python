# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawPayoutsClient, RawPayoutsClient
from .types.create_payouts_request_body import CreatePayoutsRequestBody
from .types.create_payouts_response import CreatePayoutsResponse
from .types.list_payouts_request_source import ListPayoutsRequestSource
from .types.list_payouts_request_status import ListPayoutsRequestStatus
from .types.list_payouts_response import ListPayoutsResponse
from .types.list_payouts_response_data_item import ListPayoutsResponseDataItem
from .types.retrieve_payouts_response import RetrievePayoutsResponse

if typing.TYPE_CHECKING:
    from .methods.client import AsyncMethodsClient, MethodsClient
    from .supported_methods.client import AsyncSupportedMethodsClient, SupportedMethodsClient
# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PayoutsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPayoutsClient(client_wrapper=client_wrapper)
        self._client_wrapper = client_wrapper
        self._methods: typing.Optional[MethodsClient] = None
        self._supported_methods: typing.Optional[SupportedMethodsClient] = None

    @property
    def with_raw_response(self) -> RawPayoutsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPayoutsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        currency: typing.Optional[str] = None,
        status: typing.Optional[ListPayoutsRequestStatus] = None,
        source: typing.Optional[ListPayoutsRequestSource] = None,
        payout_method_id: typing.Optional[str] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ListPayoutsResponseDataItem, ListPayoutsResponse]:
        """
        Lists an account's or user's payouts, newest first.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        currency : typing.Optional[str]
            Optional currency code filter, for example `usd`.

        status : typing.Optional[ListPayoutsRequestStatus]
            Filter to payouts whose `status` reads this word, matching exactly what this version displays — `reversed` finds settled payouts the bank later returned. Requires Api-Version-Date 2026-08-21 or later.

        source : typing.Optional[ListPayoutsRequestSource]
            Filter by how the payout was created. Payouts created before source tracking or through internal tooling carry no source and never match.

        payout_method_id : typing.Optional[str]
            Filter to payouts sent to one saved payout method (a pytk_ identifier). An unknown id matches nothing.

        created_before : typing.Optional[dt.datetime]
            Only payouts created before this ISO 8601 time (exclusive).

        created_after : typing.Optional[dt.datetime]
            Only payouts created at or after this ISO 8601 time (inclusive).

        first : typing.Optional[int]
            Number of payouts to return from the start of the window.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            Number of payouts to return from the end of the window.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ListPayoutsResponseDataItem, ListPayoutsResponse]
            payouts filtered by source, payout method, and created window

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.payouts.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            user_id=user_id,
            currency=currency,
            status=status,
            source=source,
            payout_method_id=payout_method_id,
            created_before=created_before,
            created_after=created_after,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self, *, request: CreatePayoutsRequestBody, request_options: typing.Optional[RequestOptions] = None
    ) -> CreatePayoutsResponse:
        """
        Sends money from an account or user balance to a saved payout method for that owner.

        Parameters
        ----------
        request : CreatePayoutsRequestBody

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreatePayoutsResponse
            payout created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payouts.create(
            request={"key": "value"},
        )
        """
        _response = self._raw_client.create(request=request, request_options=request_options)
        return _response.data

    def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RetrievePayoutsResponse:
        """
        Fetches one payout by its `wdrl_` ID, or by the `cofr_` conversion request ID a stablecoin payout carries as `payout_request_id` — both ids answer with the same payout object.

        Parameters
        ----------
        id : str
            Payout ID, prefixed `wdrl_` for a payout returned by `GET /payouts` or `cofr_` for the payout request returned by `POST /payouts`.

        account_id : typing.Optional[str]
            Owning account ID, prefixed `biz_`. Provide exactly one of `account_id` or `user_id`.

        user_id : typing.Optional[str]
            Owning user ID, prefixed `user_`. Provide exactly one of `account_id` or `user_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrievePayoutsResponse
            payout found for a user

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.payouts.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(
            id, account_id=account_id, user_id=user_id, request_options=request_options
        )
        return _response.data

    @property
    def methods(self):
        if self._methods is None:
            from .methods.client import MethodsClient  # noqa: E402

            self._methods = MethodsClient(client_wrapper=self._client_wrapper)
        return self._methods

    @property
    def supported_methods(self):
        if self._supported_methods is None:
            from .supported_methods.client import SupportedMethodsClient  # noqa: E402

            self._supported_methods = SupportedMethodsClient(client_wrapper=self._client_wrapper)
        return self._supported_methods


class AsyncPayoutsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPayoutsClient(client_wrapper=client_wrapper)
        self._client_wrapper = client_wrapper
        self._methods: typing.Optional[AsyncMethodsClient] = None
        self._supported_methods: typing.Optional[AsyncSupportedMethodsClient] = None

    @property
    def with_raw_response(self) -> AsyncRawPayoutsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPayoutsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        currency: typing.Optional[str] = None,
        status: typing.Optional[ListPayoutsRequestStatus] = None,
        source: typing.Optional[ListPayoutsRequestSource] = None,
        payout_method_id: typing.Optional[str] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ListPayoutsResponseDataItem, ListPayoutsResponse]:
        """
        Lists an account's or user's payouts, newest first.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        currency : typing.Optional[str]
            Optional currency code filter, for example `usd`.

        status : typing.Optional[ListPayoutsRequestStatus]
            Filter to payouts whose `status` reads this word, matching exactly what this version displays — `reversed` finds settled payouts the bank later returned. Requires Api-Version-Date 2026-08-21 or later.

        source : typing.Optional[ListPayoutsRequestSource]
            Filter by how the payout was created. Payouts created before source tracking or through internal tooling carry no source and never match.

        payout_method_id : typing.Optional[str]
            Filter to payouts sent to one saved payout method (a pytk_ identifier). An unknown id matches nothing.

        created_before : typing.Optional[dt.datetime]
            Only payouts created before this ISO 8601 time (exclusive).

        created_after : typing.Optional[dt.datetime]
            Only payouts created at or after this ISO 8601 time (inclusive).

        first : typing.Optional[int]
            Number of payouts to return from the start of the window.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            Number of payouts to return from the end of the window.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ListPayoutsResponseDataItem, ListPayoutsResponse]
            payouts filtered by source, payout method, and created window

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.payouts.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            user_id=user_id,
            currency=currency,
            status=status,
            source=source,
            payout_method_id=payout_method_id,
            created_before=created_before,
            created_after=created_after,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self, *, request: CreatePayoutsRequestBody, request_options: typing.Optional[RequestOptions] = None
    ) -> CreatePayoutsResponse:
        """
        Sends money from an account or user balance to a saved payout method for that owner.

        Parameters
        ----------
        request : CreatePayoutsRequestBody

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreatePayoutsResponse
            payout created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payouts.create(
                request={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(request=request, request_options=request_options)
        return _response.data

    async def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RetrievePayoutsResponse:
        """
        Fetches one payout by its `wdrl_` ID, or by the `cofr_` conversion request ID a stablecoin payout carries as `payout_request_id` — both ids answer with the same payout object.

        Parameters
        ----------
        id : str
            Payout ID, prefixed `wdrl_` for a payout returned by `GET /payouts` or `cofr_` for the payout request returned by `POST /payouts`.

        account_id : typing.Optional[str]
            Owning account ID, prefixed `biz_`. Provide exactly one of `account_id` or `user_id`.

        user_id : typing.Optional[str]
            Owning user ID, prefixed `user_`. Provide exactly one of `account_id` or `user_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrievePayoutsResponse
            payout found for a user

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.payouts.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(
            id, account_id=account_id, user_id=user_id, request_options=request_options
        )
        return _response.data

    @property
    def methods(self):
        if self._methods is None:
            from .methods.client import AsyncMethodsClient  # noqa: E402

            self._methods = AsyncMethodsClient(client_wrapper=self._client_wrapper)
        return self._methods

    @property
    def supported_methods(self):
        if self._supported_methods is None:
            from .supported_methods.client import AsyncSupportedMethodsClient  # noqa: E402

            self._supported_methods = AsyncSupportedMethodsClient(client_wrapper=self._client_wrapper)
        return self._supported_methods
