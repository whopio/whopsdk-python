# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.card_transaction import CardTransaction
from .raw_client import AsyncRawCardTransactionsClient, RawCardTransactionsClient
from .types.list_card_transactions_request_direction import ListCardTransactionsRequestDirection
from .types.list_card_transactions_request_order import ListCardTransactionsRequestOrder
from .types.list_card_transactions_request_status import ListCardTransactionsRequestStatus
from .types.list_card_transactions_response import ListCardTransactionsResponse


class CardTransactionsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCardTransactionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCardTransactionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCardTransactionsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        transaction_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        card_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        cardholder_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        status: typing.Optional[ListCardTransactionsRequestStatus] = None,
        created_after: typing.Optional[str] = None,
        created_before: typing.Optional[str] = None,
        order: typing.Optional[ListCardTransactionsRequestOrder] = None,
        direction: typing.Optional[ListCardTransactionsRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[CardTransaction, ListCardTransactionsResponse]:
        """
        Lists an account's card transactions, newest first. Defaults to the account the credential belongs to. Covers every card the owner has ever had, including canceled cards and spend that predates a re-application, and team members only see transactions on the cards assigned to them. Pass `transaction_ids` to fetch specific transactions instead of paging for them.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The account whose card transactions to list, prefixed `biz_`. Defaults to the credential's account.

        transaction_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return only these card transactions, each prefixed `citx_`. Repeat the parameter, or pass one comma-separated value.

        card_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return only transactions charged to these cards, each prefixed `icrd_`.

        cardholder_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return only transactions on cards assigned to these users, each prefixed `user_`.

        status : typing.Optional[ListCardTransactionsRequestStatus]
            Return only transactions with this status.

        created_after : typing.Optional[str]
            Return only transactions authorized at or after this ISO 8601 timestamp.

        created_before : typing.Optional[str]
            Return only transactions authorized at or before this ISO 8601 timestamp.

        order : typing.Optional[ListCardTransactionsRequestOrder]
            The field to sort by. Defaults to `created_at`.

        direction : typing.Optional[ListCardTransactionsRequestDirection]
            The sort direction. Defaults to `desc`.

        first : typing.Optional[int]
            The number of card transactions to return.

        after : typing.Optional[str]
            A cursor; returns card transactions after this position.

        last : typing.Optional[int]
            The number of card transactions to return, counting back from the end.

        before : typing.Optional[str]
            A cursor; returns card transactions before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[CardTransaction, ListCardTransactionsResponse]
            no owner passed falls back to the credential's account

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.card_transactions.list(
            transaction_ids=["citx_xxxxxxxxxxxxxx"],
            card_id=["icrd_xxxxxxxxxxxxxx"],
            cardholder_id=["user_xxxxxxxxxxxxxx"],
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            transaction_ids=transaction_ids,
            card_id=card_id,
            cardholder_id=cardholder_id,
            status=status,
            created_after=created_after,
            created_before=created_before,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CardTransaction:
        """
        Fetches a single card transaction by its `citx_` identifier. The owner defaults to the account the credential belongs to.

        Parameters
        ----------
        id : str
            The card transaction ID, prefixed `citx_`.

        account_id : typing.Optional[str]
            The account that owns the transaction, prefixed `biz_`. Defaults to the credential's account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CardTransaction
            card transaction found

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.card_transactions.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, account_id=account_id, request_options=request_options)
        return _response.data


class AsyncCardTransactionsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCardTransactionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCardTransactionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCardTransactionsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        transaction_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        card_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        cardholder_id: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        status: typing.Optional[ListCardTransactionsRequestStatus] = None,
        created_after: typing.Optional[str] = None,
        created_before: typing.Optional[str] = None,
        order: typing.Optional[ListCardTransactionsRequestOrder] = None,
        direction: typing.Optional[ListCardTransactionsRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[CardTransaction, ListCardTransactionsResponse]:
        """
        Lists an account's card transactions, newest first. Defaults to the account the credential belongs to. Covers every card the owner has ever had, including canceled cards and spend that predates a re-application, and team members only see transactions on the cards assigned to them. Pass `transaction_ids` to fetch specific transactions instead of paging for them.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The account whose card transactions to list, prefixed `biz_`. Defaults to the credential's account.

        transaction_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return only these card transactions, each prefixed `citx_`. Repeat the parameter, or pass one comma-separated value.

        card_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return only transactions charged to these cards, each prefixed `icrd_`.

        cardholder_id : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Return only transactions on cards assigned to these users, each prefixed `user_`.

        status : typing.Optional[ListCardTransactionsRequestStatus]
            Return only transactions with this status.

        created_after : typing.Optional[str]
            Return only transactions authorized at or after this ISO 8601 timestamp.

        created_before : typing.Optional[str]
            Return only transactions authorized at or before this ISO 8601 timestamp.

        order : typing.Optional[ListCardTransactionsRequestOrder]
            The field to sort by. Defaults to `created_at`.

        direction : typing.Optional[ListCardTransactionsRequestDirection]
            The sort direction. Defaults to `desc`.

        first : typing.Optional[int]
            The number of card transactions to return.

        after : typing.Optional[str]
            A cursor; returns card transactions after this position.

        last : typing.Optional[int]
            The number of card transactions to return, counting back from the end.

        before : typing.Optional[str]
            A cursor; returns card transactions before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[CardTransaction, ListCardTransactionsResponse]
            no owner passed falls back to the credential's account

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.card_transactions.list(
                transaction_ids=["citx_xxxxxxxxxxxxxx"],
                card_id=["icrd_xxxxxxxxxxxxxx"],
                cardholder_id=["user_xxxxxxxxxxxxxx"],
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            transaction_ids=transaction_ids,
            card_id=card_id,
            cardholder_id=cardholder_id,
            status=status,
            created_after=created_after,
            created_before=created_before,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def retrieve(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CardTransaction:
        """
        Fetches a single card transaction by its `citx_` identifier. The owner defaults to the account the credential belongs to.

        Parameters
        ----------
        id : str
            The card transaction ID, prefixed `citx_`.

        account_id : typing.Optional[str]
            The account that owns the transaction, prefixed `biz_`. Defaults to the credential's account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CardTransaction
            card transaction found

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.card_transactions.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, account_id=account_id, request_options=request_options)
        return _response.data
