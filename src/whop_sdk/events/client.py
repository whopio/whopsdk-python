# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.pixel_validation import PixelValidation
from .raw_client import AsyncRawEventsClient, RawEventsClient
from .types.create_events_request_action_source import CreateEventsRequestActionSource
from .types.create_events_request_context import CreateEventsRequestContext
from .types.create_events_request_currency import CreateEventsRequestCurrency
from .types.create_events_request_user import CreateEventsRequestUser
from .types.create_events_response import CreateEventsResponse
from .types.list_events_request_attribution_model import ListEventsRequestAttributionModel
from .types.list_events_request_direction import ListEventsRequestDirection
from .types.list_events_response import ListEventsResponse
from .types.list_events_response_data_item import ListEventsResponseDataItem
from .types.pulse_events_response import PulseEventsResponse
from .types.pulse_events_response_data_item import PulseEventsResponseDataItem

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class EventsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawEventsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawEventsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawEventsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        identifier: typing.Optional[str] = None,
        account_id: typing.Optional[str] = None,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        direction: typing.Optional[ListEventsRequestDirection] = None,
        event: typing.Optional[str] = None,
        source: typing.Optional[str] = None,
        attribution_model: typing.Optional[ListEventsRequestAttributionModel] = None,
        country: typing.Optional[str] = None,
        city: typing.Optional[str] = None,
        device: typing.Optional[str] = None,
        browser: typing.Optional[str] = None,
        os: typing.Optional[str] = None,
        utm_source: typing.Optional[str] = None,
        hostname: typing.Optional[str] = None,
        page: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ListEventsResponseDataItem, ListEventsResponse]:
        """
        Lists identity-linked events, most recent first by default. Pass identifier for one person's journey, or omit it to list events for an account within an explicit time range. Pass direction=asc to read a journey forwards from where it starts. Events are shaped like the POST /events intake: attribution in context, identity in user.

        Parameters
        ----------
        identifier : typing.Optional[str]
            Any hard identifier of the person: a person ID (prsn_*), user ID, email, phone number, or a tracking cookie value (wuid, anonymous ID, fbp/fbc/ttp/ga). Omit to list recent events for the account.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Optional for account API keys; required for credentials that can access multiple accounts.

        from_ : typing.Optional[dt.datetime]
            Start of the time range as an ISO 8601 timestamp. Required when identifier is omitted.

        to : typing.Optional[dt.datetime]
            End of the time range as an ISO 8601 timestamp. Required when identifier is omitted; otherwise defaults to now.

        first : typing.Optional[int]
            The number of events to return.

        after : typing.Optional[str]
            A cursor for fetching events after a previous page.

        before : typing.Optional[str]
            A cursor for fetching events before a later page.

        direction : typing.Optional[ListEventsRequestDirection]
            The order events are returned in by time. Defaults to desc (most recent first); asc reads a journey forwards from where it starts. after and before always page forwards and backwards through that order.

        event : typing.Optional[str]
            Full event names to filter by, comma-separated (payment.completed, pixel.lead, pixel.page, pixel.custom:<name>) — the same vocabulary the events / people metrics use.

        source : typing.Optional[str]
            Canonical source path, exact or with a trailing :* prefix (whop:<campaign>:*, ext:meta:*, referrer:<domain>, direct). Restricts the list to conversion targets attributed to that source — the debuggability twin of a metric cell's source parameter.

        attribution_model : typing.Optional[ListEventsRequestAttributionModel]
            Attribution model for the source filter (defaults to last_touch).

        country : typing.Optional[str]
            Country codes to filter by, comma-separated.

        city : typing.Optional[str]
            Cities to filter by, comma-separated.

        device : typing.Optional[str]
            Device families to filter by, comma-separated (e.g. iPhone, Mac).

        browser : typing.Optional[str]
            Browser families to filter by, comma-separated (e.g. Chrome, Mobile Safari).

        os : typing.Optional[str]
            Operating system families to filter by, comma-separated (e.g. iOS, Windows).

        utm_source : typing.Optional[str]
            utm_source values to filter by, comma-separated.

        hostname : typing.Optional[str]
            Page hostnames to filter by, comma-separated.

        page : typing.Optional[str]
            Page paths to filter by, comma-separated.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ListEventsResponseDataItem, ListEventsResponse]
            events listed oldest first

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.events.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            identifier=identifier,
            account_id=account_id,
            from_=from_,
            to=to,
            first=first,
            after=after,
            before=before,
            direction=direction,
            event=event,
            source=source,
            attribution_model=attribution_model,
            country=country,
            city=city,
            device=device,
            browser=browser,
            os=os,
            utm_source=utm_source,
            hostname=hostname,
            page=page,
            request_options=request_options,
        )

    def create(
        self,
        *,
        account_id: str,
        event_name: str,
        action_source: typing.Optional[CreateEventsRequestActionSource] = OMIT,
        context: typing.Optional[CreateEventsRequestContext] = OMIT,
        currency: typing.Optional[CreateEventsRequestCurrency] = OMIT,
        custom_name: typing.Optional[str] = OMIT,
        duration: typing.Optional[int] = OMIT,
        event_id: typing.Optional[str] = OMIT,
        event_time: typing.Optional[dt.datetime] = OMIT,
        plan_id: typing.Optional[str] = OMIT,
        product_id: typing.Optional[str] = OMIT,
        referrer_url: typing.Optional[str] = OMIT,
        resumed: typing.Optional[bool] = OMIT,
        source: typing.Optional[str] = OMIT,
        title: typing.Optional[str] = OMIT,
        url: typing.Optional[str] = OMIT,
        user: typing.Optional[CreateEventsRequestUser] = OMIT,
        value: typing.Optional[float] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateEventsResponse:
        """
        Tracks a conversion or engagement event for an account.

        Parameters
        ----------
        account_id : str
            The account to associate with this event.

        event_name : str
            The type of event.

            Use a standard event (lead, submit_application, contact, complete_registration, schedule, view_content, add_to_cart) or pass your own name directly for a custom event.

        action_source : typing.Optional[CreateEventsRequestActionSource]
            Where the event originated.

        context : typing.Optional[CreateEventsRequestContext]
            Tracking and attribution context.

        currency : typing.Optional[CreateEventsRequestCurrency]
            ISO 4217 currency code.

        custom_name : typing.Optional[str]
            Custom event name when event_name is 'custom'. Maximum 35 chars for this value.

        duration : typing.Optional[int]
            For 'leave' events: milliseconds the visitor spent on the page.

        event_id : typing.Optional[str]
            Client-provided identifier for deduplication. Generated if omitted.

        event_time : typing.Optional[dt.datetime]
            When the event occurred. Defaults to now.

        plan_id : typing.Optional[str]
            The plan associated with the event.

        product_id : typing.Optional[str]
            The product associated with the event.

        referrer_url : typing.Optional[str]
            The referring URL.

        resumed : typing.Optional[bool]
            For 'page' events: true when the page was restored from the back/forward cache.

        source : typing.Optional[str]
            For 'identify' events: where the identity was captured (url, form, manual, iframe).

        title : typing.Optional[str]
            For 'page' events: the document title.

        url : typing.Optional[str]
            The URL where the event occurred.

        user : typing.Optional[CreateEventsRequestUser]
            User identity and profile data.

        value : typing.Optional[float]
            Monetary value associated with the event.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateEventsResponse
            event created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.events.create(
            account_id="biz_xxxxxxxxxxxxxx",
            event_name="coating_deposit_paid",
        )
        """
        _response = self._raw_client.create(
            account_id=account_id,
            event_name=event_name,
            action_source=action_source,
            context=context,
            currency=currency,
            custom_name=custom_name,
            duration=duration,
            event_id=event_id,
            event_time=event_time,
            plan_id=plan_id,
            product_id=product_id,
            referrer_url=referrer_url,
            resumed=resumed,
            source=source,
            title=title,
            url=url,
            user=user,
            value=value,
            request_options=request_options,
        )
        return _response.data

    def pulse(
        self,
        *,
        event: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PulseEventsResponseDataItem, PulseEventsResponse]:
        """
        Returns a fully anonymized feed of recent platform-wide money movement, most recent first: purchases, affiliate commissions, card and ad spend, app revenue, off-platform sales, wallet deposits, card loads, claimed drops, transfers between accounts, and referral bonuses. Items carry only a `type`, the underlying event name, a USD amount, a coarse location under `user`, and a timestamp coarsened to the start of the minute; missing fields are omitted, not nulled. The payload is identical for every caller; no auth is required.

        Parameters
        ----------
        event : typing.Optional[str]
            Filter to one or more types, comma separated — for example `purchase,card_spend`. These are the item's `type`, not its `event_name`: several types share the `ledger_line.created` event name. Omit for every type in the feed. Values outside the feed's own set are rejected.

        first : typing.Optional[int]
            The number of events to return.

        after : typing.Optional[str]
            A cursor for fetching events after a previous page.

        before : typing.Optional[str]
            A cursor for fetching events before a later page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PulseEventsResponseDataItem, PulseEventsResponse]
            pulse feed filtered to one type

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.events.pulse()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.pulse(
            event=event, first=first, after=after, before=before, request_options=request_options
        )

    def validate_pixel(
        self,
        *,
        account_id: typing.Optional[str] = OMIT,
        url: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PixelValidation:
        """
        Checks whether the Whop pixel is installed for an account. Recent pixel events count as proof on their own, so an account that has sent data lately comes back installed without a `url`. Pass a `url` and events from that page settle it; conversion events are also read across the hostname because they commonly fire on a later confirmation page. If the requested page hasn't sent any events lately, it is fetched and read for the pixel and conversion events wired on it. `installed` is only true when the pixel was actually seen — in the account's events or in the page.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Account to check. Defaults to the authenticated account.

        url : typing.Optional[str]
            A page to read for the pixel, e.g. an ad destination. Omit it to check the account from its events alone.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PixelValidation
            pixel validated

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.events.validate_pixel()
        """
        _response = self._raw_client.validate_pixel(account_id=account_id, url=url, request_options=request_options)
        return _response.data


class AsyncEventsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawEventsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawEventsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawEventsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        identifier: typing.Optional[str] = None,
        account_id: typing.Optional[str] = None,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        direction: typing.Optional[ListEventsRequestDirection] = None,
        event: typing.Optional[str] = None,
        source: typing.Optional[str] = None,
        attribution_model: typing.Optional[ListEventsRequestAttributionModel] = None,
        country: typing.Optional[str] = None,
        city: typing.Optional[str] = None,
        device: typing.Optional[str] = None,
        browser: typing.Optional[str] = None,
        os: typing.Optional[str] = None,
        utm_source: typing.Optional[str] = None,
        hostname: typing.Optional[str] = None,
        page: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ListEventsResponseDataItem, ListEventsResponse]:
        """
        Lists identity-linked events, most recent first by default. Pass identifier for one person's journey, or omit it to list events for an account within an explicit time range. Pass direction=asc to read a journey forwards from where it starts. Events are shaped like the POST /events intake: attribution in context, identity in user.

        Parameters
        ----------
        identifier : typing.Optional[str]
            Any hard identifier of the person: a person ID (prsn_*), user ID, email, phone number, or a tracking cookie value (wuid, anonymous ID, fbp/fbc/ttp/ga). Omit to list recent events for the account.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Optional for account API keys; required for credentials that can access multiple accounts.

        from_ : typing.Optional[dt.datetime]
            Start of the time range as an ISO 8601 timestamp. Required when identifier is omitted.

        to : typing.Optional[dt.datetime]
            End of the time range as an ISO 8601 timestamp. Required when identifier is omitted; otherwise defaults to now.

        first : typing.Optional[int]
            The number of events to return.

        after : typing.Optional[str]
            A cursor for fetching events after a previous page.

        before : typing.Optional[str]
            A cursor for fetching events before a later page.

        direction : typing.Optional[ListEventsRequestDirection]
            The order events are returned in by time. Defaults to desc (most recent first); asc reads a journey forwards from where it starts. after and before always page forwards and backwards through that order.

        event : typing.Optional[str]
            Full event names to filter by, comma-separated (payment.completed, pixel.lead, pixel.page, pixel.custom:<name>) — the same vocabulary the events / people metrics use.

        source : typing.Optional[str]
            Canonical source path, exact or with a trailing :* prefix (whop:<campaign>:*, ext:meta:*, referrer:<domain>, direct). Restricts the list to conversion targets attributed to that source — the debuggability twin of a metric cell's source parameter.

        attribution_model : typing.Optional[ListEventsRequestAttributionModel]
            Attribution model for the source filter (defaults to last_touch).

        country : typing.Optional[str]
            Country codes to filter by, comma-separated.

        city : typing.Optional[str]
            Cities to filter by, comma-separated.

        device : typing.Optional[str]
            Device families to filter by, comma-separated (e.g. iPhone, Mac).

        browser : typing.Optional[str]
            Browser families to filter by, comma-separated (e.g. Chrome, Mobile Safari).

        os : typing.Optional[str]
            Operating system families to filter by, comma-separated (e.g. iOS, Windows).

        utm_source : typing.Optional[str]
            utm_source values to filter by, comma-separated.

        hostname : typing.Optional[str]
            Page hostnames to filter by, comma-separated.

        page : typing.Optional[str]
            Page paths to filter by, comma-separated.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ListEventsResponseDataItem, ListEventsResponse]
            events listed oldest first

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.events.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            identifier=identifier,
            account_id=account_id,
            from_=from_,
            to=to,
            first=first,
            after=after,
            before=before,
            direction=direction,
            event=event,
            source=source,
            attribution_model=attribution_model,
            country=country,
            city=city,
            device=device,
            browser=browser,
            os=os,
            utm_source=utm_source,
            hostname=hostname,
            page=page,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        account_id: str,
        event_name: str,
        action_source: typing.Optional[CreateEventsRequestActionSource] = OMIT,
        context: typing.Optional[CreateEventsRequestContext] = OMIT,
        currency: typing.Optional[CreateEventsRequestCurrency] = OMIT,
        custom_name: typing.Optional[str] = OMIT,
        duration: typing.Optional[int] = OMIT,
        event_id: typing.Optional[str] = OMIT,
        event_time: typing.Optional[dt.datetime] = OMIT,
        plan_id: typing.Optional[str] = OMIT,
        product_id: typing.Optional[str] = OMIT,
        referrer_url: typing.Optional[str] = OMIT,
        resumed: typing.Optional[bool] = OMIT,
        source: typing.Optional[str] = OMIT,
        title: typing.Optional[str] = OMIT,
        url: typing.Optional[str] = OMIT,
        user: typing.Optional[CreateEventsRequestUser] = OMIT,
        value: typing.Optional[float] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateEventsResponse:
        """
        Tracks a conversion or engagement event for an account.

        Parameters
        ----------
        account_id : str
            The account to associate with this event.

        event_name : str
            The type of event.

            Use a standard event (lead, submit_application, contact, complete_registration, schedule, view_content, add_to_cart) or pass your own name directly for a custom event.

        action_source : typing.Optional[CreateEventsRequestActionSource]
            Where the event originated.

        context : typing.Optional[CreateEventsRequestContext]
            Tracking and attribution context.

        currency : typing.Optional[CreateEventsRequestCurrency]
            ISO 4217 currency code.

        custom_name : typing.Optional[str]
            Custom event name when event_name is 'custom'. Maximum 35 chars for this value.

        duration : typing.Optional[int]
            For 'leave' events: milliseconds the visitor spent on the page.

        event_id : typing.Optional[str]
            Client-provided identifier for deduplication. Generated if omitted.

        event_time : typing.Optional[dt.datetime]
            When the event occurred. Defaults to now.

        plan_id : typing.Optional[str]
            The plan associated with the event.

        product_id : typing.Optional[str]
            The product associated with the event.

        referrer_url : typing.Optional[str]
            The referring URL.

        resumed : typing.Optional[bool]
            For 'page' events: true when the page was restored from the back/forward cache.

        source : typing.Optional[str]
            For 'identify' events: where the identity was captured (url, form, manual, iframe).

        title : typing.Optional[str]
            For 'page' events: the document title.

        url : typing.Optional[str]
            The URL where the event occurred.

        user : typing.Optional[CreateEventsRequestUser]
            User identity and profile data.

        value : typing.Optional[float]
            Monetary value associated with the event.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateEventsResponse
            event created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.events.create(
                account_id="biz_xxxxxxxxxxxxxx",
                event_name="coating_deposit_paid",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            account_id=account_id,
            event_name=event_name,
            action_source=action_source,
            context=context,
            currency=currency,
            custom_name=custom_name,
            duration=duration,
            event_id=event_id,
            event_time=event_time,
            plan_id=plan_id,
            product_id=product_id,
            referrer_url=referrer_url,
            resumed=resumed,
            source=source,
            title=title,
            url=url,
            user=user,
            value=value,
            request_options=request_options,
        )
        return _response.data

    async def pulse(
        self,
        *,
        event: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PulseEventsResponseDataItem, PulseEventsResponse]:
        """
        Returns a fully anonymized feed of recent platform-wide money movement, most recent first: purchases, affiliate commissions, card and ad spend, app revenue, off-platform sales, wallet deposits, card loads, claimed drops, transfers between accounts, and referral bonuses. Items carry only a `type`, the underlying event name, a USD amount, a coarse location under `user`, and a timestamp coarsened to the start of the minute; missing fields are omitted, not nulled. The payload is identical for every caller; no auth is required.

        Parameters
        ----------
        event : typing.Optional[str]
            Filter to one or more types, comma separated — for example `purchase,card_spend`. These are the item's `type`, not its `event_name`: several types share the `ledger_line.created` event name. Omit for every type in the feed. Values outside the feed's own set are rejected.

        first : typing.Optional[int]
            The number of events to return.

        after : typing.Optional[str]
            A cursor for fetching events after a previous page.

        before : typing.Optional[str]
            A cursor for fetching events before a later page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PulseEventsResponseDataItem, PulseEventsResponse]
            pulse feed filtered to one type

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.events.pulse()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.pulse(
            event=event, first=first, after=after, before=before, request_options=request_options
        )

    async def validate_pixel(
        self,
        *,
        account_id: typing.Optional[str] = OMIT,
        url: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PixelValidation:
        """
        Checks whether the Whop pixel is installed for an account. Recent pixel events count as proof on their own, so an account that has sent data lately comes back installed without a `url`. Pass a `url` and events from that page settle it; conversion events are also read across the hostname because they commonly fire on a later confirmation page. If the requested page hasn't sent any events lately, it is fetched and read for the pixel and conversion events wired on it. `installed` is only true when the pixel was actually seen — in the account's events or in the page.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Account to check. Defaults to the authenticated account.

        url : typing.Optional[str]
            A page to read for the pixel, e.g. an ad destination. Omit it to check the account from its events alone.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PixelValidation
            pixel validated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.events.validate_pixel()


        asyncio.run(main())
        """
        _response = await self._raw_client.validate_pixel(
            account_id=account_id, url=url, request_options=request_options
        )
        return _response.data
