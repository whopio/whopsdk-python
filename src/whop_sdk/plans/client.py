# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.plan import Plan
from ..types.plan_list_item import PlanListItem
from .raw_client import AsyncRawPlansClient, RawPlansClient
from .types.calculate_tax_plans_request_address import CalculateTaxPlansRequestAddress
from .types.calculate_tax_plans_request_tax_ids_item import CalculateTaxPlansRequestTaxIdsItem
from .types.calculate_tax_plans_response import CalculateTaxPlansResponse
from .types.create_plans_request_custom_fields_item import CreatePlansRequestCustomFieldsItem
from .types.create_plans_request_image import CreatePlansRequestImage
from .types.create_plans_request_payment_method_configuration import CreatePlansRequestPaymentMethodConfiguration
from .types.create_plans_request_three_ds_level import CreatePlansRequestThreeDsLevel
from .types.delete_plans_response import DeletePlansResponse
from .types.list_plans_request_direction import ListPlansRequestDirection
from .types.list_plans_request_order import ListPlansRequestOrder
from .types.list_plans_response import ListPlansResponse
from .types.update_plans_request_custom_fields_item import UpdatePlansRequestCustomFieldsItem
from .types.update_plans_request_image import UpdatePlansRequestImage
from .types.update_plans_request_payment_method_configuration import UpdatePlansRequestPaymentMethodConfiguration
from .types.update_plans_request_three_ds_level import UpdatePlansRequestThreeDsLevel

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PlansClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPlansClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPlansClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPlansClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        direction: typing.Optional[ListPlansRequestDirection] = None,
        order: typing.Optional[ListPlansRequestOrder] = None,
        release_methods: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        visibilities: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        plan_types: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        product_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PlanListItem, ListPlansResponse]:
        """
        Returns a paginated list of plans. Omit `account_id` and pass `product_ids` to list a product's public buyable plans.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The unique identifier of the account to list plans for. Required unless `product_ids` is provided for a public product-plan read.

        direction : typing.Optional[ListPlansRequestDirection]
            The sort direction for results. Defaults to descending.

        order : typing.Optional[ListPlansRequestOrder]
            The field to sort results by. Defaults to created_at.

        release_methods : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only plans matching these release methods.

        visibilities : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only plans matching these visibility states.

        plan_types : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only plans matching these billing types.

        product_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only plans belonging to these product identifiers. When `account_id` is omitted, this is required and the response is publicly readable: only visible, non-invoice plans are returned.

        created_before : typing.Optional[str]
            Only return plans created before this timestamp.

        created_after : typing.Optional[str]
            Only return plans created after this timestamp.

        first : typing.Optional[int]
            The number of plans to return (default and max 100).

        after : typing.Optional[str]
            A cursor; returns plans after this position.

        last : typing.Optional[int]
            The number of plans to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns plans before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PlanListItem, ListPlansResponse]
            visible plans listed by product without authentication

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.plans.list(
            release_methods=["buy_now"],
            visibilities=["visible"],
            plan_types=["renewal"],
            product_ids=["prod_xxxxxxxxxxxxxx"],
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            direction=direction,
            order=order,
            release_methods=release_methods,
            visibilities=visibilities,
            plan_types=plan_types,
            product_ids=product_ids,
            created_before=created_before,
            created_after=created_after,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        account_id: typing.Optional[str] = OMIT,
        adaptive_pricing_enabled: typing.Optional[bool] = OMIT,
        billing_period: typing.Optional[int] = OMIT,
        checkout_styling: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        currency: typing.Optional[str] = OMIT,
        custom_fields: typing.Optional[typing.Sequence[CreatePlansRequestCustomFieldsItem]] = OMIT,
        description: typing.Optional[str] = OMIT,
        expiration_days: typing.Optional[int] = OMIT,
        image: typing.Optional[CreatePlansRequestImage] = OMIT,
        initial_price: typing.Optional[float] = OMIT,
        internal_notes: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        override_tax_type: typing.Optional[str] = OMIT,
        payment_method_configuration: typing.Optional[CreatePlansRequestPaymentMethodConfiguration] = OMIT,
        plan_type: typing.Optional[str] = OMIT,
        product_id: typing.Optional[str] = OMIT,
        release_method: typing.Optional[str] = OMIT,
        renewal_price: typing.Optional[float] = OMIT,
        split_pay_required_payments: typing.Optional[int] = OMIT,
        stock: typing.Optional[int] = OMIT,
        three_ds_level: typing.Optional[CreatePlansRequestThreeDsLevel] = OMIT,
        title: typing.Optional[str] = OMIT,
        trial_period_days: typing.Optional[int] = OMIT,
        unlimited_stock: typing.Optional[bool] = OMIT,
        visibility: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Plan:
        """
        Create a new pricing plan for a product. The plan defines the billing interval, price, and availability for customers.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The unique identifier of the account to create this plan for. Defaults to the caller's account.

        adaptive_pricing_enabled : typing.Optional[bool]
            Whether this plan accepts local currency payments via adaptive pricing.

        billing_period : typing.Optional[int]
            Recurring billing interval in days, such as 30 for monthly or 365 for annual.

        checkout_styling : typing.Optional[typing.Dict[str, typing.Any]]
            Checkout styling overrides for this plan.

        currency : typing.Optional[str]
            The three-letter ISO currency code for the plan's pricing. Defaults to USD.

        custom_fields : typing.Optional[typing.Sequence[CreatePlansRequestCustomFieldsItem]]
            An array of custom field definitions to collect from customers at checkout. Omitting this field clears existing custom fields.

        description : typing.Optional[str]
            A text description of the plan displayed to customers on the product page.

        expiration_days : typing.Optional[int]
            Access duration in days before the membership expires.

        image : typing.Optional[CreatePlansRequestImage]
            An image displayed on the product page to represent this plan.

        initial_price : typing.Optional[float]
            Initial amount charged in the plan's currency, e.g. 10.43 for $10.43.

        internal_notes : typing.Optional[str]
            Private notes visible only to the account owner. Not shown to customers.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` (a checkout call-to-action button label — one of the product custom CTA values, e.g. `subscribe`, `get_offer`) and `custom_cta_url` (a URL the button links to; web or `tel:`) override the product's call to action for this plan and are validated on save.

        override_tax_type : typing.Optional[str]
            Override the default tax classification for this specific plan.

        payment_method_configuration : typing.Optional[CreatePlansRequestPaymentMethodConfiguration]
            Explicit payment method configuration for the plan. When not provided, the account's defaults apply.

        plan_type : typing.Optional[str]
            Plan billing type, such as `one_time` or `renewal`.

        product_id : typing.Optional[str]
            The unique identifier of the product to attach this plan to.

        release_method : typing.Optional[str]
            Sales method for this plan.

        renewal_price : typing.Optional[float]
            The amount charged each billing period for recurring plans, in the plan's currency.

        split_pay_required_payments : typing.Optional[int]
            Installment payments required before the subscription pauses.

        stock : typing.Optional[int]
            The maximum number of units available for purchase. Ignored when unlimited_stock is true.

        three_ds_level : typing.Optional[CreatePlansRequestThreeDsLevel]
            3D Secure behavior for this plan. Send `null` to inherit the account default.

        title : typing.Optional[str]
            The display name of the plan shown to customers on the product page.

        trial_period_days : typing.Optional[int]
            Free trial duration before the first recurring charge.

        unlimited_stock : typing.Optional[bool]
            Whether the plan has unlimited stock. When true, the stock field is ignored.

        visibility : typing.Optional[str]
            Whether the plan is visible to customers or hidden from public view.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Plan
            plan created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.plans.create()
        """
        _response = self._raw_client.create(
            account_id=account_id,
            adaptive_pricing_enabled=adaptive_pricing_enabled,
            billing_period=billing_period,
            checkout_styling=checkout_styling,
            currency=currency,
            custom_fields=custom_fields,
            description=description,
            expiration_days=expiration_days,
            image=image,
            initial_price=initial_price,
            internal_notes=internal_notes,
            metadata=metadata,
            override_tax_type=override_tax_type,
            payment_method_configuration=payment_method_configuration,
            plan_type=plan_type,
            product_id=product_id,
            release_method=release_method,
            renewal_price=renewal_price,
            split_pay_required_payments=split_pay_required_payments,
            stock=stock,
            three_ds_level=three_ds_level,
            title=title,
            trial_period_days=trial_period_days,
            unlimited_stock=unlimited_stock,
            visibility=visibility,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Plan:
        """
        Retrieves the details of an existing plan.

        Parameters
        ----------
        id : str
            Plan ID, prefixed `plan_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Plan
            plan retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.plans.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeletePlansResponse:
        """
        Permanently delete a plan from a product. Existing memberships on this plan will not be affected.

        Parameters
        ----------
        id : str
            Plan ID, prefixed `plan_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletePlansResponse
            plan deleted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.plans.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        adaptive_pricing_enabled: typing.Optional[bool] = OMIT,
        billing_period: typing.Optional[int] = OMIT,
        cancel_discount_intervals: typing.Optional[int] = OMIT,
        cancel_discount_percentage: typing.Optional[int] = OMIT,
        checkout_styling: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        currency: typing.Optional[str] = OMIT,
        custom_fields: typing.Optional[typing.Sequence[UpdatePlansRequestCustomFieldsItem]] = OMIT,
        description: typing.Optional[str] = OMIT,
        expiration_days: typing.Optional[int] = OMIT,
        image: typing.Optional[UpdatePlansRequestImage] = OMIT,
        initial_price: typing.Optional[float] = OMIT,
        internal_notes: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        offer_cancel_discount: typing.Optional[bool] = OMIT,
        override_tax_type: typing.Optional[str] = OMIT,
        payment_method_configuration: typing.Optional[UpdatePlansRequestPaymentMethodConfiguration] = OMIT,
        release_method: typing.Optional[str] = OMIT,
        renewal_price: typing.Optional[float] = OMIT,
        stock: typing.Optional[int] = OMIT,
        strike_through_initial_price: typing.Optional[float] = OMIT,
        strike_through_renewal_price: typing.Optional[float] = OMIT,
        three_ds_level: typing.Optional[UpdatePlansRequestThreeDsLevel] = OMIT,
        title: typing.Optional[str] = OMIT,
        trial_period_days: typing.Optional[int] = OMIT,
        unlimited_stock: typing.Optional[bool] = OMIT,
        visibility: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Plan:
        """
        Update a plan's pricing, billing interval, visibility, stock, and other settings.

        Parameters
        ----------
        id : str
            Plan ID, prefixed `plan_`.

        adaptive_pricing_enabled : typing.Optional[bool]
            Whether this plan accepts local currency payments via adaptive pricing.

        billing_period : typing.Optional[int]
            Recurring billing interval in days, such as 30 for monthly or 365 for annual.

        cancel_discount_intervals : typing.Optional[int]
            How many renewals the retention discount applies to. Required when `offer_cancel_discount` is true.

        cancel_discount_percentage : typing.Optional[int]
            Percentage taken off each discounted renewal. Required when `offer_cancel_discount` is true.

        checkout_styling : typing.Optional[typing.Dict[str, typing.Any]]
            Checkout styling overrides for this plan.

        currency : typing.Optional[str]
            The three-letter ISO currency code for the plan's pricing. Defaults to USD.

        custom_fields : typing.Optional[typing.Sequence[UpdatePlansRequestCustomFieldsItem]]
            An array of custom field definitions to collect from customers at checkout. Omitting this field clears existing custom fields.

        description : typing.Optional[str]
            A text description of the plan displayed to customers on the product page.

        expiration_days : typing.Optional[int]
            Access duration in days before the membership expires.

        image : typing.Optional[UpdatePlansRequestImage]
            An image displayed on the product page to represent this plan.

        initial_price : typing.Optional[float]
            Initial amount charged in the plan's currency, e.g. 10.43 for $10.43.

        internal_notes : typing.Optional[str]
            Private notes visible only to the account owner. Not shown to customers.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` (a checkout call-to-action button label — one of the product custom CTA values, e.g. `subscribe`, `get_offer`) and `custom_cta_url` (a URL the button links to; web or `tel:`) override the product's call to action for this plan and are validated on save.

        offer_cancel_discount : typing.Optional[bool]
            Whether to offer a retention discount when a customer attempts to cancel.

        override_tax_type : typing.Optional[str]
            Override the default tax classification for this specific plan.

        payment_method_configuration : typing.Optional[UpdatePlansRequestPaymentMethodConfiguration]
            Explicit payment method configuration for the plan. When not provided, the account's defaults apply.

        release_method : typing.Optional[str]
            Sales method for this plan.

        renewal_price : typing.Optional[float]
            The amount charged each billing period for recurring plans, in the plan's currency.

        stock : typing.Optional[int]
            The maximum number of units available for purchase. Ignored when unlimited_stock is true.

        strike_through_initial_price : typing.Optional[float]
            A comparison price displayed with a strikethrough for the initial price.

        strike_through_renewal_price : typing.Optional[float]
            A comparison price displayed with a strikethrough for the renewal price.

        three_ds_level : typing.Optional[UpdatePlansRequestThreeDsLevel]
            3D Secure behavior for this plan. Send `null` to inherit the account default.

        title : typing.Optional[str]
            The display name of the plan shown to customers on the product page.

        trial_period_days : typing.Optional[int]
            Free trial duration before the first recurring charge.

        unlimited_stock : typing.Optional[bool]
            Whether the plan has unlimited stock. When true, the stock field is ignored.

        visibility : typing.Optional[str]
            Whether the plan is visible to customers or hidden from public view.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Plan
            plan updated

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.plans.update(
            id="id",
        )
        """
        _response = self._raw_client.update(
            id,
            adaptive_pricing_enabled=adaptive_pricing_enabled,
            billing_period=billing_period,
            cancel_discount_intervals=cancel_discount_intervals,
            cancel_discount_percentage=cancel_discount_percentage,
            checkout_styling=checkout_styling,
            currency=currency,
            custom_fields=custom_fields,
            description=description,
            expiration_days=expiration_days,
            image=image,
            initial_price=initial_price,
            internal_notes=internal_notes,
            metadata=metadata,
            offer_cancel_discount=offer_cancel_discount,
            override_tax_type=override_tax_type,
            payment_method_configuration=payment_method_configuration,
            release_method=release_method,
            renewal_price=renewal_price,
            stock=stock,
            strike_through_initial_price=strike_through_initial_price,
            strike_through_renewal_price=strike_through_renewal_price,
            three_ds_level=three_ds_level,
            title=title,
            trial_period_days=trial_period_days,
            unlimited_stock=unlimited_stock,
            visibility=visibility,
            request_options=request_options,
        )
        return _response.data

    def calculate_tax(
        self,
        id: str,
        *,
        address: typing.Optional[CalculateTaxPlansRequestAddress] = OMIT,
        ip_address: typing.Optional[str] = OMIT,
        tax_ids: typing.Optional[typing.Sequence[CalculateTaxPlansRequestTaxIdsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CalculateTaxPlansResponse:
        """
        Previews tax for a plan before checkout, based on the buyer's location.

        Parameters
        ----------
        id : str
            Plan ID, prefixed `plan_`.

        address : typing.Optional[CalculateTaxPlansRequestAddress]
            Buyer billing address used for tax calculation. Provide either `address.country` or `ip_address`; include state and postal code when available for more accurate results.

        ip_address : typing.Optional[str]
            Buyer IP address used to infer location when no billing address is provided.

        tax_ids : typing.Optional[typing.Sequence[CalculateTaxPlansRequestTaxIdsItem]]
            Optional buyer tax ID for B2B exemptions. At most one entry is supported.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CalculateTaxPlansResponse
            tax calculated

        Examples
        --------
        from whop_sdk import Whop
        from whop_sdk.plans import CalculateTaxPlansRequestAddress

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.plans.calculate_tax(
            id="id",
            address=CalculateTaxPlansRequestAddress(
                country="DE",
                postal_code="10115",
            ),
        )
        """
        _response = self._raw_client.calculate_tax(
            id, address=address, ip_address=ip_address, tax_ids=tax_ids, request_options=request_options
        )
        return _response.data


class AsyncPlansClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPlansClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPlansClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPlansClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        direction: typing.Optional[ListPlansRequestDirection] = None,
        order: typing.Optional[ListPlansRequestOrder] = None,
        release_methods: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        visibilities: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        plan_types: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        product_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PlanListItem, ListPlansResponse]:
        """
        Returns a paginated list of plans. Omit `account_id` and pass `product_ids` to list a product's public buyable plans.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The unique identifier of the account to list plans for. Required unless `product_ids` is provided for a public product-plan read.

        direction : typing.Optional[ListPlansRequestDirection]
            The sort direction for results. Defaults to descending.

        order : typing.Optional[ListPlansRequestOrder]
            The field to sort results by. Defaults to created_at.

        release_methods : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only plans matching these release methods.

        visibilities : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only plans matching these visibility states.

        plan_types : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only plans matching these billing types.

        product_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to only plans belonging to these product identifiers. When `account_id` is omitted, this is required and the response is publicly readable: only visible, non-invoice plans are returned.

        created_before : typing.Optional[str]
            Only return plans created before this timestamp.

        created_after : typing.Optional[str]
            Only return plans created after this timestamp.

        first : typing.Optional[int]
            The number of plans to return (default and max 100).

        after : typing.Optional[str]
            A cursor; returns plans after this position.

        last : typing.Optional[int]
            The number of plans to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns plans before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PlanListItem, ListPlansResponse]
            visible plans listed by product without authentication

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.plans.list(
                release_methods=["buy_now"],
                visibilities=["visible"],
                plan_types=["renewal"],
                product_ids=["prod_xxxxxxxxxxxxxx"],
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            direction=direction,
            order=order,
            release_methods=release_methods,
            visibilities=visibilities,
            plan_types=plan_types,
            product_ids=product_ids,
            created_before=created_before,
            created_after=created_after,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        account_id: typing.Optional[str] = OMIT,
        adaptive_pricing_enabled: typing.Optional[bool] = OMIT,
        billing_period: typing.Optional[int] = OMIT,
        checkout_styling: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        currency: typing.Optional[str] = OMIT,
        custom_fields: typing.Optional[typing.Sequence[CreatePlansRequestCustomFieldsItem]] = OMIT,
        description: typing.Optional[str] = OMIT,
        expiration_days: typing.Optional[int] = OMIT,
        image: typing.Optional[CreatePlansRequestImage] = OMIT,
        initial_price: typing.Optional[float] = OMIT,
        internal_notes: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        override_tax_type: typing.Optional[str] = OMIT,
        payment_method_configuration: typing.Optional[CreatePlansRequestPaymentMethodConfiguration] = OMIT,
        plan_type: typing.Optional[str] = OMIT,
        product_id: typing.Optional[str] = OMIT,
        release_method: typing.Optional[str] = OMIT,
        renewal_price: typing.Optional[float] = OMIT,
        split_pay_required_payments: typing.Optional[int] = OMIT,
        stock: typing.Optional[int] = OMIT,
        three_ds_level: typing.Optional[CreatePlansRequestThreeDsLevel] = OMIT,
        title: typing.Optional[str] = OMIT,
        trial_period_days: typing.Optional[int] = OMIT,
        unlimited_stock: typing.Optional[bool] = OMIT,
        visibility: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Plan:
        """
        Create a new pricing plan for a product. The plan defines the billing interval, price, and availability for customers.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The unique identifier of the account to create this plan for. Defaults to the caller's account.

        adaptive_pricing_enabled : typing.Optional[bool]
            Whether this plan accepts local currency payments via adaptive pricing.

        billing_period : typing.Optional[int]
            Recurring billing interval in days, such as 30 for monthly or 365 for annual.

        checkout_styling : typing.Optional[typing.Dict[str, typing.Any]]
            Checkout styling overrides for this plan.

        currency : typing.Optional[str]
            The three-letter ISO currency code for the plan's pricing. Defaults to USD.

        custom_fields : typing.Optional[typing.Sequence[CreatePlansRequestCustomFieldsItem]]
            An array of custom field definitions to collect from customers at checkout. Omitting this field clears existing custom fields.

        description : typing.Optional[str]
            A text description of the plan displayed to customers on the product page.

        expiration_days : typing.Optional[int]
            Access duration in days before the membership expires.

        image : typing.Optional[CreatePlansRequestImage]
            An image displayed on the product page to represent this plan.

        initial_price : typing.Optional[float]
            Initial amount charged in the plan's currency, e.g. 10.43 for $10.43.

        internal_notes : typing.Optional[str]
            Private notes visible only to the account owner. Not shown to customers.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` (a checkout call-to-action button label — one of the product custom CTA values, e.g. `subscribe`, `get_offer`) and `custom_cta_url` (a URL the button links to; web or `tel:`) override the product's call to action for this plan and are validated on save.

        override_tax_type : typing.Optional[str]
            Override the default tax classification for this specific plan.

        payment_method_configuration : typing.Optional[CreatePlansRequestPaymentMethodConfiguration]
            Explicit payment method configuration for the plan. When not provided, the account's defaults apply.

        plan_type : typing.Optional[str]
            Plan billing type, such as `one_time` or `renewal`.

        product_id : typing.Optional[str]
            The unique identifier of the product to attach this plan to.

        release_method : typing.Optional[str]
            Sales method for this plan.

        renewal_price : typing.Optional[float]
            The amount charged each billing period for recurring plans, in the plan's currency.

        split_pay_required_payments : typing.Optional[int]
            Installment payments required before the subscription pauses.

        stock : typing.Optional[int]
            The maximum number of units available for purchase. Ignored when unlimited_stock is true.

        three_ds_level : typing.Optional[CreatePlansRequestThreeDsLevel]
            3D Secure behavior for this plan. Send `null` to inherit the account default.

        title : typing.Optional[str]
            The display name of the plan shown to customers on the product page.

        trial_period_days : typing.Optional[int]
            Free trial duration before the first recurring charge.

        unlimited_stock : typing.Optional[bool]
            Whether the plan has unlimited stock. When true, the stock field is ignored.

        visibility : typing.Optional[str]
            Whether the plan is visible to customers or hidden from public view.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Plan
            plan created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.plans.create()


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            account_id=account_id,
            adaptive_pricing_enabled=adaptive_pricing_enabled,
            billing_period=billing_period,
            checkout_styling=checkout_styling,
            currency=currency,
            custom_fields=custom_fields,
            description=description,
            expiration_days=expiration_days,
            image=image,
            initial_price=initial_price,
            internal_notes=internal_notes,
            metadata=metadata,
            override_tax_type=override_tax_type,
            payment_method_configuration=payment_method_configuration,
            plan_type=plan_type,
            product_id=product_id,
            release_method=release_method,
            renewal_price=renewal_price,
            split_pay_required_payments=split_pay_required_payments,
            stock=stock,
            three_ds_level=three_ds_level,
            title=title,
            trial_period_days=trial_period_days,
            unlimited_stock=unlimited_stock,
            visibility=visibility,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Plan:
        """
        Retrieves the details of an existing plan.

        Parameters
        ----------
        id : str
            Plan ID, prefixed `plan_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Plan
            plan retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.plans.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeletePlansResponse:
        """
        Permanently delete a plan from a product. Existing memberships on this plan will not be affected.

        Parameters
        ----------
        id : str
            Plan ID, prefixed `plan_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletePlansResponse
            plan deleted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.plans.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        adaptive_pricing_enabled: typing.Optional[bool] = OMIT,
        billing_period: typing.Optional[int] = OMIT,
        cancel_discount_intervals: typing.Optional[int] = OMIT,
        cancel_discount_percentage: typing.Optional[int] = OMIT,
        checkout_styling: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        currency: typing.Optional[str] = OMIT,
        custom_fields: typing.Optional[typing.Sequence[UpdatePlansRequestCustomFieldsItem]] = OMIT,
        description: typing.Optional[str] = OMIT,
        expiration_days: typing.Optional[int] = OMIT,
        image: typing.Optional[UpdatePlansRequestImage] = OMIT,
        initial_price: typing.Optional[float] = OMIT,
        internal_notes: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        offer_cancel_discount: typing.Optional[bool] = OMIT,
        override_tax_type: typing.Optional[str] = OMIT,
        payment_method_configuration: typing.Optional[UpdatePlansRequestPaymentMethodConfiguration] = OMIT,
        release_method: typing.Optional[str] = OMIT,
        renewal_price: typing.Optional[float] = OMIT,
        stock: typing.Optional[int] = OMIT,
        strike_through_initial_price: typing.Optional[float] = OMIT,
        strike_through_renewal_price: typing.Optional[float] = OMIT,
        three_ds_level: typing.Optional[UpdatePlansRequestThreeDsLevel] = OMIT,
        title: typing.Optional[str] = OMIT,
        trial_period_days: typing.Optional[int] = OMIT,
        unlimited_stock: typing.Optional[bool] = OMIT,
        visibility: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Plan:
        """
        Update a plan's pricing, billing interval, visibility, stock, and other settings.

        Parameters
        ----------
        id : str
            Plan ID, prefixed `plan_`.

        adaptive_pricing_enabled : typing.Optional[bool]
            Whether this plan accepts local currency payments via adaptive pricing.

        billing_period : typing.Optional[int]
            Recurring billing interval in days, such as 30 for monthly or 365 for annual.

        cancel_discount_intervals : typing.Optional[int]
            How many renewals the retention discount applies to. Required when `offer_cancel_discount` is true.

        cancel_discount_percentage : typing.Optional[int]
            Percentage taken off each discounted renewal. Required when `offer_cancel_discount` is true.

        checkout_styling : typing.Optional[typing.Dict[str, typing.Any]]
            Checkout styling overrides for this plan.

        currency : typing.Optional[str]
            The three-letter ISO currency code for the plan's pricing. Defaults to USD.

        custom_fields : typing.Optional[typing.Sequence[UpdatePlansRequestCustomFieldsItem]]
            An array of custom field definitions to collect from customers at checkout. Omitting this field clears existing custom fields.

        description : typing.Optional[str]
            A text description of the plan displayed to customers on the product page.

        expiration_days : typing.Optional[int]
            Access duration in days before the membership expires.

        image : typing.Optional[UpdatePlansRequestImage]
            An image displayed on the product page to represent this plan.

        initial_price : typing.Optional[float]
            Initial amount charged in the plan's currency, e.g. 10.43 for $10.43.

        internal_notes : typing.Optional[str]
            Private notes visible only to the account owner. Not shown to customers.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the plan. Included in webhook payloads for payment and membership events. Max 50 keys, 100 chars per key, 500 chars per string value. The reserved keys `custom_cta` (a checkout call-to-action button label — one of the product custom CTA values, e.g. `subscribe`, `get_offer`) and `custom_cta_url` (a URL the button links to; web or `tel:`) override the product's call to action for this plan and are validated on save.

        offer_cancel_discount : typing.Optional[bool]
            Whether to offer a retention discount when a customer attempts to cancel.

        override_tax_type : typing.Optional[str]
            Override the default tax classification for this specific plan.

        payment_method_configuration : typing.Optional[UpdatePlansRequestPaymentMethodConfiguration]
            Explicit payment method configuration for the plan. When not provided, the account's defaults apply.

        release_method : typing.Optional[str]
            Sales method for this plan.

        renewal_price : typing.Optional[float]
            The amount charged each billing period for recurring plans, in the plan's currency.

        stock : typing.Optional[int]
            The maximum number of units available for purchase. Ignored when unlimited_stock is true.

        strike_through_initial_price : typing.Optional[float]
            A comparison price displayed with a strikethrough for the initial price.

        strike_through_renewal_price : typing.Optional[float]
            A comparison price displayed with a strikethrough for the renewal price.

        three_ds_level : typing.Optional[UpdatePlansRequestThreeDsLevel]
            3D Secure behavior for this plan. Send `null` to inherit the account default.

        title : typing.Optional[str]
            The display name of the plan shown to customers on the product page.

        trial_period_days : typing.Optional[int]
            Free trial duration before the first recurring charge.

        unlimited_stock : typing.Optional[bool]
            Whether the plan has unlimited stock. When true, the stock field is ignored.

        visibility : typing.Optional[str]
            Whether the plan is visible to customers or hidden from public view.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Plan
            plan updated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.plans.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            adaptive_pricing_enabled=adaptive_pricing_enabled,
            billing_period=billing_period,
            cancel_discount_intervals=cancel_discount_intervals,
            cancel_discount_percentage=cancel_discount_percentage,
            checkout_styling=checkout_styling,
            currency=currency,
            custom_fields=custom_fields,
            description=description,
            expiration_days=expiration_days,
            image=image,
            initial_price=initial_price,
            internal_notes=internal_notes,
            metadata=metadata,
            offer_cancel_discount=offer_cancel_discount,
            override_tax_type=override_tax_type,
            payment_method_configuration=payment_method_configuration,
            release_method=release_method,
            renewal_price=renewal_price,
            stock=stock,
            strike_through_initial_price=strike_through_initial_price,
            strike_through_renewal_price=strike_through_renewal_price,
            three_ds_level=three_ds_level,
            title=title,
            trial_period_days=trial_period_days,
            unlimited_stock=unlimited_stock,
            visibility=visibility,
            request_options=request_options,
        )
        return _response.data

    async def calculate_tax(
        self,
        id: str,
        *,
        address: typing.Optional[CalculateTaxPlansRequestAddress] = OMIT,
        ip_address: typing.Optional[str] = OMIT,
        tax_ids: typing.Optional[typing.Sequence[CalculateTaxPlansRequestTaxIdsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CalculateTaxPlansResponse:
        """
        Previews tax for a plan before checkout, based on the buyer's location.

        Parameters
        ----------
        id : str
            Plan ID, prefixed `plan_`.

        address : typing.Optional[CalculateTaxPlansRequestAddress]
            Buyer billing address used for tax calculation. Provide either `address.country` or `ip_address`; include state and postal code when available for more accurate results.

        ip_address : typing.Optional[str]
            Buyer IP address used to infer location when no billing address is provided.

        tax_ids : typing.Optional[typing.Sequence[CalculateTaxPlansRequestTaxIdsItem]]
            Optional buyer tax ID for B2B exemptions. At most one entry is supported.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CalculateTaxPlansResponse
            tax calculated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop
        from whop_sdk.plans import CalculateTaxPlansRequestAddress

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.plans.calculate_tax(
                id="id",
                address=CalculateTaxPlansRequestAddress(
                    country="DE",
                    postal_code="10115",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.calculate_tax(
            id, address=address, ip_address=ip_address, tax_ids=tax_ids, request_options=request_options
        )
        return _response.data
