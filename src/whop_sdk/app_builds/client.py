# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.app_build import AppBuild
from ..types.list_app_builds_request_created_after import ListAppBuildsRequestCreatedAfter
from ..types.list_app_builds_request_created_before import ListAppBuildsRequestCreatedBefore
from .raw_client import AsyncRawAppBuildsClient, RawAppBuildsClient
from .types.create_app_builds_request_attachment import CreateAppBuildsRequestAttachment
from .types.create_app_builds_request_platform import CreateAppBuildsRequestPlatform
from .types.create_app_builds_request_source_attachment import CreateAppBuildsRequestSourceAttachment
from .types.create_app_builds_request_supported_app_view_types_item import (
    CreateAppBuildsRequestSupportedAppViewTypesItem,
)
from .types.list_app_builds_request_platform import ListAppBuildsRequestPlatform
from .types.list_app_builds_request_status import ListAppBuildsRequestStatus
from .types.list_app_builds_response import ListAppBuildsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AppBuildsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAppBuildsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAppBuildsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAppBuildsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        app_id: str,
        platform: typing.Optional[ListAppBuildsRequestPlatform] = None,
        status: typing.Optional[ListAppBuildsRequestStatus] = None,
        created_before: typing.Optional[ListAppBuildsRequestCreatedBefore] = None,
        created_after: typing.Optional[ListAppBuildsRequestCreatedAfter] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[AppBuild, ListAppBuildsResponse]:
        """
        Returns a paginated list of build artifacts for an app, newest first, with optional platform, status, and creation-date filters.

        Parameters
        ----------
        app_id : str
            The app to list builds for, prefixed `app_`.

        platform : typing.Optional[ListAppBuildsRequestPlatform]
            Filter builds by target platform.

        status : typing.Optional[ListAppBuildsRequestStatus]
            Filter builds by review status.

        created_before : typing.Optional[ListAppBuildsRequestCreatedBefore]
            Only return builds created before this ISO 8601 timestamp.

        created_after : typing.Optional[ListAppBuildsRequestCreatedAfter]
            Only return builds created after this ISO 8601 timestamp.

        first : typing.Optional[int]
            The number of builds to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns builds after this position.

        last : typing.Optional[int]
            The number of builds to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns builds before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[AppBuild, ListAppBuildsResponse]
            app builds listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.app_builds.list(
            app_id="app_id",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            app_id=app_id,
            platform=platform,
            status=status,
            created_before=created_before,
            created_after=created_after,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        attachment: CreateAppBuildsRequestAttachment,
        checksum: str,
        platform: CreateAppBuildsRequestPlatform,
        ai_prompt_id: typing.Optional[str] = OMIT,
        app_id: typing.Optional[str] = OMIT,
        source_attachment: typing.Optional[CreateAppBuildsRequestSourceAttachment] = OMIT,
        supported_app_view_types: typing.Optional[
            typing.Sequence[CreateAppBuildsRequestSupportedAppViewTypesItem]
        ] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AppBuild:
        """
        Uploads a new build artifact for an app. Upload the file first (POST /files or a direct upload), then reference it here; iOS and Android take a .zip bundle, web takes a JavaScript file or a .zip archive of the hosted site.

        Parameters
        ----------
        attachment : CreateAppBuildsRequestAttachment
            The uploaded build file: `{ id }` for an existing file or `{ direct_upload_id }` for a completed direct upload.

        checksum : str
            A client-generated checksum of the build file, used to verify file integrity when unpacked.

        platform : CreateAppBuildsRequestPlatform
            The target platform for the build.

        ai_prompt_id : typing.Optional[str]
            The AI prompt that generated this build, if applicable.

        app_id : typing.Optional[str]
            The app to create the build for, prefixed `app_`. Defaults to the app behind the presented credential.

        source_attachment : typing.Optional[CreateAppBuildsRequestSourceAttachment]
            An optional compressed archive (.zip or .gz) of the source code that produced this build, stored alongside the build so it can be downloaded later. Referenced like `attachment`, and must be a different file.

        supported_app_view_types : typing.Optional[typing.Sequence[CreateAppBuildsRequestSupportedAppViewTypesItem]]
            The view types this build supports. Only list the ones its code implements.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AppBuild
            app build created

        Examples
        --------
        from whop_sdk import Whop
        from whop_sdk.app_builds import CreateAppBuildsRequestAttachment

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.app_builds.create(
            attachment=CreateAppBuildsRequestAttachment(),
            checksum="xxxxxxxxxxxxxxx",
            platform="ios",
        )
        """
        _response = self._raw_client.create(
            attachment=attachment,
            checksum=checksum,
            platform=platform,
            ai_prompt_id=ai_prompt_id,
            app_id=app_id,
            source_attachment=source_attachment,
            supported_app_view_types=supported_app_view_types,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AppBuild:
        """
        Retrieves the details of an existing app build.

        Parameters
        ----------
        id : str
            App build ID, prefixed `abld_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AppBuild
            app build retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.app_builds.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def promote(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AppBuild:
        """
        Promotes a draft or approved app build to production so it becomes the active version served to users. Draft builds enter review first.

        Parameters
        ----------
        id : str
            App build ID, prefixed `abld_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AppBuild
            app build promotion started

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.app_builds.promote(
            id="id",
        )
        """
        _response = self._raw_client.promote(id, request_options=request_options)
        return _response.data


class AsyncAppBuildsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAppBuildsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAppBuildsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAppBuildsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        app_id: str,
        platform: typing.Optional[ListAppBuildsRequestPlatform] = None,
        status: typing.Optional[ListAppBuildsRequestStatus] = None,
        created_before: typing.Optional[ListAppBuildsRequestCreatedBefore] = None,
        created_after: typing.Optional[ListAppBuildsRequestCreatedAfter] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[AppBuild, ListAppBuildsResponse]:
        """
        Returns a paginated list of build artifacts for an app, newest first, with optional platform, status, and creation-date filters.

        Parameters
        ----------
        app_id : str
            The app to list builds for, prefixed `app_`.

        platform : typing.Optional[ListAppBuildsRequestPlatform]
            Filter builds by target platform.

        status : typing.Optional[ListAppBuildsRequestStatus]
            Filter builds by review status.

        created_before : typing.Optional[ListAppBuildsRequestCreatedBefore]
            Only return builds created before this ISO 8601 timestamp.

        created_after : typing.Optional[ListAppBuildsRequestCreatedAfter]
            Only return builds created after this ISO 8601 timestamp.

        first : typing.Optional[int]
            The number of builds to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns builds after this position.

        last : typing.Optional[int]
            The number of builds to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns builds before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[AppBuild, ListAppBuildsResponse]
            app builds listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.app_builds.list(
                app_id="app_id",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            app_id=app_id,
            platform=platform,
            status=status,
            created_before=created_before,
            created_after=created_after,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        attachment: CreateAppBuildsRequestAttachment,
        checksum: str,
        platform: CreateAppBuildsRequestPlatform,
        ai_prompt_id: typing.Optional[str] = OMIT,
        app_id: typing.Optional[str] = OMIT,
        source_attachment: typing.Optional[CreateAppBuildsRequestSourceAttachment] = OMIT,
        supported_app_view_types: typing.Optional[
            typing.Sequence[CreateAppBuildsRequestSupportedAppViewTypesItem]
        ] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AppBuild:
        """
        Uploads a new build artifact for an app. Upload the file first (POST /files or a direct upload), then reference it here; iOS and Android take a .zip bundle, web takes a JavaScript file or a .zip archive of the hosted site.

        Parameters
        ----------
        attachment : CreateAppBuildsRequestAttachment
            The uploaded build file: `{ id }` for an existing file or `{ direct_upload_id }` for a completed direct upload.

        checksum : str
            A client-generated checksum of the build file, used to verify file integrity when unpacked.

        platform : CreateAppBuildsRequestPlatform
            The target platform for the build.

        ai_prompt_id : typing.Optional[str]
            The AI prompt that generated this build, if applicable.

        app_id : typing.Optional[str]
            The app to create the build for, prefixed `app_`. Defaults to the app behind the presented credential.

        source_attachment : typing.Optional[CreateAppBuildsRequestSourceAttachment]
            An optional compressed archive (.zip or .gz) of the source code that produced this build, stored alongside the build so it can be downloaded later. Referenced like `attachment`, and must be a different file.

        supported_app_view_types : typing.Optional[typing.Sequence[CreateAppBuildsRequestSupportedAppViewTypesItem]]
            The view types this build supports. Only list the ones its code implements.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AppBuild
            app build created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop
        from whop_sdk.app_builds import CreateAppBuildsRequestAttachment

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.app_builds.create(
                attachment=CreateAppBuildsRequestAttachment(),
                checksum="xxxxxxxxxxxxxxx",
                platform="ios",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            attachment=attachment,
            checksum=checksum,
            platform=platform,
            ai_prompt_id=ai_prompt_id,
            app_id=app_id,
            source_attachment=source_attachment,
            supported_app_view_types=supported_app_view_types,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AppBuild:
        """
        Retrieves the details of an existing app build.

        Parameters
        ----------
        id : str
            App build ID, prefixed `abld_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AppBuild
            app build retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.app_builds.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def promote(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> AppBuild:
        """
        Promotes a draft or approved app build to production so it becomes the active version served to users. Draft builds enter review first.

        Parameters
        ----------
        id : str
            App build ID, prefixed `abld_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AppBuild
            app build promotion started

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.app_builds.promote(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.promote(id, request_options=request_options)
        return _response.data
