# This file was auto-generated by Fern from our API Definition.

import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import encode_path_param
from ..core.pagination import AsyncPager, SyncPager
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..core.serialization import convert_and_respect_annotation_metadata
from ..errors.bad_request_error import BadRequestError
from ..errors.conflict_error import ConflictError
from ..errors.forbidden_error import ForbiddenError
from ..errors.not_found_error import NotFoundError
from ..errors.unauthorized_error import UnauthorizedError
from ..types.bounty import Bounty
from ..types.bounty_list_item import BountyListItem
from ..types.v1error_response import V1ErrorResponse
from .types.create_bounties_request_business_goal_type import CreateBountiesRequestBusinessGoalType
from .types.create_bounties_request_capture_spec import CreateBountiesRequestCaptureSpec
from .types.create_bounties_request_frequency import CreateBountiesRequestFrequency
from .types.list_bounties_request_business_goal_type import ListBountiesRequestBusinessGoalType
from .types.list_bounties_request_direction import ListBountiesRequestDirection
from .types.list_bounties_request_order import ListBountiesRequestOrder
from .types.list_bounties_request_status import ListBountiesRequestStatus
from .types.list_bounties_response import ListBountiesResponse
from .types.update_bounties_request_business_goal_type import UpdateBountiesRequestBusinessGoalType
from .types.update_bounties_request_frequency import UpdateBountiesRequestFrequency
from pydantic import ValidationError

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawBountiesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        status: typing.Optional[ListBountiesRequestStatus] = None,
        business_goal_type: typing.Optional[ListBountiesRequestBusinessGoalType] = None,
        country: typing.Optional[str] = None,
        experience_id: typing.Optional[str] = None,
        query: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        created_before: typing.Optional[str] = None,
        order: typing.Optional[ListBountiesRequestOrder] = None,
        direction: typing.Optional[ListBountiesRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[BountyListItem, ListBountiesResponse]:
        """
        Lists bounties visible to the credential — for an account API key, the account's bounties including scheduled drafts; for a user token, the bounties the user can see and work.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Scope the list to this account (`biz_` tag). Requires read access to the account; account API keys may pass their own account or a connected account.

        user_id : typing.Optional[str]
            List the bounties this user participated in (`user_` tag). Must be the authenticated user.

        status : typing.Optional[ListBountiesRequestStatus]
            Filter by lifecycle state.

        business_goal_type : typing.Optional[ListBountiesRequestBusinessGoalType]
            Filter by the poster's declared goal. Bounties created before the goal taxonomy carry no goal and never match this filter.

        country : typing.Optional[str]
            Only bounties workable from this country, as an ISO 3166-1 alpha-2 code. Bounties with no country targeting are workable worldwide and always match.

        experience_id : typing.Optional[str]
            Only bounties posted to this forum experience, prefixed `exp_`. An unknown experience, or one outside the caller's scope, matches nothing.

        query : typing.Optional[str]
            Substring match on the bounty title or ID.

        created_after : typing.Optional[str]
            Only bounties created after this ISO 8601 timestamp.

        created_before : typing.Optional[str]
            Only bounties created before this ISO 8601 timestamp.

        order : typing.Optional[ListBountiesRequestOrder]
            Sort field.

        direction : typing.Optional[ListBountiesRequestDirection]
            Sort direction.

        first : typing.Optional[int]
            Number of bounties to return from the start of the window.

        after : typing.Optional[str]
            Cursor to paginate forwards from.

        last : typing.Optional[int]
            Number of bounties to return from the end of the window.

        before : typing.Optional[str]
            Cursor to paginate backwards from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[BountyListItem, ListBountiesResponse]
            bounties listed
        """
        _response = self._client_wrapper.httpx_client.request(
            "bounties",
            method="GET",
            params={
                "account_id": account_id,
                "user_id": user_id,
                "status": status,
                "business_goal_type": business_goal_type,
                "country": country,
                "experience_id": experience_id,
                "query": query,
                "created_after": created_after,
                "created_before": created_before,
                "order": order,
                "direction": direction,
                "first": first,
                "after": after,
                "last": last,
                "before": before,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListBountiesResponse,
                    parse_obj_as(
                        type_=ListBountiesResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""
                    _get_next = lambda: self.list(
                        account_id=account_id,
                        user_id=user_id,
                        status=status,
                        business_goal_type=business_goal_type,
                        country=country,
                        experience_id=experience_id,
                        query=query,
                        created_after=created_after,
                        created_before=created_before,
                        order=order,
                        direction=direction,
                        first=first,
                        after=_parsed_next,
                        last=last,
                        before=before,
                        request_options=request_options,
                    )
                return SyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def create(
        self,
        *,
        description: str,
        gross_reward_amount: float,
        title: str,
        accepted_submissions_limit: typing.Optional[int] = OMIT,
        accepted_submissions_per_user_limit: typing.Optional[int] = OMIT,
        account_id: typing.Optional[str] = OMIT,
        allowed_country_codes: typing.Optional[typing.Sequence[str]] = OMIT,
        business_goal_type: typing.Optional[CreateBountiesRequestBusinessGoalType] = OMIT,
        capture_spec: typing.Optional[CreateBountiesRequestCaptureSpec] = OMIT,
        experience_id: typing.Optional[str] = OMIT,
        frequency: typing.Optional[CreateBountiesRequestFrequency] = OMIT,
        publish_at: typing.Optional[str] = OMIT,
        publish_at_timezone: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[Bounty]:
        """
        Creates a bounty and escrows its reward pool. Publishes immediately, or as a scheduled draft when you set `publish_at`.

        Parameters
        ----------
        description : str
            Full task instructions shown to workers.

        gross_reward_amount : float
            Gross bounty-pool amount (USD) escrowed per accepted submission, in whole dollars. Platform fees and affiliate shares are paid from this amount.

        title : str
            Short name of the task shown to workers.

        accepted_submissions_limit : typing.Optional[int]
            Number of submissions that can be accepted (winner slots). Defaults to 1. The escrowed total is `gross_reward_amount` times this limit and must be at least $5.

        accepted_submissions_per_user_limit : typing.Optional[int]
            How many winner slots one worker can win. Defaults to `1`. Wins plus proofs awaiting review never exceed this number, and a worker runs one attempt at a time. Cannot exceed `accepted_submissions_limit`.

        account_id : typing.Optional[str]
            Account whose balance funds the bounty pool (`biz_` tag). Defaults to the caller's personal balance. Requires permission to move the account's funds.

        allowed_country_codes : typing.Optional[typing.Sequence[str]]
            Countries whose residents can work the bounty, as ISO 3166 alpha-2 codes. Empty means worldwide.

        business_goal_type : typing.Optional[CreateBountiesRequestBusinessGoalType]
            What the poster wants the work to achieve, declared once here.

        capture_spec : typing.Optional[CreateBountiesRequestCaptureSpec]
            Per-bounty overrides of the served capture contract. Only accepted when `business_goal_type` is `data_capture`; omitted fields keep the platform defaults, and the resulting contract is echoed back as `capture_spec` on the bounty.

        experience_id : typing.Optional[str]
            Experience to host the bounty in (`exp_` tag). Any visibility — public for an open bounty, private for an invited one. Required unless account_id is set, in which case the bounty anchors in that account's public forum.

        frequency : typing.Optional[CreateBountiesRequestFrequency]
            How often the schedule creates a new bounty. Each occurrence is a separate bounty. Defaults to `once`; only applies with `publish_at`.

        publish_at : typing.Optional[str]
            ISO 8601 time to publish the bounty. When set, the bounty is created as a hidden draft and funded + published at this time instead of immediately.

        publish_at_timezone : typing.Optional[str]
            IANA timezone for recurring occurrences. Required when publish_at is set.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Bounty]
            bounty created
        """
        _response = self._client_wrapper.httpx_client.request(
            "bounties",
            method="POST",
            json={
                "accepted_submissions_limit": accepted_submissions_limit,
                "accepted_submissions_per_user_limit": accepted_submissions_per_user_limit,
                "account_id": account_id,
                "allowed_country_codes": allowed_country_codes,
                "business_goal_type": business_goal_type,
                "capture_spec": convert_and_respect_annotation_metadata(
                    object_=capture_spec, annotation=CreateBountiesRequestCaptureSpec, direction="write"
                ),
                "description": description,
                "experience_id": experience_id,
                "frequency": frequency,
                "gross_reward_amount": gross_reward_amount,
                "publish_at": publish_at,
                "publish_at_timezone": publish_at_timezone,
                "title": title,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Bounty,
                    parse_obj_as(
                        type_=Bounty,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> HttpResponse[Bounty]:
        """
        Retrieves a bounty by ID. Authentication is optional: a request with no credential reads the bounty when it is publicly visible — published or completed, and not restricted to a private experience's members. Bounties outside the caller's scope, and bounties not publicly visible to an anonymous caller, return `404`.

        Parameters
        ----------
        id : str
            Bounty ID (`bnty_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Bounty]
            bounty retrieved
        """
        _response = self._client_wrapper.httpx_client.request(
            f"bounties/{encode_path_param(id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Bounty,
                    parse_obj_as(
                        type_=Bounty,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def update(
        self,
        id: str,
        *,
        accepted_submissions_limit: typing.Optional[int] = OMIT,
        accepted_submissions_per_user_limit: typing.Optional[int] = OMIT,
        allowed_country_codes: typing.Optional[typing.Sequence[str]] = OMIT,
        business_goal_type: typing.Optional[UpdateBountiesRequestBusinessGoalType] = OMIT,
        description: typing.Optional[str] = OMIT,
        frequency: typing.Optional[UpdateBountiesRequestFrequency] = OMIT,
        gross_reward_amount: typing.Optional[float] = OMIT,
        publish_at: typing.Optional[str] = OMIT,
        publish_at_timezone: typing.Optional[str] = OMIT,
        title: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[Bounty]:
        """
        Updates a bounty. A published bounty accepts title, description, and country targeting while it is still open with nothing under review. A scheduled (not-yet-published) draft additionally accepts the reward, winner slots, and schedule.

        Parameters
        ----------
        id : str
            Bounty ID (`bnty_` tag).

        accepted_submissions_limit : typing.Optional[int]
            Scheduled drafts only. Number of submissions that can be accepted (winner slots).

        accepted_submissions_per_user_limit : typing.Optional[int]
            How many winner slots one worker can win. Defaults to `1`. Wins plus proofs awaiting review never exceed this number, and a worker runs one attempt at a time. Cannot exceed `accepted_submissions_limit`. Editable while the bounty is still open with nothing under review.

        allowed_country_codes : typing.Optional[typing.Sequence[str]]
            Replace the countries whose residents can work the bounty, as ISO 3166 alpha-2 codes. Empty means worldwide.

        business_goal_type : typing.Optional[UpdateBountiesRequestBusinessGoalType]
            What the poster wants the work to achieve, declared once here.

        description : typing.Optional[str]
            New full task instructions.

        frequency : typing.Optional[UpdateBountiesRequestFrequency]
            Scheduled drafts only. How often the schedule creates a new bounty.

        gross_reward_amount : typing.Optional[float]
            Scheduled drafts only. Gross bounty-pool amount (USD) escrowed per accepted submission. The escrowed total (this times accepted_submissions_limit) must stay at least $5.

        publish_at : typing.Optional[str]
            Scheduled drafts only. New ISO 8601 time to publish the draft. Must be in the future.

        publish_at_timezone : typing.Optional[str]
            Scheduled drafts only. IANA timezone for recurring occurrences.

        title : typing.Optional[str]
            New short name of the task.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Bounty]
            bounty updated
        """
        _response = self._client_wrapper.httpx_client.request(
            f"bounties/{encode_path_param(id)}",
            method="PATCH",
            json={
                "accepted_submissions_limit": accepted_submissions_limit,
                "accepted_submissions_per_user_limit": accepted_submissions_per_user_limit,
                "allowed_country_codes": allowed_country_codes,
                "business_goal_type": business_goal_type,
                "description": description,
                "frequency": frequency,
                "gross_reward_amount": gross_reward_amount,
                "publish_at": publish_at,
                "publish_at_timezone": publish_at_timezone,
                "title": title,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Bounty,
                    parse_obj_as(
                        type_=Bounty,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def cancel(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> HttpResponse[Bounty]:
        """
        Cancels a bounty. With no in-flight work, it cancels immediately and refunds the funder. Otherwise it stops new submissions and cancels once the in-flight work resolves and pays out. Repeating the request is a no-op. A bounty that already paid out every slot returns `400`.

        Parameters
        ----------
        id : str
            Bounty ID (`bnty_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[Bounty]
            cancellation requested
        """
        _response = self._client_wrapper.httpx_client.request(
            f"bounties/{encode_path_param(id)}/cancel",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Bounty,
                    parse_obj_as(
                        type_=Bounty,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawBountiesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        status: typing.Optional[ListBountiesRequestStatus] = None,
        business_goal_type: typing.Optional[ListBountiesRequestBusinessGoalType] = None,
        country: typing.Optional[str] = None,
        experience_id: typing.Optional[str] = None,
        query: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        created_before: typing.Optional[str] = None,
        order: typing.Optional[ListBountiesRequestOrder] = None,
        direction: typing.Optional[ListBountiesRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[BountyListItem, ListBountiesResponse]:
        """
        Lists bounties visible to the credential — for an account API key, the account's bounties including scheduled drafts; for a user token, the bounties the user can see and work.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Scope the list to this account (`biz_` tag). Requires read access to the account; account API keys may pass their own account or a connected account.

        user_id : typing.Optional[str]
            List the bounties this user participated in (`user_` tag). Must be the authenticated user.

        status : typing.Optional[ListBountiesRequestStatus]
            Filter by lifecycle state.

        business_goal_type : typing.Optional[ListBountiesRequestBusinessGoalType]
            Filter by the poster's declared goal. Bounties created before the goal taxonomy carry no goal and never match this filter.

        country : typing.Optional[str]
            Only bounties workable from this country, as an ISO 3166-1 alpha-2 code. Bounties with no country targeting are workable worldwide and always match.

        experience_id : typing.Optional[str]
            Only bounties posted to this forum experience, prefixed `exp_`. An unknown experience, or one outside the caller's scope, matches nothing.

        query : typing.Optional[str]
            Substring match on the bounty title or ID.

        created_after : typing.Optional[str]
            Only bounties created after this ISO 8601 timestamp.

        created_before : typing.Optional[str]
            Only bounties created before this ISO 8601 timestamp.

        order : typing.Optional[ListBountiesRequestOrder]
            Sort field.

        direction : typing.Optional[ListBountiesRequestDirection]
            Sort direction.

        first : typing.Optional[int]
            Number of bounties to return from the start of the window.

        after : typing.Optional[str]
            Cursor to paginate forwards from.

        last : typing.Optional[int]
            Number of bounties to return from the end of the window.

        before : typing.Optional[str]
            Cursor to paginate backwards from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[BountyListItem, ListBountiesResponse]
            bounties listed
        """
        _response = await self._client_wrapper.httpx_client.request(
            "bounties",
            method="GET",
            params={
                "account_id": account_id,
                "user_id": user_id,
                "status": status,
                "business_goal_type": business_goal_type,
                "country": country,
                "experience_id": experience_id,
                "query": query,
                "created_after": created_after,
                "created_before": created_before,
                "order": order,
                "direction": direction,
                "first": first,
                "after": after,
                "last": last,
                "before": before,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _parsed_response = typing.cast(
                    ListBountiesResponse,
                    parse_obj_as(
                        type_=ListBountiesResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                _items = _parsed_response.data
                _has_next = False
                _get_next = None
                if _parsed_response.page_info is not None:
                    _parsed_next = _parsed_response.page_info.end_cursor
                    _has_next = _parsed_next is not None and _parsed_next != ""

                    async def _get_next():
                        return await self.list(
                            account_id=account_id,
                            user_id=user_id,
                            status=status,
                            business_goal_type=business_goal_type,
                            country=country,
                            experience_id=experience_id,
                            query=query,
                            created_after=created_after,
                            created_before=created_before,
                            order=order,
                            direction=direction,
                            first=first,
                            after=_parsed_next,
                            last=last,
                            before=before,
                            request_options=request_options,
                        )

                return AsyncPager(has_next=_has_next, items=_items, get_next=_get_next, response=_parsed_response)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def create(
        self,
        *,
        description: str,
        gross_reward_amount: float,
        title: str,
        accepted_submissions_limit: typing.Optional[int] = OMIT,
        accepted_submissions_per_user_limit: typing.Optional[int] = OMIT,
        account_id: typing.Optional[str] = OMIT,
        allowed_country_codes: typing.Optional[typing.Sequence[str]] = OMIT,
        business_goal_type: typing.Optional[CreateBountiesRequestBusinessGoalType] = OMIT,
        capture_spec: typing.Optional[CreateBountiesRequestCaptureSpec] = OMIT,
        experience_id: typing.Optional[str] = OMIT,
        frequency: typing.Optional[CreateBountiesRequestFrequency] = OMIT,
        publish_at: typing.Optional[str] = OMIT,
        publish_at_timezone: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[Bounty]:
        """
        Creates a bounty and escrows its reward pool. Publishes immediately, or as a scheduled draft when you set `publish_at`.

        Parameters
        ----------
        description : str
            Full task instructions shown to workers.

        gross_reward_amount : float
            Gross bounty-pool amount (USD) escrowed per accepted submission, in whole dollars. Platform fees and affiliate shares are paid from this amount.

        title : str
            Short name of the task shown to workers.

        accepted_submissions_limit : typing.Optional[int]
            Number of submissions that can be accepted (winner slots). Defaults to 1. The escrowed total is `gross_reward_amount` times this limit and must be at least $5.

        accepted_submissions_per_user_limit : typing.Optional[int]
            How many winner slots one worker can win. Defaults to `1`. Wins plus proofs awaiting review never exceed this number, and a worker runs one attempt at a time. Cannot exceed `accepted_submissions_limit`.

        account_id : typing.Optional[str]
            Account whose balance funds the bounty pool (`biz_` tag). Defaults to the caller's personal balance. Requires permission to move the account's funds.

        allowed_country_codes : typing.Optional[typing.Sequence[str]]
            Countries whose residents can work the bounty, as ISO 3166 alpha-2 codes. Empty means worldwide.

        business_goal_type : typing.Optional[CreateBountiesRequestBusinessGoalType]
            What the poster wants the work to achieve, declared once here.

        capture_spec : typing.Optional[CreateBountiesRequestCaptureSpec]
            Per-bounty overrides of the served capture contract. Only accepted when `business_goal_type` is `data_capture`; omitted fields keep the platform defaults, and the resulting contract is echoed back as `capture_spec` on the bounty.

        experience_id : typing.Optional[str]
            Experience to host the bounty in (`exp_` tag). Any visibility — public for an open bounty, private for an invited one. Required unless account_id is set, in which case the bounty anchors in that account's public forum.

        frequency : typing.Optional[CreateBountiesRequestFrequency]
            How often the schedule creates a new bounty. Each occurrence is a separate bounty. Defaults to `once`; only applies with `publish_at`.

        publish_at : typing.Optional[str]
            ISO 8601 time to publish the bounty. When set, the bounty is created as a hidden draft and funded + published at this time instead of immediately.

        publish_at_timezone : typing.Optional[str]
            IANA timezone for recurring occurrences. Required when publish_at is set.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Bounty]
            bounty created
        """
        _response = await self._client_wrapper.httpx_client.request(
            "bounties",
            method="POST",
            json={
                "accepted_submissions_limit": accepted_submissions_limit,
                "accepted_submissions_per_user_limit": accepted_submissions_per_user_limit,
                "account_id": account_id,
                "allowed_country_codes": allowed_country_codes,
                "business_goal_type": business_goal_type,
                "capture_spec": convert_and_respect_annotation_metadata(
                    object_=capture_spec, annotation=CreateBountiesRequestCaptureSpec, direction="write"
                ),
                "description": description,
                "experience_id": experience_id,
                "frequency": frequency,
                "gross_reward_amount": gross_reward_amount,
                "publish_at": publish_at,
                "publish_at_timezone": publish_at_timezone,
                "title": title,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Bounty,
                    parse_obj_as(
                        type_=Bounty,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[Bounty]:
        """
        Retrieves a bounty by ID. Authentication is optional: a request with no credential reads the bounty when it is publicly visible — published or completed, and not restricted to a private experience's members. Bounties outside the caller's scope, and bounties not publicly visible to an anonymous caller, return `404`.

        Parameters
        ----------
        id : str
            Bounty ID (`bnty_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Bounty]
            bounty retrieved
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"bounties/{encode_path_param(id)}",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Bounty,
                    parse_obj_as(
                        type_=Bounty,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def update(
        self,
        id: str,
        *,
        accepted_submissions_limit: typing.Optional[int] = OMIT,
        accepted_submissions_per_user_limit: typing.Optional[int] = OMIT,
        allowed_country_codes: typing.Optional[typing.Sequence[str]] = OMIT,
        business_goal_type: typing.Optional[UpdateBountiesRequestBusinessGoalType] = OMIT,
        description: typing.Optional[str] = OMIT,
        frequency: typing.Optional[UpdateBountiesRequestFrequency] = OMIT,
        gross_reward_amount: typing.Optional[float] = OMIT,
        publish_at: typing.Optional[str] = OMIT,
        publish_at_timezone: typing.Optional[str] = OMIT,
        title: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[Bounty]:
        """
        Updates a bounty. A published bounty accepts title, description, and country targeting while it is still open with nothing under review. A scheduled (not-yet-published) draft additionally accepts the reward, winner slots, and schedule.

        Parameters
        ----------
        id : str
            Bounty ID (`bnty_` tag).

        accepted_submissions_limit : typing.Optional[int]
            Scheduled drafts only. Number of submissions that can be accepted (winner slots).

        accepted_submissions_per_user_limit : typing.Optional[int]
            How many winner slots one worker can win. Defaults to `1`. Wins plus proofs awaiting review never exceed this number, and a worker runs one attempt at a time. Cannot exceed `accepted_submissions_limit`. Editable while the bounty is still open with nothing under review.

        allowed_country_codes : typing.Optional[typing.Sequence[str]]
            Replace the countries whose residents can work the bounty, as ISO 3166 alpha-2 codes. Empty means worldwide.

        business_goal_type : typing.Optional[UpdateBountiesRequestBusinessGoalType]
            What the poster wants the work to achieve, declared once here.

        description : typing.Optional[str]
            New full task instructions.

        frequency : typing.Optional[UpdateBountiesRequestFrequency]
            Scheduled drafts only. How often the schedule creates a new bounty.

        gross_reward_amount : typing.Optional[float]
            Scheduled drafts only. Gross bounty-pool amount (USD) escrowed per accepted submission. The escrowed total (this times accepted_submissions_limit) must stay at least $5.

        publish_at : typing.Optional[str]
            Scheduled drafts only. New ISO 8601 time to publish the draft. Must be in the future.

        publish_at_timezone : typing.Optional[str]
            Scheduled drafts only. IANA timezone for recurring occurrences.

        title : typing.Optional[str]
            New short name of the task.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Bounty]
            bounty updated
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"bounties/{encode_path_param(id)}",
            method="PATCH",
            json={
                "accepted_submissions_limit": accepted_submissions_limit,
                "accepted_submissions_per_user_limit": accepted_submissions_per_user_limit,
                "allowed_country_codes": allowed_country_codes,
                "business_goal_type": business_goal_type,
                "description": description,
                "frequency": frequency,
                "gross_reward_amount": gross_reward_amount,
                "publish_at": publish_at,
                "publish_at_timezone": publish_at_timezone,
                "title": title,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Bounty,
                    parse_obj_as(
                        type_=Bounty,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def cancel(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[Bounty]:
        """
        Cancels a bounty. With no in-flight work, it cancels immediately and refunds the funder. Otherwise it stops new submissions and cancels once the in-flight work resolves and pays out. Repeating the request is a no-op. A bounty that already paid out every slot returns `400`.

        Parameters
        ----------
        id : str
            Bounty ID (`bnty_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[Bounty]
            cancellation requested
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"bounties/{encode_path_param(id)}/cancel",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    Bounty,
                    parse_obj_as(
                        type_=Bounty,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        V1ErrorResponse,
                        parse_obj_as(
                            type_=V1ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
