# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.promo_code import PromoCode
from ..types.promo_code_list_item import PromoCodeListItem
from .raw_client import AsyncRawPromoCodesClient, RawPromoCodesClient
from .types.create_promo_codes_request_base_currency import CreatePromoCodesRequestBaseCurrency
from .types.create_promo_codes_request_promo_type import CreatePromoCodesRequestPromoType
from .types.delete_promo_codes_response import DeletePromoCodesResponse
from .types.list_promo_codes_request_direction import ListPromoCodesRequestDirection
from .types.list_promo_codes_request_order import ListPromoCodesRequestOrder
from .types.list_promo_codes_request_status import ListPromoCodesRequestStatus
from .types.list_promo_codes_response import ListPromoCodesResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PromoCodesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPromoCodesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPromoCodesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPromoCodesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: str,
        status: typing.Optional[ListPromoCodesRequestStatus] = None,
        product_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        plan_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        order: typing.Optional[ListPromoCodesRequestOrder] = None,
        direction: typing.Optional[ListPromoCodesRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PromoCodeListItem, ListPromoCodesResponse]:
        """
        Lists promo codes for an account with cursor pagination, filters, and sorting.

        Parameters
        ----------
        account_id : str
            Account whose promo codes are listed (`biz_` tag).

        status : typing.Optional[ListPromoCodesRequestStatus]
            Promo-code status. `expired` groups inactive and archived codes.

        product_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only promo codes scoped to these product IDs.

        plan_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only promo codes scoped to these plan IDs.

        created_before : typing.Optional[dt.datetime]
            Only promo codes created before this ISO 8601 timestamp.

        created_after : typing.Optional[dt.datetime]
            Only promo codes created after this ISO 8601 timestamp.

        order : typing.Optional[ListPromoCodesRequestOrder]
            Sort field.

        direction : typing.Optional[ListPromoCodesRequestDirection]
            Sort direction.

        first : typing.Optional[int]
            Number of promo codes to return from the start of the window.

        after : typing.Optional[str]
            Cursor to paginate forwards from.

        last : typing.Optional[int]
            Number of promo codes to return from the end of the window.

        before : typing.Optional[str]
            Cursor to paginate backwards from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PromoCodeListItem, ListPromoCodesResponse]
            promo codes listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.promo_codes.list(
            account_id="account_id",
            product_ids=["prod_xxxxxxxxxxxxxx"],
            plan_ids=["plan_xxxxxxxxxxxxxx"],
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            status=status,
            product_ids=product_ids,
            plan_ids=plan_ids,
            created_before=created_before,
            created_after=created_after,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def create(
        self,
        *,
        account_id: str,
        amount_off: float,
        base_currency: CreatePromoCodesRequestBaseCurrency,
        code: str,
        new_users_only: bool,
        promo_duration_months: int,
        promo_type: CreatePromoCodesRequestPromoType,
        churned_users_only: typing.Optional[bool] = OMIT,
        existing_memberships_only: typing.Optional[bool] = OMIT,
        expires_at: typing.Optional[str] = OMIT,
        one_per_customer: typing.Optional[bool] = OMIT,
        plan_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        product_id: typing.Optional[str] = OMIT,
        stock: typing.Optional[int] = OMIT,
        unlimited_stock: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PromoCode:
        """
        Creates a promo code for an account. First-party sessions may attach an affiliate.

        Parameters
        ----------
        account_id : str

        amount_off : float

        base_currency : CreatePromoCodesRequestBaseCurrency

        code : str

        new_users_only : bool

        promo_duration_months : int

        promo_type : CreatePromoCodesRequestPromoType

        churned_users_only : typing.Optional[bool]

        existing_memberships_only : typing.Optional[bool]

        expires_at : typing.Optional[str]

        one_per_customer : typing.Optional[bool]

        plan_ids : typing.Optional[typing.Sequence[str]]

        product_id : typing.Optional[str]

        stock : typing.Optional[int]

        unlimited_stock : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PromoCode
            promo code created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.promo_codes.create(
            account_id="biz_xxxxxxxxxxxxxx",
            amount_off=25.0,
            base_currency="usd",
            code="AFFILIATE25",
            new_users_only=True,
            promo_duration_months=3,
            promo_type="percentage",
        )
        """
        _response = self._raw_client.create(
            account_id=account_id,
            amount_off=amount_off,
            base_currency=base_currency,
            code=code,
            new_users_only=new_users_only,
            promo_duration_months=promo_duration_months,
            promo_type=promo_type,
            churned_users_only=churned_users_only,
            existing_memberships_only=existing_memberships_only,
            expires_at=expires_at,
            one_per_customer=one_per_customer,
            plan_ids=plan_ids,
            product_id=product_id,
            stock=stock,
            unlimited_stock=unlimited_stock,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PromoCode:
        """
        Retrieves a promo code by ID.

        Parameters
        ----------
        id : str
            Promo code ID (`promo_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PromoCode
            promo code retrieved

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.promo_codes.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def delete(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DeletePromoCodesResponse:
        """
        Archives a promo code so it cannot be used in future checkouts.

        Parameters
        ----------
        id : str
            Promo code ID (`promo_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletePromoCodesResponse
            promo code deleted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.promo_codes.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, request_options=request_options)
        return _response.data

    def activate(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PromoCode:
        """
        Turns an inactive promo code back on so it can be redeemed at checkout.

        Parameters
        ----------
        id : str
            Promo code ID (`promo_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PromoCode
            promo code activated

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.promo_codes.activate(
            id="id",
        )
        """
        _response = self._raw_client.activate(id, request_options=request_options)
        return _response.data

    def deactivate(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PromoCode:
        """
        Turns off an active promo code so it can no longer be redeemed at checkout.

        Parameters
        ----------
        id : str
            Promo code ID (`promo_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PromoCode
            promo code deactivated

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.promo_codes.deactivate(
            id="id",
        )
        """
        _response = self._raw_client.deactivate(id, request_options=request_options)
        return _response.data


class AsyncPromoCodesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPromoCodesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPromoCodesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPromoCodesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: str,
        status: typing.Optional[ListPromoCodesRequestStatus] = None,
        product_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        plan_ids: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        order: typing.Optional[ListPromoCodesRequestOrder] = None,
        direction: typing.Optional[ListPromoCodesRequestDirection] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PromoCodeListItem, ListPromoCodesResponse]:
        """
        Lists promo codes for an account with cursor pagination, filters, and sorting.

        Parameters
        ----------
        account_id : str
            Account whose promo codes are listed (`biz_` tag).

        status : typing.Optional[ListPromoCodesRequestStatus]
            Promo-code status. `expired` groups inactive and archived codes.

        product_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only promo codes scoped to these product IDs.

        plan_ids : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Only promo codes scoped to these plan IDs.

        created_before : typing.Optional[dt.datetime]
            Only promo codes created before this ISO 8601 timestamp.

        created_after : typing.Optional[dt.datetime]
            Only promo codes created after this ISO 8601 timestamp.

        order : typing.Optional[ListPromoCodesRequestOrder]
            Sort field.

        direction : typing.Optional[ListPromoCodesRequestDirection]
            Sort direction.

        first : typing.Optional[int]
            Number of promo codes to return from the start of the window.

        after : typing.Optional[str]
            Cursor to paginate forwards from.

        last : typing.Optional[int]
            Number of promo codes to return from the end of the window.

        before : typing.Optional[str]
            Cursor to paginate backwards from.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PromoCodeListItem, ListPromoCodesResponse]
            promo codes listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.promo_codes.list(
                account_id="account_id",
                product_ids=["prod_xxxxxxxxxxxxxx"],
                plan_ids=["plan_xxxxxxxxxxxxxx"],
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            status=status,
            product_ids=product_ids,
            plan_ids=plan_ids,
            created_before=created_before,
            created_after=created_after,
            order=order,
            direction=direction,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        account_id: str,
        amount_off: float,
        base_currency: CreatePromoCodesRequestBaseCurrency,
        code: str,
        new_users_only: bool,
        promo_duration_months: int,
        promo_type: CreatePromoCodesRequestPromoType,
        churned_users_only: typing.Optional[bool] = OMIT,
        existing_memberships_only: typing.Optional[bool] = OMIT,
        expires_at: typing.Optional[str] = OMIT,
        one_per_customer: typing.Optional[bool] = OMIT,
        plan_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        product_id: typing.Optional[str] = OMIT,
        stock: typing.Optional[int] = OMIT,
        unlimited_stock: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PromoCode:
        """
        Creates a promo code for an account. First-party sessions may attach an affiliate.

        Parameters
        ----------
        account_id : str

        amount_off : float

        base_currency : CreatePromoCodesRequestBaseCurrency

        code : str

        new_users_only : bool

        promo_duration_months : int

        promo_type : CreatePromoCodesRequestPromoType

        churned_users_only : typing.Optional[bool]

        existing_memberships_only : typing.Optional[bool]

        expires_at : typing.Optional[str]

        one_per_customer : typing.Optional[bool]

        plan_ids : typing.Optional[typing.Sequence[str]]

        product_id : typing.Optional[str]

        stock : typing.Optional[int]

        unlimited_stock : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PromoCode
            promo code created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.promo_codes.create(
                account_id="biz_xxxxxxxxxxxxxx",
                amount_off=25.0,
                base_currency="usd",
                code="AFFILIATE25",
                new_users_only=True,
                promo_duration_months=3,
                promo_type="percentage",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            account_id=account_id,
            amount_off=amount_off,
            base_currency=base_currency,
            code=code,
            new_users_only=new_users_only,
            promo_duration_months=promo_duration_months,
            promo_type=promo_type,
            churned_users_only=churned_users_only,
            existing_memberships_only=existing_memberships_only,
            expires_at=expires_at,
            one_per_customer=one_per_customer,
            plan_ids=plan_ids,
            product_id=product_id,
            stock=stock,
            unlimited_stock=unlimited_stock,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PromoCode:
        """
        Retrieves a promo code by ID.

        Parameters
        ----------
        id : str
            Promo code ID (`promo_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PromoCode
            promo code retrieved

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.promo_codes.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def delete(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DeletePromoCodesResponse:
        """
        Archives a promo code so it cannot be used in future checkouts.

        Parameters
        ----------
        id : str
            Promo code ID (`promo_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeletePromoCodesResponse
            promo code deleted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.promo_codes.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(id, request_options=request_options)
        return _response.data

    async def activate(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PromoCode:
        """
        Turns an inactive promo code back on so it can be redeemed at checkout.

        Parameters
        ----------
        id : str
            Promo code ID (`promo_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PromoCode
            promo code activated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.promo_codes.activate(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.activate(id, request_options=request_options)
        return _response.data

    async def deactivate(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> PromoCode:
        """
        Turns off an active promo code so it can no longer be redeemed at checkout.

        Parameters
        ----------
        id : str
            Promo code ID (`promo_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PromoCode
            promo code deactivated

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.promo_codes.deactivate(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.deactivate(id, request_options=request_options)
        return _response.data
