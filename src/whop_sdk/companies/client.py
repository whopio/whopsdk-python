# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.company import Company
from ..types.company_list_item import CompanyListItem
from ..types.countries import Countries
from ..types.direction import Direction
from ..types.permission_system_roles import PermissionSystemRoles
from .raw_client import AsyncRawCompaniesClient, RawCompaniesClient
from .types.create_api_key_companies_request_permissions_item import CreateApiKeyCompaniesRequestPermissionsItem
from .types.create_api_key_companies_response import CreateApiKeyCompaniesResponse
from .types.create_companies_request_logo import CreateCompaniesRequestLogo
from .types.list_companies_response import ListCompaniesResponse
from .types.update_companies_request_banner_image import UpdateCompaniesRequestBannerImage
from .types.update_companies_request_logo import UpdateCompaniesRequestLogo
from .types.update_companies_request_social_links_item import UpdateCompaniesRequestSocialLinksItem

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class CompaniesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCompaniesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCompaniesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCompaniesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        parent_company_id: typing.Optional[str] = None,
        direction: typing.Optional[Direction] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[CompanyListItem, ListCompaniesResponse]:
        """
        Returns a paginated list of companies. When parent_company_id is provided, lists connected accounts under that platform. When omitted, lists companies the current user has access to.

        Required permissions:
         - `company:basic:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        parent_company_id : typing.Optional[str]
            The unique identifier of the parent platform company. When provided, lists connected accounts under that platform. Omit to list the current user's own companies.

        direction : typing.Optional[Direction]

        created_before : typing.Optional[dt.datetime]
            Only return companies created before this timestamp.

        created_after : typing.Optional[dt.datetime]
            Only return companies created after this timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[CompanyListItem, ListCompaniesResponse]
            A successful response

        Examples
        --------
        import datetime

        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.companies.list(
            first=42,
            last=42,
            created_before=datetime.datetime.fromisoformat(
                "2023-12-01 05:00:00+00:00",
            ),
            created_after=datetime.datetime.fromisoformat(
                "2023-12-01 05:00:00+00:00",
            ),
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            after=after,
            before=before,
            first=first,
            last=last,
            parent_company_id=parent_company_id,
            direction=direction,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )

    def create(
        self,
        *,
        title: str,
        country: typing.Optional[Countries] = OMIT,
        description: typing.Optional[str] = OMIT,
        email: typing.Optional[str] = OMIT,
        logo: typing.Optional[CreateCompaniesRequestLogo] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        parent_company_id: typing.Optional[str] = OMIT,
        send_customer_emails: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Company:
        """
        Create a new company. Pass parent_company_id to create a connected account under a platform, or omit it to create a company for the current user.

        Required permissions:
         - `company:create`
         - `company:basic:read`

        Parameters
        ----------
        title : str
            The display name of the company shown to customers.

        country : typing.Optional[Countries]
            The country the company is located in. Defaults to the parent company's country for connected accounts, or the owner's IP-derived country.

        description : typing.Optional[str]
            A promotional pitch displayed to potential customers on the company's store page.

        email : typing.Optional[str]
            The email address of the user who will own the connected account. Required when parent_company_id is provided.

        logo : typing.Optional[CreateCompaniesRequestLogo]
            The company's logo image. Accepts PNG, JPEG, or GIF format.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            A key-value JSON object of custom metadata to store on the company.

        parent_company_id : typing.Optional[str]
            The unique identifier of the parent platform company. When provided, creates a connected account under that platform. Omit to create a company for the current user.

        send_customer_emails : typing.Optional[bool]
            Whether Whop sends transactional emails to customers on behalf of this company. Only applies when creating a connected account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Company
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.companies.create(
            title="title",
        )
        """
        _response = self._raw_client.create(
            title=title,
            country=country,
            description=description,
            email=email,
            logo=logo,
            metadata=metadata,
            parent_company_id=parent_company_id,
            send_customer_emails=send_customer_emails,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Company:
        """
        Retrieves the details of an existing company.

        Required permissions:
         - `company:basic:read`

        Parameters
        ----------
        id : str
            The unique identifier or route slug of the company.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Company
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.companies.retrieve(
            id="biz_xxxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        affiliate_application_required: typing.Optional[bool] = OMIT,
        affiliate_instructions: typing.Optional[str] = OMIT,
        banner_image: typing.Optional[UpdateCompaniesRequestBannerImage] = OMIT,
        description: typing.Optional[str] = OMIT,
        featured_affiliate_product_id: typing.Optional[str] = OMIT,
        logo: typing.Optional[UpdateCompaniesRequestLogo] = OMIT,
        route: typing.Optional[str] = OMIT,
        send_customer_emails: typing.Optional[bool] = OMIT,
        social_links: typing.Optional[typing.Sequence[UpdateCompaniesRequestSocialLinksItem]] = OMIT,
        target_audience: typing.Optional[str] = OMIT,
        title: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Company:
        """
        Update a company's title, description, logo, and other settings.

        Required permissions:
         - `company:update`
         - `company:basic:read`

        Parameters
        ----------
        id : str
            The unique identifier of the company to update.

        affiliate_application_required : typing.Optional[bool]
            Whether prospective affiliates must submit an application before they can promote this company.

        affiliate_instructions : typing.Optional[str]
            Guidelines and instructions shown to affiliates explaining how to promote this company's products.

        banner_image : typing.Optional[UpdateCompaniesRequestBannerImage]
            The company's banner image. Accepts PNG or JPEG format.

        description : typing.Optional[str]
            A promotional pitch displayed to potential customers on the company's store page.

        featured_affiliate_product_id : typing.Optional[str]
            The ID of the product to feature on this company's affiliate page. Pass null to clear.

        logo : typing.Optional[UpdateCompaniesRequestLogo]
            The company's logo image. Accepts PNG, JPEG, or GIF format.

        route : typing.Optional[str]
            The unique URL slug for the company's store page. Must be lowercase and can include hyphens (e.g., 'my-company'). If not provided, the route will remain unchanged.

        send_customer_emails : typing.Optional[bool]
            Whether Whop sends transactional emails (receipts, renewals, cancelations) to customers on behalf of this company.

        social_links : typing.Optional[typing.Sequence[UpdateCompaniesRequestSocialLinksItem]]
            The social media links to display on the company's store page. Pass the full list of desired social links — any existing links not included will be removed.

        target_audience : typing.Optional[str]
            The target audience for this company (e.g., 'beginner day traders aged 18-25 looking to learn options').

        title : typing.Optional[str]
            The display name of the company shown to customers.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Company
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.companies.update(
            id="biz_xxxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.update(
            id,
            affiliate_application_required=affiliate_application_required,
            affiliate_instructions=affiliate_instructions,
            banner_image=banner_image,
            description=description,
            featured_affiliate_product_id=featured_affiliate_product_id,
            logo=logo,
            route=route,
            send_customer_emails=send_customer_emails,
            social_links=social_links,
            target_audience=target_audience,
            title=title,
            request_options=request_options,
        )
        return _response.data

    def create_api_key(
        self,
        parent_company_id: str,
        *,
        child_company_id: str,
        name: typing.Optional[str] = OMIT,
        permissions: typing.Optional[typing.Sequence[CreateApiKeyCompaniesRequestPermissionsItem]] = OMIT,
        role: typing.Optional[PermissionSystemRoles] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateApiKeyCompaniesResponse:
        """
        Create an API key for a connected account (child company) owned by a parent company.

        Parameters
        ----------
        parent_company_id : str
            The unique identifier of the parent platform company (e.g. 'biz_xxx').

        child_company_id : str
            The unique identifier of the connected account to create the API key for (e.g. 'biz_xxx').

        name : typing.Optional[str]
            A human-readable name for the API key, such as 'Production API Key'.

        permissions : typing.Optional[typing.Sequence[CreateApiKeyCompaniesRequestPermissionsItem]]
            Granular permission statements defining which actions this API key can perform. Either permissions or role must be provided.

        role : typing.Optional[PermissionSystemRoles]
            A system role to inherit permissions from (e.g. owner, admin, moderator). Either role or permissions must be provided.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateApiKeyCompaniesResponse
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.companies.create_api_key(
            parent_company_id="parent_company_id",
            child_company_id="child_company_id",
        )
        """
        _response = self._raw_client.create_api_key(
            parent_company_id,
            child_company_id=child_company_id,
            name=name,
            permissions=permissions,
            role=role,
            request_options=request_options,
        )
        return _response.data


class AsyncCompaniesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCompaniesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCompaniesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCompaniesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        parent_company_id: typing.Optional[str] = None,
        direction: typing.Optional[Direction] = None,
        created_before: typing.Optional[dt.datetime] = None,
        created_after: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[CompanyListItem, ListCompaniesResponse]:
        """
        Returns a paginated list of companies. When parent_company_id is provided, lists connected accounts under that platform. When omitted, lists companies the current user has access to.

        Required permissions:
         - `company:basic:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        parent_company_id : typing.Optional[str]
            The unique identifier of the parent platform company. When provided, lists connected accounts under that platform. Omit to list the current user's own companies.

        direction : typing.Optional[Direction]

        created_before : typing.Optional[dt.datetime]
            Only return companies created before this timestamp.

        created_after : typing.Optional[dt.datetime]
            Only return companies created after this timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[CompanyListItem, ListCompaniesResponse]
            A successful response

        Examples
        --------
        import asyncio
        import datetime

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.companies.list(
                first=42,
                last=42,
                created_before=datetime.datetime.fromisoformat(
                    "2023-12-01 05:00:00+00:00",
                ),
                created_after=datetime.datetime.fromisoformat(
                    "2023-12-01 05:00:00+00:00",
                ),
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            after=after,
            before=before,
            first=first,
            last=last,
            parent_company_id=parent_company_id,
            direction=direction,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        title: str,
        country: typing.Optional[Countries] = OMIT,
        description: typing.Optional[str] = OMIT,
        email: typing.Optional[str] = OMIT,
        logo: typing.Optional[CreateCompaniesRequestLogo] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        parent_company_id: typing.Optional[str] = OMIT,
        send_customer_emails: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Company:
        """
        Create a new company. Pass parent_company_id to create a connected account under a platform, or omit it to create a company for the current user.

        Required permissions:
         - `company:create`
         - `company:basic:read`

        Parameters
        ----------
        title : str
            The display name of the company shown to customers.

        country : typing.Optional[Countries]
            The country the company is located in. Defaults to the parent company's country for connected accounts, or the owner's IP-derived country.

        description : typing.Optional[str]
            A promotional pitch displayed to potential customers on the company's store page.

        email : typing.Optional[str]
            The email address of the user who will own the connected account. Required when parent_company_id is provided.

        logo : typing.Optional[CreateCompaniesRequestLogo]
            The company's logo image. Accepts PNG, JPEG, or GIF format.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            A key-value JSON object of custom metadata to store on the company.

        parent_company_id : typing.Optional[str]
            The unique identifier of the parent platform company. When provided, creates a connected account under that platform. Omit to create a company for the current user.

        send_customer_emails : typing.Optional[bool]
            Whether Whop sends transactional emails to customers on behalf of this company. Only applies when creating a connected account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Company
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.companies.create(
                title="title",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            title=title,
            country=country,
            description=description,
            email=email,
            logo=logo,
            metadata=metadata,
            parent_company_id=parent_company_id,
            send_customer_emails=send_customer_emails,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Company:
        """
        Retrieves the details of an existing company.

        Required permissions:
         - `company:basic:read`

        Parameters
        ----------
        id : str
            The unique identifier or route slug of the company.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Company
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.companies.retrieve(
                id="biz_xxxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        affiliate_application_required: typing.Optional[bool] = OMIT,
        affiliate_instructions: typing.Optional[str] = OMIT,
        banner_image: typing.Optional[UpdateCompaniesRequestBannerImage] = OMIT,
        description: typing.Optional[str] = OMIT,
        featured_affiliate_product_id: typing.Optional[str] = OMIT,
        logo: typing.Optional[UpdateCompaniesRequestLogo] = OMIT,
        route: typing.Optional[str] = OMIT,
        send_customer_emails: typing.Optional[bool] = OMIT,
        social_links: typing.Optional[typing.Sequence[UpdateCompaniesRequestSocialLinksItem]] = OMIT,
        target_audience: typing.Optional[str] = OMIT,
        title: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Company:
        """
        Update a company's title, description, logo, and other settings.

        Required permissions:
         - `company:update`
         - `company:basic:read`

        Parameters
        ----------
        id : str
            The unique identifier of the company to update.

        affiliate_application_required : typing.Optional[bool]
            Whether prospective affiliates must submit an application before they can promote this company.

        affiliate_instructions : typing.Optional[str]
            Guidelines and instructions shown to affiliates explaining how to promote this company's products.

        banner_image : typing.Optional[UpdateCompaniesRequestBannerImage]
            The company's banner image. Accepts PNG or JPEG format.

        description : typing.Optional[str]
            A promotional pitch displayed to potential customers on the company's store page.

        featured_affiliate_product_id : typing.Optional[str]
            The ID of the product to feature on this company's affiliate page. Pass null to clear.

        logo : typing.Optional[UpdateCompaniesRequestLogo]
            The company's logo image. Accepts PNG, JPEG, or GIF format.

        route : typing.Optional[str]
            The unique URL slug for the company's store page. Must be lowercase and can include hyphens (e.g., 'my-company'). If not provided, the route will remain unchanged.

        send_customer_emails : typing.Optional[bool]
            Whether Whop sends transactional emails (receipts, renewals, cancelations) to customers on behalf of this company.

        social_links : typing.Optional[typing.Sequence[UpdateCompaniesRequestSocialLinksItem]]
            The social media links to display on the company's store page. Pass the full list of desired social links — any existing links not included will be removed.

        target_audience : typing.Optional[str]
            The target audience for this company (e.g., 'beginner day traders aged 18-25 looking to learn options').

        title : typing.Optional[str]
            The display name of the company shown to customers.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Company
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.companies.update(
                id="biz_xxxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            affiliate_application_required=affiliate_application_required,
            affiliate_instructions=affiliate_instructions,
            banner_image=banner_image,
            description=description,
            featured_affiliate_product_id=featured_affiliate_product_id,
            logo=logo,
            route=route,
            send_customer_emails=send_customer_emails,
            social_links=social_links,
            target_audience=target_audience,
            title=title,
            request_options=request_options,
        )
        return _response.data

    async def create_api_key(
        self,
        parent_company_id: str,
        *,
        child_company_id: str,
        name: typing.Optional[str] = OMIT,
        permissions: typing.Optional[typing.Sequence[CreateApiKeyCompaniesRequestPermissionsItem]] = OMIT,
        role: typing.Optional[PermissionSystemRoles] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateApiKeyCompaniesResponse:
        """
        Create an API key for a connected account (child company) owned by a parent company.

        Parameters
        ----------
        parent_company_id : str
            The unique identifier of the parent platform company (e.g. 'biz_xxx').

        child_company_id : str
            The unique identifier of the connected account to create the API key for (e.g. 'biz_xxx').

        name : typing.Optional[str]
            A human-readable name for the API key, such as 'Production API Key'.

        permissions : typing.Optional[typing.Sequence[CreateApiKeyCompaniesRequestPermissionsItem]]
            Granular permission statements defining which actions this API key can perform. Either permissions or role must be provided.

        role : typing.Optional[PermissionSystemRoles]
            A system role to inherit permissions from (e.g. owner, admin, moderator). Either role or permissions must be provided.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateApiKeyCompaniesResponse
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.companies.create_api_key(
                parent_company_id="parent_company_id",
                child_company_id="child_company_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_api_key(
            parent_company_id,
            child_company_id=child_company_id,
            name=name,
            permissions=permissions,
            role=role,
            request_options=request_options,
        )
        return _response.data
