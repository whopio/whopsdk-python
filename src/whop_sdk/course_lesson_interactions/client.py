# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.course_lesson_interaction import CourseLessonInteraction
from ..types.course_lesson_interaction_list_item import CourseLessonInteractionListItem
from .raw_client import AsyncRawCourseLessonInteractionsClient, RawCourseLessonInteractionsClient
from .types.list_course_lesson_interactions_response import ListCourseLessonInteractionsResponse


class CourseLessonInteractionsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawCourseLessonInteractionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawCourseLessonInteractionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawCourseLessonInteractionsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        user_id: typing.Optional[str] = None,
        lesson_id: typing.Optional[str] = None,
        course_id: typing.Optional[str] = None,
        completed: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[CourseLessonInteractionListItem, ListCourseLessonInteractionsResponse]:
        """
        Returns a paginated list of lesson interactions, filtered by lesson, course, user, or completion status.

        Required permissions:
         - `courses:read`
         - `course_analytics:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        user_id : typing.Optional[str]
            The unique identifier of the user to filter lesson interactions for.

        lesson_id : typing.Optional[str]
            The unique identifier of the lesson to filter interactions for.

        course_id : typing.Optional[str]
            The unique identifier of the course to filter interactions for.

        completed : typing.Optional[bool]
            Whether to filter for completed or in-progress lesson interactions.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[CourseLessonInteractionListItem, ListCourseLessonInteractionsResponse]
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.course_lesson_interactions.list(
            first=42,
            last=42,
            user_id="user_xxxxxxxxxxxxx",
            lesson_id="lesn_xxxxxxxxxxxxx",
            course_id="cors_xxxxxxxxxxxxx",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            after=after,
            before=before,
            first=first,
            last=last,
            user_id=user_id,
            lesson_id=lesson_id,
            course_id=course_id,
            completed=completed,
            request_options=request_options,
        )

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> CourseLessonInteraction:
        """
        Retrieves the details of an existing course lesson interaction.

        Required permissions:
         - `courses:read`
         - `course_analytics:read`

        Parameters
        ----------
        id : str
            The unique identifier of the lesson interaction to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CourseLessonInteraction
            A successful response

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.course_lesson_interactions.retrieve(
            id="crsli_xxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data


class AsyncCourseLessonInteractionsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawCourseLessonInteractionsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawCourseLessonInteractionsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawCourseLessonInteractionsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        after: typing.Optional[str] = None,
        before: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        last: typing.Optional[int] = None,
        user_id: typing.Optional[str] = None,
        lesson_id: typing.Optional[str] = None,
        course_id: typing.Optional[str] = None,
        completed: typing.Optional[bool] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[CourseLessonInteractionListItem, ListCourseLessonInteractionsResponse]:
        """
        Returns a paginated list of lesson interactions, filtered by lesson, course, user, or completion status.

        Required permissions:
         - `courses:read`
         - `course_analytics:read`

        Parameters
        ----------
        after : typing.Optional[str]
            Returns the elements in the list that come after the specified cursor.

        before : typing.Optional[str]
            Returns the elements in the list that come before the specified cursor.

        first : typing.Optional[int]
            Returns the first _n_ elements from the list.

        last : typing.Optional[int]
            Returns the last _n_ elements from the list.

        user_id : typing.Optional[str]
            The unique identifier of the user to filter lesson interactions for.

        lesson_id : typing.Optional[str]
            The unique identifier of the lesson to filter interactions for.

        course_id : typing.Optional[str]
            The unique identifier of the course to filter interactions for.

        completed : typing.Optional[bool]
            Whether to filter for completed or in-progress lesson interactions.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[CourseLessonInteractionListItem, ListCourseLessonInteractionsResponse]
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.course_lesson_interactions.list(
                first=42,
                last=42,
                user_id="user_xxxxxxxxxxxxx",
                lesson_id="lesn_xxxxxxxxxxxxx",
                course_id="cors_xxxxxxxxxxxxx",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            after=after,
            before=before,
            first=first,
            last=last,
            user_id=user_id,
            lesson_id=lesson_id,
            course_id=course_id,
            completed=completed,
            request_options=request_options,
        )

    async def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> CourseLessonInteraction:
        """
        Retrieves the details of an existing course lesson interaction.

        Required permissions:
         - `courses:read`
         - `course_analytics:read`

        Parameters
        ----------
        id : str
            The unique identifier of the lesson interaction to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CourseLessonInteraction
            A successful response

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.course_lesson_interactions.retrieve(
                id="crsli_xxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data
