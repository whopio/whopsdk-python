# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.economic_intelligence import EconomicIntelligence
from .raw_client import AsyncRawEconomicIntelligenceClient, RawEconomicIntelligenceClient
from .types.list_economic_intelligence_request_status import ListEconomicIntelligenceRequestStatus
from .types.list_economic_intelligence_response import ListEconomicIntelligenceResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class EconomicIntelligenceClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawEconomicIntelligenceClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawEconomicIntelligenceClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawEconomicIntelligenceClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        status: typing.Optional[ListEconomicIntelligenceRequestStatus] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[EconomicIntelligence, ListEconomicIntelligenceResponse]:
        """
        Lists every recommendation the account has been given, newest first: requests the engine is still answering, cards ready to run, cards already run, and cards a newer one replaced. `status=ready` is what the dashboard shows; an account with nothing ready gets `generation_pending` true while cards are being generated, so poll until it clears.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        status : typing.Optional[ListEconomicIntelligenceRequestStatus]
            Only recommendations in this state. `ready` for the cards the owner can run now.

        first : typing.Optional[int]
            The number of recommendations to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns recommendations after this position.

        last : typing.Optional[int]
            The number of recommendations to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns recommendations before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[EconomicIntelligence, ListEconomicIntelligenceResponse]
            empty for a connected (child) account

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-11",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.economic_intelligence.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            status=status,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    def run(
        self,
        *,
        input: str,
        account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EconomicIntelligence:
        """
        Runs the engine toward what the owner wants, in their own words, such as "get more repeat buyers for my taurine supplement". The recommendation comes back right away with status `queued` and only the owner's `input` filled in; the engine moves it to `pending` while it works and then to `ready`, with the title and brief written, or to `failed`. Watch it in the list.

        Parameters
        ----------
        input : str
            What the owner wants, in their own words. Up to 1000 characters.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EconomicIntelligence
            recommendation queued

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-11",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.economic_intelligence.run(
            input="get more repeat buyers for my taurine supplement",
        )
        """
        _response = self._raw_client.run(input=input, account_id=account_id, request_options=request_options)
        return _response.data


class AsyncEconomicIntelligenceClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawEconomicIntelligenceClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawEconomicIntelligenceClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawEconomicIntelligenceClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        status: typing.Optional[ListEconomicIntelligenceRequestStatus] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[EconomicIntelligence, ListEconomicIntelligenceResponse]:
        """
        Lists every recommendation the account has been given, newest first: requests the engine is still answering, cards ready to run, cards already run, and cards a newer one replaced. `status=ready` is what the dashboard shows; an account with nothing ready gets `generation_pending` true while cards are being generated, so poll until it clears.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        status : typing.Optional[ListEconomicIntelligenceRequestStatus]
            Only recommendations in this state. `ready` for the cards the owner can run now.

        first : typing.Optional[int]
            The number of recommendations to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns recommendations after this position.

        last : typing.Optional[int]
            The number of recommendations to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns recommendations before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[EconomicIntelligence, ListEconomicIntelligenceResponse]
            empty for a connected (child) account

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-11",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.economic_intelligence.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            status=status,
            first=first,
            after=after,
            last=last,
            before=before,
            request_options=request_options,
        )

    async def run(
        self,
        *,
        input: str,
        account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EconomicIntelligence:
        """
        Runs the engine toward what the owner wants, in their own words, such as "get more repeat buyers for my taurine supplement". The recommendation comes back right away with status `queued` and only the owner's `input` filled in; the engine moves it to `pending` while it works and then to `ready`, with the title and brief written, or to `failed`. Watch it in the list.

        Parameters
        ----------
        input : str
            What the owner wants, in their own words. Up to 1000 characters.

        account_id : typing.Optional[str]
            Account ID, prefixed `biz_`. Defaults to the API key's own account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EconomicIntelligence
            recommendation queued

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-11",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.economic_intelligence.run(
                input="get more repeat buyers for my taurine supplement",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.run(input=input, account_id=account_id, request_options=request_options)
        return _response.data
