# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.waitlist_entry import WaitlistEntry
from .raw_client import AsyncRawWaitlistEntriesClient, RawWaitlistEntriesClient
from .types.approve_all_waitlist_entries_response import ApproveAllWaitlistEntriesResponse
from .types.create_waitlist_entries_request_custom_field_responses_item import (
    CreateWaitlistEntriesRequestCustomFieldResponsesItem,
)
from .types.list_waitlist_entries_request_direction import ListWaitlistEntriesRequestDirection
from .types.list_waitlist_entries_request_order import ListWaitlistEntriesRequestOrder
from .types.list_waitlist_entries_request_status import ListWaitlistEntriesRequestStatus
from .types.list_waitlist_entries_response import ListWaitlistEntriesResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class WaitlistEntriesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawWaitlistEntriesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawWaitlistEntriesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawWaitlistEntriesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        plan_id: typing.Optional[str] = None,
        account_id: typing.Optional[str] = None,
        product_id: typing.Optional[str] = None,
        status: typing.Optional[ListWaitlistEntriesRequestStatus] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        order: typing.Optional[ListWaitlistEntriesRequestOrder] = None,
        direction: typing.Optional[ListWaitlistEntriesRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[WaitlistEntry, ListWaitlistEntriesResponse]:
        """
        Lists visible waitlist signups. waitlist_entry:read grants the user's own signups; plan:waitlist:read grants signups for authorized seller accounts. With both permissions, returns their union. Account credentials are limited to their account. Filters narrow this set.

        Parameters
        ----------
        first : typing.Optional[int]
            Number of results to return from the start of the range.

        after : typing.Optional[str]
            Return results after this cursor. Use `page_info.end_cursor` from the previous response to fetch the next page.

        last : typing.Optional[int]
            Number of results to return from the end of the range.

        before : typing.Optional[str]
            Return results before this cursor. Use `page_info.start_cursor` from the previous response to fetch the previous page.

        plan_id : typing.Optional[str]
            Only return signups for this plan, prefixed `plan_`.

        account_id : typing.Optional[str]
            Only return signups submitted to this seller account, prefixed `biz_`.

        product_id : typing.Optional[str]
            Only return signups for plans on this product, prefixed `prod_`.

        status : typing.Optional[ListWaitlistEntriesRequestStatus]
            Only return signups in this state. Canceled signups are returned only when `status` is `canceled`.

        created_before : typing.Optional[str]
            Only return signups submitted at or before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Only return signups submitted at or after this ISO 8601 timestamp.

        order : typing.Optional[ListWaitlistEntriesRequestOrder]
            The field to sort results by. Defaults to `created_at`.

        direction : typing.Optional[ListWaitlistEntriesRequestDirection]
            The sort direction for results. Defaults to descending.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[WaitlistEntry, ListWaitlistEntriesResponse]
            own signups only

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.waitlist_entries.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            first=first,
            after=after,
            last=last,
            before=before,
            plan_id=plan_id,
            account_id=account_id,
            product_id=product_id,
            status=status,
            created_before=created_before,
            created_after=created_after,
            order=order,
            direction=direction,
            request_options=request_options,
        )

    def create(
        self,
        *,
        plan_id: str,
        custom_field_responses: typing.Optional[
            typing.Sequence[CreateWaitlistEntriesRequestCustomFieldResponsesItem]
        ] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> WaitlistEntry:
        """
        Joins a free waitlist plan as the authenticated user. Requires waitlist_entry:create. Repeated joins return the existing pending entry, or an approved entry with a valid membership. Paid plans are rejected; no payment method is collected and no membership is granted.

        Parameters
        ----------
        plan_id : str
            The free waitlist plan to join, prefixed `plan_`.

        custom_field_responses : typing.Optional[typing.Sequence[CreateWaitlistEntriesRequestCustomFieldResponsesItem]]
            Answers to the plan's checkout questions. Every required question must be answered.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the signup. Max 50 keys, 100 chars per key, 500 chars per string value. Ignored when the request returns an existing signup.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            joined

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.waitlist_entries.create(
            plan_id="plan_xxxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.create(
            plan_id=plan_id,
            custom_field_responses=custom_field_responses,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def approve_all(
        self,
        *,
        account_id: str,
        plan_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApproveAllWaitlistEntriesResponse:
        """
        Queues approval of every pending signup for an account, optionally narrowed to a plan. Requires plan:waitlist:manage. Paid signups may charge saved payment methods. Approval runs asynchronously: list signups with `status` set to `pending` to follow progress, and retrieve a signup to read its outcome. Signups created after this request are excluded.

        Parameters
        ----------
        account_id : str
            The seller account whose pending signups to approve, prefixed `biz_`.

        plan_id : typing.Optional[str]
            Only approve signups for this plan, prefixed `plan_`. Omit to include every waitlist plan on the account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApproveAllWaitlistEntriesResponse
            approval queued

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.waitlist_entries.approve_all(
            account_id="biz_xxxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.approve_all(
            account_id=account_id, plan_id=plan_id, request_options=request_options
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> WaitlistEntry:
        """
        Retrieves a signup owned by the caller with waitlist_entry:read, or submitted to an account they can read with plan:waitlist:read.

        Parameters
        ----------
        id : str
            Waitlist signup ID, prefixed `entry_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            signup

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.waitlist_entries.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def approve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> WaitlistEntry:
        """
        Queues approval of a pending signup. Requires plan:waitlist:manage on its seller account. Paid signups may charge their saved payment method. Returns the signup's current state; retrieve it to read `status` and `approval_failure_reason` after processing.

        Parameters
        ----------
        id : str
            Waitlist signup ID, prefixed `entry_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            current signup state

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.waitlist_entries.approve(
            id="id",
        )
        """
        _response = self._raw_client.approve(id, request_options=request_options)
        return _response.data

    def cancel(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> WaitlistEntry:
        """
        Withdraws the caller's pending personal signup. Requires waitlist_entry:cancel. Does not cancel an approved membership.

        Parameters
        ----------
        id : str
            Waitlist signup ID, prefixed `entry_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            current signup state

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.waitlist_entries.cancel(
            id="id",
        )
        """
        _response = self._raw_client.cancel(id, request_options=request_options)
        return _response.data

    def deny(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> WaitlistEntry:
        """
        Denies a pending signup. Requires plan:waitlist:manage on its seller account.

        Parameters
        ----------
        id : str
            Waitlist signup ID, prefixed `entry_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            current signup state

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.waitlist_entries.deny(
            id="id",
        )
        """
        _response = self._raw_client.deny(id, request_options=request_options)
        return _response.data


class AsyncWaitlistEntriesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawWaitlistEntriesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawWaitlistEntriesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawWaitlistEntriesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        plan_id: typing.Optional[str] = None,
        account_id: typing.Optional[str] = None,
        product_id: typing.Optional[str] = None,
        status: typing.Optional[ListWaitlistEntriesRequestStatus] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        order: typing.Optional[ListWaitlistEntriesRequestOrder] = None,
        direction: typing.Optional[ListWaitlistEntriesRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[WaitlistEntry, ListWaitlistEntriesResponse]:
        """
        Lists visible waitlist signups. waitlist_entry:read grants the user's own signups; plan:waitlist:read grants signups for authorized seller accounts. With both permissions, returns their union. Account credentials are limited to their account. Filters narrow this set.

        Parameters
        ----------
        first : typing.Optional[int]
            Number of results to return from the start of the range.

        after : typing.Optional[str]
            Return results after this cursor. Use `page_info.end_cursor` from the previous response to fetch the next page.

        last : typing.Optional[int]
            Number of results to return from the end of the range.

        before : typing.Optional[str]
            Return results before this cursor. Use `page_info.start_cursor` from the previous response to fetch the previous page.

        plan_id : typing.Optional[str]
            Only return signups for this plan, prefixed `plan_`.

        account_id : typing.Optional[str]
            Only return signups submitted to this seller account, prefixed `biz_`.

        product_id : typing.Optional[str]
            Only return signups for plans on this product, prefixed `prod_`.

        status : typing.Optional[ListWaitlistEntriesRequestStatus]
            Only return signups in this state. Canceled signups are returned only when `status` is `canceled`.

        created_before : typing.Optional[str]
            Only return signups submitted at or before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Only return signups submitted at or after this ISO 8601 timestamp.

        order : typing.Optional[ListWaitlistEntriesRequestOrder]
            The field to sort results by. Defaults to `created_at`.

        direction : typing.Optional[ListWaitlistEntriesRequestDirection]
            The sort direction for results. Defaults to descending.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[WaitlistEntry, ListWaitlistEntriesResponse]
            own signups only

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.waitlist_entries.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            first=first,
            after=after,
            last=last,
            before=before,
            plan_id=plan_id,
            account_id=account_id,
            product_id=product_id,
            status=status,
            created_before=created_before,
            created_after=created_after,
            order=order,
            direction=direction,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        plan_id: str,
        custom_field_responses: typing.Optional[
            typing.Sequence[CreateWaitlistEntriesRequestCustomFieldResponsesItem]
        ] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> WaitlistEntry:
        """
        Joins a free waitlist plan as the authenticated user. Requires waitlist_entry:create. Repeated joins return the existing pending entry, or an approved entry with a valid membership. Paid plans are rejected; no payment method is collected and no membership is granted.

        Parameters
        ----------
        plan_id : str
            The free waitlist plan to join, prefixed `plan_`.

        custom_field_responses : typing.Optional[typing.Sequence[CreateWaitlistEntriesRequestCustomFieldResponsesItem]]
            Answers to the plan's checkout questions. Every required question must be answered.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value pairs to store on the signup. Max 50 keys, 100 chars per key, 500 chars per string value. Ignored when the request returns an existing signup.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            joined

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.waitlist_entries.create(
                plan_id="plan_xxxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            plan_id=plan_id,
            custom_field_responses=custom_field_responses,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def approve_all(
        self,
        *,
        account_id: str,
        plan_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ApproveAllWaitlistEntriesResponse:
        """
        Queues approval of every pending signup for an account, optionally narrowed to a plan. Requires plan:waitlist:manage. Paid signups may charge saved payment methods. Approval runs asynchronously: list signups with `status` set to `pending` to follow progress, and retrieve a signup to read its outcome. Signups created after this request are excluded.

        Parameters
        ----------
        account_id : str
            The seller account whose pending signups to approve, prefixed `biz_`.

        plan_id : typing.Optional[str]
            Only approve signups for this plan, prefixed `plan_`. Omit to include every waitlist plan on the account.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ApproveAllWaitlistEntriesResponse
            approval queued

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.waitlist_entries.approve_all(
                account_id="biz_xxxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.approve_all(
            account_id=account_id, plan_id=plan_id, request_options=request_options
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> WaitlistEntry:
        """
        Retrieves a signup owned by the caller with waitlist_entry:read, or submitted to an account they can read with plan:waitlist:read.

        Parameters
        ----------
        id : str
            Waitlist signup ID, prefixed `entry_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            signup

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.waitlist_entries.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def approve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> WaitlistEntry:
        """
        Queues approval of a pending signup. Requires plan:waitlist:manage on its seller account. Paid signups may charge their saved payment method. Returns the signup's current state; retrieve it to read `status` and `approval_failure_reason` after processing.

        Parameters
        ----------
        id : str
            Waitlist signup ID, prefixed `entry_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            current signup state

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.waitlist_entries.approve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.approve(id, request_options=request_options)
        return _response.data

    async def cancel(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> WaitlistEntry:
        """
        Withdraws the caller's pending personal signup. Requires waitlist_entry:cancel. Does not cancel an approved membership.

        Parameters
        ----------
        id : str
            Waitlist signup ID, prefixed `entry_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            current signup state

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.waitlist_entries.cancel(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.cancel(id, request_options=request_options)
        return _response.data

    async def deny(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> WaitlistEntry:
        """
        Denies a pending signup. Requires plan:waitlist:manage on its seller account.

        Parameters
        ----------
        id : str
            Waitlist signup ID, prefixed `entry_`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WaitlistEntry
            current signup state

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-23",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.waitlist_entries.deny(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.deny(id, request_options=request_options)
        return _response.data
