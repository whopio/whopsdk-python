# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.social_account import SocialAccount
from ..types.social_account_post import SocialAccountPost
from .raw_client import AsyncRawSocialAccountsClient, RawSocialAccountsClient
from .types.connect_social_accounts_request_platform import ConnectSocialAccountsRequestPlatform
from .types.connect_social_accounts_request_scopes_item import ConnectSocialAccountsRequestScopesItem
from .types.connect_social_accounts_response import ConnectSocialAccountsResponse
from .types.create_social_accounts_request_platform import CreateSocialAccountsRequestPlatform
from .types.delete_social_accounts_response import DeleteSocialAccountsResponse
from .types.lead_forms_social_accounts_response import LeadFormsSocialAccountsResponse
from .types.list_social_accounts_request_direction import ListSocialAccountsRequestDirection
from .types.list_social_accounts_request_order import ListSocialAccountsRequestOrder
from .types.list_social_accounts_request_platform import ListSocialAccountsRequestPlatform
from .types.list_social_accounts_request_scopes_item import ListSocialAccountsRequestScopesItem
from .types.list_social_accounts_response import ListSocialAccountsResponse
from .types.posts_social_accounts_response import PostsSocialAccountsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class SocialAccountsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawSocialAccountsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawSocialAccountsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSocialAccountsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        platform: typing.Optional[ListSocialAccountsRequestPlatform] = None,
        verified: typing.Optional[bool] = None,
        scopes: typing.Optional[
            typing.Union[ListSocialAccountsRequestScopesItem, typing.Sequence[ListSocialAccountsRequestScopesItem]]
        ] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListSocialAccountsRequestOrder] = None,
        direction: typing.Optional[ListSocialAccountsRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[SocialAccount, ListSocialAccountsResponse]:
        """
        Lists the social accounts linked to an account or user.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The Account that the social accounts are connected to. Provide either this or user_id.

        user_id : typing.Optional[str]
            The User that the social accounts are connected to. Provide either this or account_id.

        platform : typing.Optional[ListSocialAccountsRequestPlatform]
            Only return social accounts for the platform that is specified.

        verified : typing.Optional[bool]
            Only return social accounts that are verified on the platform.

        scopes : typing.Optional[typing.Union[ListSocialAccountsRequestScopesItem, typing.Sequence[ListSocialAccountsRequestScopesItem]]]
            Only return social accounts that have these scopes.

        first : typing.Optional[int]
            The number of social accounts to return.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            The number of social accounts to return from the end of the range.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        order : typing.Optional[ListSocialAccountsRequestOrder]
            The field to sort social accounts by.

        direction : typing.Optional[ListSocialAccountsRequestDirection]
            Sort direction.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[SocialAccount, ListSocialAccountsResponse]
            filtered to accounts holding the requested scope

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.social_accounts.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            user_id=user_id,
            platform=platform,
            verified=verified,
            scopes=scopes,
            first=first,
            after=after,
            last=last,
            before=before,
            order=order,
            direction=direction,
            request_options=request_options,
        )

    def create(
        self,
        *,
        platform: CreateSocialAccountsRequestPlatform,
        account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SocialAccount:
        """
        Creates or returns a Whop-managed Facebook page for an account.

        Parameters
        ----------
        platform : CreateSocialAccountsRequestPlatform
            The platform to create the social account on. `facebook` requires the account's `banner_image`, `logo`, and `description`; configure them with [Update Account](/api-reference/beta/accounts/update-account).

        account_id : typing.Optional[str]
            The Account (biz_ identifier) to create the social account for. An account-scoped API key may omit this to default to its own account. Account API keys cannot update their own account's branding through Update Account; use a user-authenticated path.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SocialAccount
            social account created

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.social_accounts.create(
            platform="facebook",
        )
        """
        _response = self._raw_client.create(platform=platform, account_id=account_id, request_options=request_options)
        return _response.data

    def connect(
        self,
        *,
        platform: ConnectSocialAccountsRequestPlatform,
        account_id: typing.Optional[str] = OMIT,
        redirect_url: typing.Optional[str] = OMIT,
        scopes: typing.Optional[typing.Sequence[ConnectSocialAccountsRequestScopesItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ConnectSocialAccountsResponse:
        """
        Starts an OAuth connection flow and returns an authorize_url where the user can connect a social account.

        Parameters
        ----------
        platform : ConnectSocialAccountsRequestPlatform
            The platform to connect the social account on. Supported options are `meta_business` and `tiktok`.

        account_id : typing.Optional[str]
            The Account (biz_ identifier) to connect the social account for. An account-scoped API key may omit this to default to its own account.

        redirect_url : typing.Optional[str]
            The Whop URL to redirect the user to after they finish connecting.

        scopes : typing.Optional[typing.Sequence[ConnectSocialAccountsRequestScopesItem]]
            Capabilities to grant for the connected social account. Use `advertise` when connecting a Meta Business or TikTok account for ads.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ConnectSocialAccountsResponse
            authorize url returned

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.social_accounts.connect(
            platform="meta_business",
        )
        """
        _response = self._raw_client.connect(
            platform=platform,
            account_id=account_id,
            redirect_url=redirect_url,
            scopes=scopes,
            request_options=request_options,
        )
        return _response.data

    def delete(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeleteSocialAccountsResponse:
        """
        Disconnects a social account from an account or user without deleting the underlying platform account.

        Parameters
        ----------
        id : str
            The ID of the social account to disconnect.

        account_id : typing.Optional[str]
            The Account that the social account is connected to. Provide either this or user_id.

        user_id : typing.Optional[str]
            The User that the social account is connected to. Provide either this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteSocialAccountsResponse
            user social account disconnected

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.social_accounts.delete(
            id="id",
        )
        """
        _response = self._raw_client.delete(id, account_id=account_id, user_id=user_id, request_options=request_options)
        return _response.data

    def lead_forms(
        self, id: str, *, account_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> LeadFormsSocialAccountsResponse:
        """
        Lists the active lead (instant) forms that already exist on a connected Facebook page, so an ad can reuse one as its `lead_gen_form_id` instead of authoring a new form. Every active form comes back in a single response — the list is not paginated.

        Parameters
        ----------
        id : str
            The social account (a sacc_ identifier) whose lead forms to list.

        account_id : str
            The Account (a biz_ identifier) the social account is connected to.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LeadFormsSocialAccountsResponse
            lead forms listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.social_accounts.lead_forms(
            id="id",
            account_id="account_id",
        )
        """
        _response = self._raw_client.lead_forms(id, account_id=account_id, request_options=request_options)
        return _response.data

    def posts(
        self,
        id: str,
        *,
        account_id: str,
        post_id: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[SocialAccountPost, PostsSocialAccountsResponse]:
        """
        Lists the existing posts of a connected Facebook page, Instagram account, or TikTok account.

        Parameters
        ----------
        id : str
            The social account (a sacc_ identifier) whose posts to list.

        account_id : str
            The Account (a biz_ identifier) the social account is connected to.

        post_id : typing.Optional[str]
            Return only the single post with this platform id, instead of the full list.

        first : typing.Optional[int]
            The number of posts to return.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[SocialAccountPost, PostsSocialAccountsResponse]
            single post returned when post_id is given

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.social_accounts.posts(
            id="id",
            account_id="account_id",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.posts(
            id, account_id=account_id, post_id=post_id, first=first, after=after, request_options=request_options
        )


class AsyncSocialAccountsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawSocialAccountsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSocialAccountsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSocialAccountsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        platform: typing.Optional[ListSocialAccountsRequestPlatform] = None,
        verified: typing.Optional[bool] = None,
        scopes: typing.Optional[
            typing.Union[ListSocialAccountsRequestScopesItem, typing.Sequence[ListSocialAccountsRequestScopesItem]]
        ] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListSocialAccountsRequestOrder] = None,
        direction: typing.Optional[ListSocialAccountsRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[SocialAccount, ListSocialAccountsResponse]:
        """
        Lists the social accounts linked to an account or user.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The Account that the social accounts are connected to. Provide either this or user_id.

        user_id : typing.Optional[str]
            The User that the social accounts are connected to. Provide either this or account_id.

        platform : typing.Optional[ListSocialAccountsRequestPlatform]
            Only return social accounts for the platform that is specified.

        verified : typing.Optional[bool]
            Only return social accounts that are verified on the platform.

        scopes : typing.Optional[typing.Union[ListSocialAccountsRequestScopesItem, typing.Sequence[ListSocialAccountsRequestScopesItem]]]
            Only return social accounts that have these scopes.

        first : typing.Optional[int]
            The number of social accounts to return.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        last : typing.Optional[int]
            The number of social accounts to return from the end of the range.

        before : typing.Optional[str]
            Cursor to fetch the page before (from page_info.start_cursor).

        order : typing.Optional[ListSocialAccountsRequestOrder]
            The field to sort social accounts by.

        direction : typing.Optional[ListSocialAccountsRequestDirection]
            Sort direction.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[SocialAccount, ListSocialAccountsResponse]
            filtered to accounts holding the requested scope

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.social_accounts.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            user_id=user_id,
            platform=platform,
            verified=verified,
            scopes=scopes,
            first=first,
            after=after,
            last=last,
            before=before,
            order=order,
            direction=direction,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        platform: CreateSocialAccountsRequestPlatform,
        account_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SocialAccount:
        """
        Creates or returns a Whop-managed Facebook page for an account.

        Parameters
        ----------
        platform : CreateSocialAccountsRequestPlatform
            The platform to create the social account on. `facebook` requires the account's `banner_image`, `logo`, and `description`; configure them with [Update Account](/api-reference/beta/accounts/update-account).

        account_id : typing.Optional[str]
            The Account (biz_ identifier) to create the social account for. An account-scoped API key may omit this to default to its own account. Account API keys cannot update their own account's branding through Update Account; use a user-authenticated path.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SocialAccount
            social account created

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.social_accounts.create(
                platform="facebook",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            platform=platform, account_id=account_id, request_options=request_options
        )
        return _response.data

    async def connect(
        self,
        *,
        platform: ConnectSocialAccountsRequestPlatform,
        account_id: typing.Optional[str] = OMIT,
        redirect_url: typing.Optional[str] = OMIT,
        scopes: typing.Optional[typing.Sequence[ConnectSocialAccountsRequestScopesItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ConnectSocialAccountsResponse:
        """
        Starts an OAuth connection flow and returns an authorize_url where the user can connect a social account.

        Parameters
        ----------
        platform : ConnectSocialAccountsRequestPlatform
            The platform to connect the social account on. Supported options are `meta_business` and `tiktok`.

        account_id : typing.Optional[str]
            The Account (biz_ identifier) to connect the social account for. An account-scoped API key may omit this to default to its own account.

        redirect_url : typing.Optional[str]
            The Whop URL to redirect the user to after they finish connecting.

        scopes : typing.Optional[typing.Sequence[ConnectSocialAccountsRequestScopesItem]]
            Capabilities to grant for the connected social account. Use `advertise` when connecting a Meta Business or TikTok account for ads.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ConnectSocialAccountsResponse
            authorize url returned

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.social_accounts.connect(
                platform="meta_business",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.connect(
            platform=platform,
            account_id=account_id,
            redirect_url=redirect_url,
            scopes=scopes,
            request_options=request_options,
        )
        return _response.data

    async def delete(
        self,
        id: str,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeleteSocialAccountsResponse:
        """
        Disconnects a social account from an account or user without deleting the underlying platform account.

        Parameters
        ----------
        id : str
            The ID of the social account to disconnect.

        account_id : typing.Optional[str]
            The Account that the social account is connected to. Provide either this or user_id.

        user_id : typing.Optional[str]
            The User that the social account is connected to. Provide either this or account_id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteSocialAccountsResponse
            user social account disconnected

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.social_accounts.delete(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(
            id, account_id=account_id, user_id=user_id, request_options=request_options
        )
        return _response.data

    async def lead_forms(
        self, id: str, *, account_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> LeadFormsSocialAccountsResponse:
        """
        Lists the active lead (instant) forms that already exist on a connected Facebook page, so an ad can reuse one as its `lead_gen_form_id` instead of authoring a new form. Every active form comes back in a single response — the list is not paginated.

        Parameters
        ----------
        id : str
            The social account (a sacc_ identifier) whose lead forms to list.

        account_id : str
            The Account (a biz_ identifier) the social account is connected to.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LeadFormsSocialAccountsResponse
            lead forms listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.social_accounts.lead_forms(
                id="id",
                account_id="account_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.lead_forms(id, account_id=account_id, request_options=request_options)
        return _response.data

    async def posts(
        self,
        id: str,
        *,
        account_id: str,
        post_id: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[SocialAccountPost, PostsSocialAccountsResponse]:
        """
        Lists the existing posts of a connected Facebook page, Instagram account, or TikTok account.

        Parameters
        ----------
        id : str
            The social account (a sacc_ identifier) whose posts to list.

        account_id : str
            The Account (a biz_ identifier) the social account is connected to.

        post_id : typing.Optional[str]
            Return only the single post with this platform id, instead of the full list.

        first : typing.Optional[int]
            The number of posts to return.

        after : typing.Optional[str]
            Cursor to fetch the page after (from page_info.end_cursor).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[SocialAccountPost, PostsSocialAccountsResponse]
            single post returned when post_id is given

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-08-21-1",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.social_accounts.posts(
                id="id",
                account_id="account_id",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.posts(
            id, account_id=account_id, post_id=post_id, first=first, after=after, request_options=request_options
        )
