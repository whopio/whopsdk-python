# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.datetime_utils import serialize_datetime
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..errors.bad_request_error import BadRequestError
from ..errors.forbidden_error import ForbiddenError
from ..errors.not_found_error import NotFoundError
from ..errors.unauthorized_error import UnauthorizedError
from .types.list_financial_activity_request_line_types_item import ListFinancialActivityRequestLineTypesItem
from .types.list_financial_activity_response import ListFinancialActivityResponse
from pydantic import ValidationError


class RawFinancialActivityClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        include_owned_accounts: typing.Optional[bool] = None,
        include_resource: typing.Optional[bool] = None,
        line_types: typing.Optional[
            typing.Union[
                ListFinancialActivityRequestLineTypesItem, typing.Sequence[ListFinancialActivityRequestLineTypesItem]
            ]
        ] = None,
        currency: typing.Optional[str] = None,
        posted_after: typing.Optional[dt.datetime] = None,
        posted_before: typing.Optional[dt.datetime] = None,
        available_after: typing.Optional[dt.date] = None,
        available_before: typing.Optional[dt.date] = None,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[ListFinancialActivityResponse]:
        """
        Returns an account's or user's activity feed: every movement of money in or out.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        include_owned_accounts : typing.Optional[bool]
            When true, aggregates the authenticated user's personal ledger with the businesses they own (owner role with balance read) into one feed. Requires user_id to be the authenticated user; cannot be combined with account_id or the settlement-date filters. Each returned row includes the owning `account`.

        include_resource : typing.Optional[bool]
            Whether to include the `resource` field in the response or not. Consider passing `false` if you need a fast response without as many rich details.

        line_types : typing.Optional[typing.Union[ListFinancialActivityRequestLineTypesItem, typing.Sequence[ListFinancialActivityRequestLineTypesItem]]]
            Optional ledger line categories to include. Some categories (for example `onchain_deposit`, which covers inbound crypto deposits such as MoonPay onramps) are only returned when explicitly requested here.

        currency : typing.Optional[str]
            Optional currency code filter, for example `usd`.

        posted_after : typing.Optional[dt.datetime]
            Only include rows posted after this ISO 8601 timestamp.

        posted_before : typing.Optional[dt.datetime]
            Only include rows posted before this ISO 8601 timestamp.

        available_after : typing.Optional[dt.date]
            Only include rows whose funds became withdrawable on or after this `YYYY-MM-DD` settlement date (UTC), distinct from posted_at. Requires currency.

        available_before : typing.Optional[dt.date]
            Only include rows whose funds became withdrawable on or before this `YYYY-MM-DD` settlement date (UTC). Set equal to available_after for a single day. Requires currency.

        limit : typing.Optional[int]
            Maximum number of rows to return.

        cursor : typing.Optional[str]
            Cursor returned by the previous page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[ListFinancialActivityResponse]
            activities listed for a user
        """
        _response = self._client_wrapper.httpx_client.request(
            "financial-activity",
            method="GET",
            params={
                "account_id": account_id,
                "user_id": user_id,
                "include_owned_accounts": include_owned_accounts,
                "include_resource": include_resource,
                "line_types": line_types,
                "currency": currency,
                "posted_after": serialize_datetime(posted_after) if posted_after is not None else None,
                "posted_before": serialize_datetime(posted_before) if posted_before is not None else None,
                "available_after": str(available_after) if available_after is not None else None,
                "available_before": str(available_before) if available_before is not None else None,
                "limit": limit,
                "cursor": cursor,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListFinancialActivityResponse,
                    parse_obj_as(
                        type_=ListFinancialActivityResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawFinancialActivityClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        include_owned_accounts: typing.Optional[bool] = None,
        include_resource: typing.Optional[bool] = None,
        line_types: typing.Optional[
            typing.Union[
                ListFinancialActivityRequestLineTypesItem, typing.Sequence[ListFinancialActivityRequestLineTypesItem]
            ]
        ] = None,
        currency: typing.Optional[str] = None,
        posted_after: typing.Optional[dt.datetime] = None,
        posted_before: typing.Optional[dt.datetime] = None,
        available_after: typing.Optional[dt.date] = None,
        available_before: typing.Optional[dt.date] = None,
        limit: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[ListFinancialActivityResponse]:
        """
        Returns an account's or user's activity feed: every movement of money in or out.

        Parameters
        ----------
        account_id : typing.Optional[str]
            The owning account ID (a biz_ identifier). Provide this or user_id.

        user_id : typing.Optional[str]
            The owning user ID (a user_ identifier). Provide this or account_id.

        include_owned_accounts : typing.Optional[bool]
            When true, aggregates the authenticated user's personal ledger with the businesses they own (owner role with balance read) into one feed. Requires user_id to be the authenticated user; cannot be combined with account_id or the settlement-date filters. Each returned row includes the owning `account`.

        include_resource : typing.Optional[bool]
            Whether to include the `resource` field in the response or not. Consider passing `false` if you need a fast response without as many rich details.

        line_types : typing.Optional[typing.Union[ListFinancialActivityRequestLineTypesItem, typing.Sequence[ListFinancialActivityRequestLineTypesItem]]]
            Optional ledger line categories to include. Some categories (for example `onchain_deposit`, which covers inbound crypto deposits such as MoonPay onramps) are only returned when explicitly requested here.

        currency : typing.Optional[str]
            Optional currency code filter, for example `usd`.

        posted_after : typing.Optional[dt.datetime]
            Only include rows posted after this ISO 8601 timestamp.

        posted_before : typing.Optional[dt.datetime]
            Only include rows posted before this ISO 8601 timestamp.

        available_after : typing.Optional[dt.date]
            Only include rows whose funds became withdrawable on or after this `YYYY-MM-DD` settlement date (UTC), distinct from posted_at. Requires currency.

        available_before : typing.Optional[dt.date]
            Only include rows whose funds became withdrawable on or before this `YYYY-MM-DD` settlement date (UTC). Set equal to available_after for a single day. Requires currency.

        limit : typing.Optional[int]
            Maximum number of rows to return.

        cursor : typing.Optional[str]
            Cursor returned by the previous page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[ListFinancialActivityResponse]
            activities listed for a user
        """
        _response = await self._client_wrapper.httpx_client.request(
            "financial-activity",
            method="GET",
            params={
                "account_id": account_id,
                "user_id": user_id,
                "include_owned_accounts": include_owned_accounts,
                "include_resource": include_resource,
                "line_types": line_types,
                "currency": currency,
                "posted_after": serialize_datetime(posted_after) if posted_after is not None else None,
                "posted_before": serialize_datetime(posted_before) if posted_before is not None else None,
                "available_after": str(available_after) if available_after is not None else None,
                "available_before": str(available_before) if available_before is not None else None,
                "limit": limit,
                "cursor": cursor,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    ListFinancialActivityResponse,
                    parse_obj_as(
                        type_=ListFinancialActivityResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise BadRequestError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 401:
                raise UnauthorizedError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 403:
                raise ForbiddenError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        typing.Any,
                        parse_obj_as(
                            type_=typing.Any,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
