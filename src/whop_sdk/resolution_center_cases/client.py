# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.resolution_center_case import ResolutionCenterCase
from ..types.resolution_event import ResolutionEvent
from .raw_client import AsyncRawResolutionCenterCasesClient, RawResolutionCenterCasesClient
from .types.accept_resolution_center_cases_request_attachments_item import (
    AcceptResolutionCenterCasesRequestAttachmentsItem,
)
from .types.appeal_resolution_center_cases_request_attachments_item import (
    AppealResolutionCenterCasesRequestAttachmentsItem,
)
from .types.create_resolution_center_cases_request_attachments_item import (
    CreateResolutionCenterCasesRequestAttachmentsItem,
)
from .types.create_resolution_center_cases_request_reason import CreateResolutionCenterCasesRequestReason
from .types.deny_resolution_center_cases_request_attachments_item import DenyResolutionCenterCasesRequestAttachmentsItem
from .types.events_resolution_center_cases_response import EventsResolutionCenterCasesResponse
from .types.list_resolution_center_cases_request_direction import ListResolutionCenterCasesRequestDirection
from .types.list_resolution_center_cases_request_order import ListResolutionCenterCasesRequestOrder
from .types.list_resolution_center_cases_request_outcome_item import ListResolutionCenterCasesRequestOutcomeItem
from .types.list_resolution_center_cases_request_reason_item import ListResolutionCenterCasesRequestReasonItem
from .types.list_resolution_center_cases_request_status_item import ListResolutionCenterCasesRequestStatusItem
from .types.list_resolution_center_cases_response import ListResolutionCenterCasesResponse
from .types.reply_resolution_center_cases_request_attachments_item import (
    ReplyResolutionCenterCasesRequestAttachmentsItem,
)
from .types.request_info_resolution_center_cases_request_attachments_item import (
    RequestInfoResolutionCenterCasesRequestAttachmentsItem,
)
from .types.summary_resolution_center_cases_request_groups_item import SummaryResolutionCenterCasesRequestGroupsItem
from .types.summary_resolution_center_cases_request_outcome_item import SummaryResolutionCenterCasesRequestOutcomeItem
from .types.summary_resolution_center_cases_request_reason_item import SummaryResolutionCenterCasesRequestReasonItem
from .types.summary_resolution_center_cases_request_status_item import SummaryResolutionCenterCasesRequestStatusItem
from .types.summary_resolution_center_cases_response import SummaryResolutionCenterCasesResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ResolutionCenterCasesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawResolutionCenterCasesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawResolutionCenterCasesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawResolutionCenterCasesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListResolutionCenterCasesRequestOrder] = None,
        direction: typing.Optional[ListResolutionCenterCasesRequestDirection] = None,
        status: typing.Optional[
            typing.Union[
                ListResolutionCenterCasesRequestStatusItem, typing.Sequence[ListResolutionCenterCasesRequestStatusItem]
            ]
        ] = None,
        reason: typing.Optional[
            typing.Union[
                ListResolutionCenterCasesRequestReasonItem, typing.Sequence[ListResolutionCenterCasesRequestReasonItem]
            ]
        ] = None,
        outcome: typing.Optional[
            typing.Union[
                ListResolutionCenterCasesRequestOutcomeItem,
                typing.Sequence[ListResolutionCenterCasesRequestOutcomeItem],
            ]
        ] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ResolutionCenterCase, ListResolutionCenterCasesResponse]:
        """
        Lists resolution center cases. Without `account_id` you get every case you can read — the ones you opened as a buyer and every account you are a team member of; the filters narrow that list.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Only cases filed against this account (`biz_` tag). With read access to the account this lists its whole queue; without, only the cases you opened against it.

        user_id : typing.Optional[str]
            Only cases opened by this customer — a `user_` tag, or `me` for the calling user. It narrows what you can already read, so `me` lists the cases you opened without the ones on accounts you are a team member of.

        first : typing.Optional[int]
            The number of cases to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns cases after this position.

        last : typing.Optional[int]
            The number of cases to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns cases before this position.

        order : typing.Optional[ListResolutionCenterCasesRequestOrder]
            The field to sort cases by.

        direction : typing.Optional[ListResolutionCenterCasesRequestDirection]
            Sort direction.

        status : typing.Optional[typing.Union[ListResolutionCenterCasesRequestStatusItem, typing.Sequence[ListResolutionCenterCasesRequestStatusItem]]]
            Only cases in these statuses. Repeat the parameter to pass several — one paginated list covers all of them.

        reason : typing.Optional[typing.Union[ListResolutionCenterCasesRequestReasonItem, typing.Sequence[ListResolutionCenterCasesRequestReasonItem]]]
            Only cases opened for these reasons. Repeat the parameter to pass several.

        outcome : typing.Optional[typing.Union[ListResolutionCenterCasesRequestOutcomeItem, typing.Sequence[ListResolutionCenterCasesRequestOutcomeItem]]]
            Only closed cases that ended these ways. Repeat the parameter to pass several.

        created_before : typing.Optional[str]
            Only cases created before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Only cases created after this ISO 8601 timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ResolutionCenterCase, ListResolutionCenterCasesResponse]
            cases listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.resolution_center_cases.list()
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            account_id=account_id,
            user_id=user_id,
            first=first,
            after=after,
            last=last,
            before=before,
            order=order,
            direction=direction,
            status=status,
            reason=reason,
            outcome=outcome,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )

    def create(
        self,
        *,
        message: str,
        reason: CreateResolutionCenterCasesRequestReason,
        receipt_id: str,
        attachments: typing.Optional[typing.Sequence[CreateResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Opens a case, as the customer, against one of your own payments. Provide the payment (`receipt_id`), the `reason`, and a `message`.

        Parameters
        ----------
        message : str
            The customer's explanation.

        reason : CreateResolutionCenterCasesRequestReason
            What went wrong. Uses the same vocabulary as `/disputes`.

        receipt_id : str
            The payment to open the case against (`pay_` tag).

        attachments : typing.Optional[typing.Sequence[CreateResolutionCenterCasesRequestAttachmentsItem]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case opened

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.create(
            message="The mobile detailer never showed up for the Ceramic Coating appointment.",
            reason="fraudulent",
            receipt_id="pay_xxxxxxxxxxxxxx",
        )
        """
        _response = self._raw_client.create(
            message=message,
            reason=reason,
            receipt_id=receipt_id,
            attachments=attachments,
            request_options=request_options,
        )
        return _response.data

    def summary(
        self,
        *,
        groups: typing.Optional[
            typing.Union[
                SummaryResolutionCenterCasesRequestGroupsItem,
                typing.Sequence[SummaryResolutionCenterCasesRequestGroupsItem],
            ]
        ] = None,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        status: typing.Optional[
            typing.Union[
                SummaryResolutionCenterCasesRequestStatusItem,
                typing.Sequence[SummaryResolutionCenterCasesRequestStatusItem],
            ]
        ] = None,
        reason: typing.Optional[
            typing.Union[
                SummaryResolutionCenterCasesRequestReasonItem,
                typing.Sequence[SummaryResolutionCenterCasesRequestReasonItem],
            ]
        ] = None,
        outcome: typing.Optional[
            typing.Union[
                SummaryResolutionCenterCasesRequestOutcomeItem,
                typing.Sequence[SummaryResolutionCenterCasesRequestOutcomeItem],
            ]
        ] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SummaryResolutionCenterCasesResponse:
        """
        Aggregates the same cases `GET /resolution_center_cases` lists, using the same filters. Use it to build status tabs and issue filters without paging the whole list.

        Parameters
        ----------
        groups : typing.Optional[typing.Union[SummaryResolutionCenterCasesRequestGroupsItem, typing.Sequence[SummaryResolutionCenterCasesRequestGroupsItem]]]
            Which breakdowns to return, keyed by these names under `groups`. Repeat the parameter to ask for several; omit it for all of them.

        account_id : typing.Optional[str]
            The account to summarize cases for (`biz_` tag).

        user_id : typing.Optional[str]
            Only cases opened by this customer — a `user_` tag, or `me` for the calling user.

        status : typing.Optional[typing.Union[SummaryResolutionCenterCasesRequestStatusItem, typing.Sequence[SummaryResolutionCenterCasesRequestStatusItem]]]
            Only cases in these statuses.

        reason : typing.Optional[typing.Union[SummaryResolutionCenterCasesRequestReasonItem, typing.Sequence[SummaryResolutionCenterCasesRequestReasonItem]]]
            Only cases opened for these reasons.

        outcome : typing.Optional[typing.Union[SummaryResolutionCenterCasesRequestOutcomeItem, typing.Sequence[SummaryResolutionCenterCasesRequestOutcomeItem]]]
            Only closed cases that ended these ways.

        created_before : typing.Optional[str]
            Only count cases created before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Only count cases created after this ISO 8601 timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SummaryResolutionCenterCasesResponse
            summary returned

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.summary()
        """
        _response = self._raw_client.summary(
            groups=groups,
            account_id=account_id,
            user_id=user_id,
            status=status,
            reason=reason,
            outcome=outcome,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> ResolutionCenterCase:
        """
        Retrieves a single resolution center case with its full event timeline.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case returned

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def accept(
        self,
        id: str,
        *,
        attachments: typing.Optional[typing.Sequence[AcceptResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        message: typing.Optional[str] = OMIT,
        terminate_membership: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Accepts the case in the customer's favor, as the merchant: refunds the payment in full and closes the case.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        attachments : typing.Optional[typing.Sequence[AcceptResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        message : typing.Optional[str]
            An optional note to the customer, recorded on the case timeline.

        terminate_membership : typing.Optional[bool]
            Whether to also terminate the customer's membership.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case accepted

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.accept(
            id="id",
        )
        """
        _response = self._raw_client.accept(
            id,
            attachments=attachments,
            message=message,
            terminate_membership=terminate_membership,
            request_options=request_options,
        )
        return _response.data

    def appeal(
        self,
        id: str,
        *,
        message: str,
        attachments: typing.Optional[typing.Sequence[AppealResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Appeals a decision, as the customer, on a case that closed in the merchant's favor. Escalates the case to Whop for platform review. A case can be appealed once.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        message : str
            Why you are appealing the decision.

        attachments : typing.Optional[typing.Sequence[AppealResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case appealed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.appeal(
            id="id",
            message="The coating is already flaking on the hood two weeks later.",
        )
        """
        _response = self._raw_client.appeal(
            id, message=message, attachments=attachments, request_options=request_options
        )
        return _response.data

    def deny(
        self,
        id: str,
        *,
        message: str,
        attachments: typing.Optional[typing.Sequence[DenyResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Denies the case, as the merchant: rejects the claim and closes the case with no refund.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        message : str
            Why the claim is being denied. Shown to the customer.

        attachments : typing.Optional[typing.Sequence[DenyResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case denied

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.deny(
            id="id",
            message="The ceramic coating was applied and the vehicle was collected on 2026-01-05.",
        )
        """
        _response = self._raw_client.deny(id, message=message, attachments=attachments, request_options=request_options)
        return _response.data

    def events(
        self,
        id: str,
        *,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[ResolutionEvent, EventsResolutionCenterCasesResponse]:
        """
        Lists the case timeline, newest first. Events the viewer is not allowed to see are omitted — a customer reads the customer-visible timeline, the merchant reads the full one.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        first : typing.Optional[int]
            The number of events to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns events after this position.

        last : typing.Optional[int]
            The number of events to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns events before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[ResolutionEvent, EventsResolutionCenterCasesResponse]
            events listed

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        response = client.resolution_center_cases.events(
            id="id",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.events(
            id, first=first, after=after, last=last, before=before, request_options=request_options
        )

    def reply(
        self,
        id: str,
        *,
        message: str,
        attachments: typing.Optional[typing.Sequence[ReplyResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Replies to an open request for information on the case. As the merchant this answers Whop's request (valid while the case awaits your information); as the customer it provides the information requested from you. The actor is resolved from the credential.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        message : str
            The reply to add to the case.

        attachments : typing.Optional[typing.Sequence[ReplyResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            reply added

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.reply(
            id="id",
            message="Here are the before and after photos from the Burnet Rd bay.",
        )
        """
        _response = self._raw_client.reply(
            id, message=message, attachments=attachments, request_options=request_options
        )
        return _response.data

    def request_info(
        self,
        id: str,
        *,
        attachments: typing.Optional[typing.Sequence[RequestInfoResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        message: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Asks the customer for more information, as the merchant. Allowed up to 3 times per case before you must accept or deny it.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        attachments : typing.Optional[typing.Sequence[RequestInfoResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        message : typing.Optional[str]
            What you need from the customer.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            information requested

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.request_info(
            id="id",
        )
        """
        _response = self._raw_client.request_info(
            id, attachments=attachments, message=message, request_options=request_options
        )
        return _response.data

    def withdraw(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> ResolutionCenterCase:
        """
        Withdraws (cancels) a case you opened, as the customer. Only possible while the case is still open.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case withdrawn

        Examples
        --------
        from whop_sdk import Whop

        client = Whop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )
        client.resolution_center_cases.withdraw(
            id="id",
        )
        """
        _response = self._raw_client.withdraw(id, request_options=request_options)
        return _response.data


class AsyncResolutionCenterCasesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawResolutionCenterCasesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawResolutionCenterCasesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawResolutionCenterCasesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        order: typing.Optional[ListResolutionCenterCasesRequestOrder] = None,
        direction: typing.Optional[ListResolutionCenterCasesRequestDirection] = None,
        status: typing.Optional[
            typing.Union[
                ListResolutionCenterCasesRequestStatusItem, typing.Sequence[ListResolutionCenterCasesRequestStatusItem]
            ]
        ] = None,
        reason: typing.Optional[
            typing.Union[
                ListResolutionCenterCasesRequestReasonItem, typing.Sequence[ListResolutionCenterCasesRequestReasonItem]
            ]
        ] = None,
        outcome: typing.Optional[
            typing.Union[
                ListResolutionCenterCasesRequestOutcomeItem,
                typing.Sequence[ListResolutionCenterCasesRequestOutcomeItem],
            ]
        ] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ResolutionCenterCase, ListResolutionCenterCasesResponse]:
        """
        Lists resolution center cases. Without `account_id` you get every case you can read — the ones you opened as a buyer and every account you are a team member of; the filters narrow that list.

        Parameters
        ----------
        account_id : typing.Optional[str]
            Only cases filed against this account (`biz_` tag). With read access to the account this lists its whole queue; without, only the cases you opened against it.

        user_id : typing.Optional[str]
            Only cases opened by this customer — a `user_` tag, or `me` for the calling user. It narrows what you can already read, so `me` lists the cases you opened without the ones on accounts you are a team member of.

        first : typing.Optional[int]
            The number of cases to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns cases after this position.

        last : typing.Optional[int]
            The number of cases to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns cases before this position.

        order : typing.Optional[ListResolutionCenterCasesRequestOrder]
            The field to sort cases by.

        direction : typing.Optional[ListResolutionCenterCasesRequestDirection]
            Sort direction.

        status : typing.Optional[typing.Union[ListResolutionCenterCasesRequestStatusItem, typing.Sequence[ListResolutionCenterCasesRequestStatusItem]]]
            Only cases in these statuses. Repeat the parameter to pass several — one paginated list covers all of them.

        reason : typing.Optional[typing.Union[ListResolutionCenterCasesRequestReasonItem, typing.Sequence[ListResolutionCenterCasesRequestReasonItem]]]
            Only cases opened for these reasons. Repeat the parameter to pass several.

        outcome : typing.Optional[typing.Union[ListResolutionCenterCasesRequestOutcomeItem, typing.Sequence[ListResolutionCenterCasesRequestOutcomeItem]]]
            Only closed cases that ended these ways. Repeat the parameter to pass several.

        created_before : typing.Optional[str]
            Only cases created before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Only cases created after this ISO 8601 timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ResolutionCenterCase, ListResolutionCenterCasesResponse]
            cases listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.resolution_center_cases.list()
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            account_id=account_id,
            user_id=user_id,
            first=first,
            after=after,
            last=last,
            before=before,
            order=order,
            direction=direction,
            status=status,
            reason=reason,
            outcome=outcome,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        message: str,
        reason: CreateResolutionCenterCasesRequestReason,
        receipt_id: str,
        attachments: typing.Optional[typing.Sequence[CreateResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Opens a case, as the customer, against one of your own payments. Provide the payment (`receipt_id`), the `reason`, and a `message`.

        Parameters
        ----------
        message : str
            The customer's explanation.

        reason : CreateResolutionCenterCasesRequestReason
            What went wrong. Uses the same vocabulary as `/disputes`.

        receipt_id : str
            The payment to open the case against (`pay_` tag).

        attachments : typing.Optional[typing.Sequence[CreateResolutionCenterCasesRequestAttachmentsItem]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case opened

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.create(
                message="The mobile detailer never showed up for the Ceramic Coating appointment.",
                reason="fraudulent",
                receipt_id="pay_xxxxxxxxxxxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            message=message,
            reason=reason,
            receipt_id=receipt_id,
            attachments=attachments,
            request_options=request_options,
        )
        return _response.data

    async def summary(
        self,
        *,
        groups: typing.Optional[
            typing.Union[
                SummaryResolutionCenterCasesRequestGroupsItem,
                typing.Sequence[SummaryResolutionCenterCasesRequestGroupsItem],
            ]
        ] = None,
        account_id: typing.Optional[str] = None,
        user_id: typing.Optional[str] = None,
        status: typing.Optional[
            typing.Union[
                SummaryResolutionCenterCasesRequestStatusItem,
                typing.Sequence[SummaryResolutionCenterCasesRequestStatusItem],
            ]
        ] = None,
        reason: typing.Optional[
            typing.Union[
                SummaryResolutionCenterCasesRequestReasonItem,
                typing.Sequence[SummaryResolutionCenterCasesRequestReasonItem],
            ]
        ] = None,
        outcome: typing.Optional[
            typing.Union[
                SummaryResolutionCenterCasesRequestOutcomeItem,
                typing.Sequence[SummaryResolutionCenterCasesRequestOutcomeItem],
            ]
        ] = None,
        created_before: typing.Optional[str] = None,
        created_after: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SummaryResolutionCenterCasesResponse:
        """
        Aggregates the same cases `GET /resolution_center_cases` lists, using the same filters. Use it to build status tabs and issue filters without paging the whole list.

        Parameters
        ----------
        groups : typing.Optional[typing.Union[SummaryResolutionCenterCasesRequestGroupsItem, typing.Sequence[SummaryResolutionCenterCasesRequestGroupsItem]]]
            Which breakdowns to return, keyed by these names under `groups`. Repeat the parameter to ask for several; omit it for all of them.

        account_id : typing.Optional[str]
            The account to summarize cases for (`biz_` tag).

        user_id : typing.Optional[str]
            Only cases opened by this customer — a `user_` tag, or `me` for the calling user.

        status : typing.Optional[typing.Union[SummaryResolutionCenterCasesRequestStatusItem, typing.Sequence[SummaryResolutionCenterCasesRequestStatusItem]]]
            Only cases in these statuses.

        reason : typing.Optional[typing.Union[SummaryResolutionCenterCasesRequestReasonItem, typing.Sequence[SummaryResolutionCenterCasesRequestReasonItem]]]
            Only cases opened for these reasons.

        outcome : typing.Optional[typing.Union[SummaryResolutionCenterCasesRequestOutcomeItem, typing.Sequence[SummaryResolutionCenterCasesRequestOutcomeItem]]]
            Only closed cases that ended these ways.

        created_before : typing.Optional[str]
            Only count cases created before this ISO 8601 timestamp.

        created_after : typing.Optional[str]
            Only count cases created after this ISO 8601 timestamp.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SummaryResolutionCenterCasesResponse
            summary returned

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.summary()


        asyncio.run(main())
        """
        _response = await self._raw_client.summary(
            groups=groups,
            account_id=account_id,
            user_id=user_id,
            status=status,
            reason=reason,
            outcome=outcome,
            created_before=created_before,
            created_after=created_after,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ResolutionCenterCase:
        """
        Retrieves a single resolution center case with its full event timeline.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case returned

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def accept(
        self,
        id: str,
        *,
        attachments: typing.Optional[typing.Sequence[AcceptResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        message: typing.Optional[str] = OMIT,
        terminate_membership: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Accepts the case in the customer's favor, as the merchant: refunds the payment in full and closes the case.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        attachments : typing.Optional[typing.Sequence[AcceptResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        message : typing.Optional[str]
            An optional note to the customer, recorded on the case timeline.

        terminate_membership : typing.Optional[bool]
            Whether to also terminate the customer's membership.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case accepted

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.accept(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.accept(
            id,
            attachments=attachments,
            message=message,
            terminate_membership=terminate_membership,
            request_options=request_options,
        )
        return _response.data

    async def appeal(
        self,
        id: str,
        *,
        message: str,
        attachments: typing.Optional[typing.Sequence[AppealResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Appeals a decision, as the customer, on a case that closed in the merchant's favor. Escalates the case to Whop for platform review. A case can be appealed once.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        message : str
            Why you are appealing the decision.

        attachments : typing.Optional[typing.Sequence[AppealResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case appealed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.appeal(
                id="id",
                message="The coating is already flaking on the hood two weeks later.",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.appeal(
            id, message=message, attachments=attachments, request_options=request_options
        )
        return _response.data

    async def deny(
        self,
        id: str,
        *,
        message: str,
        attachments: typing.Optional[typing.Sequence[DenyResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Denies the case, as the merchant: rejects the claim and closes the case with no refund.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        message : str
            Why the claim is being denied. Shown to the customer.

        attachments : typing.Optional[typing.Sequence[DenyResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case denied

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.deny(
                id="id",
                message="The ceramic coating was applied and the vehicle was collected on 2026-01-05.",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.deny(
            id, message=message, attachments=attachments, request_options=request_options
        )
        return _response.data

    async def events(
        self,
        id: str,
        *,
        first: typing.Optional[int] = None,
        after: typing.Optional[str] = None,
        last: typing.Optional[int] = None,
        before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[ResolutionEvent, EventsResolutionCenterCasesResponse]:
        """
        Lists the case timeline, newest first. Events the viewer is not allowed to see are omitted — a customer reads the customer-visible timeline, the merchant reads the full one.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        first : typing.Optional[int]
            The number of events to return (default 20, max 100).

        after : typing.Optional[str]
            A cursor; returns events after this position.

        last : typing.Optional[int]
            The number of events to return from the end of the range.

        before : typing.Optional[str]
            A cursor; returns events before this position.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[ResolutionEvent, EventsResolutionCenterCasesResponse]
            events listed

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            response = await client.resolution_center_cases.events(
                id="id",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.events(
            id, first=first, after=after, last=last, before=before, request_options=request_options
        )

    async def reply(
        self,
        id: str,
        *,
        message: str,
        attachments: typing.Optional[typing.Sequence[ReplyResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Replies to an open request for information on the case. As the merchant this answers Whop's request (valid while the case awaits your information); as the customer it provides the information requested from you. The actor is resolved from the credential.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        message : str
            The reply to add to the case.

        attachments : typing.Optional[typing.Sequence[ReplyResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            reply added

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.reply(
                id="id",
                message="Here are the before and after photos from the Burnet Rd bay.",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.reply(
            id, message=message, attachments=attachments, request_options=request_options
        )
        return _response.data

    async def request_info(
        self,
        id: str,
        *,
        attachments: typing.Optional[typing.Sequence[RequestInfoResolutionCenterCasesRequestAttachmentsItem]] = OMIT,
        message: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ResolutionCenterCase:
        """
        Asks the customer for more information, as the merchant. Allowed up to 3 times per case before you must accept or deny it.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        attachments : typing.Optional[typing.Sequence[RequestInfoResolutionCenterCasesRequestAttachmentsItem]]
            Up to 3 evidence files, by existing file `id` or `direct_upload_id`.

        message : typing.Optional[str]
            What you need from the customer.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            information requested

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.request_info(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.request_info(
            id, attachments=attachments, message=message, request_options=request_options
        )
        return _response.data

    async def withdraw(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ResolutionCenterCase:
        """
        Withdraws (cancels) a case you opened, as the customer. Only possible while the case is still open.

        Parameters
        ----------
        id : str
            The resolution center case ID (`reso_` tag).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ResolutionCenterCase
            case withdrawn

        Examples
        --------
        import asyncio

        from whop_sdk import AsyncWhop

        client = AsyncWhop(
            "2026-09-02-2",
            idempotency_key="YOUR_IDEMPOTENCY_KEY",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.resolution_center_cases.withdraw(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.withdraw(id, request_options=request_options)
        return _response.data
